# 2026-09-22 — 복원 경로 ③④ 소급 판정 슬라이스 21 · **docstring 표면 첫 칸(원장 #11·#13·#22, 70줄)**

`rct_20260922-0005` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 20](2026-09-22-paths-3-4-slice-20.md)이 줄 주석 표면을 **67/67 · 잔량 0** 으로 닫으면서,
이 축에 남은 유일한 미판정 표면이 **docstring 22행 1,571줄**임을 못박았다. 이 패스가 그 표면의
**첫 칸**을 묻는다.

줄 번호는 base `596aa94`(#197 = 슬라이스 20) 기준이다.

## 선택 — 인계의 「선결 0」을 한 단계 아래에서 재실측해 1순위를 바꿨다

슬라이스 20 은 선결 0 후보로 **`#3` `smoke.py`(14줄) · `#13` `check_doc_inventory.py`(29줄) ·
`#11` `resource_generic_ac1.py`(22줄)** 셋을 권했고, 경고는 `#9` `resource_read_smoke.py` 하나
(슬라이스 17 이 `tableAccept` 제거의 **① 소유자**로 세운 자리)였다.

**그 「선결 0」을 원장 행 단위로 다시 재니 `smoke.py` 가 걸린다.** 이 행은 다른 두 docstring 행이
**자기 유지 근거로 인용하는 선례 소유자**다:

| 인용한 행 | 인용 문면 |
| --- | --- |
| docstring `#4` (`_helpers.py` 외 7파일, 220줄) | 요약 줄의 「(매칭 단위가 아니다)」 괄호는 `smoke.py` 행이 같은 자리의 「(규칙 3 비-AC 파일 …)」을 유지한 **선례를 따라** 남긴다 |
| docstring `#9` (`resource_read_smoke.py`, 22줄) | 마지막 문단은 `smoke.py` 행이 같은 성격의 「이것은 platform-auth-safety/AC5 가 아니다」를 유지한 **선례를 따른다** |

`smoke.py` 행에서 지금 제거 판정을 내리면 **두 자매 행의 기재된 근거가 소급해 낡는다** — 그런데
R10 은 그 두 행을 여전히 초록으로 통과시킨다(앵커는 자기 행의 지문에만 묶인다). 이것은 이 래칫이
고치려는 바로 그 결함이라, **선례로 피인용되지 않는 행부터** 간다.

교차 인용을 원장 두 표면 전체에서 기계로 훑은 결과, 이번 세 칸은 어느 행도 선례로 인용하지 않는다
(`resource_generic_ac1.py` 를 언급하는 docstring `#25` · 줄 주석 `#37` 은 **그 파일의 런타임 전제**를
가리킬 뿐 그 행의 *판정*을 인용하지 않는다).

## 이 패스의 범위 (완료 기준)

docstring 원장 **세 행**에 대해 복원 경로 ③④ 를 물어 판정하고 현재 지문에 못박는다(R10).

| | 전 | 후 |
| --- | --- | --- |
| docstring ③④ 판정 | 10/32 (잔량 **22**) | **13/32 (잔량 19)** |
| 판정 합계 | 2124줄 | **2112줄** |
| `#11` `resource_generic_ac1.py` | 22줄 `bf7e3ea0aef1` | **16줄 `8ea11175d3e4`** |
| `#13` `check_doc_inventory.py` | 29줄 `87d383322c27` | **24줄 `f8f8fc3a7427`** |
| `#22` `resource_generic_ac13.py` | 19줄 `21bfc52e1092` | **18줄 `bcf0a682b610`** |
| 줄 주석 표면 | 67/67 · 잔량 0 · 3338줄 | **무이동** |

범위 밖: docstring 잔량 **19행**(`smoke.py` 의 선례 얽힘과 `resource_read_smoke.py` 의 ① 소유자 의존
해소를 포함) · `README.md` 개정(정책 소유자 몫 6건) · 주석의 **정확성**(이 모델의 표면 밖).

## 측정

세 칸 다 **파일 하나짜리**라 분할 산물 함정이 서지 않는다(`--follow` = 경로 `git log`). 저작 PR:

| 행 | 저작 · 관련 PR |
| --- | --- |
| `#11` `resource_generic_ac1.py` | #86(저작) · #88(①② 판정) · #126(병렬 레인 선언) |
| `#13` `check_doc_inventory.py` | #94(저작) · #95(①② 판정) · #144 · #179 |
| `#22` `resource_generic_ac13.py` | #149(저작, 단일) |

**④ 는 재실측했고 여전히 대상 부재다.** README 의 「오늘 시점 ④ 의 적용 대상은 0」은 2026-09-20 실측이고
그 문서 자신이 「이 실태는 낡을 수 있고, 낡으면 그 자체가 신호다」라고 적어 두었다. 최근 120커밋에서
제목에 `(#N)` 이 없는 27건을 뽑아 **커밋마다 연결된 PR 을 조회**한 결과 — 26건은 `chore(deploy):` 이미지
핀(건드린 파일은 이 정책의 범위 밖 `k8s/deployment.yaml` 하나, PR 없음), 나머지 `e04e5d1` 은 정책 범위를
건드렸지만 **PR #193 으로 착지**했다(제목에 번호가 없을 뿐이다). 즉 정책 범위의 main 직접 push 는 0.

> ⚠️ 「제목에 `(#N)` 이 없다 = 직접 push」는 **틀린 대리 지표**다. `e04e5d1` 이 그 반례이고, 그것만 보고
> 세면 ④ 대상이 1 로 잡힌다. 커밋별 `/commits/<sha>/pulls` 조회가 맞는 측정이다.

## 판정 — 제거 12줄

제거선은 README 「③④는 *다음 편집자*에 대한 복원 경로다」 절의 **3분 표**다. 첫째 줄(그때의 결정·경위)만
걷고, 둘째 줄(앞으로의 편집을 구속하는 서 있는 불변식)과 셋째 줄(원본이 주석 쪽)은 남긴다.

### `#11` `resource_generic_ac1.py` — 제거 6 · 유지 16

- **¶1(셀렉터 반증 조건) −4** — #86 본문이 두 자리에서 담는다: 「픽스처」 절의 「객체가 하나면 1 → 1 이라
  셀렉터를 통째로 무시하는 구현도 통과한다」(= 반증 조건)와 케이스 요약의 「셀렉터가 결과 수를 **엄격히**
  줄이고, 대조군이 있는 네 종류는 남은/빠진 객체를 이름으로 확인한다」(= 단언의 모양). ✅
- **¶3(스코프 거부 대조군) −2** — #86 본문에 거의 축자로 있다: 「거부가 스코프 때문이지 그 종류를 못
  읽어서가 아님을 보이는 대조군」. ✅
- **유지 ¶2** — 「이벤트에는 대상별 레이블이 없어 좁히는 축 자체가 다르다」는 **근거**를 #86 은 적지 않는다
  (본문은 단언의 모양만 든다). 셋째 줄.
- **유지 ¶4** — 디스커버리 재해석에 **기대지 않기로 한 선택**이 요점인데, ① 은 `resolve` 의 *존재*만 담고
  #86 은 「자기 선행 조건을 멱등 폴링으로 성립시킨다」까지만 적는다. 둘째 줄.

### `#13` `check_doc_inventory.py` — 제거 5 · 유지 24

- **¶2 끝 문장 + ¶3(경위 전체) −5** — #94 본문이 담고, **원본보다 정확하게** 담는다. 본문은 「#74 이후
  `passes/` 에 판정 패스 문서를 하나씩 들여온 PR **다섯 건**(#78 · #81 · #88 · #90 · #93)이 연속으로 이 줄을
  건드리지 않았다. 다섯 번 다 CI 는 초록이었다. 이 줄을 읽는 기계가 하나도 없었기 때문이다」이고 집계도
  **34 → 39** 로 적는데, docstring 쪽은 「**네 번**의 PR」·「실측은 **38**」로 **낡아 있었다**. ✅
  두 문단은 「그 지목은 맞았지만」으로 이어져 한 단위라 함께 걷고, 「이 스크립트가 그 줄을 읽는 기계다」
  한 절만 남겼다.
- **유지 ¶2 의 ⑴⑵ 주장 구조** — 게이트가 재는 대상 그 자체다. ① `CLAIM_RE` 는 줄의 *형태*를 박을 뿐
  「두 가지를 주장한다」는 독법을 담지 않는다.
- **유지 ¶4 · ¶5(D1~D4 열거)** — 원장 docstring `#1` 이 고정한 문장이 그대로 적용된다(실패 메시지 어디에도
  규칙 **집합**은 없다 — 각 메시지는 자기 규칙만 말한다).
- **갈려서 유지 1건 — ¶6「D2 를 D4 와 함께 두는 것이 중요하다」** — #94 본문에도 거의 축자로 있어 **③ 는
  히트**다. 그럼에도 남긴 것은 그 내용이 **앞으로의 편집을 구속하는 서 있는 불변식**(D2 를 떼면 「허브 도달
  가능」이라는 말이 검사되지 않은 채 남는다)이어서다 — 3분 표 둘째 줄이 첫째 줄보다 우선하는 자리다.
  ③ 재판정 후보가 아니라 **갈림**으로 센다.

### `#22` `resource_generic_ac13.py` — 제거 1 · 유지 18

- **마지막 문단의 사유절 −1** — #149 본문이 거의 축자로 담는다: 「대상 파드(`stdin:true`·`stdinOnce:false`)는
  파일이 스스로 세운다 — 그 조합은 이 시나리오 전용이라 `tests/k8s/kind/` 에 두면 아무도 쓰지 않는 픽스처가
  CI 에 상주한다」. ✅
- **인용은 걷고 포인터는 남겼다**(원장 `#10` 이 고정한 처리) — 「각 파일이 자기 선행 조건을 스스로
  성립시킨다」는 **레포 전역 관례**라 #149 본문에 없고, 그것까지 지우면 다음 사람이 이 파일만의 선택으로
  읽는다.
- **유지 앞 세 문단** — 「새 프로세스가 뜨지 않았다」·「stdin 이 대상 프로세스에 닿았다」의 **두 증인 구조**와
  `readSeconds=31` 거부가 승인 요청보다 **앞**이어야 하는 이유를, #149 본문은 결론으로만 적는다. 단언의
  형태를 정하는 근거가 복원되지 않으므로 셋째 줄.

## 런타임 무영향

docstring 편집은 AST 안에서 일어나므로 슬라이스 17~20 이 쓰던 `ast.dump` 동등은 **이 표면에서 성립하지
않는다**(그 검증은 줄 주석용이다). 대신 **docstring 노드를 걷어낸 뒤의 AST 동등**을 썼다 — 세 파일 모두
편집 전후 동일. `internal/`·`main.go`·다른 `scripts/`·`tests/k8s/` 0바이트.

**자매 축 무영향** — `check_mock_policy` `084bfbeb1c98` · `check_ac_mapping` `6aa2fb1e795a` 두 출력 md5 가
base 와 동일. `check_doc_inventory.py` 는 이 패스 문서 1건만큼 움직인다(77 → 78 · 정책 37 → 38).

## 결론

| | |
| --- | --- |
| 물은 행 | **3**(`#11` · `#13` · `#22`, 70줄) |
| 제거 | **12줄** (제거율 17% — Go/Python 표본 범위 안) |
| docstring 표면 | 10/32 → **13/32 · 잔량 19** |
| 줄 주석 표면 | **무이동**(67/67 · 잔량 0 · 3338) |
| 갈려서 남긴 것 | 갈림 **+1**(`check_doc_inventory` ¶6) · ③ 재판정 후보 **+0** · ① 재판정 후보 **+0** |

**관찰 — 「선결 0」은 파일 의존이 아니라 *선례 인용* 으로도 선다.** 앞선 스무 패스가 쓴 선결 개념은
「① 소유자 관계」(한 주석이 다른 행의 제거를 떠받친다)였는데, 이 표면에서 처음으로 **판정끼리의 의존**이
나왔다 — `smoke.py` 행은 코드를 하나도 소유하지 않지만 두 행이 그 *판정*을 선례로 인용한다. R9·R10 의
앵커는 자기 행 지문에만 묶이므로 이 방향의 낡음은 **게이트가 원리적으로 보지 못한다**. 다음 패스가
`smoke.py` 를 물 때는 그 두 행을 같은 PR 에서 함께 재판정해야 한다.

## 다음 패스의 입력

- **다음 칸 제안**: `#12` `resource_generic_ac20.py`(36줄) · `#14` `resource_generic_ac3.py`(23줄) ·
  `#20` `resource_generic_ac12.py`(27줄) — 셋 다 파일 하나짜리이고 선례 피인용이 없다.
- **얽힌 칸 둘은 함께 물 것**: `#3` `smoke.py`(14줄)는 `#4`·`#9` 가 선례로 인용하므로 **세 행을 한 PR 에서**
  재판정해야 하고, 그 `#9` `resource_read_smoke.py` 는 슬라이스 17 의 ① 소유자 의존도 함께 진다.
- ③ 재판정 후보 누적: **44**(이 패스 +0).
- 갈림 누적: **3 + 1 = 4**.
- ① 재판정 후보: 승계 **6** 그대로.
- 정책 소유자 몫(승계 6, 신규 **1** → 7): **한 행의 판정이 다른 행의 유지 근거로 인용됐을 때, 그 행을
  재판정하는 PR 은 인용한 행들을 함께 물어야 하는가.** 이 패스는 「함께 문다」로 운용했으나 R9·R10 이
  그것을 강제하지 않는다 — 앵커가 자기 행 지문에만 묶이기 때문이다.
