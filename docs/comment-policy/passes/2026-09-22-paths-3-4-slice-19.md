# 2026-09-22 — 복원 경로 ③④ 소급 판정 슬라이스 19 · **`tests/k8s/kind` 픽스처 행(29줄)**

`rct_20260922-0003` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 18](2026-09-22-paths-3-4-slice-18.md)이 `internal/server` 행을 닫고 잔량 2행을 이름으로 남기며 다음 칸을
**`e82057fcdeba`** 로 못박았다. 이 패스는 그 칸을 묻는다. **선택을 재실측했다** — 두 후보의 줄 수·③ 모집단을
게이트 로직과 `git log -- <파일들>` 로 다시 셌고, 값이 슬라이스 18 이 적은 것과 정확히 일치했다:

| 후보 행 | 줄 | ③ 모집단 | 분할 산물 | 이 축이 물은 표면 |
| --- | ---: | ---: | --- | --- |
| **`e82057fcdeba`**(이 패스) | **29** | 16 | 아니다(여섯 파일 다 생성 커밋 하나) | **YAML — 이 축이 한 번도 묻지 않았다** |
| `5aa2ddea6731` | 132 | 29 | **그렇다**(#43·#44·#49 추출) | Python 헬퍼 |

**슬라이스 18 과 달리 판별식이 붙지 않았다.** 그때는 줄당 판독량이 0.171 vs 0.174 로 갈리지 않아 둘째 근거가 갈랐지만,
여기서는 한 후보가 **줄 4.6배 · 모집단 1.8배 · 분할 산물이라 모집단을 두 갈래로 재는 비용까지** 더 든다 — 세 축이 모두
같은 방향이라 둘째 근거를 부르지 않아도 선다. docstring 22행은 슬라이스 17 이 남긴 의존 경고
(`resource_read_smoke.py` docstring = 원장 docstring #9 가 `tableAccept` 제거의 ① 소유자)가 그대로 서 있어 이번에도 열지 않았다.

줄 번호는 base `40d339a`(#195 = 슬라이스 18) 기준이다.

## 이 패스의 범위 (완료 기준)

줄 주석 원장 **한 행**(`e82057fcdeba`, `tests/k8s/kind/` 픽스처 6파일 29줄)에 대해 복원 경로 ③④ 를 물어 판정하고,
그 판정을 현재 지문에 못박는다(R9). 범위 밖: 나머지 줄 주석 잔량 1행(`5aa2ddea6731`)과 docstring 잔량 22행,
`README.md` 개정(정책 소유자 몫 5건), 주석의 **정확성**(이 모델의 표면 밖).

**한 칸만 연다.** 남은 두 줄 주석 행을 한 task 에 몰면 R9 앵커를 두 번 옮기며 지문이 서로를 낡게 만든다.

## 측정 — 한 행과 ③④ 의 모집단

여섯 파일은 **개명·분할 이력이 없다** — `git log --follow --diff-filter=AR` 가 파일마다 생성 커밋 하나씩만 돌려준다
(`minio.yaml` #23 · `opensearch.yaml` #27 · `github-mock.yaml`·`grafana-mock.yaml` #17 · `dear-baby-fixture.yaml` #11 ·
`kustomization.yaml` #5). 그래서 슬라이스 16 의 함정(분할 산물에서 `blame` 만 믿으면 저작 PR 을 놓친다)은 서지 않고,
경로 기준 `git log` 가 곧 모집단이다.

경로 기준 모집단은 **16건**이지만, `blame --line-porcelain` 으로 29줄을 행별 귀속하면 **현재 살아 있는 줄을 들여온
PR 은 다섯뿐**이다:

| PR | 들여온 줄 | 커밋 |
| --- | --- | --- |
| #23 | `minio.yaml` 19–22 (4) | `f8e9b667` |
| #27 | `opensearch.yaml` 8–15 (8) | `e54fa08a` |
| #47 | `등재:` 4줄(`opensearch.yaml` 40 · `github-mock.yaml` 4 · `grafana-mock.yaml` 4 · `dear-baby-fixture.yaml` 11) + `kustomization.yaml` 41 (5) | `ed8d0c53` |
| #138 | `github-mock.yaml` 36–39 · 116–117 (6) | `00c6a113` |
| #163 | `kustomization.yaml` 15–20 (6) | `0f75a246` |

나머지 열하나는 남긴 줄이 0이라 판정에 들지 않는다. **슬라이스 18 의 교훈이 그대로 재현됐다 — 모집단 크기는 판독량의
상한이지 판독량이 아니다.** 슬라이스 18 의 인계표가 이 행의 「새로 읽을 본문」을 14 · 줄당 0.483 으로 추정했는데,
실제로 읽어야 했던 본문은 **다섯**(줄당 0.172)이었다.

**④ 는 다섯 건 전부 ❌이고, 리뷰 코멘트도 다섯 PR 전부 0건이다.** 다섯 커밋 모두 squash 착지라 본문이
`Co-authored-by` 트레일러 한 줄(`f8e9b667`·`e54fa08a`·`ed8d0c53`·`00c6a113`)이거나 아예 0줄(`0f75a246`)이다 —
README 의 ④ 실태(「오늘 시점 ④의 적용 대상은 0」)가 이 행에서도 그대로다. 그래서 이 행의 ③ 는 **PR 제목·본문뿐**이다.

## 판정 기준 — 슬라이스 1~18 의 구분 그대로

README 「③④는 *다음 편집자*에 대한 복원 경로다(2026-09-20 확정)」의 3분 표를 그대로 쓴다: **그때의 결정·경위 → 지운다** ·
**앞으로의 편집을 구속하는 서 있는 불변식 → 유지** · **PR·커밋이 스스로 「이 내용은 주석에 적었다」고 가리키는 것 → 유지**.

## 항목별 — 제거 12줄 (29 → 17)

### 제거 ⑴ — `minio.yaml` 머리말 (−4, 4 → 0줄)

```
# S3-compatible object store standing in for AWS S3 + STS. The server assumes
# AWS_CONFIG_ROLE_ARN against MinIO's STS endpoint and reads the seeded object
# with the resulting credentials, exercising the full aws_config_get path
# end-to-end without touching real AWS.
```

**#23 본문이 네 절을 축자로 담는다.** 「Add a MinIO kind fixture (`tests/k8s/kind/minio.yaml`) that stands in for S3 + STS」
(1행) · 「Route STS and S3 calls to `AWS_CONFIG_S3_ENDPOINT` … so the assume-role → GetObject path can run against an
S3-compatible server」(3행) · 검증 절 「Confirmed MinIO's credential-based STS AssumeRole accepts the (dummy)
`AWS_CONFIG_ROLE_ARN` and the resulting credentials read the object」(2행) · 「Production leaves it unset and uses real
AWS unchanged」(4행 「without touching real AWS」). 앞으로의 금지는 한 절도 없다 — 3분 표 **첫째 줄**.

이 제거로 `minio.yaml` 은 정책 표면 주석이 **0줄**이 되어 지문의 파일 수가 150 → 149 로 줄었다(게이트 R2 는 행의
파일 목록만 보므로 등재는 그대로 유효하다 — 원장 #40·#45 가 같은 형이다).

### 제거 ⑵ — `opensearch.yaml` 머리말 (−5, 5 → 0줄)

```
# Single-node OpenSearch standing in for the AWS OpenSearch Serverless
# collection. The security plugin is disabled, so the server's SigV4-signed
# requests are accepted as-is; combined with the MinIO STS endpoint this
# exercises the full AssumeRole -> SigV4 -> data-plane path of the
# opensearch_* tools end-to-end without touching real AWS.
```

**#27 본문이 세 절을 축자로 담는다.** 「`tests/k8s/kind/opensearch.yaml`: single-node OpenSearch 2 fixture — security
plugin off」 · 「Raw HTTP client signing every data-plane request with SigV4 (service `aoss`) using short-lived STS
AssumeRole credentials」 · 통합 테스트 절 「all through the MinIO STS AssumeRole path」. 「standing in for the AWS
OpenSearch Serverless collection」은 제목(「add OpenSearch Serverless tools」)과 후속 절(「production smoke … against
the real `kubernetes-docs` collection」)이 받는다.

**그리고 「플러그인이 꺼져 있다」가 질 법한 앞으로의 금지는 이 머리말이 지고 있지 않다** — 같은 파일 40–41행의
`등재:` + `mock-exception: GATE — 이 플러그인은 SigV4 를 검증하지 못한다(프로덕션은 AWS 관리형 front door).
켜면 basic auth 로 갈아타야 해서 충실도가 오히려 내려간다.` 쌍이 규약이 정한 자리에서 그것을 그대로 말하고,
그 쌍은 이 패스가 **유지**한다(기계 판독 주석은 애초에 판정 대상 밖). 슬라이스 5 가 `gatekeeper-variant.yaml`
머리말에서 세운 판정과 **같은 형**이다 — 규약 자리의 쌍이 원본이고 머리말 산문은 그 사본. 3분 표 첫째 줄.

### 제거 ⑶ — `github-mock.yaml` 노브 열거 (−3, 4 → 1줄)

```
# Knobs test-github-app-installation-token.md#시나리오 5 needs: an installation
# whose grant carries statuses:write, a mint that hands back more than it was
# asked for, and an unreadable installation. All three default to off so every
# other e2e sees the stock GitHub shapes.
```
→
```
# All three knobs default to off so every other e2e sees the stock GitHub shapes.
```

**첫 문장(노브 열거)은 지우고, 둘째 문장(기본 꺼짐 계약)은 남겼다.**

- 첫 문장은 **주석이 스스로 가리키는 ② 문서**(`docs/test-github-app-installation-token.md#시나리오 5`)의 재진술이고,
  **③ #138 본문**의 전용 절 「왜 상시가 아니라 모드 토글인가」가 같은 세 전제를 적는다 — 3분 표 첫째 줄.
- 둘째 문장은 **3분 표 둘째 줄**이다. 「기본은 꺼짐」은 바로 아래 `DEFAULT_CONFIG` 로 ① 복원되지만, 그 주석이 막는 것은
  *앞으로의 손* — 노브를 상시 켜는 편집이다. 슬라이스 5 가 `gatekeeper-variant.yaml` 117–123 `RESOURCE_GATED_KINDS`
  에서 내린 판정과 같은 형(본문 축자 히트가 있어도 「앞으로의 금지」는 본문이 집행하지 않는다).

**⑶ 은 2026-09-19 판정을 뒤집은 것이다.** 원장의 `[2026-09-19 ③④ 재판정 · rct_20260919-0001]` 절은 같은 ③ 히트를
이미 인정했으면서도 「닫힌 PR 본문이 *이 픽스처를 앞으로 고치는 사람*에 대한 복원 경로인가」에서 판단이 갈려
절차 5번으로 유지했다. **그 논거 자체를 README 가 다음 날 닫았다** — 「③④는 *다음 편집자*에 대한 복원 경로다
(2026-09-20 확정)」 절의 첫 문단이 「그가 닫힌 PR을 먼저 뒤지리라 기대할 수 없다」를 **「제거를 막는 사유가 되지
못한다」**로 못박는다. 갈림의 근거가 사라졌으므로 유지 판정도 함께 사라진다. 뒤집힌 것은 그 논거 하나뿐이라
둘째 문장의 불변식 판정은 그대로 살렸다.

**이것이 이 패스의 관찰 ⑴이다 — ③④ 축의 판정은 정책 문면이 움직이면 소급해 낡는다.** 슬라이스 17 이 「③ 는
낡는다(본문이 코드에 대해 거짓이 될 수 있다)」를 세웠는데, 여기서는 **판정 근거 쪽이 낡았다**. 원장에 「갈림」으로
적힌 자리는 다음 패스의 입력일 뿐 아니라, **정책이 바뀔 때마다 다시 물어야 하는 자리**다.

## 항목별 — 유지 17줄

| 자리 | 줄 | ③ | 판정 |
| --- | ---: | --- | --- |
| `opensearch.yaml` 13–15 heap 512Mi | 3 | #27 본문은 「JVM heap pinned to 512Mi, explicit 1Gi limits」로 **값만** 적는다 | 유지 — 「공용 CI 러너를 굶기지 않기 위해」라는 **근거**는 ③④ 어디에도 없다(절차 3 「확인되지 않는다」). 힙을 올리는 손을 막는 불변식이기도 하다 |
| `등재:` 5줄(`opensearch.yaml` 40 · `github-mock.yaml` 4 · `grafana-mock.yaml` 4 · `dear-baby-fixture.yaml` 11 · `kustomization.yaml` 41) | 5 | #47 본문 「표기 주석 5개 — 각 스텁·스위치를 **정의하는** 파일에서 그 ID 토큰이 나타나는 줄 바로 앞」 | 유지 — **3분 표 셋째 줄**. 본문이 이 주석들을 자기 산출물로 지목하므로 원본은 주석 쪽이다. 슬라이스 5 의 `gatekeeper-variant.yaml` 109행 판정 그대로 |
| `github-mock.yaml` 116–117 `_admin/config` | 2 | **0** — #138 본문은 엔드포인트 이름만 나열한다 | 유지 — 2026-09-19 판정 그대로(「본문이 설정 전체를 싣는다 ⇒ `{}` 가 곧 초기화」는 구현이 *하는 일*이지 **의도된 계약**임을 코드가 말하지 않는다. 호출자 `reset_mock()` 이 그 성질에 의존한다) |
| `github-mock.yaml` 노브 불변식 | 1 | #138 본문 축자 | 유지 — 3분 표 둘째 줄(위 ⑶) |
| `kustomization.yaml` 15–18 env 교차 제약 | 4 | **#163 본문 「왜 kind 오버레이에 패치가 따라붙는가」 절이 네 절을 축자로 담는다** | 유지 — **3분 표 둘째 줄**. 본문은 *그때의 결정*이고 주석이 막는 것은 「이 패치는 중복이니 지우자」는 앞으로의 손이다. 그 손이 닿으면 `github_commit_status_ac3.py` 의 포지티브 컨트롤이 **조용히** 거부로 바뀐다(거부 여섯 건은 그대로 통과한다) — #39 `auth-fixture.yaml` env 교차 제약이 같은 근거로 유지 판정을 받은 계열 |
| `kustomization.yaml` 19–20 `target`/`[noNs]` | 2 | **0** — #163 본문에 `target`·`[noNs]` 가 한 번도 나오지 않는다 | 유지 — 근거가 CI run 35540532938 실측이라 ③④ 밖(절차 3 마지막). 렌더된 YAML 에 그 실패가 남지 않는다 |

## 검증 (로컬, 클러스터 불요)

- `python3 scripts/check_comment_policy.py` → **rc=0**. 줄 주석 3371 → **3359**줄 / 150 → **149**파일
  (YAML 433 → **421**, 나머지 세 버킷 불변) · ③④ 판정 **65/67 → 66/67**, 잔량 **2 → 1**.
  docstring 표면 **무이동**(10/32 · 잔량 22 · 2124줄 · 지문 `897a853d…`).
- **비주석 바이트 0** — 세 파일 모두 `#`·빈 줄을 걷어낸 잔여가 편집 전후 sha256 동일.
  `minio.yaml`·`opensearch.yaml` 은 `yaml.safe_load_all` 파이썬 객체 동등(문서 5/5 · 3/3).
  `github-mock.yaml` 은 주석이 `server.py` **블록 스칼라 안**이라 객체 동등이 성립하지 않으므로
  대신 그 내장 스크립트를 `ast.parse` → `ast.dump` 로 대조해 **AST 동등**을 확인했다.
- **자매 축 무영향** — `check_mock_policy.py` · `check_ac_mapping.py` 출력 md5 가 base 와 동일.
  `check_doc_inventory.py` 는 이 패스 문서 1건만큼 움직인다(75 → 76).

## 결론

| | |
| --- | --- |
| 이 패스가 닫은 행 | `e82057fcdeba` — `tests/k8s/kind/` 픽스처 6파일 |
| 줄 | 29 → **17**(제거 12) |
| 지문 | `e82057fcdeba` → `4ac0f10e0833` |
| ③④ 판정 완료 / 전체 | 65/67 → **66/67**, 잔량 **2 → 1** |
| docstring 표면 | **무이동**(10/32 · 22 · 2124) |
| 갈려서 남긴 것 | **갈림 +0** · ③ 재판정 후보 **+2**(#27 의 heap 값 한 절 · #138 의 노브 불변식 한 문장 — 둘 다 부분 히트) · ① 재판정 후보 **+0** |
| 해소한 갈림 | **1**(2026-09-19 ⑴ — 이 축의 패스 문서 계수에는 들어 있지 않던 원장 내 갈림) |

**관찰 둘.** ⑴ 위의 ⑶ — **판정 근거가 낡는다.** ⑵ **YAML 표면의 제거율은 Go/Python 표본과 달랐다.**
이 축의 앞선 열여덟 패스는 제거율이 0~17%(슬라이스 18 의 20/117)였는데 여기서는 **12/29 = 41%** 다.
이유는 픽스처 주석이 「이 픽스처가 무엇을 대신하는가」를 적는 **머리말 산문** 형이고, 그 산문은 그 픽스처를
들여온 PR 본문이 거의 예외 없이 자기 요약에 적기 때문이다. 모델 등록 메모가 꼽은 중복 유형 ③(「매니페스트
재진술」)과는 다른 경로로 제거가 났다 — 재진술의 원본이 매니페스트가 아니라 **PR 본문**이었다.

## 다음 패스의 입력

- 줄 주석 잔량 **1행** — `5aa2ddea6731`(`tests/integration/` 공유 헬퍼 7파일 132줄 · ③ 29, **분할 산물** —
  #43·#44·#49 추출이라 모집단을 두 갈래(`git log -S` + 부모 파일 계보)로 잴 것). 이 행이 닫히면 줄 주석 표면의
  ③④ 잔량이 **0** 이 된다.
- ③ 재판정 후보 누적: 이 패스 **+2** → 슬라이스 18 까지의 39 + 2 = **41**.
- 갈림 누적: 이 패스 **+0** → **4** 그대로(해소 1건은 이 계수 밖의 원장 내 갈림이었다).
- ① 재판정 후보: 승계 **6** 그대로(`resource_test.go` 324–326 · `exec.go` 14–17 + 신규 4).
- docstring 잔량 **22행** — 여전히 한 번도 이 축으로 묻지 않은 표면. 슬라이스 17 의 의존 경고
  (`resource_read_smoke.py` docstring = 원장 docstring #9 가 `tableAccept` 제거의 ① 소유자)는 그대로 서 있다.
- 정책 소유자 몫(승계 5, 신규 0): PR 안의 개별 커밋 메시지가 ③ 인가 · ③ 본문이 코드와 어긋날 때의 규칙 ·
  한 덩어리로 「유지」된 다줄 논증 안의 개별 절을 ①② 축이 다시 물어야 하는가 · ① 소유자가 같은 행 안의 테스트
  쪽일 때 · 본문이 주석을 인용해 유지 근거로 삼은 자리(#59 형)를 ③ 히트로 셀 것인가.
  **이 패스가 하나를 더 물게 만든다 — 「정책 문면이 바뀌면 지난 갈림을 언제 다시 묻는가」**(관찰 ⑴).
  지금은 그 재개봉이 *다음 패스가 그 행을 우연히 집을 때*에만 일어난다. 이 패스는 README 개정 없이 판정 가능했다.
