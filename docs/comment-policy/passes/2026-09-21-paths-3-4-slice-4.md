# 2026-09-21 — 복원 경로 ③④ 소급 판정 슬라이스 4 · **PR 2~3개 행의 Go·Python 넷**

`rct_20260921-0008` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 3](2026-09-21-paths-3-4-slice-3.md)은 다음 정렬을 「PR 2~3개의 Go·YAML 행 여덟 → PR 4개 이상」으로 넘겼다. 이 패스는
그 여덟을 다시 쟀고(`git log --format=%h -- <파일>` 의 커밋 수, 전부 squash PR), **한 행이 정렬을 벗어났다** — `118864349507`
(grafana)은 #172 가 주석 집합을 움직여 PR 4개(#24·#26·#59·#172)다. 남은 일곱을 **복원처의 층**으로 갈랐다: Go·Python 소스 넷은
저작 PR 이 설계 판단·검증을 케이스 단위로 적는 층이고, kind 픽스처 YAML 셋(`e6747e1eba38` 43줄 · `6448f26b0e72` 59줄 ·
`20eaf535b30c` 75줄)은 저작 PR 이 인프라 착지 경위를 적는 층이라 ③ 히트의 모양이 다르다(슬라이스 3 의 관찰 — 「밀도를 정하는 것은
PR 수가 아니라 본문의 층」). 이 패스는 앞의 넷을 묻고 YAML 셋을 다음 패스의 첫 칸으로 남긴다.

## 이 패스의 범위 (완료 기준)

| | 기준 | 값 |
| --- | --- | --- |
| ⑴ | 줄 주석 ③④ 잔량 마커 | **27 → 23**(③④ 39/66 → **43/66**행) |
| ⑵ | 실제 제거 | **1줄** — `cba720423913`(`scripts/check_doc_inventory.py`) `CATEGORIES` 의 `#:` 1줄. 나머지 세 행 0 |
| ⑶ | 줄 주석 판정 합계 | 3455 → **3454**(base `d8a605a`/#178 기준 — 자매 착지로 base 가 움직이면 게이트 실측으로 재기입) |
| ⑷ | docstring 표면 | **무이동**(9/31 · 잔량 22 · 합계 2017) |

**범위 밖**: 줄 주석 잔량 23행 — 다음 정렬 첫 칸은 kind 픽스처 YAML 셋(위), 그다음 PR 4개 이상(`118864349507` 포함 · #172 가 움직인
`internal/` 행은 새 지문 `6d60307a513a`·`d8fa67959048`·`f6d31e81324c`·`8a7dc1e7869f`·`973f03eb84bf`(#177 로 한 번 더) · #177 이 움직인
`f73c08478f50`·`b036567ad0f0`), docstring 잔량 22행, `e82057fcdeba`(k8s 픽스처 6파일 — PR 16개).

## 측정 — 네 행과 ③ 의 모집단

이슈 코멘트·리뷰 코멘트는 열한 PR 전부 0 이다. **④ 는 대상 부재** — 네 행의 파일을 건드린 커밋은 전부 PR 경유이고 README 실태
(main 직접 push 는 배포 핀뿐)가 그대로다.

| 행 | 파일 | 줄 | PR | 본문 | 머지 커밋 |
| --- | --- | ---: | --- | ---: | --- |
| `02f201383c52` | `internal/k8s/create{,_test}.go` · `internal/mcp/create{,_gate,_test}.go` | 2 | **#112** · #172 · #177 | 3,167 · 4,875 · 4,695자 | `5d6fbf8b94fa` · `7065a1449b8b` · `42441d6801dc` |
| `4ea5b103f043` | `internal/k8s/precondition{,_test}.go` | 103 | **#104** · #113 | 6,932 · 3,777자 | `17cb8bf4a9e5` · `8f37a6c20a8f` |
| `4c9ff2211279` | `resource_generic_ac{2,4}.py` | 6 | **#79** · #81 · #126 | 6,028 · 3,193 · 2,579자 | `bc87a4481b38` · `a18387af0fa8` · `25532fff1c24` |
| `cba720423913` → `edd31d7005fc` | `scripts/check_doc_inventory.py` | 8 → 7 | **#94** · #95 · #144 | 2,980 · 3,923 · 1,401자 | `901947b8df60` · `6ccd6d1b6208` · `8532a79e9f94` |

굵은 것이 저작 PR 이다. #81·#95·#113 은 이 모델의 ①② 판정 PR 이라 본문이 **유지 근거를 인용**하는 쪽이고(README 3분 표 셋째 줄이
걸리는 형), #126 은 `병렬 레인:` 선언뿐, #172·#177·#144 는 이 행의 주석 집합을 움직이지 않은 채 코드·경로만 고쳤다.

## 판정 기준 — 슬라이스 1~3 의 구분 그대로

1. **본문이 그 주장을 대신 말하면 지운다.** 본문이 *결과*·*무엇*만 적고 주석이 지는 *이유*·*교차 제약*은 적지 않으면 「다른 주장」.
2. **본문이 스스로 주석을 가리키면 원본은 주석 쪽** — 유지.
3. **본문이 결론을 담더라도 앞으로의 편집을 구속하는 서 있는 불변식이면 유지**(README 3분 표 둘째 줄). 슬라이스 3 이 `rg_ac6`
   ⑴⑵ 에서 그은 경계를 이 패스도 한 자리(`precondition.go` 15–25)에서 같은 방향으로 긋는다.

## 항목별 — `02f201383c52` (제거 0줄)

2줄 전부 exported `CreateRef`·`CreateResource` 의 이름으로 시작하는 1줄 doc 이다. 판정 절차 **2번**에서 멈추므로 ③④ 를 물을 본문이
없다 — 슬라이스 3 의 `86b0510d18a0` 과 같은 공허 판정. #112 본문은 재시도 비활성·409 보존·AC6 부재 대상 예외를 적지만 대조할 주석이
없고, #172·#177 은 이 다섯 파일의 코드만 움직였다(지문이 #112 착지 이래 그대로).

## 항목별 — `4ea5b103f043` (제거 0줄, 부분 후보 1)

**이 행에서 ③ 은 ② 와 집합이 같다.** #104 본문의 「설계 판단 셋」·「Accept 문자열」 절은 `docs/doc-tracker/2026-09.md` 의 같은 착지
문단 ⑴~⑷ 와 같은 문장이다(저작 PR 이 tracker 에 자기 본문을 옮겨 적었다). #113 이 ①② 로 이 행을 재판정하며 ② 히트를 보고 남긴
세 묶음 — ⑴ 폴백을 제시하지 않는 이유 · ⑵ `TargetReader` 분리 이유(회수) · ⑶ 테스트 픽스처 제약 — 은 ③ 에서 새 근거를 얻지 않는다.
그래서 이 패스가 물은 것은 「③ 이 ② 밖에 더 복원하는 것이 있는가」이고, 답은 검증 절(일곱 변이 표 · 토큰 단언)뿐이다.

| # | 주석 | ③ (#104 본문) | 판정 |
| --- | --- | --- | --- |
| `precondition.go` 15–25 | `metadataAccept` 둘째·셋째 문단 — 파생 이유는 `tableAccept` 포인터, 틀렸을 때의 대가(빈 표 vs 승인 전 Secret), 그래서 `application/json` 폴백을 같이 제시하지 않는다 · 폴백을 적는 것이 어긋남을 거부 아닌 응답으로 만든다 · 이 미디어 타입만이면 406 · `readTargetState` 가 kind 도 본다 | 「ⓐ 폴백을 같이 제시하지 않는다 — 폴백을 적는 것이 바로 어긋난 파라미터를 거부가 아니라 전체 객체로 답하게 만드는 장치이고, 여기서는 그 전체 객체가 유출이다 ⓑ 응답 kind 를 확인해 거부한다」 | **유지 — 3분 표 둘째 줄.** 본문은 결론을 담지만 주석이 막는 것은 *앞으로의* 손이다 — 폴백을 붙이거나 `tableAccept` 와 「통일」하는 편집. 코드는 폴백 없는 문자열만 보여 주고 그 부재가 의도임은 코드 어디에도 없다(테스트 152–154 가 문자열 모양을 재지만 *왜*는 같은 행의 주석뿐). 「406 으로 온다」는 상류 동작은 본문 0. 다만 15–18 첫 문장(「빈 표 vs 승인 전 Secret」)은 AC11 ②·본문 ⓐ 와 축자에 가까워 **③ 재판정 후보(부분)** |
| `_test.go` 22–33 | `metadataNegotiatingServer` — honor=false 분기가 오류가 아니라 전체 객체인 이유(AC11 문면 인용) · 406 픽스처는 존재하지 않는 서버 · 자기가 만든 PartialObjectMetadata 를 먹이면 어긋남을 원리상 못 본다 | 「표현을 협상하는 httptest 서버로 honor/ignore 양쪽을 태운다 · 자기가 만든 응답을 먹이는 테스트로는 이 계열을 볼 수 없다는 경고가 AC11 문면에 있다」 | 유지 — 본문은 *무엇*(협상 서버를 썼다)이고 「406 픽스처는 존재하지 않는 서버를 시험한다」·「honor=false 가 오류가 아닌 이유」는 픽스처 제약(#113 ⑶ 그대로) |
| `_test.go` 17–19 · 84–91 · 141–145 | `secretTokenInFixture` 의 존재 이유 · 「거부됐다 ≠ 그래도 썼다」가 성질의 전부 · 값을 단언하는 이유 | 「ignore 쪽 픽스처는 `data` 에 고유 토큰 · 단언은 「거부됐고 그 토큰이 오류 문면에도 없다」」 | 유지 — 본문은 단언의 *형태*, 주석은 그 형태가 **왜 전체 성질인가**(토큰이 오류 문면에 없다 = 프로세스에 들어오지 않았다 · 「PartialObjectMetadata 를 언급한다」로 재면 새는 헤더도 통과한다) — 다른 주장 |
| 나머지 | 44–53 `Subresource`/`MetadataOnly` · 57–74 `TargetState` 필드 · 83–104 · 136–162 · `_test.go` 52–53 · 71–72 · 152–162 · 192–194 · 210–212 | 본문 0 히트 | 유지 — AC 번호 포인터 · 구조적 이유 · 픽스처 제약 |

## 항목별 — `4c9ff2211279` (제거 0줄, 갈림 1 승계)

#79 본문이 축자로 담는 세 자리(「헤더 행이 대문자 컬럼명」 · NAME/READY 대응 · 「키 집합 등가로 과잉 제거까지」)는 **#81 이 ①②③ 으로
이미 지운 줄**이라 현재 지문에 없다 — ③ 을 물었다면 같은 결론이었을 자리다. 남은 여섯 줄:

| # | 주석 | ③ (#79 본문) | 판정 |
| --- | --- | --- | --- |
| `ac2` 좌표 고정 2줄 | 두 경로가 같은 대상을 보고 있다는 것부터 고정한다 — 다른 좌표를 견주면 크기 비교가 아무것도 말하지 않는다 | 「대조군은 도구가 아니라 `kubectl get -o json` 이다 — 도구의 다른 호출로 만들면 같은 축약 로직을 지나 공전한다」 | 유지 — 본문은 **대조군의 출처**, 주석은 **좌표 일치를 먼저 고정하는 이유**. 다른 주장 |
| `ac2` NAME/READY 잔여 1줄 | apiserver 의 Deployment Table 정의가 항상 내는 것이라 픽스처가 바뀌어도 흔들리지 않는다 | 0 | 유지 — 상류 동작 |
| `ac2` `#:` 2줄 | `MIN_SHRINK_FACTOR` · `_DUMP` | 「객체 전문 대비 크기」(무엇) · 직렬화 옵션 0 | 유지 — #81 판정 그대로(수치 근거의 앵커 · 「같은 규칙으로 잰다」는 제약) |
| `ac4` 1줄 | 어노테이션이 last-applied 뿐이었으면 빈 맵을 남기지 않고 키째 지운다 | 「잡음 둘만 제거 · `spec` 원본 동일」은 제거 범위, 빈 맵 처리 0 | 유지 — **갈림**(#81 의 갈림 그대로 승계) |

## 항목별 — `cba720423913` → `edd31d7005fc` (제거 1줄)

### 제거 — `CATEGORIES` 의 `#:` 1줄

```python
#: PRD(도구)와 PRD(공통)는 표에서만 갈리고 집계에서는 한 칸이므로 여기서도 합친다.
CATEGORIES = ("가치", "PRD", "테스트", "상태 추적", "정책")
```

#95 가 이 줄을 남긴 이유는 정확히 하나였다 — 「두 표를 나란히 놓으면 ② 로 도출되지만 **어느 쪽을 따르기로 했는가**는 어느 표에도
없다」. 그 「어느 쪽」이 #94 본문에 있다: D1 「모든 `docs/**/*.md` 가 인벤토리 표의 **다섯 종류** 중 하나로 분류된다」, D4 「종류별
내역」, 검증 출력 `PRD: 14`(한 칸). 결정은 ③ 에 있고(3분 표 첫째 줄), 서 있는 불변식으로 남길 잔여도 없다 — 같은 파일의 `CLAIM_RE`
(다섯 캡처, `PRD` 한 칸)가 결정을 코드로 박고, D1 실패 메시지 「다섯 종류(가치·PRD·테스트·상태 추적·정책)」·「doc-tracker 의 「문서
인벤토리」 표와 이 스크립트의 `categorize()` 를 함께 갱신할 것」이 교차 제약을 실행 시점에 말한다. 지운 뒤 `ast.dump` 가 main 과 동일하고
`check_doc_inventory.py` 는 같은 집계를 출력한다.

### 유지 — 7줄

| # | 주석 | ③ | 판정 |
| --- | --- | --- | --- |
| 구역 배너 3줄 | `# --- D1: 분류 ---` 류 | ③ 와 무관(주장이 없는 항해 표지) | 유지 — #95·원장 #12 선례 |
| BFS 1줄 | 도달 불가 문서 안의 끊긴 링크도 끊긴 링크다. BFS 는 그 문서를 펴 보지 않으므로 따로 돈다 | #94 D3 는 「전부 실재 파일을 가리킨다」까지 · **#95 본문이 이 문장을 유지 근거로 인용** | 유지 — 3분 표 셋째 줄(원본은 주석 쪽) |
| `HUB_LINK_RE` 2줄 | `reader.html?doc=` 규약은 doc-tracker 「이 레포에 맞춘 뷰어 규약」 절이 정의한다 | #94·#144 본문에 규약 언급 0 | 유지 — SSOT 포인터(교차 제약) |
| `CLAIM_RE` 1줄 | 형태가 바뀌면 파싱 실패로 멈춘다(조용히 0 으로 떨어지지 않게) | #94 「분류 불가 = 실패. 조용히 흘려보내지 않는다」는 D1 의 문장 | 유지 — 집계 줄 파싱의 함정은 본문 0 |

## 결론

| | 값 |
| --- | --- |
| 이 패스의 제거 | **1줄**(0 + 0 + 0 + 1) |
| 줄 주석 판정 합계 | 3455 → **3454** |
| ③④ 판정 완료 / 전체 | 39/66 → **43/66**, 잔량 **27 → 23** |
| docstring 표면 | **무이동**(9/31 · 22 · 2017) |
| 갈려서 남긴 것 | 승계 1건(`ac4` 빈 맵) + 부분 후보 1건(`precondition.go` 15–18 첫 문장) |

**관찰 둘.** ⑴ 저작 PR 이 tracker 에 자기 본문을 옮겨 적은 행(`4ea5b103f043`)에서는 ③ 이 ② 의 부분집합이라, ①② 판정이 이미 선 행의
③④ 패스는 **확인**이지 재판정이 아니다 — 이런 행은 앞으로도 0 이 정상이다. ⑵ ③ 이 값을 낸 자리는 「①② 판정이 *갈림* 으로 남긴 곳」
이었다(`CATEGORIES`) — 갈림의 사유가 「결정의 출처가 없다」인 행은 저작 PR 본문이 그 출처일 확률이 높다. 다음 정렬에서 ③ 재판정 후보
목록의 「결정 출처 부재」 형 갈림을 먼저 볼 것.

## 다음 패스의 입력

- 줄 주석 잔량 **23행**. 다음 정렬 첫 칸은 PR 2~3개 kind 픽스처 YAML 셋 — `e6747e1eba38`(`http-trace.yaml` 43줄 · #41·#62) ·
  `6448f26b0e72`(`gatekeeper-fixture.yaml`·`gatekeeper-variant.yaml` 59줄 · #106·#130·#161) · `20eaf535b30c`(`session-platform.yaml` 75줄 ·
  #50·#58·#62). 그다음 PR 4개 이상(`118864349507` grafana 39줄 #24·#26·#59·#172 부터).
- ③ 재판정 후보 누적: `precondition.go` 15–18 첫 문장(부분, 이 패스) + 슬라이스 3 의 `rg_ac6` ⑴⑵ · `ac20` 333 · `ac20` 59–60 둘째 문장 +
  슬라이스 2 의 `ac15` ⑶⑻ · `ac11` ⑴ 첫 절 · `recordTarget` doc 3줄 · `Principal` AC1 철자 2줄 · `rbac-narrow-fixture.yaml` 머리말 불릿.
  갈림 승계: `ac4` 빈 맵 1줄(#81 이래).
- docstring 잔량 **22행** — 여전히 한 번도 이 축으로 묻지 않은 표면.
