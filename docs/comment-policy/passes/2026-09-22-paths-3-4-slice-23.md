# 2026-09-22 — 복원 경로 ③④ 소급 판정 슬라이스 23 · **docstring 표면 셋째 칸(원장 `#15`·`#19`·`#23`, 150줄)**

`rct_20260922-0007` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 22](2026-09-22-paths-3-4-slice-22.md)가 둘째 칸을 닫아 잔량을 19 → 16 으로 내렸다. 이
패스가 셋째 칸을 묻는다. 줄 번호는 base `d5b3f63`(#201) 기준이다.

## 선택 — 인계 제안을 받되 그 근거는 양방향으로 다시 쟀다

슬라이스 22 는 다음 칸으로 `#15`(70줄) · `#19`(41줄) · `#23`(39줄)을 권하면서 **그 제안이 양방향
으로 재지 않은 것**임을 스스로 경고했다. 칸의 *선택*은 그대로 받고 근거만 다시 쟀다.

먼저 잔량을 게이트 출력이 아니라 **원장 파서로 독립 재계산**했다(`parse_ledger` ·
`PATHS34_ANCHOR_RE` 를 그대로 import). docstring 원장 **32행 = 완료 16 + 잔량 16**, 행 줄 수 합
**2,095** 로 두 마커와 전건 일치했다. 게이트 rc=0 은 「잔량이 0」이 아니라 「잔량이 정직하게
등재됐다」만 말한다.

그다음 얽힘을 **양방향**으로 기계 실측했다 — ⑴ **나를** 선례로 인용하는 행(나가는 방향),
⑵ **내 결과 칸이** 인용하는 선례 행·정책 문면(들어오는 방향).

| 행 | 실측된 얽힘 | 이 패스의 처리 |
| --- | --- | --- |
| `#15` `resource_generic_ac5.py` | **나가는**: docstring `#17`(`approval_gate_ac6/ac10`, **이미 앵커된 행**)이 「대체 수단 — 게이트 대조군 선례」로 인용 | 그 선례가 사는 자리(케이스 docstring)를 **건드리지 않는 쪽**으로 제거선을 그었다 |
| `#15` | **들어오는**: 유지 논거 하나(「삭제된 파일의 판단이라 `git log` 로만 도달한다」)가 `README.md` 「2026-09-20 확정」 절이 뒤집은 바로 그 형태 | 그 **논거 하나만** 번복. 같은 문단의 다른 절이 서 있는 불변식이라 문단은 유지 |
| `#19` `resource_generic_ac11.py` | 줄 주석 `#52`(`ac14`)가 인용하는 자리는 `MAX_OUTPUT_BYTES` 위 **줄 주석** — 표면이 다르다 | 얽힘 없음으로 확정(docstring 편집은 그 자리에 닿지 않는다) |
| `#23` `resource_generic_ac15.py` | **나가는**: docstring `#26`(`resource_generic_ac17.py`, **이미 앵커된 행**)이 「픽스처를 파일이 스스로 세우는 이유」를 선례로 인용 | 그 인용이 가리키는 것은 **코드의 거동**이라 산문 제거로 낡지 않음을 실측해 기록 |

`#26` 의 얽힘은 슬라이스 22 가 겪은 실패 모드(앵커된 행이 조용히 낡는다)와 같은 모양이지만
**결론이 반대**다. 선례가 산문에 있으면 산문을 지울 때 낡고, **코드의 거동에 있으면 낡지 않는다.**
「인용당하고 있다」만으로 물러서면 영원히 못 여는 칸이 생긴다 — 물어야 할 것은 *무엇이* 선례를
지는가다.

## 이 패스의 범위 (완료 기준)

docstring 원장 **세 행**에 ③④ 를 물어 판정하고 현재 지문에 못박는다(R10).

| | 전 | 후 |
| --- | --- | --- |
| docstring ③④ 판정 | 16/32 (잔량 **16**) | **19/32 (잔량 13)** |
| 판정 합계 | 2,095줄 | **2,073줄** |
| `#15` `resource_generic_ac5.py` | 70줄 `5c71486a91bf` | **62줄 `82f1cd9b73bd`** |
| `#19` `resource_generic_ac11.py` | 41줄 `2504f13acc7d` | **36줄 `b79826a62848`** |
| `#23` `resource_generic_ac15.py` | 39줄 `d978f47ad313` | **30줄 `cc51afa8768d`** |
| 줄 주석 표면 | 67/67 · 잔량 0 · 3,338줄 `7f5f3fadd92b` | **무이동** |

완료 기준은 **절대 지문이 아니라 부모 대비 상대값**으로 건다(「잔량 −3행 · 합계 −22줄 · 줄 주석
표면 무이동」). 자매 PR 이 분 단위로 착지하는 레포라 절대값은 태어날 때부터 낡는다.

범위 밖: docstring 잔량 **13행** · `README.md` 개정(정책 소유자 몫) · 주석의 **정확성**(이 모델의
표면이 아니다) · 제품 코드(0줄).

## 측정

세 칸 다 파일 하나짜리라 분할 산물 함정이 서지 않는다(개명·분할 이력 0 ⇒ 경로 `git log` =
`--follow`). ③ 모집단: `#15` → **#103**(저작) · **#106**(실물 gatekeeper 착지 재판정) ·
**#126**(`병렬 레인:` 선언 — 기계 판독이라 표면 밖) / `#19` → **#142** / `#23` → **#153**(저작,
+#173 은 줄 주석 표면 패스라 docstring 무접촉). **리뷰 코멘트는 다섯 PR 모두 0건**이고 이슈
코멘트 둘은 reopen 공지·리베이스 공지라 판정 입력이 아니다 ⇒ ③ 의 실체는 **본문뿐**이다.

**④ 는 재실측했고 여전히 대상 부재다.** 최근 120커밋에서 제목에 `(#N)` 이 없는 **26건**을 뽑아
커밋마다 `/commits/<sha>/pulls` 를 조회했다 — 연결 PR 이 없는 **25건은 전부** `chore(deploy):`
이미지 핀이고 건드린 파일은 범위 밖 `k8s/deployment.yaml` 하나, 나머지 1건은 번호만 없이 PR 로
착지했다. 정책 범위의 main 직접 push 는 **0**.

> ⚠️ 「제목에 `(#N)` 이 없다 = 직접 push」는 틀린 대리 지표다. 커밋별 조회가 맞는 측정이다.

## 판정 — 제거 22줄 / 150줄 (제거율 15%)

제거선은 `README.md` 「③④는 *다음 편집자*에 대한 복원 경로다 (2026-09-20 확정)」 절의 **3분 표**
이고, 거기에 슬라이스 14·17·22 가 세운 선례를 더한다: **③ 축자 + 같은 등재 범위 안에 살아 있는
① 강한 사본 = 이중 복원 → 소유자는 사용 지점이고 모듈 머리말의 사본을 접는다.**

### `#15` `resource_generic_ac5.py` — 제거 8 · 유지 54

- **¶2 뒷문장 −1** — #103 「**절단은 개수가 아니라 「어느 줄이 남았는지」로** 잰다. 개수만 보면
  앞에서 5줄을 잘라 오는 구현도 통과한다」가 축자 ✅. 더해서 **같은 원장 행 안에 완전한 ① 사본이
  살아 있다** — 케이스 `test_resource_generic_ac5_tail_lines_actually_truncates` 의 docstring 이
  같은 명제에 「`tail` 이 약속하는 것은 개수가 아니라 **어느 쪽 끝인지**」까지 더해 진다. 잃는
  명제가 0이라 사용 지점이 소유자다. 픽스처 머리말을 가리키는 포인터 한 문장은 남겼다.
- **¶4 전체 −7** — #103 「「승인 요청이 생기지 않음」은 성공만 보면 공허하다 — 게이트를 아예 달지
  않은 서버도 통과한다. 같은 세션에서 게이트 대상(`v1/Secret` 읽기)이 **거부**되는 대조군을
  나란히 둔다」가 축자 ✅. 마지막 두 줄(「approval gate is not configured」 문면이 배선 착지로
  바뀌었다)은 **#106 의 경위 그 자체**라 3분 표 첫째 줄이다. 여기서도 케이스
  `test_resource_generic_ac5_reads_never_reach_the_approval_gate` 의 15줄 docstring 이 **거절 댄스
  기법까지 포함한 완전한 ① 사본**이다.
  - ⚠️ **`#17` 의 인용은 그 케이스 docstring 을 가리킨다** — 「대체 수단(게이트 대조군 선례)」이
    사는 자리를 남겼으므로 인용은 낡지 않는다. 모듈 머리말 쪽 사본만 접혔다.
- **🔴 ¶1 은 유지하되 논거 하나를 번복한다.** 직전 판정은 「삭제된 파일의 판단이라 `git log` 로만
  도달하는데 그 결론을 이 파일이 승계한다」를 유지 근거로 들었는데, 그 형태는 README 2026-09-20
  확정이 **정확히 뒤집은 것**이고(「닫힌 PR 을 먼저 뒤지리라 기대할 수 없다」는 제거를 막는 사유가
  되지 못한다), 실제로 #103 「택하지 않은 길과 그 이유」 첫 항이 그 판단을 축자로 담는다. 그럼에도
  **유지**인 것은 같은 문단의 다른 절 — 「지우면 다음 사람이 기존 픽스처에 컨테이너를 더해 같은
  함정을 다시 판다」 — 이 **앞으로의 편집을 구속하는 서 있는 불변식**(3분 표 둘째 줄)이기
  때문이다. 번복은 낡은 논거 하나에만 적용했다.
- **유지** ¶3(「클램프가 아니라 거부」가 거부 하나만으로는 성립하지 않는다 — 무엇이 조용히
  통과하는가) · ¶5(선행 조건을 도구가 아니라 kubectl 로 세우는 이유) · `_control_secret` ·
  케이스 doc 일곱.

### `#19` `resource_generic_ac11.py` — 제거 5 · 유지 36

- **¶2 마지막 문장 −1** — 「이 렌즈의 산출물은 e2e 뿐이라 픽스처를 새로 만들지 않고 …」가 #142
  「**제품 코드 0줄** — … 산출물은 e2e 와 등재 문서뿐이다(이 렌즈의 경계 조항)」의 축자 ✅. 대상
  파드·컨테이너 이름은 **바로 아래 상수**(`MULTI_WORKLOAD`·`CHATTY`·`QUIET`)로 복원되는 ① 이라
  이중 복원이다. 무엇을 쓰는지 적은 앞 문장은 남겼다.
- **「한 번 물렸다가 돌아왔다」 경위 −4** — #142 의 차단축 표 ⑶ (「제품 결함(성공한 exec 가
  `exitCode: null · success: false`) 2026-09-18 #137」)과 「#137 이 닫은 두 자리에 대응하는 단언을
  명시」 절이 축자 ✅. 이 문단 **자신이** 「경위는 `docs/doc-tracker.md` 변경 이력」이라 적어 ②
  소유자를 지목한다. **회귀 방지선 선언은 남겼다** — 「이 두 단언을 지우지 마라」는 앞으로의
  금지이고 ③ 은 그것을 집행하지 않는다. 선행어를 잃지 않게 「#137 이 닫은 두 자리」로 **한 구만**
  다시 이었다(새 명제 0줄).
- **유지** ¶1(Go 단위가 이미 단언하는 것과 이 파일이 재는 실물 kubelet 왕복의 경계 — #142 본문에
  없다) · ¶3(`resource_exec` 이 언제나 승인을 타므로 컨테이너 누락 거절이 「승인 뒤 kubelet 의
  답」이라는 것 = 「후보 이름」 기대의 출처) · ¶4(픽스처 파드를 **기다려** 집는 이유 둘 — 레이블이
  Deployment 메타데이터에만 달려 파드 조회가 **항상** 비는 것과 `ci.yml` 이 이 픽스처엔
  `rollout status` 를 걸지 않는 것. 둘 다 #142 본문에 없고 코드로도 복원되지 않는 「픽스처가
  실환경과 갈리는 지점」) · ¶5(`병렬 레인:` 미선언이 의도) · ¶6(`yes` 왕복에 `isError` 를
  단언하지 않는 것이 의도 — #142 본문에 축자로 있으나 **앞으로의 금지**라 3분 표 둘째 줄).

### `#23` `resource_generic_ac15.py` — 제거 9 · 유지 30

🔴 **#153 은 스스로 「설계 결정 둘을 여기 남긴다(복원 경로 ③)」고 선언한다.** 3분 표 셋째 줄
(「PR 이 스스로 「이 내용은 주석에 적었다」고 가리킨다 → 원본은 주석 쪽」)의 **정확히 반대**
방향이라, 그 두 결정에 대해서는 ③ 이 원본임이 본문에 박혀 있다.

- **¶1 의 Go 단위 계약 열거 −3** — #153 「Go 단위 아홉(`internal/mcp/proxy_test.go` 넷 ·
  `internal/k8s/proxy_test.go` 다섯)이 이 도구의 계약을 이미 단언한다 — 메서드→verb 다섯 쌍이 …
  그 아홉은 전부 **가짜 게이트와 가짜 k8s 서비스** 위에서 돈다」가 축자 ✅. 열거된 네 계약은 그
  아홉의 **테스트 이름으로 복원되는 ①** 이라 이중 복원이다(슬라이스 22 가 `#20` ¶1 에서 쓴 가름과
  같은 형). 「그 아홉이 가짜 위에서 돈다」는 절은 다음 문단의 **선행어**라 남겼다.
- **쌍 열거 −2** — 세 쌍(`get on pods/proxy` · `create on services/proxy` · `get on nodes/proxy`)은
  네 `_pending(...)` 호출부와 그 단언으로 복원되는 ① 이고, 「마지막 둘이 같은 쌍」은 테스트 이름
  `..._share_a_pair_and_only_the_screen_tells_them_apart` 자신이 진다. 단언의 **형태**를 정하는
  문장(「각 화면에 쌍이 있다는 것만 재면 「같다」는 관측이 되지 않는다」)은 남겼다.
- **「스스로 세운다」 경위 −4** — #153 설계 결정 2 가 「대상 파드·Service 는 픽스처 YAML·`ci.yml`
  을 늘리는 대신 파일이 스스로 세운다(자매 `resource_generic_ac14.py` 와 같은 방식, 이미지도 이미
  클러스터에 있는 `busybox:1.36`). 러너 자동 발견이라 배선 무변경」으로 축자 ✅ — 그때의 결정이다.
  **80 번을 듣는 이유는 남겼다**: 포트를 바꾸는 편집을 구속하는 서 있는 불변식이다.
  - ⚠️ `#26` 이 이 자리를 선례로 인용하지만, 그 인용이 가리키는 것은 **코드의 거동**(파드·Service
    를 파일이 세운다)이고 그 거동은 그대로다 — 산문이 아니라 코드가 선례를 진다.
- **유지** ¶2(가짜 게이트는 받은 `context` 문자열을 기록할 뿐이라 화면을 **만드는** 코드와
  **전달하는** 경로가 갈라져도 둘 다 통과하고, 가짜 k8s 서비스는 호출 수만 세어 왕복 실재를 말하지
  못한다 = 무엇이 조용히 통과하는가) · ¶3 의 대조 이유 · ¶5(kubelet `/exec` 응답 코드를 단언하지
  않는 이유 — #153 설계 결정 1 의 축자이지만 **앞으로의 금지**라 유지. 이 문단이 없으면 다음
  사람은 그 자리를 「빠뜨렸다」로 읽는다) · ¶6(`병렬 레인:` 미선언 선언) ·
  `_node_name`·`_serving_pod_and_service`·`_pending` 의 doc.

## 런타임 무영향

docstring 편집은 AST 안에서 일어나므로 줄 주석 슬라이스가 쓰던 맨 `ast.dump` 동등은 이 표면에서
성립하지 않는다. 슬라이스 21 이 세운 대로 **docstring 노드를 걷어낸 뒤의 AST 동등**을 썼다 — 세
파일 모두 편집 전후 동일. 기계가 읽는 선언(`검증 시나리오:`·`실행 대상:`·`병렬 레인:`)은 세 파일
모두 축자로 살아 있다. `internal/`·`main.go`·`scripts/`·`tests/k8s/`·다른 `tests/integration/`
**0바이트**.

**자매 축 무영향** — `check_mock_policy` `084bfbeb1c98` · `check_ac_mapping` `6aa2fb1e795a` 두 출력
md5 가 base 와 동일. `check_doc_inventory.py` 는 이 패스 문서 1건만큼 움직인다.

**게이트를 양방향으로 굴렸다** — 고친 트리 rc=0(`복원경로 ③④ 판정 19/32행(잔량 13)` ·
`docstring 2073줄` 축자 확인) / 원장만 부모 값으로 되돌린 트리 **rc=1 7건**: **R6** 여섯(세 행의
줄 수 셋 + 지문 셋)과 **R8**(합계 2,095 + 잔량 0 != 실측 2,073). R10 은 되돌린 트리에서 자기들끼리
정합이라 울지 않는다 — **잔량 마커만으로는 제거를 지키지 못하고 R6·R8 이 함께 지킨다**는 것이
여기서 관측된다.

## 결론

| | |
| --- | --- |
| 물은 행 | **3**(`#15` · `#19` · `#23`, 150줄) |
| 제거 | **22줄** (제거율 15% — 지난 슬라이스 20%와 같은 대역) |
| docstring 표면 | 16/32 → **19/32 · 잔량 13** |
| 줄 주석 표면 | **무이동**(67/67 · 잔량 0 · 3,338) |
| 갈려서 남긴 것 | 갈림 **+0** · ③ 재판정 후보 **+2** · ① 재판정 후보 **+0** |

**관찰 — 「인용당하고 있다」는 그 자체로 물러설 이유가 아니다.** 슬라이스 22 가 남긴 교훈(앵커된
행이 자기 인용 대상의 변경으로 조용히 낡는다)을 그대로 적용하면 `#15`·`#23` 은 **둘 다 인용당하고
있으므로** 손대지 말아야 할 칸이 된다. 실제로 물어야 할 것은 한 단계 아래다 — **그 선례를 지는
것이 산문인가 코드인가.** `#17` 이 인용하는 「게이트 대조군 선례」는 케이스 docstring 이 지고(산문 →
그 자리를 남겨 제거선을 그었다), `#26` 이 인용하는 「스스로 세운다」는 파일의 거동이 진다(코드 →
산문을 지워도 낡지 않는다). 이 구분 없이 「피인용 있음 = 보류」로 접으면 영원히 못 여는 칸이 생긴다.

## 다음 패스의 입력

- **다음 칸 제안**: `#3` `smoke.py`(14줄) · `#9` `resource_read_smoke.py`(22줄) · `#18`
  `github_app_installation_token_ac5.py`(30줄). ⚠️ 앞의 둘은 **함께 물 것** — 슬라이스 22 가
  적었듯 `#3` 은 `#4`·`#9` 가 선례로 인용하고, `#9` 는 슬라이스 17 의 ① 소유자 의존을 진다.
  고르기 전에 이 패스가 쓴 가름(**선례를 지는 것이 산문인가 코드인가**)을 먼저 적용할 것.
- **남은 큰 칸들**(`#4`·`#5`·`#6`·`#7`, 7~17파일)은 ③ 모집단이 PR 수십 개로 불어난다 — 한
  슬라이스에 담으려면 파일 단위로 행을 쪼개는 원장 편집이 선행돼야 하고, 그것은 **R7(이중 등재
  금지)** 을 건드리므로 별도 패스로 설계할 것.
- ③ 재판정 후보 누적: **47**(이 패스 +2 — `#15` ¶1 의 번복된 논거, `#23` ¶1 의 접힌 열거).
- 갈림 누적: **4** 그대로. ① 재판정 후보: 승계 **6** 그대로.
- 정책 소유자 몫(승계 8, 신규 **1** → 9): **R9·R10 의 앵커는 「이 행이 인용하는 다른 행」을 보지
  못한다.** 이 패스는 인용 방향을 손으로 갈랐지만, 「선례를 지는 것이 산문인가 코드인가」를 게이트가
  물을 수 있는지는 정책 소유자의 물음이다.
