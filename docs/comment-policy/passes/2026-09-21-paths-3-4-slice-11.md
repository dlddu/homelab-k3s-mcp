# 2026-09-21 — 복원 경로 ③④ 소급 판정 슬라이스 11 · **PR 8개 정렬의 나머지 두 행(`tests/k8s/kind/auth-fixture.yaml`·`test-deployment.yaml` + `tests/integration/run_all.py`·`check_ac_mapping.py`)**

`rct_20260921-0017` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 10](2026-09-21-paths-3-4-slice-10.md)이 `internal/github/` 행을 닫고 다음 칸을 「PR 8 정렬의 나머지 둘 `e1daa9adec21`(17줄) ·
`b5869fe61fba`(49줄) — #44 를 공유하므로 한 패스로 묶을 수 있다(슬라이스 6 의 기준)」라 명명했다. 이 패스는 **그 두 행 = 66줄**을 한 번에 묻는다.
묶은 이유는 슬라이스 6 과 같다 — 전체 이력(228 커밋)으로 잰 ③ 모집단이 **#44 를 공유**한다(#44 가 `no_config.py`·`auth.py`·`smoke.py` 를 쪼개며
`auth-fixture.yaml` 헤더의 파일명 3곳을 개명하고 `check_ac_mapping.py` 에 `check_cases_are_run` 을 더했다). 모집단은 행마다 `git log -- <파일>` 로
재실측했다 — `auth-fixture.yaml`·`test-deployment.yaml` 은 PR 8건(#8 · #28 · #35 · #37 · #39 · #44 · #62 · #67), `run_all.py`·`check_ac_mapping.py` 도
PR 8건(#42 · #44 · #52 · #60 · #66 · #106 · #126 · #144 — 감지가 「7 + 트래커 PR」로 센 것의 그 트래커 PR 이 #60 이다). 합집합 **15건**, 전부 squash 착지,
리뷰 코멘트 0(이슈 코멘트는 #106 의 「CI 이벤트 드롭으로 reopen」 하나뿐 — 판정 무관), `git log` 제목이 전부 `(#N)` 을 단다 → ④ 대상 부재.
`git blame` 으로 66줄 전부가 이 열다섯 커밋에 귀속된다 — 두 행이 서로 다른 커밋을 든다(#44 도 남긴 줄은 `auth-fixture.yaml` 9줄뿐). 줄 번호는
base `2b9b5c3`(#187) 기준이다.

## 이 패스의 범위 (완료 기준)

| | 기준 | 값 |
| --- | --- | --- |
| ⑴ | 줄 주석 ③④ 잔량 마커 | **13 → 11**(③④ 54/67 → **56/67**행 — base `2b9b5c3`/#187 기준) |
| ⑵ | 실제 제거 | **2줄** — `auth-fixture.yaml` 10–12 의 3줄 → 1줄(`b5869fe61fba` → `9273b4a899a5`, 49 → 47). `e1daa9adec21` 은 0줄·지문 불변. 비주석 바이트 0, YAML 파싱 결과 deep-equal |
| ⑶ | 줄 주석 판정 합계 | 3417 → **3415**(base `2b9b5c3` 기준 — 자매 착지로 base 가 움직이면 게이트 실측으로 재기입) |
| ⑷ | docstring 표면 | **무이동**(10/32 · 잔량 22 · 합계 2124) |

**범위 밖**: 줄 주석 잔량 11행(`f73c08478f50` server 117 · `b036567ad0f0` main.go 36 · `e82057fcdeba` k8s 픽스처 6파일 29 · `5aa2ddea6731` 공유 헬퍼 132 ·
`dfc76f31359a` session_list 46 · `31d67e48d0e5` opensearch_document_delete 4 · `469b96c4df5a` aws_config_get 32 · `87dce628e24d` dear_baby 4 ·
`4a12a66791fc` k8s/resource 195 · `921ed7505e16` mcp/resource.go 56 · `9013205ddb59` resource-generic 픽스처 45 — 게이트 R9 의 앵커 부재 행 그대로),
docstring 잔량 22행, 아래 ③ 재판정 후보.

## 측정 — 두 행과 ③ 의 모집단

| 행 | 파일 | 줄 | PR | 본문 | 머지 커밋 |
| --- | --- | ---: | --- | ---: | --- |
| `b5869fe61fba` → `9273b4a899a5` | `tests/k8s/kind/auth-fixture.yaml` · `test-deployment.yaml` | 49 → 47 | #8 · **#28** · **#35** · **#37** · **#39** · **#44** · **#62** · **#67** | 219 · 1,344 · 2,957 · 2,730 · 4,069 · 6,398 · 1,947 · 5,034자 | `3c77aa0` · `ca1f28f` · `de915ba` · `823842a` · `448a710` · `4e00d36` · `ac58383` · `343b817` |
| `e1daa9adec21` | `tests/integration/run_all.py` · `check_ac_mapping.py` | 17 | **#42** · #44 · #52 · **#60** · **#66** · #106 · **#126** · #144 | 3,220 · 6,398 · 5,098 · 4,241 · 4,771 · 3,138 · 2,579 · 1,401자 | `32fde9f` · `4e00d36` · `b2b6b68` · `8fdbfa3` · `85132ae` · `fb2eeb1` · `25532ff` · `8532a79` |

굵은 것이 주석을 들여온 PR 이다. 첫 행: #35 가 변형 신설(머리말 10), #37 이 「계약」 문단(4), #44 가 관측 파일 목록 개명(9), #62 가 이 정책의 첫
판정(「Auth ENABLED」 2문장 제거 — 남긴 교차 제약 2), #67 이 `MCP_K8S_DISABLED`(부재 이유 5 + 교차 제약 5 + 구분 1), #28 이 crash-once 픽스처(8),
#39 가 목록 조회 전용 오브젝트(5); #8 은 `test-deployment.yaml` 을 신설했지만 남긴 주석 0. 둘째 행: #42 가 러너·체커 신설(항해 표지·`#:` 6),
#66 이 축 개정(8), #60 이 규칙 7(2), #126 이 `## 병렬 레인` 절 제목(1 — docstring 절 제목이 `^\s*#` 에 걸린 것); #44·#52·#106·#144 는 파일을
건드렸으나 남긴 줄 주석 0.

**이 층의 ③ 모양 — 픽스처 저작 PR 이 「접근」·「주의」 절에 픽스처의 존재 이유를 적고, 그 뒤 PR 이 그것을 계약으로 굳히며 「주석에 적어 뒀다」고
스스로 가리킨다.** #35 의 「primary 는 ungated 라 게이트를 관측할 수 없다」·「시크릿 없이도 서버가 뜬다 — 그것이 게이트 단언에 필요한 전부」는
`auth-fixture.yaml` 3–5 · 10–12 를 축자로 담고, #37 의 「미부착 상태는 이제 계약 … 픽스처 헤더 주석에 그 취지를 적어 뒀다」는 20 · 28–29 를,
#67 의 「k8s 하나만 예외 — ServiceAccount 를 읽기 때문」 + 변경표 「부재 이유·교차 제약 주석」은 14–18 · 88–92 를, #39 의 「러닝 파드를 기다리는
케이스가 없어 대기 스텝이 필요 없다」는 `test-deployment.yaml` 95–96 을 담는다. 둘째 행은 ③ 히트가 사실상 없다 — 17줄 전부가 **표지**(docstring
절 제목 3 · `#:` 상수 doc 4 · `main()` 의 `# --- 규칙 N ---` 10)라 결정·경위를 담을 자리가 아니다.

**본문과 코드의 대조**(#24 선례): #28 본문이 픽스처를 「매 인스턴스가 마커를 출력하고 `sleep 1` 후 exit 1 → **의도적으로 Ready 가 되지 않는**
픽스처」라 적는데, 착지한 `crashloop-fixture` 는 emptyDir 센티널로 **한 번만** 죽고 이후 Running 이다 — 본문이 초안을 말하고 코드·주석이
최종이다(③ 본문 ↔ 코드 어긋남, 슬라이스 6 #24 계열). 정책의 「충돌하면 주석이 틀린 것으로 본다」는 주석 ↔ **코드** 의 충돌에 대한 규칙이고 여기
주석은 코드와 맞으므로 본문 쪽이 낡은 것이다 — 이 경우 ③ 은 그 주석을 복원하지 못한다. 그 밖의 낡음: #35 「`MCP_API_KEYS=ci-e2e-key`」·#37
「`tools/list` 14개」·#67 「17종」·#42 「9스텝」·#44 「26 + 9」·#66 「61개」는 그때 값이고 판정 대상 주석과 무관.

## 판정 기준 — 슬라이스 1~10 의 구분 그대로

1. **본문이 그 주장을 대신 말하면 지운다.** 본문이 *결과*·*무엇*만 적고 주석이 지는 *이유*·*교차 제약*은 적지 않으면 「다른 주장」.
2. **본문이 스스로 주석을 가리키면 원본은 주석 쪽** — 유지. 이 층에서는 #37 「픽스처 헤더 주석에 그 취지를 적어 뒀다」, #67 변경표의 「부재
   이유·교차 제약 주석」, #62 「교차 제약 … 만 남겼다」가 그것이다.
3. **본문이 결론을 담더라도 앞으로의 편집을 구속하는 서 있는 불변식이면 유지**(README 3분 표 둘째 줄). 이 층에서는 **계약**(「자격증명을 붙이지
   말 것」)과 **교차 제약**(「이 줄을 지우면 그 파일이 빨개진다」·「`API_KEY` 와 같아야 한다」), **부재의 주석**(「`MCP_AUTH_DISABLED` is left unset」)이
   여기 든다 — 슬라이스 10 `mintInstallationToken` 288–291 과 동형.

이 층에서 새로 확인한 것: **뒤 PR 이 앞 PR 의 편의를 계약으로 대체했다고 스스로 적으면, 앞 PR 의 편의 논거는 첫째 줄이다.** `auth-fixture.yaml`
10–12 의 「서버가 시크릿 없이도 뜬다 — 그것이 게이트 단언에 필요한 전부」는 #35 가 시크릿을 붙이지 않은 그때의 경위이고, #37 이 바로 아래 20행에
「That absence is now a contract, not just a convenience」를 넣어 그 편의를 계약으로 갈아 끼웠다. 서 있는 불변식은 20–29 행(계약 + 관측 파일
목록)이지 그 편의가 아니다.

## 항목별 ⑴ — `b5869fe61fba` → `9273b4a899a5` (`tests/k8s/kind/`, 제거 2줄)

### 제거 — `auth-fixture.yaml` 10–12 의 편의 논거 (3줄 → 1줄, −2)

```
# key authorizes. No credential secrets are attached: main.go's buildXService
# helpers degrade to an "unavailable" service when a secret is absent and the
# server still starts, which is all the auth-gate assertions need.
```
→ 「No credential secrets are attached.」 만 남긴다(20행 「That absence is now a contract」의 선행사).

**③ #35 「No credential secrets — `buildXService` degrades to "unavailable" when a secret is absent and the server still starts, which is all the gate
assertions need」** ✅ 축자(같은 문장) + **③ #37 「`main.go` 의 `build*Service` 가 모두 `NewUnavailable("")` 로 degrade 한 채 서버는 정상 기동한다」** ✅.
그 위에 ① `main.go::buildGate` doc 「the server's degradation rule is that a missing integration disables its own tools and nothing else」와 같은 트리의
`_auth_variant.py` 21–27 「An empty reason is what main.go passes when the primary env var is unset, which is precisely the situation this fixture
reproduces」(#67 이 「픽스처의 자기 서술」로 인용한 자리), ② `prd-platform-auth-safety` AC5(27행이 `platform_auth_safety_ac5.py` 로 가리킨다) — 네 경로
전부에서 복원된다. 지운 것은 「왜 #35 가 시크릿을 붙이지 않았는가」(그때의 경위)이고, 「붙이지 말 것」(앞으로의 금지)은 20–29 행이 든다.

### 유지 — 45줄

| # | 주석 | ③ | 판정 |
| --- | --- | --- | --- |
| `auth-fixture.yaml` 1 | 파일 머리 1줄 | #35 제목 | 관례 |
| 3–5 | primary 는 `MCP_AUTH_DISABLED=1`(overlay `configMapGenerator`) 이라 `/mcp` 가 ungated — 게이트를 거기서 관측할 수 없다 | **#35 「The primary kind deployment runs with `MCP_AUTH_DISABLED=1`, so `/mcp` is ungated there and the auth gate can't be observed」** ✅ 축자 · ① `kustomization.yaml:43` | 유지 — 두 번째 Deployment 가 **왜 존재하는가**는 YAML 형태에서 보이지 않는다(구조적 보장의 이유; `oidc-fixture.yaml` 4–6 도 같은 명제를 자기 존재 이유로 든다). 첫째 줄(경위)과의 경계가 좁다 — **③ 재판정 후보(부분)** |
| 5–8 | 같은 이미지 · `MCP_AUTH_DISABLED` is left unset · `MCP_API_KEYS` 한 개 → `platform_auth_safety_ac1.py`·`ac7.py` 가 401/인가를 단언 | #35 「Same image, `MCP_AUTH_DISABLED` unset and `MCP_API_KEYS=ci-e2e-key`」 · 케이스 목록 | 유지 — 「left unset」은 부재의 주석(둘째 줄), 소비자 파일 둘은 교차 제약 포인터(#44 가 개명한 자리) |
| 14–18 | k8s 만은 부재로 꺼지지 않는다(ServiceAccount) → `MCP_K8S_DISABLED` · 그래서 시나리오 2 를 관측할 유일한 배포 | **#67 「k8s 하나만 예외 — 시크릿이 아니라 in-cluster ServiceAccount 를 읽기 때문 … 그 예외를 없애 픽스처를 자기 계약에 맞춘다」** ✅ | 유지 — **셋째 줄**: #67 변경표가 「`MCP_K8S_DISABLED: "1"` + 부재 이유·교차 제약 주석」이라 이 주석을 원본 자리로 지목 + 부재 이유(09-08 재판정 그대로) |
| 20 · 28–29 | 「That absence is now a contract, not just a convenience」 · 「Do not attach credential secrets or credential env here — wire them into the primary」 | **#37 「"자격증명 시크릿 미부착" 상태는 이제 계약이다 … 그 변형에 자격증명을 붙이면 안 되며, 픽스처 헤더 주석에 그 취지를 적어 뒀다」** ✅ | 유지 — **셋째 줄**(PR 이 주석을 원본으로 지목) + 둘째 줄(앞으로의 금지) |
| 21–27 | 계약을 관측하는 파일 7개와 AC | #37 표 · #44 신규 파일 표 — *무엇* | 유지 — 「붙이면 무엇이 빨개지는가」의 교차 제약(88–92 와 같은 계열, 09-08 판정의 `_auth_variant.py` `API_KEY` 계열) |
| 84–85 | 「A throwaway CI credential; must match `API_KEY` in `_auth_variant.py`」 | **#62 「교차 제약인 「`_auth_variant.py` 의 `API_KEY` 와 같아야 한다」만 남겼다」** | 유지 — 정책 패스가 스스로 남긴 자리(셋째 줄) + 교차 제약 |
| 88–92 | 끄는 것이 `namespace_list_ac2.py` 의 *subject* … 이 줄을 지우면 그 파일이 빨개진다 | #67 ✅ + 변경표 「교차 제약 주석」 | 유지 — 셋째 줄 + 되돌리면 빨개지는 불변식(둘째 줄) |
| `test-deployment.yaml` 37–44 | crash-once 픽스처 — 센티널로 한 번만 죽는다 · 영구 crash loop 은 containerd GC 와 겹쳐 플레이키했다 | ❌ — #28 본문은 영구 crash loop 초안을 적고(위 대조), 플레이키 관측은 어느 본문에도 없다 | 유지 — 관측된 실패 모드(09-05 판정 그대로) |
| 92–94 | `workload_list` AC1 「각 enum 종류」 · `workload_scale` AC2 — **실재하는** DaemonSet 이라야 거부가 *종류*에 대한 것임이 관측된다 | #39 「3 enum 전부 호출」·「scale enum 에 DaemonSet 추가」(음성 통제) — *무엇* | 유지 — 「이 어서션이 왜 이 형태인가」(정책 ② 절의 명시 예외) |
| 95–96 | 「Both are list-only targets: no case waits for their pods, so ci.yml needs no extra rollout step」 | **#39 「목록 조회 전용 오브젝트 2개만 더했다 — 이들의 러닝 파드를 기다리는 케이스가 없어 대기 스텝이 필요 없다」** ✅ 축자 | 유지 — `ci.yml` 롤아웃 대기의 **부재**를 적은 것이라 이 오브젝트의 파드를 기다리는 케이스를 더하는 편집을 구속한다(둘째 줄)와 #39 의 경위(첫째 줄) 사이 — 애매하면 남긴다. **③ 재판정 후보(부분)** |

## 항목별 ⑵ — `e1daa9adec21` (`tests/integration/run_all.py` · `check_ac_mapping.py`, 제거 0줄)

| # | 주석 | ③ | 판정 |
| --- | --- | --- | --- |
| `run_all.py` 14 · 33 · 44 | `## 파일이 신고하는 것` · `## 병렬 레인` · `## 사용` | #42 · #126 — 절이 *있다* | 유지 — docstring 절 제목이 `^\s*#` 에 걸린 것(09-18 판정 그대로), 내용은 docstring 표면 몫 |
| 72 · 79 | `#:` `NOT_MATCHING_UNIT`(공유 헬퍼와 이 하네스 자신) · `NON_SCENARIO_MARKER`(규칙 3, 비-시나리오 파일) | #66 「매칭 단위가 아닌 것 목록에 `run_all.py` 가 빠져 있다」 — 모델 정의 얘기 · #44 「규칙 3 의 비-AC 파일 등재」 | 유지 — 상수의 의미(09-05 「규칙 3 이 읽는 값의 의미」 그대로) |
| `check_ac_mapping.py` 70 · 72 | `#:` `SCENARIO_HEADING_RE` · `SCENARIO_ID` | #66 「시나리오 식별 규약」 — *무엇* | 유지 — 정규식이 무엇을 딴다는 라벨 |
| 82 · 319 · 355 · 377 · 415 · 418 · 422 · 446 | `# --- 규칙 7/5/3/6/7 · 하네스 무결성 · 집계 · 불변식 ---` | #42 · #44 · #60 · #66 — 규칙이 무엇인지 | 유지 — 긴 `main()` 의 항해 표지(09-05 판정, 자매 행 `check_comment_policy.py` 와 동일) |
| 347 · 369 | 「예외와 구현 대기는 겹치면 안 된다 (영구 면제 vs 임시 보류)」 · 「예외·구현 대기: 선언과 겹치면 안 된다」 | **#66 「예외(영구 면제)와 구현 대기(임시 보류)를 별도 표로 갈랐다 — 모델 정의가 섞지 말라고 명문화한다」** ✅ 축자 | 유지 — 바로 아래 `problems.append` 문면(「영구 면제와 임시 보류를 섞지 않는다」, ①)의 표지이지 결정·경위가 아니다 |

#144 의 「코드 주석 속 옛 경로 참조를 고치면 지문이 어긋나 재판정 대상 — 의도적으로 둔다」는 이 두 파일에 해당 참조 0(`#:` 70 은 `docs/test-<domain>.md`).

## 결론

| | 값 |
| --- | --- |
| 이 패스의 제거 | **2줄**(`auth-fixture.yaml` 만) — 비주석 바이트 0(`grep -vE '^\s*#'` diff 0 · `yaml.safe_load_all` deep-equal 3 문서) · `run_all.py`·`check_ac_mapping.py` 0바이트 |
| 줄 주석 판정 합계 | 3417 → **3415** |
| ③④ 판정 완료 / 전체 | 54/67 → **56/67**, 잔량 **13 → 11** |
| docstring 표면 | **무이동**(10/32 · 22 · 2124) |
| 갈려서 남긴 것 | 갈림 0 · 부분 후보 2(`auth-fixture.yaml` 3–5 · `test-deployment.yaml` 95–96) |

**관찰 둘.** ⑴ 픽스처 층의 ③ 히트는 거의 전부 **셋째 줄**이었다 — 저작 PR 이 「주석에 적어 뒀다」(#37)·「부재 이유·교차 제약 주석」(#67)·「교차
제약만 남겼다」(#62)로 스스로 주석을 원본으로 지목하고, 남는 것은 계약·교차 제약·부재의 주석(둘째 줄)이다. 첫째 줄로 떨어진 것은 **뒤 PR 이 스스로
「편의 → 계약」으로 대체한 앞 PR 의 편의 논거** 하나뿐이다. ⑵ ③ 본문이 초안을 말하고 코드가 최종인 경우(#28)는 ③ 이 그 주석을 복원하지 못한다 —
「충돌하면 주석이 틀린 것으로 본다」는 주석 ↔ 코드의 규칙이지 주석 ↔ PR 본문의 규칙이 아니다(슬라이스 6 #24 가 남긴 정책 소유자 질문의 사례 하나 더).

## 다음 패스의 입력

- 줄 주석 잔량 **11행** — 게이트 R9 의 앵커 부재 행: `f73c08478f50`(`internal/server/` 117줄) · `b036567ad0f0`(`main.go`·`version.go` 36줄) ·
  `e82057fcdeba`(k8s 픽스처 6파일 29줄 — PR 16개) · `5aa2ddea6731`(공유 헬퍼 7파일 132줄) · `dfc76f31359a`(session_list 46) · `31d67e48d0e5`(4) ·
  `469b96c4df5a`(aws_config_get 32) · `87dce628e24d`(4) · `4a12a66791fc`(`internal/k8s/resource.go`·`resource_test.go` 195) · `921ed7505e16`(`internal/mcp/resource.go`
  56) · `9013205ddb59`(resource-generic 픽스처 45). 모집단은 행마다 전체 이력 `git log -- <파일>` 로 재실측할 것. 이 패스가 ① 소유자로 인용한 `main.go::buildGate`
  doc 과 `_auth_variant.py` 21–27 은 아직 ③④ 미판정 행(`b036567ad0f0` · `5aa2ddea6731`)에 있다 — 그 행을 물을 때 이 패스의 제거가 그쪽 사본을 유일한 자리로
  만들었음을 세울 것.
- ③ 재판정 후보 누적: 이 패스의 둘(`auth-fixture.yaml` 3–5 존재 이유 · `test-deployment.yaml` 95–96 롤아웃 대기 부재) + 슬라이스 10 의 둘(`github.go` 288–291 ·
  `github_test.go` 279–283) + 슬라이스 9 의 하나(`resource_test.go` 646–647) + 슬라이스 8 의 하나(`sessionplatform.go` 24–26) + 슬라이스 7 의 둘(`gatekeeper.go`
  194–200 · 327–328) + 슬라이스 6 의 셋(`grafana.go` 73–75 · `exposure_test.go` 11–14 · `opensearch.go` 309–310) + 슬라이스 5 의 셋(`http-trace` 119–123 ·
  `gatekeeper-variant` 117–118 · `session-platform` 23–27) + 슬라이스 4 의 `precondition.go` 15–18 첫 문장 + 슬라이스 3 의 `rg_ac6` ⑴⑵ · `ac20` 333 · `ac20` 59–60
  둘째 문장 + 슬라이스 2 의 `ac15` ⑶⑻ · `ac11` ⑴ 첫 절 · `recordTarget` doc 3줄 · `Principal` AC1 철자 2줄 · `rbac-narrow-fixture.yaml` 머리말 불릿(누적 19).
  갈림 승계: `ac4` 빈 맵 1줄(#81 이래) · `TRACE_ROUTES` 앵커 1줄(#62 이래) · `resource_test.go` 154–155 · 324–326(슬라이스 9) — 이 패스 신규 0.
- docstring 잔량 **22행** — 여전히 한 번도 이 축으로 묻지 않은 표면.
- 정책 소유자 몫(승계 3 · 신규 0 — 사례 1 추가): PR 안의 개별 커밋 메시지가 ③ 인가(`http-trace.yaml`). **③ 본문이 코드와 어긋날 때의 규칙**(슬라이스 6 #24
  — 이 패스의 #28 이 같은 형: 본문은 초안, 코드가 최종). 한 덩어리로 「유지」된 다줄 논증 안의 개별 절을 ①② 축이 다시 물어야 하는가(슬라이스 7·9). 이
  패스는 README 개정 없이 판정 가능했다.
