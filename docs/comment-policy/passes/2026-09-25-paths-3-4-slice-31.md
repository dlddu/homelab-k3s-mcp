# 2026-09-25 — 복원 경로 ③④ 소급 판정 슬라이스 31 · **opensearch 도구 e2e (원장 `#6`, 100줄 / 14파일) — 이 표면의 마지막 행**

`rct_20260925-0005` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 30](2026-09-25-paths-3-4-slice-30.md)이 `#5`(session 도구 9파일)를 닫아 잔량을 2 → 1 로
내리고, 남은 한 행 `#6` 을 「판정은 승계하지 말 것」이라는 단서와 함께 넘겼다. 이 패스가 그 행을
묻는다. **행 선택 판단은 없다 — 잔량이 1행뿐이다.** 줄 번호는 base `f681f9f`(#213 = 슬라이스 30)
기준이다.

## 인계를 승계하지 않고 이 tip 에서 다시 쟀다

`ast` 로 커밋마다 14파일의 docstring 집합을 부모와 대조해 갈랐다. 인계 값이 재현됐다:

| | 실측 |
| --- | ---: |
| 파일을 건드린 커밋 | 6 |
| **docstring 을 움직인 커밋** | **4** |
| 그중 판정 패스 | 2 (#63 `ed933698d60a` · #65 `c4f7c090aff5`) |
| **저작 PR** | **2 — #43 `b25798e08e4d`(+124/-0) · #44 `4e00d3683532`(+46/-0)** |
| 파일당 docstring 이동 커밋 | 2~3 (**단독 커밋 0**) |

**⒝ R7 파일 단위 분할은 이 행에도 산출이 없다.** 단독 커밋이 0이므로 행을 쪼개도 지문이 낡는
사건은 조각 전부에서 동시에 일어난다 — 원장 행만 늘고 해상도는 그대로다. 슬라이스 29·30 이 이 행에
대해 미리 적어 둔 예측과 같고, **승계하지 않고 다시 세어** 확인했다.

**모집단은 승계하지 않았다.** 슬라이스 29 가 `#7` 을 닫으며 #43·#44 를 이미 읽었지만, 슬라이스 28 이
세운 규율대로 **판정은 물려받지 않고** 이 행의 블록을 저작 본문과 직접 대조했다 — 같은 PR 이라도
어느 블록을 적었는지가 갈린다.

## ③ 모집단은 저작 PR 과 판정 패스 PR 을 갈라 셌다

#63·#65 는 판정 패스이고, 그 본문이 이 파일들의 주석을 인용하는 것은 **판정 기록이 주석을 가리킨
것**이라 README 3분 표 **셋째 줄**(원본은 주석 쪽)에 걸린다. 세면 정책이 지키라는 주석이 다음
패스에서 전부 제거 대상이 되는 **자기 무효화 고리**가 생기므로 모집단에서 제외했다(슬라이스 30 과
같은 근거).

리뷰 코멘트는 두 PR 모두 **0**이다. 이슈 코멘트는 **#43 에 1건**(CI 실측 보고) 있고, 이 패스의
수확 둘 중 하나가 거기서 나왔다 — **본문만 읽었으면 놓쳤을 자리다.**

## 제거 4줄 — 두 자리

| # | 자리 | 줄 | 근거 |
| --- | --- | ---: | --- |
| ⑴ | `opensearch_document_put_ac1.py` 머리말 「자기 인덱스 `ci-put-ac1-<RUN_ID>` 와 자기 질의 토큰만 쓰므로 다른 파일이 무엇을 색인하든 관측하지 않는다」 | 2 | **#43 본문 축자** — 「케이스별 전용 인덱스 `ci-<case>-<RUN_ID>` + 유일 질의 토큰 … 파일이 갈라지면 `RUN_ID` 도 파일별로 새로 뽑히므로 격리는 오히려 강해진다」. 검토 쟁점 3 과 CI 코멘트(「인덱스 접미사가 파일마다 다르다는 것은 케이스별 격리가 프로세스 분리 후 오히려 강해졌음을 그대로 보여준다」)가 겹쳐 준다. **이 파일만의 사정이 0** — #43 이 11개 케이스 전부에 세운 일반 명제의 사본이고, 분할이 안전한 이유는 *그때의 결정*(3분 표 첫째 줄) |
| ⑵ | `opensearch_document_put_ac4.py` 머리말 「AssumeRole 레코드는 서버가 자격증명을 캐시해 한 번만 발급되지만 프록시가 그 레코드만은 evict 하지 않으므로, 파일이 나뉘어도 상관 관계 대조가 성립한다」 | 2 | **#43 CI 코멘트 축자** — 「서버가 자격증명을 캐시해 AssumeRole 을 한 번만 발급하고, 프록시가 그 레코드만은 evict 하지 않기 때문이다 — 분할이 접근 경로 단정을 깨뜨리지 않는다는 **유일한 비자명한 전제가 실측으로 확인된 지점**」. 코멘트 쪽이 실측 키 `885ZQ…` 와 파일별 인덱스 접미사 셋(`4491e435`·`69331839`·`aed4fcce`)까지 담아 **더 자세하다** — 지워도 지식이 어디에도 안 남는 자리가 아니다 |

머리말 첫 줄(「도구 응답이 아니라 ``tests/k8s/kind/http-trace.yaml`` 프록시의 기록을 읽는다」)은
③ 히트가 아니라 남겼다. `opensearch_document_delete_ac4.py` 의 「opensearch-document-put/AC4 와 같은
이유」 상호 참조가 가리키는 것도 **그 줄**이라 참조가 끊기지 않는다.

## 🔴 보존된 ①② 판정의 「전부 복원 경로 넷 밖이다」가 소급해 일부 거짓이었다

이 행의 2026-09-07 결과 칸은 유지 근거를 다섯 자리로 열거하며 **「…가 전부 복원 경로 넷 밖이다」**
로 닫는다. 실측하면 그중 **둘째**가 #43 코멘트에 있다:

* 「AssumeRole 레코드는 … 프록시가 그 레코드만은 evict 하지 않아 파일이 나뉘어도 상관 관계가
  성립한다」 ↔ **#43 CI 코멘트** (위 표 ⑵)

R10 이 서기(2026-09-20) 전의 ①② 판정이라 **③④ 를 실제로 뒤진 것이 아니라 묻지 않은 채 없다고
단정**한 것이고, 앵커가 없어 게이트는 이 행을 계속 초록으로 통과시켰다. 슬라이스 28·29·30 이 만난
것과 같은 형이다. **번복은 그 한 절의 근거에만 적용했고 나머지 넷은 그대로 살렸다.**

### ⚠️ 그 열거의 다섯째 항은 애초에 이 표면의 것이 아니다

「페이로드 해시까지 서명해야 본문 바꿔치기가 막힌다」를 레포 전수로 찾으면
`opensearch_search_ac3.py:**52**` 의 **`#` 줄 주석**이다 — docstring 표면에는 한 줄도 없고 **줄 주석
원장 소관**이다. 즉 docstring 행의 유지 근거 열거가 이웃 표면의 주석을 한 항 끌어다 썼다.

**지우지 않고 실측을 원장에 적어 두었다.** 어긋난 문면을 지우면 이 자리를 다시 만나는 판정자가
같은 헛짚음을 반복하고, 그 사실 자체가 「표면을 갈라 놓고도 판정 산문은 갈리지 않았다」는 라이브
케이스다.

## 유지 — ③ 히트인데 남긴 것 하나 (3분 표 둘째 줄)

`opensearch_search_ac1.py` 머리말 「질의 토큰이 이 프로세스의 `RUN_ID` 를 포함하는 단일 텀이라,
**인덱스를 지정하지 않는 컬렉션 전체 검색도** 다른 파일의 문서와 겹치지 않는다」.

격리 일반 명제 쪽은 ⑴ 과 같은 #43 축자다. 갈리는 지점은 **「unscoped 검색도」** 한 구절이다 —
14파일 중 **인덱스를 지정하지 않고 컬렉션 전체를 훑는 것은 이 파일뿐이고**(나머지는 전부 자기
인덱스로 스코프된다), #43 은 그 위험을 한 줄도 적지 않는다. 질의 토큰에서 `RUN_ID` 를 빼는 다음
편집자에게 이 파일만 형제 파일의 문서를 집기 시작한다는 것을 알리는 **서 있는 불변식**이다. ③ 은
*그때의 결정*을 적을 뿐 *앞으로의 금지*를 집행하지 않는다.

**③ 히트가 곧 제거는 아니다** — ⑴ 과 이 자리는 같은 #43 문장에 히트하고도 판정이 갈렸다. 가른 것은
히트의 유무가 아니라 **그 파일만의 사정이 그 문장에 있는가**다.

## 유지 — 모집단에 히트가 없는 것

키워드 전수 대조에서 저작 두 PR 본문 + #43 코멘트에 **히트 0**: `upsert` · `created` · `updated` ·
`자동 생성` · `404` · `not_found` · `size` · `상한` · `destructive` · `파괴` · `unsigned` ·
`무서명` · `aoss` · `session token` · `idempot` · `vacuous` · `control`.

따라서 아래는 전부 유지다 — 업서트 세 절(`created` → `updated` → id 없는 두 번 쓰기), 인덱스 자동
생성의 「색인 **전** 404 를 먼저 단정하지 않으면 자동 생성이 아무것도 보이지 못한다」, size 기본값의
「12건을 시드하는 이유 — 10건 이하면 「기본값 10」과 「무제한」을 구분할 수 없다」, `destructiveHint`
메타데이터 절, 부재 문서 두 갈래와 뒤이은 `ping`(「AC 의 후반을 가정이 아니라 단언으로 만든다」),
「security 플러그인이 꺼진 픽스처는 무서명 요청도 똑같이 답하므로 응답으로는 접근 경로를 구분할 수
없다」, SigV4 체인 단정(`aoss` 스코프·리전·세션 토큰·「never the static base key」), `auth-variant`
미설정 거부 셋.

**#44 는 모집단에 들되 수확 0이다.** 그 본문이 이 14파일 중 셋(`*_ac5.py` 둘 · `search_ac4.py`)을
낳았지만, 적은 것은 `auth-variant` 배차 증가(2 → 9)와 비-AC 파일 등재 판단이라 **이 행의 docstring 이
적는 축과 겹치지 않는다**. ③ 수확은 모집단 크기가 아니라 「그 PR 이 무엇을 적었는가」로 갈린다는
슬라이스 24·26·29·30 의 관찰이 여기서도 재현됐다 — 그리고 이번에는 **본문이 아니라 코멘트**가
수확처였다.

## ④ 재실측 — 적용 대상 0

base `f681f9f` 에서 최근 120커밋 중 제목에 PR 번호가 없는 **21건**을 커밋별 `/commits/<sha>/pulls`
조회로 갈랐다(제목 유무는 **틀린 대리 지표**다): **20건이 정책 범위 밖** `chore(deploy):` 이미지
핀(건드린 파일은 `k8s/deployment.yaml` 하나), **1건은 PR 로 착지**. 정책 범위 파일을 건드린 main
직접 push **0** ⇒ ④ 의 적용 대상은 오늘도 0이다.

## 이 패스로 docstring 표면의 ③④ 소급 판정이 완주한다

| | 전 | 후 |
| --- | --- | --- |
| docstring ③④ 판정 | 31/32 (잔량 **1**) | **32/32 (잔량 0)** |
| docstring 합계 | 1979줄 / 109파일 | **1975줄 / 109파일** |
| 원장 행 `#6` | `0cd15939e543` 100줄 | **`32876b76fbbc` 96줄** |
| 줄 주석 표면 | 3337줄 · 67/67 · 잔량 0 | **무이동**(지문 `91d88b628c41…` 불변) |

**두 표면 모두 32/32 · 67/67 · 잔량 0 이다.** 다음 감지에서 「잔여 0 → `no_action_needed`」가 정상
결론이 되는 자리다 — 이 표면에서 ③④ 잔량을 다시 여는 것은 **새 범위의 유입**(R8 이 막아 세우는
사건)이나 **기존 행의 재판정**(R10 이 잔량을 되돌리는 사건)뿐이고, 둘 다 그때 diff 에 선언으로
남는다. 잔여를 찾아 헤맬 필요가 없다.

## 검증

**게이트 네 종 rc=0** — `check_comment_policy.py` · `check_ac_mapping.py` · `check_mock_policy.py` ·
`check_doc_inventory.py`.

**양방향(음성 프로브)** — 원장만 부모 값으로 되돌린 트리에서 rc=1 이 나오는 것을 **단정 단위**로
확인했다: R6 줄 수 칸 · R6 지문 칸 · R8 합계 마커 · R10 ③④ 잔량 마커 · R10 앵커 문면(지문만 옛
값으로) **5건**.

**런타임 무영향** — 두 파일에서 docstring 노드를 걷어낸 뒤의 `ast.dump` 가 부모와 **바이트 동일**
하고, 제품 코드(`main.go`·`internal/`)는 **0파일**이다. 기계가 읽는 선언(`검증 AC:` ·
`검증 시나리오:` · `실행 대상:` · `추가 인자:` · `병렬 레인:`)은 diff 에서 **0줄**이다.

**줄 주석 표면 무이동** — 3337줄 · 67/67 · 잔량 0 과 전체 지문 `91d88b628c41…` 이 전후 동일하다
(이 패스는 docstring 표면만 만진다).

## 범위 밖(후속)

**docstring 잔량 0 · 줄 주석 잔량 0 — ③④ 축에 남은 행이 없다.**

`#6` 에서 **③④ 축으로는 묻지 않은 채 남긴 것** 셋을 ①② 축의 입력으로 기록해 둔다. 이 패스는 ③④
래칫이므로 그 축의 재판정은 열지 않았다 — 열려면 그 축의 task 로 와야 한다.

* `opensearch_document_delete_ac1.py` 머리말 「같은 인덱스에 남는 문서를 함께 두어 "지목한 하나만"
  사라졌음을 관측한다」(2줄) ↔ 바로 아래 케이스 docstring 이 같은 것을 영어로 적는다(「the surviving
  one is the control」) — **자기 파일 중복**(①).
* `opensearch_search_ac3.py` 머리말 「픽스처가 무서명 검색도 똑같이 답하므로 응답으로는 접근 경로를
  구분할 수 없다」(2줄) ↔ 같은 파일 케이스 docstring 첫 문단이 같은 논증을 적는다 — 같은 형.
* `opensearch_document_put_ac1.py` 케이스의 「Walks the AC's verification method clause by
  clause…」(7줄)는 ② `docs/prd-opensearch-document-put.md` AC1 의 검증 방법 문면을 절 단위로 따라간다.
  슬라이스 30 이 `session_read_ac1.py` 에서 기록한 것과 같은 형이다.

그리고 이 표면의 판정 산문 한 항이 이웃 표면을 가리키는 것(위 ⚠️ 절)은 **원장 소관 경계의 문제**라
줄 주석 원장 행을 만지는 task 가 함께 볼 자리다.
