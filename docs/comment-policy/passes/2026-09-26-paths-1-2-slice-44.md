# 복원 경로 ①② 판정 — 슬라이스 44: 이름으로 지목돼 있던 ① 재판정 후보 둘 (원장 `#11`·`#32`)

- **task**: `rct_20260926-0009` (`tbm_homelab-k3s-mcp-comment-redundancy`)
- **기준 커밋**: `17eeefe` (슬라이스 43 / PR #232 → #233 → #234 착지 직후. 준비 중 자매 둘이
  착지해 리베이스했다 — 아래 「자매 착지 재실측」 참조)
- **대상**: 줄 주석 원장 행 `#11`(`main.go` · `internal/version/version.go` 36줄) + 행 `#32`
  (`internal/mcp/resource_test.go` 59줄) — 합 **95줄**
- **결과**: **제거 30줄 · 추가 0줄**. `#11` 36 → **16**(`b036567ad0f0` → `a1632bc1fe0a`) ·
  `#32` 59 → **49**(`da695a8db69a` → `ec0ba2425ab2`).
  `판정-합계` 3361 → **3331** · 복원 경로 ①② 판정 **14/69행 → 16/69행**(잔량 55 → **53**) ·
  `등재 범위` 69 · `판정-잔량` 0 · ③④ 69/69(잔량 0, **두 행 재앵커**) ·
  docstring 표면 전건 불변(①② 4/33 · 잔량 29).

## 슬라이스 선택 — 「지목 채널 소진」은 낱말 하나 때문에 거짓이었다

슬라이스 36 이 두 표면 99행 전수의 결과 칸을 정확 문구로 훑어 「`①② 재판정 후보` 지목은 둘뿐이고
소진됐다」를 세웠고, 그 뒤 슬라이스 37~43 은 **크기·가족** 기준으로 행을 골랐다. 그 sweep 을 다시
돌려 보니 결론 자체는 맞다 — 그런데 **그 정확 문구 하나만 세면 놓치는 채널이 하나 더 있었다.**

| 정확 문구 | 히트 | 미판정 행 |
| --- | --- | --- |
| `①② 재판정 후보` | 2 | 0 (`#20`·`#23` 둘 다 ①② 판정 완료) |
| `② 재판정 후보` | 2 | 0 (같은 두 행) |
| **`① 재판정 후보`** | **4** | **4 — `L#5` · `L#11` · `L#27` · `D#18`** |

넷 다 아직 ①② 미판정인 행이고, 넷 다 **다른 행의 특정 자리를 이름으로 지목한다.** 그중
「지목받은 쪽」(= 제거가 일어나야 하는 행)은 줄 주석 표면에서 **`#11`·`#32` 둘**이다 —
`L#5` 는 `#11` 의 후보 둘에 대해 소유자를 세운 뒤 「**제거는 이 PR 이 하지 않는다** — `main.go` 는
원장 `#11` 이고 다른 행의 줄을 지우면 두 행이 함께 재판정 대상이 된다」라 적어 넘겼고,
`L#27` 은 「소유자는 여기다(`resource_test.go` 324–326 은 그 사본, **① 재판정 후보로 인계**)」라
적었다. `D#18` 의 한 건은 자매 표면이고, 그 행을 집으면 자매 줄 주석 `L#46` 이 딸려 오면서
`# Must match GITHUB_APP_INSTALLATION_ID …` 가 `{22,46,59}` 로 누출돼 **넷을 함께 집는 381줄**이
되므로(슬라이스 43 의 실측) 이번 슬라이스에 넣지 않았다.

**사본 집합 닫힘**은 게이트의 `comment_lines()` 를 `importlib` 로 빌려 미판정 84행 전체를
`정규화된 줄 → {원장 행}` 으로 모아 재측정했다(길이 3 이하의 `//`·`#` 구분자 제외):

| 조합 | 줄 | 누출 | 자매 docstring 겹침 |
| --- | --- | --- | --- |
| **`(11,32)`** | **95** | **0** | **0**(둘 다 `.go`) |
| `(60,63)` kind 픽스처 | 79 | 0 | 0 |
| `(56,58)`+`D#24`·`D#26` | 155 | 1(`않는다.` → `D#30`, 파편) | 2행 |
| `(25)` gate.go — 감지의 1순위 | 403 | **1** → `(25,48)` **488줄** | 0 |

🔴 **감지의 1순위를 그대로 받지 않은 이유가 마지막 줄이다.** 감지는 큰 자리로 `#25`
(`internal/mcp/gate.go`·`gate_test.go` 403줄)를 먼저 들었는데, `// AC3's closing clause: no approval
request is made for a call whose …` 가 `#48` 에도 바이트 동일로 있어 **닫히는 조합은 `(25,48)`
488줄**이다 — 직전 최대 슬라이스(43 의 226줄)의 2.2배다. 크기로 고르면 그 조합이 1순위가 되지만,
**이름으로 적힌 수취인이 있으면 그것이 먼저다**(슬라이스 35 가 세운 순서).

## 🔴 인계 명제를 한 단계 아래에서 다시 재니 축이 틀려 있었다

`#11` 의 인계는 「132–133 ↔ `metrics` 패키지 doc(원장 `#1` 소유자) · 146–147 ↔ `server.MetricsApp`
doc 47–50(원장 `#5` 행)」이고, `#32` 의 인계는 「320–322 ↔ `internal/mcp/resource.go::parseDeleteTarget`
doc(원장 `#27` 소유자)」이다. **지목된 소유자 셋이 전부 *다른 주석*이다.**

복원 경로는 ①~④ 넷뿐이고 「다른 주석이 이미 말한다」는 그 넷에 없다 — 슬라이스 43 이
`parseExecTarget` 사슬과 `Attach/PortForwardDefaultReadSeconds` 쌍에 대해 세운 그 판정선이 그대로
적용된다(**README 개정이 선결**이고 한 패스의 재량이 아니다). 그래서 인계를 이렇게 처리했다.

- **`#11`**: 제거의 근거를 **사라지지 않는 쪽**으로 옮겨 ② `docs/prd-metrics.md`·
  `docs/prd-approval-gate.md` 와 ① 바로 아래 줄에서 **전건 다시 세웠다.** 주석→주석 겹침은 근거로
  쓰지 않았다. ⇒ 인계 **집행**.
- **`#32`**: ② 쪽이 서지 않아 **유지로 닫았다.** `prd-approval-gate` AC6 은 `resourceVersion`·`uid`
  의 TOCTOU 를 정하고, AC1 은 「판정은 **verb에만** 걸리고 패치 내용이나 경로를 보지 않는다」라
  **쌍 불일치가 곧 승인–실행 어긋남**이라는 그 3줄의 명제를 축자로 담지 않는다.
  `test-resource-generic` 시나리오 10 은 「`subresource` 는 승인 요청이 만들어지기 전에 거부된다」
  (*무엇*)까지만 적는다 ⇒ 절차 5 「애매하면 남긴다」. ⇒ 인계 **유지로 종결**.

⚠️ `#27` 이 적은 `resource_test.go` **324–326** 은 `internal/k8s/` 가 아니라
**`internal/mcp/resource_test.go`**(=행 `#32`)이고, 줄 번호는 그 패스의 base 기준이라 현재 HEAD 에서는
**320–322** 다. 파일 이름과 줄 번호를 그대로 받으면 원장 `#26`(`internal/k8s/resource.go` 177줄)을
수취인으로 오독한다 — 지목은 **행 서수로 환산해** 판정 집합과 대조할 것.

## 🔴 「낡음이 아니라 아직 묻지 않은 경로」 세 번째·네 번째 실례 — 그리고 번복 1건

제거 근거로 든 ② 문면마다 `git log -S` 로 도입 커밋을 재서 선행 판정일과 비교했다.

| ② 문면 | 도입 | 그 자리를 유지한 판정 | 판독 |
| --- | --- | --- | --- |
| AC1 「그 어긋남은 런타임에 조용히 통과한다」 | `67764e5` (#72) **2026-09-13** | 2026-09-21 (③ 무히트) | 아직 묻지 않은 경로 |
| AC11 「AC19가 결번이 되어 대조는 없어졌지만 선언은 남긴다」 | `723af13` (#83) **2026-09-14** | 2026-09-15 (① 축) | 아직 묻지 않은 경로 |
| AC5 ¶2 graceful degradation 절 | `67764e5` (#72) **2026-09-13** | 2026-09-13 (**② 축**) | **번복** |
| `prd-metrics` 「같은 지점에서 파생한다」·AC5 「인그레스로 공개하지 않는다」 | `f5ca0bd` (#162) 2026-09-20 | 2026-09-21 (③ 셋째 줄) | 축이 다르다 |

**번복은 한 건이고 그 범위는 논거 하나다.** 2026-09-13 판정이 `buildGate` doc 에 대해
「`prd-approval-gate` AC5 와 주제가 겹치지만 AC 는 「거부한다」를 정하고 이 주석은 「그런데도 왜
여기서 죽지 않는가」를 적어 **② 재진술이 아니다**」라고 적었는데, AC5 의 **둘째 단락 전체**가 바로 그
「그런데도 왜 여기서 죽지 않는가」이고 — 「이는 이 서버의 기존 graceful degradation 원칙과 어긋나지
않는다. 통합이 미설정이면 **해당 도구만** 에러를 반환하고 서버는 계속 산다는 원칙은 그대로이며,
여기서는 그 "해당 도구"가 게이트 대상 호출 전체일 뿐이다 … 승인 경로가 죽었는데 변경이 나가면
게이트가 있으나 마나다」 — **같은 커밋 `67764e5`(2026-09-13)에 이미 있었다.** 즉 정책·문서 개정으로
낡은 것이 아니라 **처음부터 사실과 달랐다.** 함께 서 있던 「슬라이스 11(#188)의 제거가 이 doc 을 그
명제의 유일한 **실행 코드 쪽** 자리로 세웠다」도 ② 로 답한다 — 그 명제를 담은 자리가 사라지는 것이
아니라 PRD 본문 세 절로 남는다. 1차 판정 문면은 원장에 그대로 보존했다.

🟢 **그리고 「축이 다르다」를 한 줄로 못박아 둔다.** 2026-09-21 이 132–133·146–147 을 유지한 근거는
3분 표 **셋째 줄**(「#177 본문의 「원본은 코드 doc 쪽이다」 절이 이 doc 을 이름으로 지목 → 사본을
근거로 원본을 지우면 SSOT 가 뒤집힌다」)이고 그것은 **③ 축**이다. 이번 제거의 근거는 **PRD 본문**
(② 축)이라 그 판정과 결론은 **무효화되지 않는다** — 다음 패스가 「③ 판정이 뒤집혔다」로 오독하지
않게 한다(슬라이스 36 이 docstring `#8` 에 대해 같은 구분을 적어 둔 선례).

## 제거 30줄

### 행 `#11` — 20줄 (36 → 16)

| 자리 | 축 | 근거 |
| --- | --- | --- |
| 57–61 전건 → `// prd-approval-gate AC1.` | ② | AC1 축자(표 위 첫 줄) |
| 69–72 전건 → `// prd-approval-gate AC11.` | ② | AC11 축자 |
| 110–112 → `// Not os.Exit, unlike the MCP listener above (prd-metrics AC5).` | ① | **바로 아래 줄** `slog.Error("metrics listener failed; the exposition is off, /mcp is unaffected")` |
| 132–136 → 2줄(sink 순서만) | ②+① | `prd-metrics` 「표면 개요」 축자 · `slog.Warn("… metrics are counted but not exposed")` 축자 |
| 146–148 → 1줄 | ②+① | AC5 「인그레스로 공개하지 않는다」 · 함수 본문 세 줄의 선언 재진술 |
| 239–243 전건 → 1줄 | ② | AC5 ¶2 축자(위 번복) |
| 265–268 → 2줄(`TargetReader` 포인터만) | ① | **바로 아래 줄** `!ok → NewUnavailableTargetReader` + `slog.Warn(… gated calls will be refused)` |

⚠️ 마지막 줄은 2026-09-15 의 「② AC5 재진술이 아니다」를 **뒤집지 않는다** — 그 판정은 ② 축이고
옳다(AC5 는 타입 단정 실패를 열거하지 않는다). 제거 근거는 ① 이다.

### 행 `#32` — 10줄 (59 → 49) · 전부 ②

| 자리 | 근거 |
| --- | --- |
| 536–538 전건 → `// AC12.` (−2) | `prd-resource-generic` AC12 + 검증 방법 축자. 2026-09-21 이 스스로 「**AC12 의 번역 = ② 자리**」로 예고한 자리다 |
| 82–83 (−1) | `test-resource-generic` 시나리오 9 자동화 칸 「… 단위 층이 볼 수 없고 **통합 쪽 몫이다**」 축자 |
| 198–199 (−1) | 시나리오 8 기대 결과 「승인 요청조차 만들지 않음」 축자 — 주석이 그 한국어를 인용 부호째 옮겨 적었다 |
| 272–273 (−1) | 시나리오 10 기대 결과 「인자 검증에서 거부됨」 축자 |
| 411–413 (−2) | AC6 「`watchSeconds`(기본 10, 상한 60) 창 동안 … 모아 반환하고 닫는다」 축자 |
| 484–485 (−2) | AC6 「변경 스트림은 객체 전문을 밀어 주므로 Secret을 `watch`하면 `data`가 그대로 흘러나온다 — `get`과 같은 노출이므로 같은 취급을 받는 것이 맞다」 축자 |
| 642 (−1) | `prd-approval-gate` AC6 「`exec`는 대상 파드의 `uid`로 같은 확인을 한다 (같은 이름의 새 파드는 다른 파드다)」 축자 |

**번복 0건** — ⑵⑶⑷⑸⑹ 는 2026-09-21 이 「③ **무히트**로 남긴 것」 목록에 base 줄 번호로 열거한
자리들이고(82–84 · 415–418 · 468–470 · 488–491), ③ 무히트는 **축 한정 유지**라 ①② 는 첫 판정이다.
**소유자 포인터는 전부 남겼다** — `(updatePairs)` · `(deletePairs)` · `(AC6)` · `// AC12.` ·
`// AC1/AC10.`(원장 `#23` 이 고정한 처리: Go 단위 테스트에는 `검증 AC:` 같은 기계 판독 선언이 없어
그 포인터가 AC ↔ 코드를 잇는 유일한 자리다).

## 🔴 유지로 남긴 ② 후보 둘 — 다음 패스가 다시 묻지 않게

- **350–352 `TestDeleteIsRefusedWithoutApproval` 머리** — 시나리오 10 자동화 칸이 **축자로** 담는다
  (「그 거부 단언들이 공허해지지 않는다는 대조군은 `TestDeleteIsRefusedWithoutApproval` 이 선다
  (게이트를 아예 타지 않는 도구는 「게이트가 안 불렸다」를 전부 통과한다)」). 그런데 2026-09-15 가
  「이 한 문장이 없으면 다음 사람이 「중복 같은데」로 읽고 지워 **거부 단언 일곱 개가 동시에
  공허해진다**」로 세운 **서 있는 편집 구속**이라 3분 표 둘째 줄로 유지한다 — ② 가 사실을 복원해도,
  이 주석의 기능은 *그 제거 자체를 막는 것*이다.
- **365–367 `TestUpdateReplacesWithTheCallersManifest` 머리** — 시나리오 8 「전체 교체가 PUT으로
  반영됨」과 겹쳐 보이나, 2026-09-21 이 「AC7 절은 실행 코드 쪽 사본(원장 `#26` `parseManifest`)과
  **서로 복원되지 않는다**」 + 「② 재진술은 없다 — AC8 문면은 인용하지 않고 포인터로만 가리킨다」로
  ② 축을 **실질 분석해** 세운 유지형(유형 ⑵)이다 ⇒ 절차 5 로 보존.

## 자매 착지 재실측 — #234 가 내 ② 원본을 고쳤다

준비 중 main 이 `0bc77de` → **`17eeefe`** 로 움직였다(#233 CI · **#234 `prd-approval-gate` AC1 개정**).
리베이스는 충돌 0이고, 세 ② 인용을 **개정 후 본문에서 다시 확인했다.**

- 🟢 **#234 는 AC1 에 두 겹 명세를 *덧붙였고* 내가 인용한 절은 문면 그대로다** — 「선언되지 않은 쌍을
  행사하는 도구는 **등록 자체가 거부된다** — 선언과 실제 행사가 어긋나면 게이트 표가 거짓말을 하게
  되고, 그 어긋남은 런타임에 조용히 통과한다」(`grep -c` 각 1). AC5 ¶2·AC11 절도 불변이다
  (AC11 은 문장이 줄바꿈을 걸쳐 있어 한 줄 `grep` 으로는 0히트가 난다 — 줄 단위로 확인할 것).
- 🔴 **오히려 제거 근거가 강해졌다.** #234 의 `doc-tracker` 변경 이력이 로컬 프로브로 적는다 —
  「기동 검사(`validateRegistry`)는 이름 일치와 핸들러 존재만 보고 **빈 선언을 통과시켜**, 선언 없는
  도구가 기동하고 승인 없이 쿠버네티스에 닿는다」. 즉 내가 걷는 `main.go` 57–61 의 「so it stops the
  process here instead」는 **현행 구현에 대해 거짓**이고, 정책의 「충돌하면 주석이 틀린 것으로 본다 —
  어긋난 주석은 제거 근거가 오히려 강해진다」가 그대로 적용된다. 남긴 `// prd-approval-gate AC1.`
  포인터가 이제 「약속은 저기, 구현은 미충족(매트릭스 ⚠️)」으로 정확히 가리킨다.
- 🟢 **수치 충돌 0** — #234 는 `docs/` 에 `.md` 를 더하지 않았고 허브 집계 줄(124행)을 건드리지
  않았다. 내 `100/100 · 정책 60` → `101/101 · 정책 61` 은 그대로 서고, 스캔 범위
  (`main.go`·`internal`·`tests`·`scripts`)에 든 파일도 0건이라 `판정-합계` 3331 이 불변이다.

## 무영향 증명 — 주석-only 3중 종결

1. **비주석 diff 0줄** — `git diff -U0 | grep -E '^[+-][^+-]' | grep -vE '^[+-][[:space:]]*(//|#)'`
   가 **0건**. 두 파일 다 `.go` 라 docstring 표면 사각지대가 없고(`ast` 대조가 필요한 `.py` 0건),
   `gofmt -l .` 도 0줄이다.
2. **게이트 4종 rc=0** — `check_comment_policy.py` · `check_mock_policy.py` ·
   `check_doc_inventory.py` · `tests/integration/check_ac_mapping.py` 전부 0(파이프 없이 `$?` 로 읽음).
3. **Go 전량 초록** — `go build ./...` · `go vet ./...` · `go test ./...` 전부 통과.

## 탐침 — 이 초록이 공백을 서명하지 않는다는 증거

| 탐침 | 조작 | 기대 | 실측 |
| --- | --- | --- | --- |
| ⒜ | `복원경로①②-잔량` 53 → 54 | `[R11]` 16+54≠69 | ✅ |
| ⒝ | 행 `#11` 의 `복원경로 ①② 판정 @지문` 앵커만 옛 지문으로 | `[R11]` 현재 지문 미결속 | ✅ |
| ⒞ | 행 `#32` 결과 칸에서 `복원경로 ②:` **토큰만** 제거 | `[R11]` | ✅ |
| ⒟ | `판정-합계` 3331 → 3361(갱신 누락 재현) | `[R4]` | ✅ |
| ⒠ | 패스 파일을 두고 허브 집계 미갱신 | `[D4]` 100≠101 · 정책 60≠61 | ✅ |
| ⒡ | 양성 대조 — 판정 완료 행 `#43` 에 동종 편집 | rc=0 | ✅ |

## 다음 슬라이스로 넘기는 것

- **살아 있는 `① 재판정 후보` 지목 둘이 남는다** — `L#5`(`internal/server/*` 97줄, 자기 행의
  「unavailable 케이스 머리말 셋 … 묶어 이월」 포함)와 `D#18`
  (`github_app_installation_token_ac5.py` 29줄). 🔴 `D#18` 을 집으면 자매 `L#46` 이 딸려 오고
  `# Must match GITHUB_APP_INSTALLATION_ID …` 가 `{22,46,59}` 로 누출돼 **`(22,46,57,59)`+자매 넷 =
  381줄**이 된다. `L#5` 는 단독으로 닫힌다(누출 0).
- **닫히는 조합 실측(base `17eeefe`)**: ~~`(11,32)` 95~~ **소진** · `(60,63)` kind 픽스처 **79**
  (누출 0 · 자매 0) · `(56,58)`+`D#24`·`D#26` **155**(혼합 — 무접촉 증명은 docstring 걷은 AST 대조) ·
  `(5)` **97**(누출 0) · `(26)` **177**(누출 0 · 자매 0) · `(7)` 126 · `(50)` 124 · `(1)` 76.
- 🔴 **`(25)` 는 `(25,48)` 488줄로만 닫힌다** — 크기로 1순위지만 직전 최대의 2.2배다. 쪼개면
  `// AC3's closing clause …` 의 사본이 갈려 **공유 규약의 첫 이탈**이 열린다.
- 🔴 **`L#24`(`internal/gatekeeper/gatekeeper.go` 125줄)는 이 논거로 걷지 말 것** — 두 PRD 상호 지목
  문면이 없고 2026-09-13 ③④ 판정의 「반환값 계약 → 유지」가 서 있다(슬라이스 40 이 못박은 인계).
- 🟢 **`① 재판정 후보` 라는 정확 문구를 다음 sweep 에 반드시 넣을 것.** 「지목 채널 소진」은
  `①② 재판정 후보` 하나만 세어 내린 결론이었고, 그 낱말 차이가 네 건을 두 패스 동안 가렸다.
