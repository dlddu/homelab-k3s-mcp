# 2026-09-21 — 복원 경로 ③④ 소급 판정 슬라이스 12 · **③ 모집단이 가장 작은 두 행(`tests/k8s/kind/resource-generic-*` + `resource_generic_ac1.py` · `tests/integration/opensearch_*_ac*.py`)**

`rct_20260921-0018` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 11](2026-09-21-paths-3-4-slice-11.md)이 PR 8 정렬의 나머지 두 행을 닫고 잔량 11행을 지문·경로·줄수로 명명했다. 이 패스는 그 11행의
③ 모집단을 **전체 이력(229 커밋) `git log -- <파일>`** 로 다시 재어 가장 작은 둘 — `9013205ddb59`(45줄 · PR 5)과 `31d67e48d0e5`(4줄 · 경로 기준
PR 6)을 한 번에 묻는다. 슬라이스 9~11 이 「PR 수 오름차순」으로 잰 정렬을 그대로 잇는 것이고(다음 칸은 `dfc76f31359a` · `469b96c4df5a` 의 9), 두
행은 #126 을 공유한다. 합집합 **11건**(#41 포함), 전부 squash 착지, 리뷰 코멘트 0(이슈 코멘트는 #43 의 「CI 실측」 하나뿐 — 판정 무관),
`git log` 제목이 전부 `(#N)` 을 단다 → ④ 대상 부재. `git blame` 으로 49줄 전부가 이 커밋들에 귀속된다. 줄 번호는 base `b8a75c9`(#188) 기준이다.

## 이 패스의 범위 (완료 기준)

| | 기준 | 값 |
| --- | --- | --- |
| ⑴ | 줄 주석 ③④ 잔량 마커 | **11 → 9**(③④ 56/67 → **58/67**행 — base `b8a75c9`/#188 기준) |
| ⑵ | 실제 제거 | **1문장 · 0줄** — `resource-generic-fixture.yaml` 142 의 둘째 문장(`9013205ddb59` → `1d28ffe525ff`, 45 불변). `31d67e48d0e5` 는 0줄·지문 불변. 비주석 바이트 0, YAML 128 문서 deep-equal |
| ⑶ | 줄 주석 판정 합계 | **3415 불변**(base `b8a75c9` 기준 — 자매 착지로 base 가 움직이면 게이트 실측으로 재기입) |
| ⑷ | docstring 표면 | **무이동**(10/32 · 잔량 22 · 합계 2124) |

**범위 밖**: 줄 주석 잔량 9행(`f73c08478f50` server 117 · `b036567ad0f0` main.go 36 · `e82057fcdeba` k8s 픽스처 6파일 29 · `5aa2ddea6731` 공유 헬퍼 132 ·
`dfc76f31359a` session_list 46 · `469b96c4df5a` aws_config_get 32 · `87dce628e24d` dear_baby 4 · `4a12a66791fc` k8s/resource 195 · `921ed7505e16` mcp/resource.go 56 —
게이트 R9 의 앵커 부재 행 그대로), docstring 잔량 22행, 아래 ③ 재판정 후보.

## 측정 — 두 행과 ③ 의 모집단

| 행 | 파일 | 줄 | PR | 본문 | 머지 커밋 |
| --- | --- | ---: | --- | ---: | --- |
| `9013205ddb59` → `1d28ffe525ff` | `tests/k8s/kind/resource-generic-fixture.yaml` · `tests/integration/resource_generic_ac1.py` · `tests/k8s/kind/resource-generic-samples.yaml` | 45 → 45 | **#86** · **#88** · **#100** · **#103** · #126 | 5,704 · 4,806 · 4,158 · 5,739 · 2,579자 | `e4304b3` · `2d62b62` · `fea75ac` · `41ee356` · `25532ff` |
| `31d67e48d0e5` | `tests/integration/opensearch_document_delete_ac{1..5}.py` · `opensearch_document_put_ac{1..5}.py` · `opensearch_search_ac{1..4}.py` | 4 | **#41**(저작) · #43(이동) · #44 · #63 · #65 · #66 · #126 | 4,521 · 4,270 · 6,398 · 5,275 · 5,468 · 4,771 · 2,579자 | `56d2ef6` · `b25798e` · `4e00d36` · `ed93369` · `c4f7c09` · `85132ae` · `25532ff` |

굵은 것이 주석을 들여온 PR 이다. 첫 행: #86 이 픽스처·e2e·samples 를 신설(14줄), #88 이 이 정책의 첫 판정으로 머리말을 포인터로 접음(8줄), #100 이
페이징 ConfigMap 120 의 머리말(9줄), #103 이 다중 컨테이너 파드의 머리말(14줄); #126 은 세 파일에 남긴 줄 주석 0. 둘째 행: **경로 기준 `git log` 는 저작자를
놓친다** — 네 줄 전부 `git blame` 이 #43 을 가리키지만 #43 은 `opensearch.py` 를 AST 세그먼트로 떠서 13 파일로 옮긴 「순수 이동」이고, `git log -S '<문장>'` 로
재실측하니 네 줄은 **#41**(2026-08-14, `opensearch.py`)이 썼다. 분할·개명으로 생긴 행은 경로 모집단에 `-S` 를 한 번 더 물어야 한다(#44·#63·#65·#66·#126 은
14 파일 중 다른 파일을 건드리거나 docstring 만 만졌고 이 네 줄엔 무접촉).

**본문과 코드의 대조**(#24·#28 선례): #41 본문은 AC3(AssumeRole·SigV4)를 「관측 수단 부재 — ⬜ 보강 필요」로 **내린** 08-13 초안이고, 같은 PR 의 커밋
`56d2ef6` 은 08-14 에 http-trace 기록 프록시(`tests/k8s/kind/http-trace.yaml` 445줄)와 `assert_assumed_role_access` 를 실어 AC3 를 ✅ 로 올렸다 — 본문이
자기 커밋보다 낡았다. ② `docs/doc-tracker.md` 08-14 이력 행이 그 승격을 적지만 「서명 스코프(`s3`/`aoss`)」까지이고 서명 헤더 목록은 아니다. 그 밖의 낡음:
#86 「78 − 1 − 31 = 46」·#100 「구현 대기 29→28」·#103 「네 워크로드」·#41 「✅45·⬜18」·#43 「primary 13 → 24」는 그때 값이고 판정 대상 주석과 무관(#103 의
「네」만 이 패스가 지우는 문장 자체다).

## 판정 기준 — 슬라이스 1~11 의 구분 그대로

1. **본문이 그 주장을 대신 말하면 지운다.** 본문이 *결과*·*무엇*만 적고 주석이 지는 *이유*·*교차 제약*은 적지 않으면 「다른 주장」.
2. **본문이 스스로 주석을 가리키면 원본은 주석 쪽** — 유지. 이 층에서는 #88(이 정책의 패스)이 「포인터는 남기고 인용만 걷었다」·「그 단언 자리 쪽을
   남겼다」로 스스로 남긴 자리가 그것이다(슬라이스 11 의 #62 「교차 제약만 남겼다」와 동형).
3. **본문이 결론을 담더라도 앞으로의 편집을 구속하는 서 있는 불변식이면 유지**(README 3분 표 둘째 줄). 이 층에서는 **금지**(⚠️ `$(` 를 쓰지 않는다),
   **교차 제약**(레이블을 떼면 모집단이 123 · `LOG_LINE_COUNT`), **무엇이 조용히 통과하는가**(컨테이너 하나면 후보 이름을 잴 대상이 없다 · 객체 하나면
   1 → 1)가 여기 든다.

이 층에서 새로 확인한 것: **①② 축이 「관측된 사실」로 남긴 과거형 문장은 ③ 축의 첫째 줄이다.** 09-15 판정(#103 저작 패스)이 「기존 네 워크로드는 전부
단일 컨테이너였다」를 「관측된 사실」로 유지했는데, 그 사실을 복원하는 경로가 바로 #103 본문이고(2026-09-20 결정 — ③ PR 본문 = 다음 편집자의 복원 경로),
문장은 과거형·그때 값이라 앞으로의 편집을 구속하지 않는다. 같은 문단의 앞 절(apiserver 거동 → 무엇이 조용히 통과하는가)은 둘째 줄이라 남는다 — 절 단위
판정(슬라이스 9·10 과 같다).

## 항목별 ⑴ — `9013205ddb59` → `1d28ffe525ff` (제거 1문장 · 0줄)

### 제거 — `resource-generic-fixture.yaml` 142 둘째 문장

```
#    대상 자체가 없다. `tests/k8s/kind/` 의 기존 네 워크로드는 전부 단일 컨테이너였다.
```
→ 「대상 자체가 없다.」 까지만 남긴다. 폭 98 col 제한 때문에 ⑴ 문단 3줄을 문면 변경 없이 2줄로 재배치할 수 없어 줄 수는 45 그대로다 — 셋째 줄만
짧아지고 지문이 움직인다.

**③ #103 「`tests/k8s/kind/`의 네 워크로드가 전부 단일 컨테이너(`pause` 또는 `boom`)였고, 기준선 `workload-fixture`는 `pause`라 로그를 한 줄도 내지
않는다」** ✅ 축자. 과거형 관측이고(「네」는 이미 그때 값 — 이 파일 자신이 다섯째 워크로드다), 앞으로의 편집을 구속하지 않으며, #103 은 「자리는 「픽스처」
절이 이미 지정해 둔 곳」이라 적을 뿐 이 주석을 원본으로 지목하지 않는다. 남긴 앞 절 「apiserver 는 파드에 컨테이너가 하나뿐이면 그냥 그것을 읽어 주므로,
단일 컨테이너 파드로는 「후보 이름이 제시됨」을 잴 대상 자체가 없다」는 둘째 컨테이너 `quiet` 를 지우는 편집이 무엇을 공허하게 만드는가(둘째 줄).

### 유지 — 44줄

| # | 주석 | ③ | 판정 |
| --- | --- | --- | --- |
| `resource-generic-fixture.yaml` 1–2 · 7–8 | 「무엇을 담고 어떻게 자라는지는 `docs/test-resource-generic.md` 「픽스처」 절이 정한다」 · 「갈라 둔 이유(`kubectl apply` 가 종류를 해석하는 시점)는 위 「픽스처」 절에 있다」 | #86 「「픽스처」 절에 … 파일이 갈린 이유 기록」 · **#88 「세 자리 모두 포인터는 남기고 인용만 걷었다」** | 유지 — ② 포인터, **셋째 줄**(정책 패스가 스스로 남긴 자리) |
| 4–5 | 객체가 하나뿐이면 1 → 1 이라 줄었다는 것이 관측되지 않고 셀렉터를 통째로 무시하는 구현도 통과한다 — 그래서 종류마다 둘씩 | **#86 「객체가 하나면 1 → 1 이라 셀렉터를 통째로 무시하는 구현도 통과한다」** ✅ 축자 | 유지 — 한 종류를 하나로 줄이는 편집이 ac1 의 「엄격히 줄인다」 단언을 공허하게 만든다(둘째 줄). ③ 축자와의 경계가 좁다 — **③ 재판정 후보(부분)** |
| 36–38 | 프린터 컬럼 — 기본 컬럼만 있으면 CRD 가 자기 컬럼을 실어 보낼 수 있는지가 관측되지 않는다 | ❌ — #86 은 「픽스처 CRD 1종」 | 유지 — 무엇이 조용히 통과하는가(09-14 판정 그대로) |
| 53–54 | 셀렉터도 엔드포인트도 없는 Service — 좌표로 조회될 대상으로 있다 | ❌ — #86 은 「Service·ConfigMap·Ingress 각 2개」 | 유지 — 픽스처가 실환경과 갈리는 지점 |
| 94–96 | kind 에는 인그레스 컨트롤러가 없다 — 그래도 되는 이유 | ❌ | 유지 — 같은 계열 |
| 138–139 · 145 · 148 | 머리글 「다중 컨테이너 파드(시나리오 5)」 · 구분 | #103 제목 | 관례 |
| 140–142 앞 절 | 컨테이너가 둘이어야 `container` 누락 거부가 관측된다 — apiserver 는 하나뿐이면 그냥 읽어 준다 | **#103 「컨테이너가 하나뿐이면 apiserver가 그냥 읽어 준다」** ✅ | 유지 — 둘째 줄(위 「제거」 절) |
| 143–144 | 로그를 실제로 내는 컨테이너가 있어야 `tailLines` 절단이 관측된다 — 기준선 `workload-fixture` 는 `pause` 라 0 → 0 | **#103 「모집단이 0줄이면 「줄었다」가 0 → 0 이 된다」** ✅ 축자 | 유지 — 이 파드가 **왜 존재하는가** + ac5 를 기준선으로 되돌리지 말라는 교차 제약(슬라이스 11 `auth-fixture.yaml` 3–5 와 동형). 첫째 줄(택하지 않은 길)과의 경계가 좁다 — **③ 재판정 후보(부분)** |
| 146–147 | `chatty` 는 찍고 그대로 산다 — 찍는 동안만 살면 `restartPolicy: Always` 가 재시작시켜 줄 수가 달라진다 | ❌ — #103 은 「상주하는 `chatty`」(무엇) | 유지 — 플레이키를 피한 형태(09-15 판정 그대로) |
| 149–151 | ⚠️ 셸 인자에 `$(` 를 쓰지 않는다 — kubelet 이 먼저 확장한다 | **#103 「⚠️ 셸 인자에 `$(`를 쓰지 않았다 … `seq \| sed` 파이프는 그 문법을 전혀 쓰지 않는다」** ✅ 축자 | 유지 — 앞으로의 편집을 구속하는 **금지**(둘째 줄, `auth-fixture.yaml` 28–29 「Do not attach …」와 동형) |
| 194–195 | ConfigMap 120 — 기본 페이지 100 이므로 두 페이지가 다 관측되는 가장 작은 모집단 | #100 「1회차 100건 … 2회차 20건」 — *무엇* | 유지 — 왜 그 수인가(09-15 판정 그대로) |
| 197–200 | 전용 레이블을 다는 이유 — 셀렉터 없이 세면 123 이 되어 「나머지 20건」이 성립하지 않는다 | **#100 「좁히지 않으면 모집단이 123」** ✅ 축자 | 유지 — 레이블을 떼는 편집이 무엇을 깨는가(교차 제약, 둘째 줄) |
| 200–202 | 자동 생성 객체에 기대어 수를 맞추는 길(119 + `kube-root-ca.crt`)은 클러스터 버전·컨트롤러 타이밍에 의존하므로 택하지 않았다 | **#100 「택하지 않았다」** ✅ 축자 | 유지 — 09-15 가 「지우면 다음 사람이 119 로 줄여 그 의존을 되살린다」로 남긴 편집 구속(둘째 줄)과 택하지 않은 길의 경위(첫째 줄) 사이 — 애매하면 남긴다. **③ 재판정 후보(부분)** |
| `resource_generic_ac1.py` 36 · 41 · 46 | `#:` 픽스처 레이블·객체 이름 · CRD 좌표 · 전체 레이블 | ❌ — #86 은 케이스 넷을 적을 뿐 | 유지 — 상수 ↔ 픽스처 매핑(원장 #17 선례) |
| 108 | 잘린 목록으로 개수를 비교하면 「줄었다」가 페이지 경계의 그림자일 수 있다 | **#86 「잘린 목록으로 개수를 비교하면 … 그림자일 수 있다」** ✅ 축자 | 유지 — **셋째 줄**: #88 이 `#:` 사본을 지우며 「그 단언 자리 쪽을 남겼다」로 소유자 지정 + `truncated is False` 단언이 왜 이 형태인가 |
| `resource-generic-samples.yaml` 1–2 | 포인터 + 「ci.yml 이 CRD → `established` → 이 파일 순」 | #86 「적용 스텝 1개(CRD → `kubectl wait` → 인스턴스)」 — *무엇* | 유지 — 이 파일이 CRD 종류를 가리켜도 되는 이유(ci.yml 과의 교차 제약). #88 갈림 승계 — **③ 재판정 후보(부분)** |

## 항목별 ⑵ — `31d67e48d0e5` (제거 0줄)

| # | 주석 | ③ | 판정 |
| --- | --- | --- | --- |
| `opensearch_search_ac1.py` 71–72 | 「The non-matching document is in index_a and searchable, so its absence from the queries above is the query filtering, not a missing document」 | ❌ — #41 은 「케이스별 유일 질의 토큰 … 다른 케이스의 문서를 볼 수 없게」(격리), #43 은 「순수 이동」 | 유지 — 대조군 문장(테스트 형태를 구속하는 논증, 슬라이스 9 `resource_test.go` 154–155 와 동형) |
| `opensearch_search_ac3.py` 52–53 | 「The payload hash has to be signed too, or the body could be swapped in flight without invalidating the signature」 | ❌ — #41 본문은 AC3 를 ⬜ 로 내린 초안(위 대조), ② `doc-tracker` 08-14 행은 「서명 스코프」까지 | 유지 — `x-amz-content-sha256` 단언이 왜 이 형태인가(단언 이유) |

나머지 12 파일은 주석 0줄이다(09-05 판정 그대로).

## 결론

| | 값 |
| --- | --- |
| 이 패스의 제거 | **1문장 · 0줄**(`resource-generic-fixture.yaml` 142) — 비주석 바이트 0(`grep -vE '^\s*#'` diff 0 · `yaml.safe_load_all` 128 문서 deep-equal) · 다른 16 파일 0바이트 |
| 줄 주석 판정 합계 | **3415 불변** |
| ③④ 판정 완료 / 전체 | 56/67 → **58/67**, 잔량 **11 → 9** |
| docstring 표면 | **무이동**(10/32 · 22 · 2124) |
| 갈려서 남긴 것 | 갈림 0 · 부분 후보 4(4–5 · 143–144 · 200–202 · samples 1–2) |

**관찰 둘.** ⑴ **경로 기준 ③ 모집단은 분할·개명 뒤 행에서 저작자를 놓친다** — `31d67e48d0e5` 의 `git blame` 은 전부 이동 커밋(#43)을 가리키고 경로
`git log` 도 #43 부터다. `git log -S` 가 #41 을 찾았다. 남은 9행 중 `469b96c4df5a`·`dfc76f31359a`(#42~#44 분할 산물)·`87dce628e24d` 도 같은 형일
수 있다. ⑵ ①② 축이 「관측된 사실」로 남긴 과거형 문장은 ③ 의 첫째 줄이다 — ③ 축은 그 사실이 어디로 복원되는가를 묻고, 답이 PR 본문이면 주석은
경위다. 반대로 「택하지 않은 길」은 09-15 가 편집 구속으로 남긴 근거가 서 있어 후보로만 둔다.

## 다음 패스의 입력

- 줄 주석 잔량 **9행** — 게이트 R9 의 앵커 부재 행: `f73c08478f50`(`internal/server/` 117줄 · PR 26) · `b036567ad0f0`(`main.go`·`version.go` 36줄 · PR 12) ·
  `e82057fcdeba`(k8s 픽스처 6파일 29줄 · PR 16) · `5aa2ddea6731`(공유 헬퍼 7파일 132줄 · PR 29) · `dfc76f31359a`(session_list 46 · PR 9) · `469b96c4df5a`(aws_config_get
  32 · PR 9) · `87dce628e24d`(4 · PR 15) · `4a12a66791fc`(`internal/k8s/resource.go`·`resource_test.go` 195 · PR 14) · `921ed7505e16`(`internal/mcp/resource.go` 56 ·
  PR 10). PR 수는 전체 이력 `git log -- <파일>` 실측(2026-09-21, 229 커밋) — 다음 칸은 PR 9 의 둘(`dfc76f31359a` · `469b96c4df5a`, #42~#44·#62·#63·#65·#66·#126 을
  공유해 한 패스로 묶을 수 있다 — 단 `-S` 로 저작 PR 을 더 물을 것). 슬라이스 11 이 ① 소유자로 인용한 `main.go::buildGate` doc 과 `_auth_variant.py` 21–27 은
  여전히 `b036567ad0f0` · `5aa2ddea6731` 에 있다 — 그 행을 물을 때 슬라이스 11 의 제거가 그쪽 사본을 유일한 자리로 만들었음을 세울 것.
- ③ 재판정 후보 누적: 이 패스의 넷(`resource-generic-fixture.yaml` 4–5 · 143–144 · 200–202 · `resource-generic-samples.yaml` 1–2) + 슬라이스 11 의 둘 + 슬라이스 10 의
  둘 + 슬라이스 9 의 하나 + 슬라이스 8 의 하나 + 슬라이스 7 의 둘 + 슬라이스 6 의 셋 + 슬라이스 5 의 셋 + 슬라이스 4 의 하나 + 슬라이스 3 의 셋 + 슬라이스 2 의
  다섯(누적 **23**). 갈림 승계: `ac4` 빈 맵 1줄(#81 이래) · `TRACE_ROUTES` 앵커 1줄(#62 이래) · `resource_test.go` 154–155 · 324–326(슬라이스 9) — 이 패스 신규 0.
- docstring 잔량 **22행** — 여전히 한 번도 이 축으로 묻지 않은 표면.
- 정책 소유자 몫(승계 3 · 신규 0 — 사례 1 추가): PR 안의 개별 커밋 메시지가 ③ 인가(`http-trace.yaml`). **③ 본문이 코드와 어긋날 때의 규칙**(슬라이스 6 #24 ·
  슬라이스 11 #28 · 이 패스의 #41 — 본문은 08-13 초안, 같은 PR 의 08-14 커밋이 http-trace 픽스처·AC3 단언을 실었다). 한 덩어리로 「유지」된 다줄 논증 안의
  개별 절을 ①② 축이 다시 물어야 하는가(슬라이스 7·9). 이 패스는 README 개정 없이 판정 가능했다.
