# 2026-09-24 — 복원 경로 ③④ 소급 판정 슬라이스 26 · **docstring 표면 중간 칸 두 행 (원장 `#16`·`#17`, 223줄 / 14파일)**

`rct_20260924-0002` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 24](2026-09-22-paths-3-4-slice-24.md)가 넷째 칸을 닫아 잔량을 13 → 10 으로 내렸다.
다섯째 칸(`#8`·`#10`·`#21`)은 **아직 열린 PR #204 가 집고 있고**, 그 PR 이 「단일·소수 파일
칸은 소진됐다 — 다음 칸 제안은 `#17`, 그 다음 `#16`」으로 인계했다. 이 패스가 그 **둘을 함께**
묻는다. 줄 번호는 base `25bc0c1`(#206) 기준이다.

> 슬라이스 25 의 패스 파일은 아직 main 에 없다(PR #204 에 실려 있다). 이 패스가 그 파일을
> 링크하지 않는 것은 의도다 — 링크하면 허브 도달성 게이트(D3)가 **#204 의 착지 순서에
> 종속**된다. 인계 문면은 그 PR 의 실행 지침에서 읽었고 여기에 번호로만 적는다.

## 선택 — 인계를 받되 세 가지를 다시 쟀다

**⑴ 잔량을 게이트 요약 줄이 아니라 `parse_ledger()` + `check_paths34()` 재실행으로 독립
재계산했다** — docstring 원장 **32행 = 완료 22 + 잔량 10**, 잔량 행의 줄 수 합 **1,207**
(표면 2,075줄의 58%). 두 마커와 전건 일치한다.

**⑵ 인계에 적힌 `#16` 의 수치가 낡아 있었다.** 슬라이스 25 의 「범위 밖(후속)」 목록은
`#16` 을 **128 · 9파일**로 적었는데, 이 패스의 트리거인 #206 이 그 사이 `_gatekeeper.py` 모듈
머리말을 **+8줄**(128 → **136**, 지문 `2b73de45ad59` → `8574725e4add`) 키웠다. 인계 목록의
수치는 적힌 순간 낡는 값이라, 칸을 집을 때마다 게이트 파서로 다시 잰다.

**⑶ `#21` 은 일부러 비웠다.** 같은 트리거 #206 이 `#21`(`resource_generic_ac14.py`)의 docstring
자체를 갈아치워(지문 `2bc8e558feeb` → `0b2ddd8e5fde`) 열린 자매 PR **#204** 가 `DIRTY` 가 됐고,
그 PR 의 실행 지침이 이 경우를 **「그 행은 이 계획의 범위 밖이다」**로 스스로 규정한다. 내가
같은 원장 줄을 동시에 집으면 착지 순서에 종속되므로 비워 두었다 — **이 패스의 두 행과 #204 의
세 행(`#8`·`#10`·`#21`)은 서로소**라 어느 쪽이 먼저 가도 된다(겹치는 것은 두 합계 마커와
`doc-tracker` 뿐이고 그것은 수치라 재측정으로 푼다).

## 얽힘 — 양방향 실측

두 표면 99행의 결과 칸을 전수 훑어, ⑴ **나를** 선례로 인용하는 행과 ⑵ **내가** 인용하는
선례·정책 문면을 갈라 쟀다.

| 방향 | 실측된 얽힘 | 이 패스의 처리 |
| --- | --- | --- |
| 나가는 | docstring `#26`(`resource_generic_ac17.py`)이 `#17` 의 `rg#16` 자리를 **「반대 이유로 반대 선택을 한 자매」**로 인용한다(「primary 에는 담당자가 없어 모드가 걸리지 않는다」) | 그 자리를 **유지**로 확정 ⇒ 인용이 낡지 않는다 |
| 나가는 | 줄 주석 `#44` 가 **같은 다섯 파일**의 줄 주석 표면을 덮고 있고, 그 행이 **PR #132 본문에 대해 이미 ③ 를 물었다**(2026-09-21 슬라이스 3) | 그 판정을 **승계**한다 — 아래 ⓐ |
| 나가는 | docstring `#15`(`resource_generic_ac5.py`)·`#20`(`ac12`)·`#30`(`_eventlog`)이 이 두 행의 파일 이름을 **선례로만** 든다(대체 수단·ConfigMap 선례·타이머 경주) | 인용이 가리키는 자리는 전부 유지 대상 ⇒ 얽힘 없음 |
| 들어오는 | `#16` 의 유지 논거 일곱(⑴~⑺)이 드는 근거는 **시나리오 문서 부재**(② 축)다 | ③ 축은 아직 안 물었으므로 낡지 않았다 — 이 패스가 그 축을 연다 |
| 들어오는 | `#17` 의 유지 논거는 **「시나리오 문서의 재진술이 아니다」**(② 축) | 같다. ③ 가 뒤집는 다섯 자리만 번복하고 ②의 근거 절은 이력에 그대로 남긴다 |

🔴 **「시나리오 문서에 없다」는 ③ 에 대해 아무것도 말하지 않는다.** 두 행 모두 ①② 축에서
「문서가 말하지 않는 설계의 이유」라는 한 가지 근거로 통째 유지됐는데, ③ 를 물으니 `#17`
에서는 다섯 자리가 저작 PR 본문의 **표 한 칸**에 축자로 들어 있었고 `#16` 에서는 **한 자리도**
없었다. 같은 ② 근거를 진 두 행의 ③ 수확이 정반대로 갈린다.

## 이 패스의 범위 (완료 기준)

docstring 원장 **두 행**에 ③④ 를 물어 판정하고 현재 지문에 못박는다(R10).

| | 전 | 후 |
| --- | --- | --- |
| docstring ③④ 판정 | 24/32 (잔량 **8**) | **26/32 (잔량 6)** |
| 판정 합계 | 2,075줄 | **2,061줄** |
| `#16` `_gatekeeper.py` 외 8파일 | 136줄 `8574725e4add` | **136줄 `8574725e4add`**(제거 0) |
| `#17` `approval_gate_ac6.py` 외 4파일 | 87줄 `16c9f3161b60` | **73줄 `7b09b3fb3d80`** |
| 줄 주석 표면 | 67/67 · 잔량 0 · 3,337줄 | **무이동** |

완료 기준은 **절대 지문이 아니라 부모 대비 상대값**이다 — **③④ 잔량 −2행** · **docstring 합계
−14줄** · **줄 주석 표면 무이동** · **제품 코드 0줄**. 자매 PR 이 분 단위로 착지하는 레포라
절대값은 태어날 때부터 낡는다.

범위 밖: docstring 잔량 **8행**(아래) · ①② 축 재판정 · 주석의 *정확성* · `README.md` 개정
(정책 소유자 몫) · 제품 코드.

## 측정

**③ 모집단은 세 PR 뿐이다.** 14파일 전수 `git log --follow` 가 돌려주는 것은
`#16` → **#106**(저작, 9파일 전부) · `#17` → **#132**(저작, 5파일 전부) · 그 위에 트리거
**#206**(14파일 중 11파일) 하나다. 개명·분할 이력 0이라 경로 `git log` = `--follow` 이고
분할 산물 함정이 서지 않는다.

**리뷰 코멘트는 세 PR 모두 0건**(`/pulls/<N>/comments` 전수) ⇒ ③ 의 실체는 **본문뿐**이다.
이슈 코멘트는 #106·#132 각 1건(재푸시 기록)으로 판정 대상 문면 0.

**④ 는 이 head 에서 재실측했고 여전히 대상 부재다.** 최근 120커밋에서 제목에 `(#N)` 이 없는
**24건**을 뽑아 커밋마다 `/commits/<sha>/pulls` 를 조회했다 — 연결 PR 이 없는 **23건은 전부**
`chore(deploy):` 이미지 핀이고 건드린 파일은 범위 밖 `k8s/deployment.yaml` 하나, 나머지 1건은
번호만 없이 PR 로 착지했다. 정책 범위의 main 직접 push 는 **0**.

> ⚠️ 「제목에 `(#N)` 이 없다 = 직접 push」는 틀린 대리 지표다. 커밋별 조회가 맞는 측정이다.

## 판정 — 제거 14줄 / 223줄 (제거율 6%)

### `#16` — 제거 0줄. ③ 히트가 구조적으로 0이다

**저작 PR #106 의 본문은 이 아홉 파일의 설계 근거를 한 자리도 담지 않는다.** 그 본문이 적는
것은 ⑴ 소유자 없이 남은 open PR 을 회수하는 경위, ⑵ 구현 대기 27 → 19 집계, ⑶ 제품 수리
+32줄을 함께 싣는 **경계 판단**, ⑷ 병합 이력과 게이트 수치다. 행이 드는 유지 논거 일곱
(ac4 만료 관측 설계 · ac7 단위·통합 분업 · ac2 왜 변형 배포인가 · ac9 모드가 곧 상태 ·
rg#8 거부 층 구분 · rg#9 json patch 마커 · rg#10 TERM 무시 파드)을 본문에 전수 대조해
**히트 0**이다.

이것은 「본문이 짧아서」가 아니다 — #106 은 5,700자짜리 긴 본문인데 **축이 다르다.** 그 PR 은
*저작*이 아니라 *착지*를 설명하는 PR 이었고(내용은 `rct_20260915-0008` 이 저작해 이미 닫힌
task 의 산출이다), 그래서 「이 파일이 왜 이렇게 재는가」는 애초에 본문의 주제가 아니었다.

> **관찰 — ③ 수확은 「저작 PR 이 있는가」가 아니라 「그 PR 이 *어느 축을* 적었는가」로 갈린다.**
> 슬라이스 24 가 `#18` 에서 같은 형을 봤다(본문이 길어도 축이 겹치지 않아 −1). 이 패스는
> 그 명제의 **가장 깨끗한 사례**다: 같은 트리에서 `#132`(저작 PR)는 −14줄을 냈고
> `#106`(착지 PR)은 −0줄을 냈다.

③ 히트는 트리거 **#206** 쪽 둘뿐이고 **둘 다 갈림 → 유지**다.

| 자리 | ③ 근거 | 판정 |
| --- | --- | --- |
| 모듈 머리말의 마커 규약 두 불변식(+8줄의 본체) | #206 본문 「4. 규약을 `_gatekeeper.py` 머리말에 적었다」 | **유지** — 본문이 **스스로 주석을 가리킨다** ⇒ 3분 표 **셋째 줄**(원본이 주석 쪽이다; 사본을 근거로 원본을 지우면 SSOT 가 뒤집힌다) |
| `gatekeeper_url` doc 2줄(빈 포트를 매번 고르는 이유) | #206 본문 「1. 고정 로컬 포트」 | **유지** — 고정 포트로 되돌리는 편집을 막는 **서 있는 불변식** ⇒ 3분 표 **둘째 줄**. 같은 PR·같은 명제를 자매 줄 주석 행 `#44` 가 이미 같은 근거로 유지했고 그 판정을 승계한다 |

`count_requests` 한 줄 doc 은 판정 절차 2(관례가 요구하는 최소 doc)에서 멈춘다.

### `#17` — 제거 14줄. #132 본문의 표 한 칸이 다섯 자리를 축자로 담는다

PR #132 본문의 표 열 **「이 파일이 실물에서만 잴 수 있는 것」**이 다섯 모듈 docstring 의
**무엇을 재는가** 절과 정면으로 겹친다.

| # | 자리 | 줄 | ③ 근거 (#132 본문) | 판정 |
| --- | --- | ---: | --- | --- |
| ⑴ | `ag#6` ¶1 「create 만 경로가 다르다 … 409 보고와 승인 context 에 `target resourceVersion` 이 없다」 | −3 | 표 1행: 「**create 만 경로가 다르다** — 재확인이 없고 apiserver 409 가 그 자리를 대신하며 승인 context 에 `target resourceVersion` 이 없다」 | **제거**(축자) |
| ⑵ | `ag#10` ¶1 네 표면 열거 + 대조군 | −1 | 표 2행: 「네 표면(승인 context · SUT 서버 로그 · 실행 실패 에러 · `resource_list`)에서 토큰 부재. 승인된 `resource_get` 응답만이 대조군」 | **제거**(축자) |
| ⑶ | `ag#10` ¶3 「전부 가려 버리는 구현은 … 그것도 이 AC 의 통과가 아니다」 | −2 | 표 2행 괄호: 「쓰기 경로는 `(masked, NB)` 까지 단언(**전부 가리는 구현도 통과가 아니다**)」 | **제거**(축자) |
| ⑷ | `rg#6` ¶1 「Go 단위가 못 보는 것」 열거 | −3 | 표 3행: 「실 apiserver 가 열린 창 안에서 `MODIFIED` 를 실제로 미는가 · `resourceVersion` resume 가 이미 본 변경을 다시 주지 않는가」 | **제거**(축자) |
| ⑸ | `rg#7` ¶1 번호 항목 둘 | −6 | 표 4행: 「실 apiserver 의 409 와 기존 객체 무변경 · **거절이면 첫째 객체조차 없음** · 부분 실패는 되돌리지 않고 좌표로 보고」 | **제거**(축자) |

🔴 **이 다섯은 ①② 축에서 「시나리오 문서의 재진술이 아니다」로 유지됐던 자리다.** 번복은
**③ 축 하나에만** 적용하고, ②의 근거 절은 행의 이력에 그대로 남긴다 — ② 판정이 틀렸던 것이
아니라 그때 ③ 를 묻지 않았을 뿐이다.

**유지 — 갈림 3건.**

| 자리 | 근거 |
| --- | --- |
| `rg#16` 「`AUTO_REJECT` 를 쓰지 않는 이유」 | #132 「재계획 결과」 표 3행이 결론을 담지만, 그것이 지는 것은 **배포 배선**(담당자 없는 primary 요청은 거부가 아니라 시한까지 PENDING)이라 3분 표 **둘째 줄**. ⚠️ **자매 줄 주석 행 `#44` 가 같은 본문·같은 명제에 이미 내린 판정**(슬라이스 3, @지문 `1026ce8c1fc3`)을 승계한다. 지우면 docstring `#26` 의 인용도 함께 낡는다 |
| `rg#16` 「k8s 호출 카운트 0」 한계 선언 | docstring 자신이 **「숨기지 않으려고 여기 적는다」**고 말한다 ⇒ 원본이 주석 쪽(3분 표 셋째 줄). 행의 ①② 논거도 같은 자리를 「지우면 다음 사람이 이 파일을 그 절의 증명으로 읽는다」로 못박았다 |
| `rg#6` 「이벤트 **수** 상한을 여기서 재지 않는 이유」 | #132 본문의 「창 상한(61 거부)」은 **인자 거부**를 가리켜 다른 명제다. 「공유 클러스터에서 비결정적이라 남의 관측을 흔든다」는 본문 **0 히트** |

**③ 축자여도 남긴 것**: 포인터 `internal/mcp/gate.go::confirmTargetUnchanged` ·
`::maskCredentialValues` · Go 단위 다섯 이름은 전부 본문 0 히트라(코드 소재는 ③ 가 담지
않는다) 그대로 뒀다. `ag#6` ¶2(「승인 **전에** 흔든다」는 댄스 순서가 시나리오 자체) ·
`ag#10` ¶2(표면을 쪼개면 각각이 서로를 덮는다) · `rg#16` ¶1 뒷절(일부만 떼어 닫으면 빠진
동사가 새는 자리가 된다)도 본문 0 히트이면서 **파일을 쪼개는 편집을 막는 불변식**이다.
`rg#7` 의 「되돌리려면 승인 없는 delete 가 필요하다」는 본문이 결과(「되돌리지 않는다」)만
담고 **이유**를 담지 않아 한 절로 줄여 남겼다.

## 검증

| # | 명령 | 결과 |
| --- | --- | --- |
| V1 | `python3 scripts/check_comment_policy.py` | **rc=0** · `복원경로 ③④ 판정 26/32행(잔량 6)` · `판정 대상 docstring 2055줄` |
| V2 | 같은 출력의 줄 주석 표면 항 | `복원경로 ③④ 판정 67/67행(잔량 0)` · `판정 대상 주석 3337줄` — **무이동** |
| V3 | `python3 scripts/check_doc_inventory.py` | **rc=0** · `허브 도달 가능: 83 / 83 · 끊긴 링크 0` |
| V4 | `python3 scripts/check_mock_policy.py` · `test_check_mock_policy.py` | **rc=0 / rc=0** · 출력 md5 불변 |
| V5 | `python3 tests/integration/check_ac_mapping.py` | **rc=0** · 출력 md5 불변 |
| V6 | `git diff 25bc0c1..HEAD -- internal cmd main.go scripts tests/k8s` | **공집합**(제품 코드 0줄) |

**런타임 무영향**은 「돌려 봤다」가 아니라 **docstring 노드를 걷어낸 뒤의 AST 동등**으로 닫았다
— 편집한 네 파일 전부 편집 전후 동일. 기계 판독 선언(`검증 시나리오:`·`실행 대상:`·
`병렬 레인:`)은 축자 생존한다.

**음성 프로브(양방향)** — 고친 트리가 rc=0 인 것만으로는 게이트가 이 편집을 *지킨다*는 증거가
못 된다. 원장만 부모 값으로 되돌린 트리를 굴려 각 단정이 우는지 따로 쟀다.

| 프로브 | 되돌린 것 | 실측 |
| --- | --- | --- |
| N1 | `#17` 의 줄 수 칸만 87 로 | **rc=1** · R6 + R8 |
| N2 | `#17` 의 지문 칸·앵커만 `16c9f3161b60` 으로 | **rc=1** · R6 |
| N3 | 두 마커만 2069·8 로 | **rc=1** · R8 + R10 |
| N4 | `#16` 의 앵커 문면만 제거 | **rc=1** · R10 |

기준 트리는 rc=0 · 위반 0이다. **N4 가 이 패스에서 가장 중요한 프로브다** — `#16` 은 제거
0줄이라 트리가 부모와 바이트 동일이고, 그러면 「원장에 판정을 적었다」를 지키는 것이 R10 의
앵커 하나뿐이다. N2 가 R6 만 울고 **R10 을 울리지 않는 것**도 같은 계열의 사각이다(R10 은
지문 칸과 앵커가 *함께* 움직이면 침묵한다 — 둘의 일치만 보기 때문이다). 잔량 마커 혼자서는
제거를 지키지 못한다는 슬라이스 23 의 관측과 같은 형이다.

## 범위 밖 (후속 — 재감지의 입력)

- docstring 잔량 **8행**: `#2`(108줄 하네스) · `#4`(220 · 7파일) · `#5`(220 · 9파일) ·
  `#6`(100 · 14파일) · `#7`(225 · 17파일) · `#8`(34 · 4파일) · `#10`(42 · 2파일) ·
  `#21`(35 · 1파일). **뒤 셋은 열린 PR #204 가 집고 있다** — 그 PR 이 착지하면 잔량은 6이
  되고, `#21` 을 떨구면 7이다(그 행은 재감지가 새 task 로 잇는다).
  **중간 칸은 이 패스로 소진됐다.** 남은 것은 하네스 행 하나와 큰 칸 넷이다.
- 큰 칸 넷(`#4`·`#5`·`#6`·`#7`, 합 765줄)은 행을 **파일 단위로 쪼개는 원장 편집**이 선행돼야
  하고 그것이 **R7(이중 등재 금지)** 를 건드린다 ⇒ 별도 패스로 설계할 것.
- ③ 재판정 후보 누적 **51** · 갈림 **9**(+3) · ① 재판정 후보 **9**(+2 — `poll_count` doc ↔
  `ag#4` ¶1 의 같은 명제 · `_api_key` doc ↔ `get_request` doc 의 자기 파일 중복) ·
  정책 소유자 몫 **9**.
- 자매 모델 `tbm_homelab-k3s-mcp-scenario-e2e` 의 as-is(`tests/` 트리 해시)가 이 PR 로 함께
  움직인다. 그 축은 별도 fire 몫이고 이 판정과 중복 계상되지 않는다.
