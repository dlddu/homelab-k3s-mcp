# 2026-09-21 — 복원 경로 ③④ 소급 판정 슬라이스 7 · **PR 5개 정렬의 첫 행(gatekeeper)**

`rct_20260921-0013` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 6](2026-09-21-paths-3-4-slice-6.md)이 PR 4개 정렬을 소진하고 다음 칸을 「PR 5개 이상: `973f03eb84bf`(gatekeeper 126줄 · PR 5) →
`d8fa67959048`(sessionplatform 174줄 · PR 6) → …」로 명명했다. 이 패스는 **그 첫 행 하나 = 126줄**을 묻는다. 둘째 행을 묶지 않은 이유:
슬라이스 6 이 셋을 묶은 근거는 ③ 모집단의 겹침(#59·#172 가 셋에 공통)이었는데 sessionplatform 행과 이 행의 모집단은 #172 하나만 겹치고,
이 행은 승인 게이트의 판정 코어라 09-13·09-15·09-21 세 판정이 이미 「복원 불가능한 지식」(AC5 읽기 순서 · 부재의 주석 · 판정 경계)으로
분류한 줄이 많아 행당 판독이 무겁다. ③ 모집단은 PR 본문 **5건**(#73 · #74 · #104 · #172 · #177 — 전부 squash 착지, 코멘트·리뷰 **0**,
`git log -- internal/gatekeeper/` 의 제목이 전부 `(#N)` 을 단다 → ④ 대상 부재)이다.

## 이 패스의 범위 (완료 기준)

| | 기준 | 값 |
| --- | --- | --- |
| ⑴ | 줄 주석 ③④ 잔량 마커 | **17 → 16**(③④ 50/67 → **51/67**행 — base `febb072`/#183 기준) |
| ⑵ | 실제 제거 | **1줄** — `973f03eb84bf` → `aea1102c4365`(126 → 125). 비주석 바이트 0 |
| ⑶ | 줄 주석 판정 합계 | 3427 → **3426**(base `febb072` 기준 — 자매 착지로 base 가 움직이면 게이트 실측으로 재기입) |
| ⑷ | docstring 표면 | **무이동**(10/32 · 잔량 22 · 합계 2124) |

**범위 밖**: 줄 주석 잔량 16행 — 다음은 `d8fa67959048`(sessionplatform 174줄 · PR 6) → PR 7+(`e2ce8a037f3b` 7 · `8a7dc1e7869f`·`e1daa9adec21`·
`b5869fe61fba` 8 · …), docstring 잔량 22행, `e82057fcdeba`(k8s 픽스처 6파일 — PR 16개), 아래 ③ 재판정 후보.

## 측정 — 한 행과 ③ 의 모집단

| 행 | 파일 | 줄 | PR | 본문 | 머지 커밋 |
| --- | --- | ---: | --- | ---: | --- |
| `973f03eb84bf` → `aea1102c4365` | `internal/gatekeeper/gatekeeper.go`(90 → 89) · `gatekeeper_test.go`(36) | 126 → 125 | **#73** · #74 · **#104** · **#172** · **#177** | 3,516 · 3,934 · 6,932 · 4,875 · 4,695자 | `76b776d82db8` · `e2a195d487f2` · `17cb8bf4a9e5` · `7065a1449b8b` · `42441d6801dc` |

굵은 것이 코드를 들여온 PR 이다 — #73 이 패키지 저작(59+21줄), #104 가 `Call.Context string` → `Describe func` 전환(+26), #172 가 `Verdict`·`Refusal`·
`unavailable` doc(+27), #177 이 `Verdicts` doc(+4). #74 는 이 정책 자신의 PR(192줄 ①② 첫 판정, 26줄 회수)이라 제거가 아니라 유지의 근거다.

**이 층의 ③ 모양 — 저작 PR 본문이 주석과 같은 논증을 문단째 적는다.** #104 의 「설계 판단 셋」 ⑶ 은 `Call.Describe` doc 첫 문단을 거의 축자로
옮긴 것이고, #172 의 사유 매핑 표는 `Verdict`·`Refusal`·`unavailable`·`ctx.Done()` 네 자리의 판단을 칸으로 편 것이다. 그런데 #172 는 문면으로
「여섯 판정의 경계는 `gatekeeper.Verdict` doc … 이 본문은 그 사본이다」라 적고(셋째 줄), #104 ⑶ 이 옮긴 것은 *그때의 결정*이 아니라 **앞으로
미설정 게이트가 `Describe` 를 부르지 않는다는 순서 불변식**이다(둘째 줄). 첫째 줄로 떨어진 것은 그 문단 뒤에 붙은 **AC3 재진술** 하나뿐이다.

**본문과 코드의 대조**(#24 선례): 다섯 본문의 사실 진술(#73 「AC5 실패 경로 8종」 · #104 ⑶ 의 시그니처 · #172 표의 칸 · #177 `Verdicts` 6)을
`git show <머지커밋>:internal/gatekeeper/gatekeeper.go` 와 현재 코드에 대조했다 — 어긋남 0. 이 행에서는 낡은 본문이 없다.

## 판정 기준 — 슬라이스 1~6 의 구분 그대로

1. **본문이 그 주장을 대신 말하면 지운다.** 본문이 *결과*·*무엇*만 적고 주석이 지는 *이유*·*교차 제약*은 적지 않으면 「다른 주장」.
2. **본문이 스스로 주석을 가리키면 원본은 주석 쪽** — 유지.
3. **본문이 결론을 담더라도 앞으로의 편집을 구속하는 서 있는 불변식이면 유지**(README 3분 표 둘째 줄).

이 층에서 새로 부딪힌 것: **①② 축이 이미 지운 재진술이 나중 PR 로 되돌아온 자리.** #74 가 `Call`·`approvalContext` doc 에서 AC3 「승인 버튼을
형식적 절차로 만든다」를 ② 로 지웠는데, #104 가 같은 문장을 `Describe` doc 의 새 문단 끝에 다시 들여왔고 09-15 재판정은 그 11줄을 「어느 AC 에도
없는 논증」으로 한 덩어리로 유지했다. ③④ 를 묻는 이 패스가 문장 단위로 다시 읽으니 그 절만 ② + ③ 둘 다에 있었다 — R9 가 판정을 지문에 묶는
이유가 바로 이것이다(한 덩어리로 유지된 판정은 그 안의 한 절을 인증하지 않는다).

## 항목별 — `973f03eb84bf` → `aea1102c4365` (`internal/gatekeeper/`, 제거 1줄)

### 제거 — `Call.Describe` doc 둘째 문단의 뒤 절 (구 202–204행 → 202–203, −1)

```
	// An error is a refusal: AC3 ends by saying a call whose detail cannot be
	// built gets no approval request at all, because an approval screen that
	// cannot say what it is approving makes the button a formality.
```
→ 「because …」 절을 지운다(앞 절은 그대로).

**③ #104 본문 ⑶ 이 든다** — 「덤으로 AC3 의 마지막 절(「상세를 만들 수 없으면 승인 요청을 만들지 않고 거부」)도 같은 자리에서 성립한다 — 렌더링이
요청 생성 **직전**이라 실패가 요청보다 먼저다」. 그리고 **② `docs/prd-approval-gate.md` AC3 162–163행**이 축자다 — 「대상 좌표를 해석할 수 없거나
상세를 만들 수 없으면 승인 요청을 만들지 않고 거부한다. 무엇을 승인하는지 모르는 승인 요청은 승인 버튼을 형식적 절차로 만든다.」 지운 절은
AC3 의 *근거 문장*의 번역이고, 다음 편집자를 구속하는 것은 남은 앞 절(오류 = 거부 · 상세 없으면 요청 없음 = 이 필드의 계약)이다. 슬라이스 6 의
구분 그대로 — exported 필드 doc 의 *계약* 문장은 남고 그 뒤에 붙은 *경위·근거의 재진술*이 ③(그리고 ②)의 자리다.

### 유지 — 125줄

| # | 주석 | ③ | 판정 |
| --- | --- | --- | --- |
| `gatekeeper.go` 1–8 패키지 주석 | 판정 절반(AC2·AC4·AC5)은 여기, **어느 호출이 오는가는 이 패키지가 정하지 않는다**(`internal/mcp` 선언표 · AC1) | #73 AC 표(AC2·AC4·AC5 → `internal/gatekeeper`, AC1 → `internal/mcp/gate.go`) | 유지 — 관례(패키지 주석) + 「분류기를 여기 두지 않는다」는 AC1 「분류기가 하나 더 생기면 우회로」의 이 층 쪽 경계(둘째 줄) |
| 44–49 `Verdict` | 여섯 값의 **경계**(409·5xx·해독 불가·미지 상태 = unreachable · timeout = 이 클라이언트가 PENDING 을 포기) | **#172 표** ✅ + **#172 「여섯 판정의 경계는 `gatekeeper.Verdict` doc … 이 본문은 그 사본」** | 유지 — 셋째 줄(`-0004` 판정 그대로) |
| 61–64 `Verdicts` | 닫힌 목록을 **여기서** 내보내는 이유 — 히스토그램 쪽에 둘째 목록이 생기지 않는다 | #177 「`gatekeeper.Verdicts` 6」·「라벨 값은 전부 서버 열거」 — *무엇*만 | 유지 — 단일 출처 불변식(둘째 줄, `-0008` 판정 그대로). #177 「원본은 코드 doc 쪽」 절은 이 doc 을 이름으로 들지 않는다 — 셋째 줄이 아니라 둘째 줄로 남는다 |
| 70–76 `Refusal` | 타입인 이유(프로즈를 파싱하지 않는다) · `Verdict` 가 빈 경우 = 판정 이전 거부, **그 뜻은 호출자만 안다** | #172 표 「Describe 가 미설정 리더로 실패 → `gate_unconfigured`」·「인자·대상 읽기로 실패 → `invalid_input`」 — 칸만 | 유지 — 표의 두 칸이 갈리는 **이유**(이 타입이 정하지 않고 호출자에게 넘긴다)는 본문 0 · #172 가 표의 근거 원본을 doc 으로 지목(셋째 줄) |
| 92–94 `unavailable` | 미설정 리더로 실패한 Describe = 미설정 게이트, 나쁜 호출이 아니다 | #172 「여섯 통합의 `Error.Unavailable()` 로 식별」 | 유지 — 표의 그 칸이 서는 규칙(둘째 줄) |
| 100–102 `Pair` | 서브리소스 인라인 = apiserver 가 주고 뺏는 단위 | ❌ (#73 은 `(verb, resource[/subresource])` 만) | 유지 — 상류 규약 |
| 117–119 `GatedKinds` 필드 | 설정 가능한 이유(무엇이 자격증명인가는 클러스터마다 다르다) | ❌ | 유지 |
| 123–125 `FromEnv` | `(nil, nil)` = 미설정 → 호출자가 거부 게이트로 | #73 「게이트 설정이 없는 현재 배포에서도 서버는 정상 기동」 — 결과만 | 유지 — 반환값 계약 |
| 194–200 `Describe` 첫 문단 | 함수인 이유(AC3 이 클러스터를 읽고 AC5 가 미설정 읽기를 금한다) → **미설정 게이트는 부르지 않는다 · 순서가 타입의 성질** | **#104 ⑶** ✅ 축자(「관례가 아니라 타입의 성질이 됐다」) | 유지 — 둘째 줄: 다음 편집자가 `Describe` 를 문자열로 되돌리거나 `Unavailable` 에서 부르는 손을 막는 순서 불변식 + 그 전제. 09-15 가 이 논증의 소유자로 정한 자리. **③ 재판정 후보(부분)** — 「rather than a string」·「now」 는 그때의 전환 경위 |
| 202–203 `Describe` 둘째 문단(남은 절) | 오류 = 거부 · 상세 없으면 요청 없음 | #104 ⑶ · ② AC3 162 | 유지 — 필드 계약(관례) |
| 223–224 `Decision` · 235 `Consume` · 249–251 `Gate.Authorize` | AC8·AC9 필드 · 1회 소비(AC7) · 「셋째 답은 없다」 | #73 AC7 행 | 유지 — 계약 |
| 255–257 `Unavailable` | 게이트를 못 세우는 곳마다 서서 미설정 배포가 조용히 실행하지 않게 | #73 AC5 행 | 유지 — 타입의 자리 |
| 269–271 `Unavailable.Authorize` | **부재의 주석** — Describe 를 부르지 않는 것이 요점(AC5 · 읽기는 Describe 에 산다) | **#104 ⑶** 「`Unavailable.Authorize` 가 그것을 부르지 않게 했다」 ✅ | 유지 — 둘째 줄: 호출이 없는 것은 코드에 보이지 않는다(#104 변이 표: 되살리면 `TestUnavailableGateRefuses` 가 깨진다) |
| 327–328 `Client.Authorize` 인라인 | 게이트가 물을 곳이 있음을 아는 첫 지점 → Describe 를 여기서(`Call.Describe` 참조) | #104 ⑶ 「`authorize` 는 게이트가 설정됐는지 알 수 없으므로」 ✅ | 유지 — 09-15 가 접은 소유자 포인터. **③ 재판정 후보(부분)** — 「which is why … rather than by the caller」 절 |
| 348–349 auto-response | 생성 시점에 확정, 폴링은 확인일 뿐 | ❌ | 유지 — 상류 동작 |
| 438–439 `ctx.Done()` | 호출자 취소도 레코드 쪽에서는 timeout | #172 표 「타임아웃·ctx 취소 → `gate_timeout`」 | 유지 — 셋째 줄(칸의 근거 원본을 본문이 doc 으로 지목) |
| 471–472 `randomExternalID` | 두 동일 호출은 두 승인 | #73 AC2 「호출마다 고유한 `externalId`」 | 유지 — id 를 재사용하는 손을 막는다 |
| `gatekeeper_test.go` 15–17 | PENDING 을 거쳐야 폴링을 관측한다 | ❌ (#73 「가짜 gatekeeper httptest」만) | 유지 — 픽스처 설계 |
| 203–206 AC5 표 머리 | 표 = AC 의 열거 · **사람은 프로즈를, 레코드는 타입을 읽고 둘은 어긋나면 안 된다** | #73 「8종이 각각 구분되는 거부」 · #172 「실패 표에 verdict 열」 — *무엇*만 | 유지 — 표에 verdict 열이 있는 이유 |
| 353–358 `TestUnavailableGateRefuses` | AC5 + 둘째 단언 = `Call.Describe` 가 존재하는 성질 | #104 변이 표 ✅ | 유지 — 09-15 가 접은 포인터 |
| 나머지 AC 머리말(118–119 · 155–156 · 171–172 · 193 · 299–302 · 338–339 · 374 · 379–380 · 398 · 415–416) | 시험 대상의 진술 | #73·#172 | 유지 — ② 자리(AC 의 번역), 결정·경위가 아니다 |

나머지(33 · 41 · 97 · 110 · 188–189 · 207–208 · 246–247 · 260–261 · 276 · 285–286 · 303–304 · 324–325 · 408 · 431 · 테스트 108–109 · 331–332)는 관례 최소치.

## 결론

| | 값 |
| --- | --- |
| 이 패스의 제거 | **1줄** — 비주석 바이트 0(`grep -vE '^\s*//'` diff 0 · `gofmt -l` 빈 출력 · `go build ./...`·`go vet`·`go test ./internal/gatekeeper/...` 무이동) |
| 줄 주석 판정 합계 | 3427 → **3426** |
| ③④ 판정 완료 / 전체 | 50/67 → **51/67**, 잔량 **17 → 16** |
| docstring 표면 | **무이동**(10/32 · 22 · 2124) |
| 갈려서 남긴 것 | 부분 후보 2건(`gatekeeper.go` 194–200 「rather than a string」 · 327–328 「rather than by the caller」) · 갈림 0 |

**관찰 둘.** ⑴ 감지의 예측대로 이 층은 「복원 불가능한 지식」이 많아 ③ 히트 자리 가운데 첫째 줄로 떨어진 것은 한 절뿐이고 나머지는 전부 둘째·셋째 줄로 남았다 — PR 5개 정렬부터는
저작 PR 이 정책 이후(09-13~)에 쓰여 **본문이 처음부터 주석을 원본으로 지목하거나 불변식을 옮겨 적는 형**이라, 제거는 ①② 축이 놓친 재진술
정도로 작아진다. ⑵ **한 덩어리로 유지된 판정은 그 안의 한 절을 인증하지 않는다** — 09-15 의 「11줄 논증 유지」 안에 ② 로 이미 지워졌던 AC3
재진술 한 절이 되돌아와 있었다. 다음 행(sessionplatform 174줄)도 09-04 ①② 판정 뒤 96줄 재판정이 한 덩어리로 얹힌 형이라 같은 모양을 예상한다.

## 다음 패스의 입력

- 줄 주석 잔량 **16행** — 다음은 `d8fa67959048`(sessionplatform 174줄 · PR 6: `git log -- internal/sessionplatform/` 로 재실측) → PR 7+
  (`e2ce8a037f3b` 7 · `8a7dc1e7869f`·`e1daa9adec21`·`b5869fe61fba` 8 · …).
- ③ 재판정 후보 누적: 이 패스의 둘(위) + 슬라이스 6 의 셋(`grafana.go` 73–75 · `exposure_test.go` 11–14 · `opensearch.go` 309–310) + 슬라이스 5 의
  셋(`http-trace` 119–123 · `gatekeeper-variant` 117–118 · `session-platform` 23–27) + 슬라이스 4 의 `precondition.go` 15–18 첫 문장 + 슬라이스 3 의
  `rg_ac6` ⑴⑵ · `ac20` 333 · `ac20` 59–60 둘째 문장 + 슬라이스 2 의 `ac15` ⑶⑻ · `ac11` ⑴ 첫 절 · `recordTarget` doc 3줄 · `Principal` AC1 철자 2줄 ·
  `rbac-narrow-fixture.yaml` 머리말 불릿. 갈림 승계: `ac4` 빈 맵 1줄(#81 이래) · `TRACE_ROUTES` 앵커 1줄(#62 이래).
- docstring 잔량 **22행** — 여전히 한 번도 이 축으로 묻지 않은 표면.
- 정책 소유자 몫(승계): PR 안의 개별 커밋 메시지(squash 로 main 이력에 없는)가 ③ 인가 — `http-trace.yaml` 15–17·19–20·75–79. **③ 본문이 코드와
  어긋날 때의 규칙**(슬라이스 6, #24). 이 패스에서 새로: **한 덩어리로 「유지」된 다줄 논증 안의 개별 절을 ①② 축이 다시 물어야 하는가** —
  R9 는 ③④ 만 지문에 묶고 ①② 재판정은 래칫이 없다(09-15 형 「N줄 논증 유지」가 그 안의 재진술을 인증한 사례).
