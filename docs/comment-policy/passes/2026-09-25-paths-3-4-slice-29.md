# 2026-09-25 — 복원 경로 ③④ 소급 판정 슬라이스 29 · **전용 e2e 큰 칸 (원장 `#7`, 225줄 / 17파일)**

`rct_20260925-0003` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 28](2026-09-25-paths-3-4-slice-28.md)이 공유 헬퍼 행(`#4`)을 닫아 잔량을 4 → 3 으로
내리고, 남은 **큰 칸 셋**(`#5` 220·9 · `#6` 100·14 · `#7` 225·17 = 545줄)에 대해 「행 크기가 아니라
**커밋 분포**를 각 행에 다시 실측해서 고를 것(승계 금지)」을 요구했다. 이 패스가 그 셋 중 `#7` 을
묻는다. 줄 번호는 base `68de35e`(#211 = 슬라이스 28) 기준이다.

## 선택 — 세 행의 커밋 분포를 다시 재서 `#7` 을 골랐다

**⑴ 파일 수는 이번에도 비용의 지표가 아니었다.** 세 행의 파일을 건드린 커밋을 전수로 세면
파일 수와 순서가 뒤집힌다:

| 행 | 줄 | 파일 | 파일을 건드린 커밋 | **docstring 을 움직인 커밋** |
| --- | ---: | ---: | ---: | ---: |
| `#5` | 220 | 9 | 9 | **6** |
| `#6` | 100 | **14** | 6 | **4** |
| `#7` | 225 | **17** | 10 | **6** |

`#6` 은 파일이 `#5` 보다 **다섯 개 많은데** 저작 커밋은 더 적다. 즉 판정 비용은 파일 수가 아니라
**블록의 저작 시점이 몇 갈래로 갈리는가**에 붙는다 — 슬라이스 28 이 `#4` 에서 세운 명제가 이
셋에서도 성립한다.

**⑵ 그러나 `#4` 와 달리 셋 다 분포가 평평하다 — ⒝ R7 분할은 이 행들에서 산출이 없다.**
`#4` 는 `_helpers.py` 한 파일이 커밋 25건(그중 21건이 단독)을 차지해 「그 파일만 떼면 원장 변경
1/6 로 같은 해상도」라는 계산이 섰다. 셋을 같은 방법으로 재면 그런 파일이 **하나도 없다**:

| 행 | 파일당 커밋 | **단독 커밋** |
| --- | --- | ---: |
| `#5` | 4~5 (균일) | **0** |
| `#6` | 4~5 (균일) | **0** |
| `#7` | 3~6 (균일) | **0** |

단독 커밋이 0 이라는 것은 **이 행들의 파일이 언제나 무리로 함께 움직였다**는 뜻이고, 그러면 행을
쪼개도 지문이 낡는 사건은 쪼갠 조각 전부에서 동시에 일어난다 — 원장 행만 늘고 해상도는 그대로다.
그래서 이 셋에 대해서는 ⒝ 를 **누적 후보에서 내린다**(슬라이스 28 이 `#4` 에 대해 올린
`_helpers.py` 2행 분할 제안은 그 행의 분포에 근거한 것이므로 그대로 살아 있다).

**⑶ `#7` 을 고른 이유는 모집단의 포함 관계다.** 세 행의 저작 커밋 집합을 실제로 비교하면

* `#6` = {#63, #65, #43, #44}
* `#7` = {#63, #65, #43, #44, #42, #52} — **`#6` 을 통째로 포함한다**
* `#5` = {#63, #65, #50, #55, #56, #58} — 공통 둘 말고는 겹치지 않는다

`#7` 을 먼저 물으면 `#6` 의 모집단이 **전부 이미 읽힌 상태**가 되고, 읽는 PR 하나당 판정하는 줄
수도 `#7` 이 가장 크다(37.5줄/PR · `#5` 36.7 · `#6` 25.0). 225줄은 직전 패스들의 실적
(슬라이스 26 223줄 · 28 220줄)과 같은 크기라 한 패스에 든다. **셋을 한 번에 집지 않은 이유**는
545줄이 그 실적의 2.5배이기 때문이고, 남는 둘은 이 패스의 착지가 `ledger.md` 를 움직여 재감지를
연다(이 task 자신이 슬라이스 28 의 착지로 열렸다).

## 이 패스의 범위 (완료 기준)

docstring 원장 **한 행**(`#7`, `2944ffe3ef0c`, 전용 e2e 17파일 225줄)에 대해 복원 경로 ③④ 를
물어 판정하고 현재 지문에 못박는다(R10). 게이트 헤드라인으로 확인 가능한 네 칸:

| | 전 | 후 |
| --- | --- | --- |
| docstring ③④ 판정 | 29/32 (잔량 **3**) | **30/32 (잔량 2)** |
| docstring 합계 | 2016줄 / 109파일 | **2001줄 / 109파일** |
| 원장 행 `#7` | `2944ffe3ef0c` 225줄 | **`2373b51baf65` 210줄** |
| 줄 주석 표면 | 3337줄 · 67/67 · 잔량 0 | **무이동** |

범위 밖: docstring 잔량 2행(`#5`·`#6`), `README.md` 개정(정책 소유자 몫), 주석의 **정확성**
(이 모델의 표면 밖).

## 측정 — 모집단은 파일이 아니라 블록의 저작 커밋으로 셌다

`git log -- <파일>` 은 그 파일을 건드린 커밋을 전부 주지만, 그중 docstring 을 **실제로 움직인**
것만이 ③ 의 모집단이다. 커밋마다 `ast` 로 그 파일의 docstring 집합을 부모와 대조해 갈랐다:
17파일을 건드린 커밋 **10건** 중 docstring 을 움직인 것은 **6건**이다(#42 · #43 · #44 · #52 ·
#63 · #65). 리뷰 코멘트는 6건 전부 **0**이고 이슈 코멘트는 #43 에 1건(CI 재실행)뿐이라, 이 행에
대한 ③ 의 실체는 **본문뿐**이다.

그 6건 중 **#63(정형 문단 제거)·#65(①② 전수 판정)는 세 큰 칸 전부에 공통**이므로 이 행 고유의
모집단은 **넷**(#42 · #43 · #44 · #52)이고, 수확은 그중 **#52 하나에 몰렸다** — 제거 15줄이 전부
#52 가 저작한 두 파일(`platform_auth_safety_ac2.py` · `_ac8.py`)에서 나왔다. #42 는 스스로
「단언을 한 줄도 바꾸지 않는 **순수 이동**」이라 적어 그 파일들의 케이스 docstring 을 저작하지
않았고(그래서 본문에 히트가 없다), #43·#44 의 본문은 분할의 산술과 배차를 적어 축이 겹치지 않는다.
**③ 수확은 모집단 크기가 아니라 「그 PR 이 무엇을 적었는가」로 갈린다**는 슬라이스 24·26 의 관찰이
여기서도 그대로 재현됐다.

## 🔴 보존된 ①② 판정의 「복원 경로 넷 밖」이 소급해 일부 거짓이었다

이 행의 2026-09-07 결과 칸은 유지 근거를 열거하며 여덟 자리를 이름으로 들고 **「…가 복원 경로 넷
밖이다」**로 닫는다. 그 가운데 둘이 저작 PR **#52** 본문에 축자에 가깝게 있다:

* `_ac2.py` 「사슬의 각 칸이 앞 칸이 준 값으로만 이어진다」 ↔ #52 「각 칸이 앞 칸이 준 값으로만
  이어지므로 중간이 낡으면 그 자리에서 끊긴다」
* `_ac8.py` 「네 배포를 대조하는 것 자체가 검증이다」 ↔ #52 「네 구성 대조가 AC8 자체라 그 파일만은
  자기 그룹의 배포 하나로 부족해」

R10 이 서기(2026-09-20) 전의 ①② 판정이라 **③④ 를 실제로 뒤진 것이 아니라 묻지 않은 채 없다고
단정**한 것이고, 앵커가 없어 게이트는 이 행을 계속 초록으로 통과시켰다. 슬라이스 28 이 `#4` 에서
만난 것과 같은 형이다. **번복은 그 두 자리의 근거에만 적용하고 나머지 절은 그대로 살렸다.**

다만 **두 자리 다 지우지는 않았다** — 아래 3분 표 둘째 줄에 걸린다.

## 제거 15줄 — 전부 #52 의 두 파일

| # | 자리 | 줄 | 근거 |
| --- | --- | ---: | --- |
| ⑴ | `_ac2.py` 모듈 「연결된 사슬」 설계 서술 | 4 | #52 본문이 `resource_metadata` → `authorization_servers[0]` → `jwks_uri` → RSA 키로 같은 사슬을 축자로 담는다(3분 표 첫째 줄). **모듈 첫 줄의 화살표 요약이 그 사슬의 ① 원본**이라 지워도 사슬 자체는 남는다 |
| ⑵ | `_ac2.py` 케이스 `auth.FromEnv` 기전 | 2 | #52 본문 축자 + ① `internal/auth` 코드 자체가 원본 |
| ⑶ | `_ac8.py` 모듈 네 구성 ↔ 픽스처 대응 | 5 | #52 본문의 **배포 표**가 (a) `auth-fixture.yaml` · (b)(c)(d) `oidc-fixture.yaml` 을 그대로 적고, ① 그 매니페스트 자신이 원본 |
| ⑷ | `_ac8.py` (c) 「이 슬라이스의 범위 밖」 | 4 | 유효 JWT 를 태우지 않은 사유는 **그때의 결정·경위** 그 자체(3분 표 첫째 줄) |

## 유지 — 갈려서 남긴 것 셋 (3분 표 둘째 줄)

⒜ `_ac2.py` 의 「사슬의 각 칸이 … 중간의 한 칸이 낡으면 그 자리에서 끊긴다」 — ③ 히트이지만
**앞으로의 편집을 구속하는 서 있는 불변식**이다(발급자·JWKS 주소를 하드코딩하면 사슬이 끊긴 것을
못 잡는다). ③ 은 *그때의 결정*을 적을 뿐 *앞으로의 금지*를 집행하지 않는다.

⒝ `_ac2.py` ↔ `_ac8.py` 의 **상호 참조 두 절** — #52 가 「(d)가 AC2의 논증을 떠받친다 … 두 파일이
서로의 근거가 된다」로 적어 ③ 히트다. 그러나 (d) 케이스를 지우는 편집이 들어오면 ac2 의 단정이
**조용히 공허해지고**, 그것을 막는 것이 이 두 절이다. 한쪽만 남기면 남은 쪽의 짝이 사라지므로
**둘 다** 유지했다.

⒞ `_ac8.py` 모듈의 (a)~(d) 열거와 「네 배포를 대조하는 것 자체가 검증」 결론 — ③ 히트이나
**이 파일을 자기 그룹 배포 하나로 옮기려는 편집을 막는 불변식**이라 유지. 위 🔴 의 번복이 이
자리를 지우지 않은 이유다.

## 유지 — 모집단에 히트가 없는 것

`grafana_token_ac1.py` 의 TTL 관측 가능성 절, `github_app_installation_token_ac1.py` 의
「~1시간은 여기서 단정하지 않는다」, `_ac4.py` 의 PEM armour 논거, `aws_config_get_ac2.py` 의
「MinIO 는 정적 키도 똑같이 받아준다」 — 저작 PR #42·#43 본문에 **히트 0**이다.

⚠️ **`#65` 본문이 grafana TTL 문장을 인용하지만 그것으로 지울 수 없다.** #65 는 이 표면의 ①②
판정 패스이고, 그 본문이 문장을 인용한 것은 **판정 기록이 주석을 가리킨 것**이다 — 3분 표
셋째 줄(「PR·커밋이 스스로 "이 내용은 주석에 적었다"고 가리키는 것 → 원본은 주석 쪽」)에 정확히
걸린다. 사본을 근거로 원본을 지우면 SSOT 가 뒤집히고, 그 길을 열면 **판정 패스가 인용한 모든 유지
주석이 다음 패스에서 제거 대상이 되는** 자기 무효화 고리가 생긴다.

`platform_auth_safety_ac6.py` 모듈의 `실행 순서: 0` 사유는 #44 가 `smoke.py` 에 대해 같은 논거를
적지만 **배차 순서를 바꾸는 편집을 구속하는 불변식**이라, 슬라이스 24 가 `smoke` ¶2 에 내린 판정과
같게 유지했다.

## ④ 재실측 — 적용 대상 0

base `68de35e` 에서 최근 120커밋 중 제목에 번호가 없는 **22건**을 커밋별 연결 PR 조회로 갈랐다:
**21건이 정책 범위 밖** `chore(deploy):` 이미지 핀(건드린 파일은 `k8s/deployment.yaml` 하나),
**1건은 PR 로 착지**(제목에 번호만 없다). 정책 범위 파일을 건드린 main 직접 push **0** ⇒ ④ 의
적용 대상은 오늘도 0이다.

## 검증

**게이트 네 종 rc=0** — `check_comment_policy.py` · `check_ac_mapping.py` ·
`check_mock_policy.py` · `check_doc_inventory.py`.

**양방향(음성 프로브)** — 원장만 부모 값으로 되돌린 트리에서 rc=1 이 나오는 것을 단정 단위로
확인했다: R6 줄 수 칸 · R6 지문 칸 · R8 합계 마커 · R10 ③④ 잔량 마커 · R10 앵커 문면(지문만
옛 값으로) **5건**.

**런타임 무영향** — 두 파일에서 docstring 노드를 걷어낸 뒤의 `ast.dump` 가 부모와 **바이트 동일**
하고, 제품 코드(`main.go`·`internal/`)는 0줄이다. 기계가 읽는 선언(`검증 AC:` · `실행 대상:` ·
`병렬 레인:`)은 한 줄도 건드리지 않았다.

**줄 주석 표면 무이동** — 3337줄 · 67/67 · 잔량 0 이 전후 동일하다(이 패스는 docstring 표면만
만진다).

## 범위 밖(후속)

docstring 잔량 **2행**:

* `#5` 220줄 / 9파일 — 저작 커밋 6(#50 · #55 · #56 · #58 + 공통 #63 · #65). 모집단이 `#7` 과
  공통 둘 말고는 겹치지 않으므로 **새로 읽어야 한다**.
* `#6` 100줄 / 14파일 — 저작 커밋 4(#43 · #44 + 공통 #63 · #65)로 **전부 이 패스가 이미 읽은
  집합의 부분집합**이다. 다만 **판정은 승계하지 말 것** — 같은 PR 이라도 블록이 다르면 무엇을
  적었는지가 갈린다(슬라이스 28 이 자매 표면 모집단을 승계했다면 제거 16줄 중 9줄을 놓쳤다).

⒝ R7 파일 단위 분할은 이 두 행에 대해서도 **산출이 없다**(단독 커밋 0). 정책 소유자 몫으로 올린
`_helpers.py` 2행 분할 제안은 `#4` 의 분포에 근거한 것이라 그대로 유효하다.
