# 2026-09-24 — 복원 경로 ③④ 소급 판정 슬라이스 25 · **docstring 표면 다섯째 칸 — 시나리오 2·4 e2e · dear-baby/ping 여섯 파일(원장 `#10`·`#8`, 76줄)**

`rct_20260924-0001` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 24](2026-09-22-paths-3-4-slice-24.md)가 넷째 칸을 닫아 잔량을 13 → 10 으로 내렸다. 이
패스가 다섯째 칸을 묻는다. 줄 번호는 base `25bc0c1`(#206) 기준이다.

## 선택 — 인계 제안 셋을 받았으나 착지 직전에 하나를 뺐다

슬라이스 24 는 다음 칸으로 `#21`(35줄, 단일 파일) · `#10`(42줄, 2파일) · `#8`(34줄, 4파일)을
권했다. 칸의 *선택*은 받고 수치는 다시 쟀다 — 게이트 요약 줄이 아니라
`check_comment_policy.py` 의 `parse_ledger()` + `check_paths34()` 로직을 그대로 재실행해 독립
재계산했다: docstring 원장 **32행 = 완료 22 + 잔량 10**, 잔량 행의 줄 수 합 **1,199**(그 시점
표면 2,065줄의 58%)로 두 마커와 전건 일치했다.

🔴 **`#21` 은 착지 직전에 뺐다 — 자매 PR 이 그 docstring 자체를 움직였다.** 판정을 마치고 PR 을
연 뒤 #205·#206(게이트 파일 병렬 레인)이 먼저 착지하면서 `resource_generic_ac14.py` 에
`병렬 레인: gate-streams` 를 선언하고 **「``병렬 레인:`` 을 선언하지 않는 것은 의도다」 문단을
재작성**했다. 그 문단은 이 패스가 「원본이 `run_all.py` 머리말 = ① 축」이라는 이유로 **유지**로
판정한 바로 그 자리다 — 판정의 대상이 사라졌으므로 판정도 낡았다. 원장 `#21` 행은 그 PR 이
**재판정**(줄 수 불변 35, 지문 `2bc8e558feeb` → `0b2ddd8e5fde`)으로 이미 갱신했고, ③④ 는 여전히
미판정이다.

⚠️ **리베이스는 이 충돌을 텍스트로 보여 주지 않았다.** 제거 대상 네 문단과 자매가 고친 문단이
겹치지 않아 `ac14.py` 는 **자동 병합됐다** — 어느 패스도 판정하지 않은 조합이 조용히 만들어진다.
충돌은 원장 행과 합계 마커에서만 났다. 그 자동 병합 결과를 버리고 `#21` 을 통째로 뺀다.

남은 큰 칸을 이번에도 담지 않은 이유는 슬라이스 23·24 가 적은 그대로다 — `#4`·`#5`·`#6`·`#7`
(7~17파일, 합 765줄)은 담으려면 원장 행을 **파일 단위로 쪼개는 편집이 선행**되어야 하는데 그것이
**R7(이중 등재 금지)** 를 건드린다. `#2`(108줄)는 유일한 하네스 행(`run_all.py` ·
`check_ac_mapping.py`)이라 ① 소유자 판정이 다르게 걸린다.

## 얽힘 — 들어오는 방향을 먼저 물었다

슬라이스 24 가 「`#10` 을 물 때는 그 행이 인용한 선례가 여전히 참인지 **들어오는 방향으로** 먼저
확인하라」고 적었다. 그대로 했다.

| 인용 | 실측 | 처리 |
| --- | --- | --- |
| `#10` → `#9` 「관례 최소 요약」 | 슬라이스 24 가 `#9` 의 케이스 doc 셋을 **전부 남겼다**(그 행 결과 칸에서 확인) | 선례 **유효** ⇒ `ac2`·`ac4` 의 함수 doc 요약 줄 다섯은 이번에도 유지 |
| `#10` → `#5`·`#8` 「사용 지점 사본을 지우고 머리말을 남긴다」 | 이미 착지한 **사실**이라 낡지 않는다 | 영향 없음 |

**나가는 방향**(이 두 행을 가리키는 다른 행)은 두 표면 99행의 결과 칸을 전수 훑어 **3건**이고,
그중 **1건이 이 패스의 제거선을 실제로 물렸다.** 세는 법: 두 행의 범위 파일 이름 여섯과 「원장
#8」·「원장 #10」 표기를 두 원장의 결과 칸 전수에 걸고, 히트마다 **가리키는 것이 산문인가
코드·거동인가**를 따로 물었다(파일 이름만 세면 표면이 섞인다 — `#8` 의 네 파일은 줄 주석 원장
`#23` 에도 **같은 이름으로** 등재돼 있다).

| 히트 | 가리키는 것 | 판정 |
| --- | --- | --- |
| 🔴 **줄 주석 `#29` → `ac4` 모듈 ¶2** | `ac4:123` 의 줄 주석을 접으며 ① 소유자로 「같은 파일 모듈 docstring ¶2」를 **명시 지목**(「사용 지점 사본을 지우고 머리말을 남기는 원장 docstring `#5`·`#8` 선례」) | **산문이 선례를 진다 ⇒ 유지.** 아래 `#10` 항 참조 — 한 번 지웠다가 되돌렸다 |
| docstring `#11`·`#22` → 「원장 #10 이 고정한 처리」 | **처리 관례**(「인용만 걷고 논증은 남긴다」 / 「포인터는 남긴다」) | 이 패스가 그 관례를 그대로 따랐다 ⇒ 인용 유효 |
| 줄 주석 `#23` → dear-baby 넷 | **자기 행의 범위 파일**이지 인용이 아니다 | 무관(`#8` 은 제거 0줄이라 어차피 영향 없음) |

**이 표가 이 패스의 가장 값진 산물이다.** 나가는 방향을 안 쟀으면 `ac4` ¶2 를 지운 채 착지해
**이미 앵커된 줄 주석 `#29`(@지문 `4c9ff2211279`)를 조용히 낡게** 만들었을 것이고, R9·R10 은 앵커가
자기 행 지문에만 묶여 있어 **그 트리를 초록으로 통과시킨다**.

## 이 패스의 범위 (완료 기준)

docstring 원장 **두 행**에 ③④ 를 물어 판정하고 현재 지문에 못박는다(R10).

| | 전 | 후 |
| --- | --- | --- |
| docstring ③④ 판정 | 22/32 (잔량 **10**) | **24/32 (잔량 8)** |
| 판정 합계 | 2,075줄 | **2,069줄** |
| `#10` `resource_generic_ac2/ac4.py` | 42줄 `acb2c66e15c4` | **36줄 `a7205309391e`** |
| `#8` dear-baby 셋 + `ping_ac1.py` | 34줄 `6db41ad6420b` | **34줄 `6db41ad6420b`**(무변) |
| 줄 주석 표면 | 67/67 · 잔량 0 · 3,337줄 | **무이동** |

완료 기준은 **절대 지문이 아니라 부모 대비 상대값**으로 건다(「잔량 −2행 · 합계 −6줄 · 줄 주석
표면 무이동」). 자매 PR 이 분 단위로 착지하는 레포라 절대값은 태어날 때부터 낡는다 — 이 패스가
`#21` 을 뺀 것이 바로 그 이유에서다.

범위 밖: docstring 잔량 **8행**(`#21` 포함) · `README.md` 개정(정책 소유자 몫) · 주석의
**정확성**(이 모델의 표면이 아니다) · ①② 축 재판정 · 제품 코드(0줄).

## 측정

`#10` 은 개명·분할 이력이 0 이라 경로 `git log` = `--follow` 다. **`#8` 은 아니다** —
`ping_ac1.py` 는 #44 가 `smoke.py`·`no_config.py`·`auth.py` 를 쪼개 만든 **분할 산물**이라 경로
`git log` 가 돌려주는 넷(#44·#63·#65·#66·#126)에는 그 케이스를 *처음 세운* PR 이 없다. 명제
토큰으로 `git log -S` 를 걸어 부모 계보 **#38**·**#21** 을 모집단에 더했다.

| 행 | ③ 모집단 | 성격 |
| --- | --- | --- |
| `#10` | **#79** | 저작(두 파일을 신설). #81 은 앞선 주석 판정 패스, #126 은 레인 선언 추가 |
| `#8` | **#42**·**#44** + 계보 **#38**·**#21** | 분할·이동 PR 들. #63·#65 는 앞선 주석 판정 패스, #66 은 매핑 축 이동 |

**리뷰 코멘트·이슈 코멘트는 열 PR 전부 0건이다**(#79·#81·#126·#42·#44·#38·#21·#63·#65·#66)
⇒ ③ 의 실체는 **본문뿐**이다.

**④ 는 대상 부재다.** base `c2e924a` 에서 최근 120커밋 중 제목에 `(#N)` 이 없는 **25건**을 뽑아
커밋마다 `/commits/<sha>/pulls` 를 조회했다 — 연결 PR 이 없는 **24건은 전부** `chore(deploy):`
이미지 핀이고 건드린 파일은 범위 밖 `k8s/deployment.yaml` 하나, 나머지 1건(`e04e5d17`)은 번호만
없이 PR #193 으로 착지했다. 그 뒤 이 head 까지 더해진 두 커밋(`ff060ac` #205 · `25bc0c1` #206)은
**둘 다 번호를 달고 PR 로 착지**했으므로 결론은 그대로다 — 정책 범위의 main 직접 push 는 **0**.

> ⚠️ 「제목에 `(#N)` 이 없다 = 직접 push」는 틀린 대리 지표다. 커밋별 조회가 맞는 측정이다.

## 판정 — 제거 6줄 / 76줄 (제거율 8%)

제거선은 `README.md` 「③④는 *다음 편집자*에 대한 복원 경로다 (2026-09-20 확정)」 절의 **3분 표**
이고, 거기에 슬라이스 14·17·22·23·24 가 세운 선례를 더한다.

### `#10` `resource_generic_ac2.py` · `resource_generic_ac4.py` — 제거 6 · 유지 36

- **`ac2` ¶2 −2** — 「크기 비교의 대조군은 도구를 거치지 않고 `kubectl get -o json` 으로 뜬다 —
  도구의 다른 호출로 대조군을 만들면 두 값이 같은 축약 로직을 통과해 「현저히 작다」가 공전한다」가
  #79 「대조군은 도구가 아니라 `kubectl get -o json` 이다 — 도구의 다른 호출로 만들면 같은 축약
  로직을 지나 「현저히 작다」가 공전한다」의 축자 ✅. 대조군이 무엇인지는 ①
  (`_kubectl_deployments_whole` 의 관례 최소 doc)이 계속 진다.
- **`ac2` ¶4 −2** — 「크기만 재면 **응답이 객체를 담은 채 그냥 작을 수도** 있다」 + 「AC17 이 list 를
  게이트 밖에 두는 근거가 바로 「표에는 값이 실리지 않는다」이므로, 이 축은 크기가 아니라 형태로
  지켜져야 한다」가 #79 의 같은 두 문장 축자 ✅.
  - 🔴 **이 자리는 원장 결과 칸 ⓑ 가 「갈려서 유지」한 곳이고, 유지 논거는 「그 뒤 문장(‘이 축은
    크기가 아니라 형태로 지켜져야 한다’)이 AC 가 테스트 형태에 부과하는 제약이라 잘라내면 논증이
    끊긴다」였다.** ③ 이 그 뒤 문장까지 **한 문장 안에** 담으므로 그 논거는 소급해 낡았다 — 잘라내도
    논증은 #79 에서 온전히 복원된다. 번복은 **이 한 논거에만** 적용하고, 같은 ⓑ 의 앞 절(함수 doc
    요약 줄 다섯 = 관례 최소 요약, 원장 `#9` 선례)은 유지 그대로다.
  - **「행의 모든 칸이 스칼라인지와 `managedFields` 문자열이 응답 어디에도 없는지를 함께 본다」는
    남겼다** — 세 단언 중 하나를 지우는 편집을 막는 자리다.
- **`ac4` ¶1 뒷절 −3** — 「그래도 **사전 조건 자체를 kubectl 로 먼저 단언한다** — 두 필드가 애초에
  없으면 「걷어냈다」가 공전하므로…」가 #79 「**걷어냈음을 단언하기 전에 걷어낼 것이 있었음을
  kubectl 로 먼저 단언한다** — 없으면 도구가 아무것도 하지 않아도 통과한다」의 축자 ✅.
  - **앞 절(픽스처 교차 제약)은 남겼다** — #79 는 「픽스처는 CI 가 이미 `kubectl apply` 로 세운다」
    까지만 적어 **어느 매니페스트인지**(`tests/k8s/kind/test-deployment.yaml`)를 복원하지 못한다.
    그 파일을 손대는 편집을 구속하는 서 있는 불변식이다.
- 🔴 **`ac4` ¶2 앞절(「포함 관계가 아니라 **등가**로 잰다 … 포함으로 재면 과잉 제거가 통과한다」)은
  ③ 축자임에도 유지한다.** #79 「「그 둘만」을 포함이 아니라 **metadata 키 집합 등가**로 재서 **과잉
  제거**까지 잡는다」의 축자가 맞다. 그런데 **줄 주석 원장 `#29` 가 `ac4:123` 의 「「그 둘만」 — 키
  집합 등가로 과잉 제거까지 잡는다」를 접으면서 ① 소유자로 「같은 파일 모듈 docstring ¶2」를 명시
  지목**했다(「사용 지점 사본을 지우고 머리말을 남기는 원장 docstring `#5`·`#8` 선례」). 그 선례를
  지는 것이 **산문**(바로 이 문단)이라, 지우면 이미 앵커된 `#29`(@지문 `4c9ff2211279`)가 조용히
  낡는다 — 슬라이스 24 가 `#9` 모듈 ¶1 ↔ 줄 주석 `#26` 에 쓴 가름 그대로다.
  - ⚠️ **이 자리는 한 번 지웠다가 나가는 방향 sweep 으로 되돌렸다.** ③ 축자만 보면 제거가 맞고,
    게이트도 지운 트리를 rc=0 으로 통과시킨다(실측). 되돌린 근거는 **다른 표면의 이미 앵커된 행**
    하나뿐이다. ③ 재판정 후보로 이름만 남긴다.
- **유지 36줄**: `ac2` 의 직렬화 규칙 고정과 `MIN_SHRINK_FACTOR` **수치 근거** · 구조 단언이 무엇을
  보는지 · 함수·케이스 doc 다섯 · `ac4` 의 픽스처 교차 제약 · **등가/과잉 제거 문단**(위 🔴) · `resourceVersion`/`annotations` 절 ·
  `McpError`(-32602) 문단 **전수**(#79 는 「`name` 누락은 `resource_list` 안내와 함께 `-32602`
  거부」라는 *사실*만 적고 「셀렉터를 여기서 풀면 list verb 를 행사하게 되어 verb 1:1 이 깨진다」는
  **구조적 보장**을 적지 않는다) · `_normalized` 의 디코더 문단 · `_kubectl_whole_object` 의
  `--show-managed-fields` 문단(**관측된 실패** 「첫 CI 에서 실제로 그랬다」 포함 — #79 무기재).

### `#8` dear-baby 셋 + `ping_ac1.py` — 제거 **0** · 유지 34

③ 히트 **0** 이다. 네 파일의 doc 이 적는 것과 여덟 PR 본문이 적는 것의 **축이 겹치지 않는다**.

| 주석이 적는 것 | 본문이 적는 것 |
| --- | --- |
| `ac1` 의 **「Not asserted」 경계** — kind 픽스처의 `/reset-user` 가 busybox 스텁이라 필드 수준 효과가 관측 불가이고, 관측하려면 실 백엔드 이미지 + 시드 DB 가 필요하다 | #42: 「단언을 한 줄도 바꾸지 않는 **순수 이동**」이 가능한 세 도메인을 고른 근거(primary 하나만 쓰고 · 프록시 인자가 없고 · 공유 가변 상태가 없다) |
| `ac2` 의 **판별자 독법** — 세 절의 판별자가 전부 「호출이 *실패한다*」인 이유(무시된 `selector` 였다면 픽스처 파드를 그대로 찾았을 것, 무시된 `container` 였다면 `backend` 로 exec 되어 성공했을 것)와 「거부 문구는 클러스터가 정할 말이지 이 서버가 정할 말이 아니다」 | #44: 읽기 전용 케이스만 쪼갠 근거와 배차 수치(전용 파일 22 → 33 · 분할 대기 27 → 16) |
| `ping_ac1` 의 **층 승격 선언** — `internal/server/mcp_test.go::TestPingToolReturnsPong` 의 in-process 단언을 배포 e2e 계층으로 올린 것 | #38: 「`smoke.py` 가 `ping` 을 **호출조차 하지 않았다**」는 *그때의* 결함 |

**관찰 — 「저작 PR 이 있으니 ③ 히트가 있을 것」은 여기서도 성립하지 않는다.** 슬라이스 24 가
`#18` 에서 같은 것을 봤지만 원인이 다르다. `#18` 은 본문이 길되 *다른 축*을 적어서였고, 이 행은
**본문이 전부 「무엇을 어디로 옮겼는가」의 산술**이라 애초에 설계 판단을 적는 자리가 없다. 분할·이동
PR 은 ③ 모집단에 이름은 올리되 수확은 내지 않는다 — 다음 패스가 칸을 고를 때 **모집단의 성격**을
PR 수보다 먼저 볼 것.
## 런타임 무영향

docstring 편집은 AST 안에서 일어나므로 슬라이스 21 이 세운 대로 **docstring 노드를 걷어낸 뒤의
AST 동등**을 썼다 — 두 파일 모두 편집 전후 동일. 기계가 읽는 선언(`검증 시나리오:`·`실행 대상:`·
`병렬 레인:`)은 축자로 살아 있고, `py_compile` 통과. `internal/`·`main.go`·`scripts/`·
`tests/k8s/`·다른 `tests/integration/` **0바이트**. **자매 축 무영향** — `check_mock_policy`
`084bfbeb1c98` · `check_ac_mapping` `6aa2fb1e795a` 두 출력 md5 가 base 와 동일.

## 게이트를 양방향으로 굴렸다

**고친 트리 rc=0** — `check_comment_policy`(docstring **2,069줄** · ③④ **24/32 잔량 8** · 줄 주석
표면 **무이동** 67/67 · 3,337) · `check_doc_inventory`(허브 **82/82** · 끊긴 링크 0, 새 패스
문서로 81→82 · 정책 41→42) · `check_mock_policy` + 자기 테스트 · `check_ac_mapping`.

**원장 `#10` 행의 줄 수·지문과 두 마커만 부모 값으로 되돌린 트리 rc=1 — 5건**:

| 규칙 | 걸린 것 |
| --- | --- |
| **R6** ×2 | `#10` 의 **줄 수**(42≠36)와 **지문**(`acb2c66e15c4`≠`a7205309391e`) |
| **R8** ×1 | docstring 합계 2,075 + 잔량 0 ≠ 실측 2,069 |
| **R10** ×2 | ③④ 판정이 **현재 지문에 묶이지 않은 행 1개**(`#10` — 결과 칸엔 새 앵커가 있는데 지문 칸이 옛 값) + 「완료 23 + 잔량 10 ≠ 전체 32」 |

⚠️ **슬라이스 23·24 와 걸리는 모양이 다르다.** 그때는 R6 여섯 + R8 로 **R10 이 울지 않았다**(잔량
마커만으로는 제거를 못 지킨다는 관측). 이번엔 **R10 이 함께 운다** — 되돌린 범위에 지문 칸이
들어가 「결과 칸의 앵커 ≠ 현재 지문」이 성립했기 때문이다. 즉 **R10 이 무엇을 잡는지는 되돌린
범위가 정하고**, R10 만 믿으면 여전히 새는 자리(지문은 맞는데 잔량만 부풀린 경우)가 있다.

🔴 **`#8` 은 되돌릴 것이 없다** — 줄 수·지문이 무변(34 · `6db41ad6420b`)이라 이 행의 ③④ 판정을
지키는 것은 **R10 의 「완료 + 잔량 = 전체」 항 하나뿐**이다. **제거 0줄인 행은 R6 이 지키지
못한다** — 판정만 적고 잔량 마커를 안 올리면 그 행은 조용히 샌다.

## 결론

| | |
| --- | --- |
| 물은 행 | **2**(`#10` · `#8`, 76줄) — `#21` 은 자매 착지로 뺐다 |
| 제거 | **6줄** (제거율 8%) |
| docstring 표면 | 22/32 → **24/32 · 잔량 8** |
| 줄 주석 표면 | **무이동**(67/67 · 잔량 0 · 3,337) |
| 갈려서 남긴 것 | ③ 재판정 후보 **+1**(`#10` `ac4` ¶2) · 번복 **1**(`#10` `ac2` ¶4) |

## 다음 패스의 입력

- **잔량 8행**: `#2`(108줄 · `run_all.py`·`check_ac_mapping.py`) · `#4`(220 · 7파일) ·
  `#5`(220 · 9파일) · `#6`(100 · 14파일) · `#7`(225 · 17파일) · `#16`(128 · 9파일) ·
  `#17`(87 · 5파일) · **`#21`**(35 · 단일 파일).
- 🔴 **다음 칸 제안은 `#21` 이다** — 이 패스가 판정까지 마쳤다가 자매 착지로 뺀 칸이고, 단일 파일
  35줄로 남은 것 중 가장 작다. **판정을 그대로 재사용하지 말 것**: 자매가 「레인 미선언」 문단을
  `병렬 레인: gate-streams` 선언 + 새 3줄로 갈아 끼웠으므로 그 자리는 **새로 물어야 한다**. 나머지
  네 문단(¶1·¶2·¶3·¶5 — 전부 #148 축자)은 자매가 건드리지 않았으니 그 실측은 그대로 선다.
  그 다음은 `#17`(87줄 · 5파일 — `approval_gate_ac6/ac10` · `resource_generic_ac6/ac7/ac16`), 그
  다음이 `#16`(128 · 9파일, `_gatekeeper.py` 가 끼어 ① 소유자 판정이 갈린다).
- **`#2` 는 성격이 다르다** — 유일한 **하네스** 행(게이트·러너의 자기 서술)이라 ① 소유자 판정이 다른
  칸들과 다르게 걸린다.
- **남은 큰 칸 `#4`·`#5`·`#6`·`#7`**(7~17파일, 합 765줄)은 슬라이스 23·24 가 적은 그대로다 — 담으려면
  행을 파일 단위로 쪼개는 원장 편집이 선행돼야 하는데 그것은 **R7(이중 등재 금지)** 를 건드린다.
  **별도 패스로 설계할 것.**
- 🔴 **나가는 방향 sweep 을 제거선 확정 *전에* 돌릴 것.** 이 패스는 ③ 축자만 보고 `ac4` ¶2 를
  지웠다가 sweep 에서 되돌렸다. **게이트는 그 트리를 rc=0 으로 통과시킨다** — R9·R10 의 앵커는 자기 행
  지문에만 묶여 있어, 다른 표면의 이미 앵커된 행이 낡는 것을 보지 못한다. 세는 법은 「범위 파일 이름 +
  「원장 #N」 표기를 두 원장 결과 칸 전수 grep → 히트마다 **산문인가 코드·거동인가**」다(이 패스 3건 중
  실제 물린 것 1건).
- 🔴 **판정과 착지 사이에 자매가 끼면 그 행은 통째로 뺀다.** 리베이스가 **텍스트 충돌 없이 자동
  병합하는 것이 오히려 정상 경로다**(제거 대상 문단과 자매가 고친 문단이 안 겹치면 그렇다) — 그렇게
  나온 트리는 **어느 패스도 판정하지 않은 조합**이다. 판별은 diff 충돌 여부가 아니라 **원장 행의
  지문이 움직였는가**로 할 것.
- ⚠️ **번복을 한 건 했다**(`#10` `ac2` ¶4 의 「잘라내면 논증이 끊긴다」). **유지 논거 하나**에만
  적용했고 같은 행의 다른 유지 절은 살렸다. 다음 패스도 「갈려서 유지」 자리를 집으면 그 논거가 선
  근거(README 절 · 인용한 선례)를 **먼저 대조**할 것.
- ③ 재판정 후보 누적: **50**(이 패스 +1 — `#10` `ac4` ¶2). 갈림 누적: **5**(+0). ① 재판정
  후보: **7** 그대로. 정책 소유자 몫: **9** 그대로.
