# 2026-09-20 — 복원 경로 ③④ 소급 판정 슬라이스 1 · **경계 확정 뒤 첫 제거**

`rct_20260920-0003` / `tbm_homelab-k3s-mcp-comment-redundancy`.

이 패스는 ③④ 를 묻는 **다섯 번째**이고, **처음으로 0 이 아닌 제거**(19줄)를 낸다.
앞 넷([`2026-09-19-restoration-paths-3-4.md`](2026-09-19-restoration-paths-3-4.md) ·
[`2026-09-20-restoration-paths-3-4-window-146-147.md`](2026-09-20-restoration-paths-3-4-window-146-147.md) ·
[`2026-09-20-docstring-paths-3-4-ratchet.md`](2026-09-20-docstring-paths-3-4-ratchet.md) ·
[`2026-09-20-restoration-paths-3-4-rows-k8s-gate.md`](2026-09-20-restoration-paths-3-4-rows-k8s-gate.md))가
전부 제거 0 으로 끝난 이유는 판정 실패가 아니라 **경계가 미결**이었기 때문이고, 그 경계를
**PR #160**(`docs(comment-policy): 복원 경로 ③④ 를 다음 편집자에 대한 경로로 확정한다`)이 닫았다.
이 패스는 그 개정이 **main 에 착지한 뒤에만 성립한다** — 선행이 빠지면 앞 네 패스와 같은 0 이 된다.
> **착지 형태(2026-09-21 재준비)**: #160 은 자매 착지(#162)로 `doc-tracker` 허브 집계 줄이 충돌해 열린 채
> 머물렀고, 이 슬라이스는 그 선행을 기다리다 한 번 `failed` 로 닫혔다(`rct_20260920-0003` 1차). 2차는 #160 의
> 커밋을 **바이트 그대로**(README·패스 문서 무수정) 이 PR 앞에 실어 **한 squash 로 함께 착지**한다 —
> 판정과 그 근거가 같은 커밋에 있으므로 「경계가 없는 상태에서 제거가 먼저 착지」하는 창이 생기지 않는다.
> 아래 절대 수치는 **2026-09-21 착지 base(`91c3831`, #170) 기준 재측정값**이다 — 09-20 계획 시점 값(합계
> 3002 → 2983 · 잔량 47 → 45)은 그 사이 자매 슬라이스(#164·#167·#168·#169·#170)가 합계와 ③④ 잔량을
> 움직여 낡았다(auth·mcp 행 ③④ 재앵커로 잔량 47 → 45 는 이미 main 에 있다).

## 이 패스의 범위 (완료 기준)

[`ledger.md`](../ledger.md) 의 ③④ **잔량 45행 중 2행**을 판정하고, 이미 판정된 1행을 **경계 확정에
따라 재판정**한다. 완료 기준은 셋이다.

| | 기준 | 값 |
| --- | --- | --- |
| ⑴ | 줄 주석 ③④ 잔량 마커 | **45 → 43** |
| ⑵ | 경계 확정이 풀어 준 2건의 실제 제거 | `010bbf3e1e73` 행에서 **5줄** |
| ⑶ | 새로 판정한 2행의 제거 | `279691d20783` **14줄** · `5a57a42faec1` **0줄** |

**범위 밖**: docstring 표면 22행(마커 `docstring-복원경로-잔량` **22 무이동**), 줄 주석 잔량 43행.
`4d6bcbef3d25`(k8s 픽스처 행)는 **고의로 뺐다** — 열린 PR #163 이 그 행의 범위에 파일을 더해
판정 직후 지문이 움직이기 때문이다.

## 왜 이 세 행인가

- `010bbf3e1e73` — [직전 패스](2026-09-20-restoration-paths-3-4-rows-k8s-gate.md)가 ③ **축자 확인
  2건**까지 세어 놓고 「③ 이 *앞으로 이 파일을 고치는 사람*에 대한 복원 경로인가」가 미결이라
  절차 5번으로 남긴 바로 그 둘이 여기 있다. 경계가 확정된 지금, **이 행이 첫 제거의 자리**다.
- `279691d20783`(99줄) · `5a57a42faec1`(124줄) — 잔량 중 **뜨거운 행**이면서 파일을 들여온 PR 이
  각각 **하나뿐**이라(#143 · #150) ③ 의 모집단이 닫혀 있다. 새 요건(「③ 로 확인될 때는 PR 번호·
  커밋 해시를 적는다」)을 치를 비용이 가장 작은 두 행이다.

## 측정 — ③ 의 모집단

| 행 | PR | 본문 | 리뷰 코멘트 | 이슈 코멘트 | 머지 커밋 |
| --- | --- | --- | --- | --- | --- |
| `279691d20783` | **#143** 하나 | 4,217자 | 0 | 0 | `286a0919c9a2` |
| `5a57a42faec1` | **#150** 하나 | 3,797자 | 0 | 0 | `ac9f9d3472be` |
| `010bbf3e1e73` | 24건(직전 패스 실측) | 103,786자 | 0 | #109 의 1건 | — |

**④ 는 이 패스에서 뒤지지 않았다.** README 개정이 ④ 의 적용 범위를 「PR 없이 main 에 직접 push 된
커밋」으로 한정했고, 세 행의 파일을 건드린 커밋 중 그런 것은 **0건**이다(전부 PR 경유). 앞 패스들이
④ 에 전건 ❌ 를 적은 것과 결과는 같지만 **사유가 다르다** — 판정 실패가 아니라 대상 부재다.

## 항목별 판정 — `010bbf3e1e73` → `a5928dd3e0b2` (제거 5줄)

### ⑴ `internal/k8s/service.go` — `ProxyResource` doc **2줄 제거**

```go
// ProxyResource reaches one object's HTTP endpoint through the apiserver
// (prd-resource-generic AC15). It is the one stream method whose verb is
// an argument rather than a constant, because AC15 makes the verb follow
// the HTTP method.
```

둘째 문장이 **PR #150** 본문 「계획에 없던 판단 1」에 축자로 있다 — 「`gatedPairs` 는 쓰기 verb
5종과 민감 종류 읽기만 게이트로 보내는데, **AC15 는 verb 를 HTTP 메서드에서 끌어내므로**
`GET`→`get on pods/proxy` 가 그 판정으로는 승인 없이 통과한다」(머지 커밋 `ac9f9d3472be`).
**③ 축자 ✅ · ④ 대상 부재 ❌ ⇒ 제거.** 첫 문장(식별자 이름으로 시작하는 관례 1줄 doc)은
유지 대상이라 남긴다.

### ⑵ `internal/k8s/types.go` — `AttachOutcome` doc **3줄 제거**

```go
// AttachOutcome captures one read window on a running container's streams. It
// is a separate type from ExecOutcome because nothing was started, so nothing
// ended: the process is still running when the window closes and there is no
// exit code to report.
```

둘째 문장이 **PR #127** 본문의 구현 목록에 축자로 있다 — 「`internal/k8s/types.go` — `AttachRef` ·
**`AttachOutcome`(exit code 없음: 아무것도 시작하지 않았으므로 아무것도 끝나지 않는다)**」
(머지 커밋 `7a8edf1667c5`). **③ 축자 ✅ · ④ 대상 부재 ❌ ⇒ 제거.** 관례 1줄 doc 은 남긴다.

### 유지 — `PortForwardOutcome.ResponseEncoding`

직전 패스의 판정을 뒤집지 않는다. **#146 본문이 「근거는 코드 주석에 있다」로 이쪽을 지목**하므로
사본을 근거로 원본을 지울 수 없고, README 개정도 이 경계를 **살려 두었다**.

## 항목별 판정 — `279691d20783` → `b1073478a0bb` (제거 14줄)

#143 본문의 「설계상 짚어 둘 두 가지」가 주석 셋의 *이유*를 축자에 가깝게 담는다.

| # | 주석 | ③ 출처(#143 본문) | 판정 |
| --- | --- | --- | --- |
| ⑴ | `callDeleteCollection` doc 의 「`Describe` 는 `gate.Authorize` 가 이미 묻겠다고 정한 뒤에 불려 승인 요청을 물릴 수 없다」 **4줄** | 1번 — 「`Describe` 는 `gate.Authorize` 가 이미 「묻겠다」로 정한 뒤에 불리므로, 거기서는 승인 요청을 물릴 수 없다. 그래서 이 도구는 `authorize` 를 타지 않고 `callCreateBatch` 와 같은 방식으로 자기 게이트 경로를 가진다」 | **제거** |
| ⑵ | 0건 분기의 「승인 없는 `deletecollection` 은 판정 없이 클러스터에 닿는 게이트 대상 verb 이고, 『어차피 아무것도 안 지웠다』는 이 층이 사후에 할 수 있는 주장이 아니다」 **5줄** | 1번 끝문장 — 같은 두 절이 한국어로 그대로 | **제거** |
| ⑶ | `confirmCollectionUnchanged` doc 의 「목록 `resourceVersion` 은 이 선택에 없던 쓰기에도 전진해 정직한 호출을 거부하면서 집합 비교가 놓치는 것은 잡지 못한다」 **5줄**(구분 `//` 포함) | 2번 — 「목록 버전은 컬렉션의 읽기 시점 리비전이라 이 선택에 없던 쓰기에도 전진해, 바쁜 클러스터에서 정직한 호출을 거부하면서 집합 비교가 놓치는 것은 하나도 잡지 못한다」 | **제거** |
| ⑷ | 「Growth 는 AC11 이 이름 붙인 경우고 줄어듦·교체도 같은 근거로 거부된다」 2줄 | 2번 끝 — 「늘어남·줄어듦·교체를 모두 불일치로 본다」 | **유지**(부분) |
| ⑸ | `Namespace` 가 이 패키지에서 유일하게 포인터가 아닌 이유 | 표의 「`namespace` 필수」는 *무엇*이고 *왜* 가 아니다 | **유지** |
| ⑹ | 「apiserver 는 빈 namespace 를 전 네임스페이스로 읽는다」 | 본문에 없음 | **유지** |
| ⑺ | `contextNameLimit` · `selectorText` · `sameTargets` · `DeleteCollectionResult` 가 삭제 수가 아니라 요청을 보고하는 이유 | 본문에 없음 | **유지** |

⑷ 가 갈린 자리다(절차 5번). 본문이 세 경우를 한 절로 묶어 적지만 **「Growth 만 AC11 이 이름 붙인
경우」라는 귀속**은 적지 않는다. 그 귀속이 이 2줄이 지는 값이므로 「애매하면 남긴다」로 보존한다.

## 항목별 판정 — `5a57a42faec1` (제거 **0줄**, 지문 불변)

#150 본문은 기전을 **의도적으로 적지 않고 주석을 원본으로 지목한다**. 세 곳이다.

> 2. **client-go 의 편의 경로 둘 다 쓸 수 없었다.** 근거는 `internal/k8s/proxy.go` 의
>    `ProxyResource` doc 주석에 있다(여기와 `doc-tracker` 양쪽에 적으면 주석 정책의 복원 경로가
>    서서 그 주석이 제거 대상이 된다 — 아래 참조).

> 이 본문은 **복원 경로 ③** 이다. 그래서 위 기전 설명을 여기에 옮겨 적지 않았다 — 적으면 같은
> 지식이 두 곳에 서고 판정 절차 3에 따라 그 주석들이 제거 대상이 되어, 코드를 고치는 사람이
> 읽을 자리가 사라진다.

> **검토자가 볼 곳** … `internal/k8s/proxy.go` 의 `ProxyResource`·`apiserverRefusal` doc 주석 —
> 왕복 경로와 403 두 갈래의 **단일 출처**다.

**③ 축자 0.** `ResponseEncoding`↔#146 과 같은 형이고, README 개정이 「PR 이 스스로 주석을 원본으로
가리키면 사본을 근거로 원본을 지울 수 없다」를 살려 둔 그 경우다.

부분 **1**: `proxyPairs` 의 「`genericPairs` 는 verb 를 상수로 받는다 — AC15 의 요점은 verb 가 HTTP
메서드를 따른다는 것」은 #150 본문 「계획에 없던 판단 1」과 문면이 겹친다. 그러나 본문이 말하는
것은 **`gatedPairs`**(`internal/mcp/gate.go` — 다른 행)의 확대이고, 이 줄이 지는 것은 「그래서
`genericPairs` 를 재사용할 수 없다」라는 **다른 주장**이다. 유지.

> **같은 문장이 한 행에서는 제거 근거가 되고 다른 행에서는 아니다.** `service.go` 의 2줄은
> #150 본문이 **그 주장을 대신 말해** 지웠고, `proxy.go`·`mcp/proxy.go` 의 주석은 #150 본문이
> **그 주장을 자기가 말하지 않겠다고 선언해** 남겼다. 갈리는 것은 문장이 아니라 **본문이 그
> 지식의 소유자를 어디로 지목했는가**다.

## 결론 — 수익률 0 이 다섯 번째에 깨졌다

| | 값 |
| --- | --- |
| 이 패스의 제거 | **19줄**(5 + 14 + 0) |
| 줄 주석 판정 합계 | 3211 → **3192** |
| ③④ 판정 완료 / 전체 | 17/62 → **19/62**, 잔량 **45 → 43** |
| docstring 표면 | **무이동**(6/28 · 잔량 22 · 합계 1895) |

앞 네 패스가 0 을 낸 것은 ③ 가 비어서가 아니라 **닫힌 PR 본문을 복원 경로로 셀 수 있는지가
미결**이어서였다. 경계가 확정되자 같은 모집단에서 19줄이 나왔다.

## 다음 패스의 입력

- 줄 주석 잔량 **43행**. 파일을 들여온 PR 이 하나뿐인 행(`3ec066d625eb` · `92c53441af53` ·
  `5a68b963ac0b` · `ef1af58d9577` · `17850d2eae26` 등 최근 창의 단일 PR 행)이 같은 이유로 싸다.
- `4d6bcbef3d25` — **PR #163 착지 뒤에** 판정할 것. 그 PR 이 이 행의 범위에 파일을 더한다(착지 시점: #163·#164 가
  착지해 그 행은 `e82057fcdeba`·29줄이 됐고 보류 조건은 풀렸다).
- docstring 잔량 **22행** — 아직 한 번도 이 축으로 묻지 않은 표면이다. 줄 주석 쪽 모집단 조사가
  그대로 재사용되지 않는다(같은 PR 이라도 docstring 은 다른 문장을 담는다).
- **관측**: 이 패스의 19줄 중 14줄이 **단일 PR 행**에서 나왔다. 모집단이 닫힌 행일수록 ③ 축자가
  잘 잡힌다는 가설이 선다 — 잔량 43행을 「들여온 PR 수」로 정렬해 고르는 것이 다음 슬라이스의
  기본값이다.
