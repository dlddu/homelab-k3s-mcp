# 2026-09-25 — 복원 경로 ③④ 소급 판정 슬라이스 30 · **session 도구 e2e (원장 `#5`, 220줄 / 9파일)**

`rct_20260925-0004` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 29](2026-09-25-paths-3-4-slice-29.md)가 전용 e2e 큰 칸(`#7`)을 닫아 잔량을 3 → 2 로
내리고, 남은 두 행(`#5` 220·9 · `#6` 100·14 = 320줄)에 대해 **「행 크기를 승계하지 말고 커밋
분포를 각 행에 다시 실측해서 고를 것」**(슬라이스 28 이 세운 규칙)을 요구했다. 이 패스가 그 둘 중
`#5` 를 묻는다. 줄 번호는 base `669e918`(#212 = 슬라이스 29) 기준이다.

## 선택 — 두 행의 커밋 분포를 이 tip 에서 다시 재서 `#5` 를 골랐다

**⑴ 재실측은 인계 값을 재현했다.** 승계하지 않고 `ast` 로 커밋마다 docstring 집합을 부모와
대조해 다시 갈랐다:

| 행 | 줄 | 파일 | 파일을 건드린 커밋 | **docstring 을 움직인 커밋** | 저작 PR(공통 제외) |
| --- | ---: | ---: | ---: | ---: | --- |
| `#5` | 220 | 9 | 9 | **6** | #50 · #55 · #56 · #58 |
| `#6` | 100 | 14 | 6 | **4** | #43 · #44 |

`#6` 은 파일이 다섯 개 많은데 저작 커밋은 더 적다 — 판정 비용이 파일 수가 아니라 **블록의 저작
시점이 몇 갈래로 갈리는가**에 붙는다는 슬라이스 28·29 의 명제가 이 tip 에서도 성립한다.

**⑵ ⒝ R7 파일 단위 분할은 이 행에도 산출이 없다.** 두 행 모두 **단독 커밋 0** 이다(`#5` 는
파일당 2~3 커밋, `#6` 은 2~3 으로 균일). 파일이 언제나 무리로 함께 움직였으므로 행을 쪼개도 지문이
낡는 사건은 조각 전부에서 동시에 일어난다 — 원장 행만 늘고 해상도는 그대로다. 슬라이스 29 가 이
두 행에 대해 미리 적어 둔 예측과 같고, **승계하지 않고 다시 세어** 확인했다.

**⑶ `#5` 를 고른 이유는 모집단이 새것이기 때문이다.** `#6` 의 고유 모집단(#43 · #44)은 슬라이스
29 가 이미 **전부 읽은** 집합이고, `#5` 의 고유 모집단(#50 · #55 · #56 · #58)은 **어느 패스도 읽지
않았다**. 읽는 PR 하나당 판정하는 줄 수도 `#5` 가 크다(36.7줄/PR · `#6` 25.0). 모르는 쪽을 먼저
열고 이미 읽힌 쪽을 남기는 순서다. 220줄은 직전 패스들의 실적(26 은 223 · 28 은 220 · 29 는 225)과
같은 크기라 한 패스에 든다 — **둘을 한 번에 집지 않은 이유**는 320줄이 그 실적의 1.4배이고,
남는 `#6` 은 이 패스의 착지가 `ledger.md` 를 움직여 재감지가 잇기 때문이다(이 task 자신이 슬라이스
29 의 착지로 열렸다).

## 이 패스의 범위 (완료 기준)

docstring 원장 **한 행**(`#5`, `85c94a02f46f`, session 도구 e2e 9파일 220줄)에 대해 복원 경로 ③④
를 물어 판정하고 현재 지문에 못박는다(R10). 게이트 헤드라인으로 확인 가능한 네 칸 — **전부 델타로
적는다**(자매 PR #208·#210 이 `ledger.md` 를 만지므로 절대 지문은 리베이스 한 번에 낡는다):

| | 전 | 후 |
| --- | --- | --- |
| docstring ③④ 판정 | 30/32 (잔량 **2**) | **31/32 (잔량 1)** |
| docstring 합계 | 2001줄 / 109파일 | **1979줄 / 109파일** |
| 원장 행 `#5` | `85c94a02f46f` 220줄 | **`a27ad2027fee` 198줄** |
| 줄 주석 표면 | 3337줄 · 67/67 · 잔량 0 | **무이동**(지문 `91d88b628c41…` 불변) |

범위 밖: docstring 잔량 1행(`#6`), `README.md` 개정(정책 소유자 몫), 주석의 **정확성**(이 모델의
판정 표면 밖), 지문 패턴 확장(tobe-modeler 소관).

## 측정 — ③ 모집단은 저작 PR 과 판정 패스 PR 을 갈라 셌다

`#5` 의 docstring 을 움직인 커밋 6건 중 **#63·#65 는 판정 패스**다(각각 「docstring 표면을 게이트로
승격 + 정형 문단 164줄 제거」와 「60파일 docstring 전수 판정」). 그 둘의 본문이 이 파일들의 주석을
인용하는 것은 **판정 기록이 주석을 가리킨 것**이라 README 3분 표 **셋째 줄**(「PR·커밋이 스스로
"이 내용은 주석에 적었다"고 가리키는 것 → 원본은 주석 쪽」)에 걸린다. 사본을 근거로 원본을 지우면
SSOT 가 뒤집히고, 그 길을 열면 판정 패스가 인용한 모든 유지 주석이 다음 패스에서 제거 대상이 되는
**자기 무효화 고리**가 생긴다. 그래서 ③ 의 모집단은 **저작 PR 넷**(#50 · #55 · #56 · #58)이다.

리뷰 코멘트는 넷 전부 **0**이고 이슈 코멘트도 **0**이라, 이 행에 대한 ③ 의 실체는 **본문뿐**이다.

## 제거 22줄 — 네 자리

| # | 자리 | 줄 | 근거 |
| --- | --- | ---: | --- |
| ⑴ | `session_list_ac1.py` 머리말 「자기 선행 조건을 스스로 성립시킨다」 | 4 | **#50 축자** — 「각 파일이 자기 선행 조건을 스스로 성립시킨다. AC1 은 제어면의 두 상태를 요구하는데 파일 간 실행 순서로 만들지 않고 한 파일 안에서 재고를 비운 뒤 시드한다」. ① `run()` 이 `empty_inventory` → `enumerates_sessions` 순으로 부르는 것이 그 구조 자체다. 「그 결합은 직전 슬라이스가 기각했다」는 **그때의 경위**(첫째 줄) |
| ⑵ | `session_read_ac1.py` 머리말 「데이터 플레인을 요구하는 첫 e2e다」 | 7 | **#55 표 축자**(`Service.Read` → `agent.Read(pod, offset)` → 파드 IP 해석 후 `GET :8090/read`) + **#58 층 표 (a)**(`DATA_PLANE_IMAGE` + `data-plane` SA). 「**첫**」·「**이번에** 갖춘 이유」가 그때의 경위다 |
| ⑶ | `session_read_ac3.py` 머리말 「두 에러의 층이 다른 것은 의도된 설계다」 | 5 | **#55 축자** — 「층이 서로 다르다는 것까지 단정한다. 후자가 AC 의 「세션 상태는 바뀌지 않는다」를 약속이 아니라 **구조**로 만든다 — 요청이 0건이면 건드릴 방법이 없다」. ①: 바로 아래 두 케이스 docstring 이 각 층을 **자기 형태로 이미 적는다**(`isError` 도구 에러 / `McpError` `-32602`) |
| ⑷ | `session_write_ac1.py` 머리말 「claude-code 절은 범위 밖이다」 | 6 | ② `docs/doc-tracker/2026-09.md` 의 「범위 밖으로 함께 남은 것」 절 + **#58 층 표 (b)** 및 「범위 밖(후속)」 절. 게다가 이 문단이 원본으로 지목한 `docs/doc-tracker.md` 는 **이미 없는 경로**다(디렉터리로 쪼개졌다) — 「어긋나면 주석이 틀린 쪽」 조항이 제거 근거를 겹쳐 준다 |

## 🔴 보존된 ①② 판정의 「복원 경로 넷 어디에도 없다」가 소급해 일부 거짓이었다

이 행의 2026-09-07 결과 칸은 유지 근거를 열거하며 여섯 자리를 이름으로 들고 **「…가 전부 복원 경로
넷 어디에도 없다」**로 닫는다. 그 가운데 **넷이 저작 PR 본문에 축자에 가깝게 있다**:

* `session_list_ac2.py` 「스텁 앞에서는 파드 불변 단정이 vacuous 하다」 ↔ **#50** 「AC2 의 "새 파드가
  기동되지 않음" 절은 스텁 앞에서는 **vacuous** 하다 … 파드 집합 불변이 **진짜 판별자**가 된다」
* `session_read_ac3.py` 「약속이 아니라 구조」 ↔ **#55** 축자(위 표 ⑶)
* `session_write_ac1.py` 「마커를 명령문 안에 두면 PTY 에코만으로 단정이 성립해 버린다」 ↔ **#58**
  「PTY는 타이핑된 명령을 되울리므로 마커가 명령문 안에 있으면 「출력에 마커가 있다」가 에코만으로
  성립한다」
* 같은 파일 「claude-code 절은 `DATA_PLANE_CLAUDE_CODE_IMAGE` 가 없어 배포 자체가 불가」 ↔ **#58**
  층 표 (b)

R10 이 서기(2026-09-20) 전의 ①② 판정이라 **③④ 를 실제로 뒤진 것이 아니라 묻지 않은 채 없다고
단정**한 것이고, 앵커가 없어 게이트는 이 행을 계속 초록으로 통과시켰다. 슬라이스 28·29 가 만난 것과
같은 형이다. **번복은 그 넷의 근거에만 적용했고 나머지 절은 그대로 살렸다** — 그리고 넷 중 둘은
아래 ⓐⓑ 로 **여전히 유지**다(③ 히트가 곧 제거는 아니다).

## 유지 — 갈려서 남긴 것 셋 (3분 표 둘째 줄)

⒜ `session_list_ac2.py` 의 「스텁 앞에서는 그 단정이 vacuous 하다 … 파드 집합 불변이 진짜
판별자가 된다」 — ③ 히트이지만 **픽스처를 실 제어면에서 스텁으로 되돌리는 손을 막는 불변식**이다.
줄 주석 원장의 `session-platform.yaml` 행(슬라이스 5)이 같은 축에서 「스텁으로 바꾸는 손」을 이유로
남긴 자리와 같다. ③ 은 *그때의 결정*을 적을 뿐 *앞으로의 금지*를 집행하지 않는다.

⒝ `session_write_ac1.py` 케이스의 「마커는 명령문에 없는 형태여야 한다 — 아니면 PTY 에코만으로
단정이 성립한다」 — ③ 히트이지만 **마커를 바꾸는 다음 편집자가 단언을 조용히 공허하게 만드는 것을
막는다**. 에코와 실행을 가르는 이 한 줄이 없으면 「출력에 마커가 있다」가 통과하면서도 아무것도
증명하지 않는다.

⒞ `session_read_ac1.py` 의 「증분을 만드는 write 는 도구를 경유하지 않는다」 — ③ 히트(**#58**)이지만
**여기서 `session_write` 를 쓰는 손을 막는다**. 그 순간 이 파일의 3번 절이 session-write/AC1 의
도구가 도는지에 함께 매달린다.

## 유지 — 모집단에 히트가 없는 것

`session_read_ac1.py` 의 「2·4번 절이 vacuous 하지 않은 이유는 `scrollback.Since` 가 그것을
**구조로** 만들기 때문」과 `_quiet_end` 의 「쉘이 자기 일정으로 쓰므로 write 중간에 읽으면 AC 와
무관한 이유로 실패한다」 — 저작 4 PR 본문에 **히트 0**이다.

`session_list_ac3.py`·`session_read_ac4.py`·`session_write_ac5.py` 의 `auth-variant` 고유 논증
(`SESSION_PLATFORM_ENDPOINT` 가 base 배포에만 있다)과 `session_write_ac3.py` 의 vacuous 논증은
2026-09-07 판정의 ⓐ·ⓒ 를 **승계**했다 — 그 판정이 ①② 축에서 내린 것이고 ③ 에도 히트가 없어 축이
갈리지 않는다.

**#56 은 모집단에 들되 수확 0이다.** 그 본문은 슬라이스 경계표(어느 AC 가 선행 없이 닫히는가)와
원장 교정(AC4 의 선행 재판정)을 적어, 이 행의 docstring 이 적는 축과 겹치지 않는다. **③ 수확은
모집단 크기가 아니라 「그 PR 이 무엇을 적었는가」로 갈린다**는 슬라이스 24·26·29 의 관찰이 여기서도
재현됐다.

## ④ 재실측 — 적용 대상 0

base `669e918` 에서 최근 120커밋 중 제목에 PR 번호가 없는 **21건**을 커밋별
`/commits/<sha>/pulls` 조회로 갈랐다(제목 유무는 **틀린 대리 지표**다 — 번호만 없이 PR 로 착지한
커밋이 실제로 있다): **20건이 정책 범위 밖** `chore(deploy):` 이미지 핀(건드린 파일은
`k8s/deployment.yaml` 하나), **1건은 PR 로 착지**. 정책 범위 파일을 건드린 main 직접 push **0** ⇒
④ 의 적용 대상은 오늘도 0이다.

## 검증

**게이트 네 종 rc=0** — `check_comment_policy.py` · `check_ac_mapping.py` ·
`check_mock_policy.py` · `check_doc_inventory.py`.

**양방향(음성 프로브)** — 원장만 부모 값으로 되돌린 트리에서 rc=1 이 나오는 것을 **단정 단위**로
확인했다: R6 줄 수 칸 · R6 지문 칸 · R8 합계 마커 · R10 ③④ 잔량 마커 · R10 앵커 문면(지문만 옛
값으로) **5건**.

**런타임 무영향** — 네 파일에서 docstring 노드를 걷어낸 뒤의 `ast.dump` 가 부모와 **바이트 동일**
하고, 제품 코드(`main.go`·`internal/`)는 **0파일**이다. 기계가 읽는 선언(`검증 시나리오:` ·
`실행 대상:` · `병렬 레인:`)은 diff 에서 **0줄**이다.

**줄 주석 표면 무이동** — 3337줄 · 67/67 · 잔량 0 과 전체 지문 `91d88b628c41…` 이 전후 동일하다
(이 패스는 docstring 표면만 만진다).

## 범위 밖(후속)

docstring 잔량 **1행**:

* `#6` 100줄 / 14파일 — 저작 커밋 4(#43 · #44 + 공통 #63 · #65). 고유 모집단 둘은 슬라이스 29 와
  이 패스가 **읽지 않은** 집합이 아니다(29 가 `#7` 에서 읽었다). 다만 **판정은 승계하지 말 것** —
  같은 PR 이라도 블록이 다르면 무엇을 적었는지가 갈린다(슬라이스 28 이 자매 표면 모집단을
  승계했다면 제거 16줄 중 9줄을 놓쳤다). 이 행이 닫히면 docstring 표면은 **32/32 · 잔량 0** 이 된다.

⒝ R7 파일 단위 분할은 `#6` 에 대해서도 **산출이 없다**(단독 커밋 0, 이 패스가 재실측).

`#5` 에서 **③④ 축으로는 묻지 않은 채 남긴 것** 하나를 기록해 둔다: `session_read_ac1.py` 의
「AC1 의 검증 방법 네 절」 열거(6줄)는 ② `docs/prd-session-read.md` AC1 의 **검증 방법 문면과 거의
축자**이고 ① 네 케이스 docstring 이 각각 같은 절을 적는다. 이 패스는 ③④ 래칫이므로 ①② 축의
재판정은 열지 않았다 — 열려면 그 축의 task 로 와야 한다.
