# 2026-09-20 — 복원 경로 ③④ 재판정 · 직전 패스가 고의로 남긴 **뜨거운 두 행**

`rct_20260920-0002` / `tbm_homelab-k3s-mcp-comment-redundancy`.

이 패스는 **주석을 지우지 않는다.** [`2026-09-20-restoration-paths-3-4-window-146-147.md`](2026-09-20-restoration-paths-3-4-window-146-147.md)
가 「다음 패스의 입력」으로 축자로 지목한 두 행 — `010bbf3e1e73`(79줄) · `92a3ce829b84`(399줄) —
에 대해 [`README.md`](../README.md) 판정 절차 3번이 묻지 않은 두 경로(③ PR 제목·본문·리뷰 코멘트,
④ 커밋 메시지)를 **현재 지문에서** 마저 묻고, 갈린 자리를 남긴다(절차 5번이 요구하는 기록).
같은 형태의 [`2026-09-19-restoration-paths-3-4.md`](2026-09-19-restoration-paths-3-4.md) ·
[`2026-09-20-restoration-paths-3-4-window-146-147.md`](2026-09-20-restoration-paths-3-4-window-146-147.md) ·
[`2026-09-20-docstring-paths-3-4-ratchet.md`](2026-09-20-docstring-paths-3-4-ratchet.md) 에 이어 **네 번째**다.

## 왜 지금 이 두 행인가 — 미룬 사유가 사라졌다

직전 패스가 이 둘을 남긴 이유는 「`ac9f9d3`(#150)이 +57줄을 들여와 **이 패스가 ③④ 에 대해 재어 본
적 없는 내용**이 들어왔다」였고, 그 위에 **승인~실행 사이에 두 행이 또 움직인다**는 현실적 차단이
있었다(자매 모델 `tbm_homelab-k3s-mcp-docs-impl` 의 task 가 열려 있었다). 이 창에서 둘 다 해소됐다.

| 차단 사유 | 이 창의 실측 |
| --- | --- |
| 자매 task 가 두 행을 움직인다 | `docs-impl` 09:52 `succeeded` · 10:19 `no_action_needed`, `scenario-e2e` 10:24 `succeeded` — homelab 레포에 열린 정합성 task 는 이 건 하나뿐 |
| 승인~실행 사이 자매 PR 이 지문을 옮긴다 | 계획 시점 **열린 PR 0건**(`/pulls?state=open` 실측) |
| 두 행이 최근 창에서 계속 움직였다 | 창 `#155`·`#156`·`#157` 을 **바이트 불변**으로 통과 — 79·399 · 지문 동일(`internal/mcp/gate.go` 의 `+1줄` 은 주석이 아니었다) |

## 측정 — ③④ 의 실체

두 행의 파일을 건드린 커밋 **24건**에서 PR 번호를 뽑아 본문·리뷰 코멘트·이슈 코멘트를 전수로 받았다.

| | 실측 |
| --- | --- |
| ③ PR 본문 24건 | **합계 103,786자**. 행 1 의 주요 출처는 #21 · #59 · #75 · #123 · #127 · #143 · #146 · #150, 행 2 는 여기에 #73 · #74 · #89 · #104 · #107 · #113 · #114 · #155 가 더해진다 |
| ③ 리뷰 코멘트 | **전건 0**. 이슈 코멘트는 #109 의 1건(1,105자)뿐이고 나머지 23건은 0 |
| ④ 커밋 24건 전문 | **합계 5,310자**. 본문 비공백 **≥5줄은 `32be7a5`(5줄) 하나**, 나머지는 0~2줄이고 그 1~2줄도 전부 `Co-authored-by` 트레일러다 |

**④ 는 이 슬라이스에서 사실상 아무것도 복원하지 않는다** — 유일한 실질 본문 `32be7a5`(#123)조차
「무엇을 등록했는가」의 목록이지 주석이 지는 *이유*가 아니다. 다만
[`2026-09-19-restoration-paths-3-4.md`](2026-09-19-restoration-paths-3-4.md) 가 세운 정정대로
**「이 레포는 squash 라 ④ 가 비어 있다」로 일반화하지 않는다**(`cba91ac` 는 본문 비공백 17줄이었다).
위 표는 **이 24커밋에 대한 실측**이다.

> **측정 방식**: 항목별 특징어를 두 코퍼스에 대고 히트 수와 **문맥**을 함께 찍는 스크립트를
> 파일로 두고 돌렸다(파이프-heredoc 으로 넣으면 stdin 이 막혀 「0건」이 통과와 구분되지 않는다).
> 코퍼스 크기를 매 실행에 찍는 것도 같은 이유다. **히트는 단서일 뿐 판정이 아니다** — 꼬리 매치를
> 축자로 세면 그릇이 통째로 없는 항목이 잔여에서 빠진다. 아래 표의 ✅ 는 전부 문맥을 읽어 확인했다.

## 항목별 판정 — `010bbf3e1e73`

`internal/k8s/client.go` · `internal/k8s/service.go` · `internal/k8s/types.go` (79줄)

| 항목 | ③ | ④ | 최종 |
| --- | --- | --- | --- |
| ⑴ `Service` · `KubeService` · `Error` · `errKind` · `LogOptions` 등 exported 1줄 doc 과 패키지 주석 | — | — | **유지 — 판정 대상 아님**(절차 2번, 유지 대상 2) |
| ⑵ `WatchResources` 가 스트림을 돌려주지 않는 이유(「도구 계층은 한 번에 한 요청을 답한다」) | ❌ **부재** — 24건 어디에도 없다 | ❌ | **유지 — 갈림 없음** |
| ⑶ `ProxyResource` 가 **verb 가 인자인 유일한 스트림 메서드**라는 것 | ✅ **축자** — #150 「AC15 는 verb 를 HTTP 메서드에서 끌어내므로」 | ❌ | **유지 — 갈림** |
| ⑷ `ExecOutcome` 의 「잘렸다고 말하는 응답과 조용히 끝난 응답은 답과 거짓말의 차이다」 | ⚠️ **부분** — #123 · `32be7a5` 가 「상한 표시」를 나열하지만 *왜 말해야 하는가*는 없다 | ⚠️ **부분**(같은 문구) | **유지 — 갈림 없음** |
| ⑸ `AttachOutcome` 이 `ExecOutcome` 과 **별 타입인 이유**(아무것도 시작하지 않았으므로 아무것도 끝나지 않았다 = exit code 가 없다) | ✅ **축자** — #127 「계획 단계 1」 | ❌ | **유지 — 갈림** |
| ⑹ `ReadSeconds` 가 **실제 쓴 창**을 되울리는 이유(호출자가 기본값을 몰라도 응답이 읽힌다) | ⚠️ **부분** — #127 · #146 은 기본값·상한만 적는다 | ❌ | **유지 — 갈림 없음** |
| ⑺ `StdinWritten` 이 답하는 질문(입력을 무시하는 프로세스의 침묵과 「도착하지 않았다」를 가른다) | ⚠️ **부분** — #127 「stdin 전달과 빈 문자열이 부재와 구분됨」은 *테스트가 무엇을 재는가*이고 필드가 존재하는 이유는 아니다 | ❌ | **유지 — 갈림 없음** |
| ⑻ `PortForwardOutcome.ResponseEncoding` 이 흔한 쪽에서도 생략되지 않는 이유(Go 의 JSON 인코더가 유효하지 않은 바이트를 **말없이** U+FFFD 로 바꾼다) | ⚠️ #146 본문에 있으나 그 절이 **「근거는 코드 주석에 있다」로 이쪽을 지목한다** | ❌ | **유지 — 갈림 없음**(사본을 근거로 원본을 지우면 SSOT 가 뒤집힌다 — 직전 패스 `3d7f820c8536` ⑶-b 와 같은 자리) |
| ⑼ `BytesSent` — 요청 없이도 응답만으로 읽히게 한다 | ❌ **부재** | ❌ | **유지 — 갈림 없음** |
| ⑽ `TunnelClosed` 가 **상수 true 인데도** 응답에 실리는 이유(1승인 1왕복이 지켜졌다는 것을 전사를 읽는 운영자가 볼 자리가 여기뿐이다) | ⚠️ **부분** — #146 「같은 승인으로 두 번째 왕복이 불가능하다」는 *계약*이고, 그것을 **응답에 싣는 이유**는 없다 | ❌ | **유지 — 갈림 없음** |
| ⑾ `ProxyOutcome.Method`/`Verb` 가 함께 실리는 이유(전사를 읽는 운영자가 `GET` 이 어느 권한을 썼는지 되짚지 않아도 되게) | ⚠️ **부분** — #150 · #146 은 메서드→verb **사상**을 적고 두 필드가 **함께 실리는** 이유는 적지 않는다 | ❌ | **유지 — 갈림 없음** |
| ⑿ `ProxyOutcome.Path` 가 「이 호출이 무엇을 했는지 말하는 유일한 필드」 | ⚠️ **부분** — #150 「경로는 거르지 않는다」는 다른 주장이다 | ❌ | **유지 — 갈림 없음** |
| ⒀ `ProxyOutcome.Status` — 프록시 대상의 404 는 **이 도구의 실패가 아니라 답**이다 | ❌ **부재** — 코퍼스의 `404` 3히트는 전부 `scale` 서브리소스 부재 판정이라 다른 자리다 | ❌ | **유지 — 갈림 없음** |
| ⒁ `BodyEncoding` 의 **한 홉 포인터**(「the PortForwardOutcome.ResponseEncoding reasoning」) | ⚠️ **부분** — #146 의 인코딩 절이 같은 판단을 담지만, 이 2줄이 주장하는 것은 *소유자가 그 필드다*라는 표시다 | ❌ | **유지 — 갈림 없음**(⑻ 과 같은 이유로, 지우면 포인터가 가리키던 관계가 사라진다) |
| ⒂ `ProxyRef` 가 **넷 중 유일하게 pod 전용이 아니다**(Service·cluster-scoped Node) | ⚠️ **부분** — #150 · #146 이 `Pod·Service·Node` 를 열거하지만 **형제 셋과의 대비**는 어디에도 없다 | ❌ | **유지 — 갈림 없음** |

**행 1 집계**: ③ 축자 **2** · 부분 **8** · 부재 **3** · 판정 대상 아님 **1군**. ④ 는 부분 1(⑷) 을 빼고 전건 ❌.

## 항목별 판정 — `92a3ce829b84`

`internal/mcp/gate.go` · `internal/mcp/gate_test.go` (399줄)

| 항목 | ③ | ④ | 최종 |
| --- | --- | --- | --- |
| ⑴ `resolve` 가 **discovery 를 참조해선 안 된다**(게이트는 apiserver 를 건드리기 전에 판정을 끝내야 한다) | ⚠️ **부분** — #104 ⑵ 가 「미승인 시 k8s 호출 카운트 0」을 적지만 그 제약이 **`resolve` 의 구현을 묶는다**는 것은 없다 | ❌ | **유지 — 갈림 없음** |
| ⑵ `target` 이 `resolve` 와 **별도 필드인 이유**(둘은 다른 질문에 다른 시점에 답한다) | ⚠️ **부분** — #127 「`target: genericTarget()` 이라 AC6 의 승인↔uid 결속이 걸린다」는 *효과*이고 분리 이유가 아니다 | ❌ | **유지 — 갈림 없음** |
| ⑶ `collectionTarget` 이 **둘째 필드인 이유**(`get on ⟨kind⟩` 와 `list on ⟨kind⟩` 는 두 권한이고 선언은 하나만 세운다) | ⚠️ **부분** — #143 의 변경 표가 필드 이름만 나열한다 | ❌ | **유지 — 갈림 없음** |
| ⑷ `outsideGate` 가 **bool 이 아니라 문장인 이유**(문서 없는 면제가 곧 AC1 이 금하는 우회다) | ⚠️ **부분** — #73 이 예외 셋을 **문서 근거와 함께** 표로 싣지만, 그 근거를 *타입에 박은* 이유는 없다 | ❌ | **유지 — 갈림 없음** |
| ⑸ `alwaysGated` 가 `outsideGate` 의 **대칭 문장**인 이유 | ✅ **축자** — #150 「`outsideGate` 의 대칭으로 **문서를 인용하는 문장** `alwaysGated` 를 더했고」 | ❌ | **유지 — 갈림** |
| ⑹ `genericPairs` — 민감 읽기는 **k8s 호출 0 에서** 거부돼야 하므로 종류 이름을 discovery 가 아니라 apimachinery 의 추측으로 얻는다(AC16) | ✅ **축자에 가깝다** — #104 ⑵ 가 AC1·AC16 의 「카운트 0」과 그 구조적 귀결을 길게 적는다 | ❌ | **유지 — 갈림** |
| ⑺ `updatePairs`·`deletePairs`·`deleteCollectionPairs` — 인자 검사가 `authorize` **보다 먼저** 돌아야 하는 이유(핸들러가 거부할 호출이 그때는 이미 승인 요청 하나를 썼다) | ✅ **축자** — #146 「거부가 승인 요청을 만들기 전에 난다」 · #127 「거부가 승인 뒤에 오면 시나리오 13이 요구한 거부가 아니다」 | ❌ | **유지 — 갈림** |
| ⑻ `genericCollectionTarget` 이 `genericTarget` 의 **거울상**을 거부한다(저쪽은 이름 없는 호출을 기술할 수 없고, 이쪽은 네임스페이스 없는 호출을 **한정할 수 없다**) | ❌ **부재** | ❌ | **유지 — 갈림 없음** |
| ⑼ `resourceIsSensitive` — 서브리소스는 **매달린 객체로** 판정한다(`secrets/<무엇이든>` 을 읽는 것은 여전히 Secret 을 읽는 것이다) | ✅ **축자** — #104 「`TestSensitiveReadGateLooksThroughSubresources` 는 `secrets/<sub>` 가 게이트를 우회하지 못함을 단언한다」 | ❌ | **유지 — 갈림** |
| ⑽ `RESOURCE_GATED_KINDS` 의 철자(`v1/Secret`)와 RBAC 의 철자(`secrets`)가 달라 **뒤 조각을 소문자·복수로** 맞춘다 | ❌ **부재** | ❌ | **유지 — 갈림 없음** |
| ⑾ `validateRegistry` — 광고면과 디스패치면은 **같은 집합**이어야 한다 | ✅ **축자** — #73 「`tools/list` 광고 목록 == 디스패처 레지스트리 (양방향)」 | ❌ | **유지 — 갈림** |
| ⑿ `Exemptions` 를 main 이 **기동 시 찍는** 이유(「지금 게이트 밖인 상태 변경 호출이 무엇인가」가 이 파일을 읽지 않고 답해져야 한다) | ✅ **축자** — #73 「셋 다 `Exemptions()` 로 등재되어 **기동할 때마다 이름이 로그에 찍힌다**」 | ❌ | **유지 — 갈림** |
| ⒀ `GatePairs` 가 **AC19 결번 뒤에도** 공표되는 이유(기계가 대조하지 않아도 검토자가 답할 수 있어야 한다) | ⚠️ **부분** — #85 · #104 가 AC19 결번과 `rbac.yaml` 모순 해소를 적지만, **그래도 계속 공표하는** 이유는 없다 | ❌ | **유지 — 갈림 없음** |
| ⒁ `confirmTargetUnchanged` — **읽기는 반환 이후의 구간을 지키지 못한다**(update 는 승인 버전을 PUT 에 싣지만 다른 verb 는 각자의 원자적 조건이 필요하다) | ✅ **축자** — #104 의 「범위 밖」 첫 항이 그 구분(merge·strategic·json·apply)을 통째로 적는다 | ❌ | **유지 — 갈림** |
| ⒂ `approvalContext` 의 scale 상세 — 「3 → 10」과 「9 → 10」은 **같은 요청이고 다른 결정**이다 | ✅ **축자** — #104 「운영자는 「3 → 10」인지 「9 → 10」인지 모른 채 승인하고 있었다」 | ❌ | **유지 — 갈림** |
| ⒃ `kubeletHighPowerPaths` 가 **denylist 가 아니다**(표시하고 막지 않는다 — 여섯 전부가 `/healthz` 와 같은 쌍을 쓰고 도착한다) | ✅ **축자(앞 절)** — #150 「`proxyPathDetail`/`kubeletHighPowerEndpoint` — **표시하고 막지 않는다**(AC3)」. 다만 **「같은 쌍을 쓰고 도착한다」는 부재** | ❌ | **유지 — 갈림**(앞 절만) |
| ⒄ `kubeletHighPowerEndpoint` 의 매칭이 **대소문자 무시**인 이유(kubelet 이 `/portForward` 와 `/portforward` 를 같이 답하므로, 한 철자에서만 켜지는 표시는 운영자가 믿지 않기를 배운다) | ❌ **부재** | ❌ | **유지 — 갈림 없음** |
| ⒅ `writesASensitiveKind` — AC16 은 구분을 「무엇을 하는 호출인가」가 아니라 **「내용이 자격증명인가」**에 둔다 | ✅ **축자** — #89 「AC16 은 근거를 「무엇을 하는 호출인가」가 아니라 **「내용이 자격증명인가」**에 둔다」 | ❌ | **유지 — 갈림** |
| ⒆ `maskCredentialValues`·`maskJSONPatchOps` — JSON 패치는 값을 `data` **키 아래가 아니라 `path` 옆**에 실어 트리 탐색이 원리적으로 못 본다 | ✅ **축자** — #89 「RFC 6902 는 값을 `data` 키 **아래가 아니라 `path` 옆**에 실어 트리 탐색이 원리적으로 못 본…」 | ❌ | **유지 — 갈림** |
| ⒇ `maskCredentialTree` 가 필드를 **임의 깊이에서** 찾는 이유(create·update·patch 가 같은 모양을 저마다 다르게 중첩한다) | ❌ **부재** | ❌ | **유지 — 갈림 없음** |
| (21) `maskedValue` — `Secret.data` 는 전선에서 base64 라 **디코드한 길이**로 센다(인코딩된 길이는 다른 질문에 답한다) | ✅ **축자** — #89 「`Secret.data` 는 base64 라 **디코드한 길이**로 센다(인코딩된 길이는 다른 질문에 답한다)」 | ❌ | **유지 — 갈림** |
| (22) `withheld` — 마스킹이 불가능하면 **렌더를 거부하는 쪽이 안전한 방향**이다(자격증명 필드를 찾지 못한 blob 은 전부가 자격증명일 수 있다) | ❌ **부재** | ❌ | **유지 — 갈림 없음** |
| (23) `replicaChange` — 읽지 못한 현재 값은 **생략이 아니라 unknown 으로** 보고한다(「서버가 말해 주지 못했다」와 「이 필드는 해당 없다」는 다른 사실이다) | ❌ **부재** | ❌ | **유지 — 갈림 없음** |
| (24) `genericTarget` 의 거부 문구가 **핸들러의 것과 같게 맞춰진** 이유(어느 층이 먼저 알아차렸는지에 따라 운영자가 다른 문장을 받아선 안 된다) | ❌ **부재** | ❌ | **유지 — 갈림 없음** |
| (25) `gate_test.countingK8s` — **거부된 호출이 이 카운터를 올렸다면 게이트가 사후에 돈 것**이다 | ✅ **축자** — #104 ⑵ 가 `countingK8s`(= `k8s.Service` 구현)와 「카운트 0」의 관계를 통째로 적는다 | ❌ | **유지 — 갈림** |
| (26) `lastDeleteCollection` · `updateErr` · `execOutcome` — 가짜가 **클러스터 사실을 연기해야** 하는 이유(호출 수로는 보이지 않는 것을 단언으로 만든다) | ⚠️ **부분** — #143 · #123 이 각 단언을 나열하지만 *가짜의 모양이 왜 그런가*는 없다 | ❌ | **유지 — 갈림 없음** |

**행 2 집계**: ③ 축자(또는 그에 준함) **12** · 부분 **6** · 부재 **8**. ④ 는 **전건 ❌**.

## 결론 — 이 패스의 제거는 **0줄**이다

두 행 합쳐 ③ 축자 **14** · 부분 **14** · 부재 **11**. 그럼에도 제거가 0인 이유는 직전 세 패스와 같다.

1. **절차 5번.** ③ 에서 확인된 항목은 전부 「③ 이 *이 파일을 앞으로 고치는 사람*에 대한 복원
   경로인가」에서 판단이 갈린다. 2026-09-14 패스가 그은 경계(「닫힌 PR 본문을 먼저 뒤지리라
   기대하는 것은 복원 경로가 아니라 **희망**이다」)가 살아 있고, **그 경계를 뒤집는 것은 README
   개정**이지 한 패스의 재량이 아니다. 그 README 는 정합성 모델의 to-be 정의 표면이라
   `reconciler-tobe-modeler`·사람의 자리다.
2. **③ 이 원본을 주석으로 지목하는 자리가 있다**(행 1 ⑻ — #146 「근거는 코드 주석에 있다」).
   사본을 근거로 원본을 지우면 SSOT 가 뒤집힌다.
3. **가장 강한 ③ 히트가 모인 곳이 하필 승인 게이트의 판정 코어다.** 행 2 의 축자 12건은
   `maskedValue`·`writesASensitiveKind`·`countingK8s` 처럼 **틀리면 자격증명이 승인 화면에
   오르거나 게이트가 사후에 도는** 자리에 붙어 있다. README 의 비대칭 비용 논거가 가장 크게
   발화하는 지점이므로, 경계가 열리기 전에는 이쪽을 먼저 지울 이유가 없다.

판정 대상 주석의 **제거는 0줄**이고, 두 행의 줄 수·지문은 **한 값도 움직이지 않는다**
(결과 칸은 게이트가 재지 않는 산문 칸이다). 움직이는 것은 `복원경로-잔량` 마커
**49 → 47** 하나뿐이다.

### 이 패스가 처음 남기는 관측 — **수익률 0 이 네 번 반복됐다**

③④ 를 실제로 물은 패스는 이제 넷이고(09-19 · 09-20 ×3), **누적 제거는 0줄**이다. 이것은
게이트가 잡을 수 있는 종류의 사실이 아니다 — R9·R10 은 「물었는가」만 재고 「무엇이 나왔는가」는
세지 않는다. 그래서 여기에 적어 둔다: **이 축의 산출은 제거가 아니라 판정 기록 자체**이고,
그 판단이 뒤집히려면 아래 질문이 답을 받아야 한다.

## 올리는 질문 — 정책 소유자에게 (재상신, **5회차**)

> **③(PR 본문)·④(커밋 메시지)는 「그 파일을 고치려는 다음 사람」에 대한 복원 경로인가?**

네 패스가 답을 받지 못한 채 같은 자리에서 멈췄다. 실측 근거는 이제 **「③④ 를 물은 네 패스의
제거 0줄」**이다. README 의 「복원 경로는 넷이다」 표가 그 구분을 문면에 담아야 이 자리가 매
창마다 다시 갈리지 않는다.

이 답이 없으면 잔여 **줄 주석 47행 / docstring 22행**(약 4,000줄)의 처분이 영구 미결로 남는다.
답이 「아니다」라면 옳은 산출은 소급 판정이 아니라 **README 의 ③④ 구간 축소**이고, 그것은 to-be
문면 개정이라 이 루프의 planner 가 할 수 있는 일이 아니다.

## 다음 패스의 입력

- **위 질문의 답. 네 창이 같은 자리에서 멈췄다.**
- **잔여 47행 / docstring 22행의 소급 판정** — 단, 위 답이 나오기 전에는 이 패스와 같은
  「제거 0 · 기록만」이 반복된다. 답을 기다리는 편이 낫다는 것이 이 패스의 관측이다.
- 직전 패스가 남긴 **`92477ed3e3e2` 의 절차 4번 슬라이스**: ⓐ~ⓓ 를 철거되지 않는 문서로 옮긴 뒤
  제거. 소관은 자매 모델 `tbm_homelab-k3s-mcp-scenario-e2e`.
- **행 1 ⒀·⒂ 과 행 2 ⑻·⑽·⒄·⒇·(22)·(23)·(24) 의 ③ 부재는 재확인 대상이 아니다** — 부재는
  유지 쪽 근거이고, 이 항목들은 **어느 경로로도 복원되지 않는** 지식이라 README 유지 대상 3번의
  정면이다. 다음 패스가 이 자리를 다시 열 이유는 지문이 움직였을 때뿐이다.
