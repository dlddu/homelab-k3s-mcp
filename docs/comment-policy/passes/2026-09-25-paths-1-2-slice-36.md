# 2026-09-25 — 복원 경로 ①② 재판정 슬라이스 36 · **`dear_baby_reset_user` + `ping` e2e 가족, 두 표면 동시** (줄 주석 원장 `#23` · docstring 원장 `#8`)

- task: `rct_20260925-0011` (`tbm_homelab-k3s-mcp-comment-redundancy`)
- base: `cf2691a` (#219)
- 대상: 줄 주석 행 **`#23`**(4줄 · `87dce628e24d`) · docstring 행 **`#8`**(34줄 · `6db41ad6420b`)
- 결과: `#23` **제거 2줄**(4 → 2 · `d1c3449612a8`) · `#8` **제거 18줄**(34 → 16 · `fea1f26dc71b`)
- 잔량: `복원경로①②-잔량` **65 → 64** · `docstring-복원경로①②-잔량` **30 → 29**
- 합계: `판정-합계` **3311 → 3309** · `docstring-합계` **1902 → 1884**
- 코드 무접촉: docstring·주석을 걷어낸 AST 가 세 파일 모두 base 와 **일치**(3/3)

## 왜 이 두 행인가 — 지목된 수취인 + 표면 교차 위험

줄 주석 표면의 첫 ①② 패스([슬라이스 35](2026-09-25-paths-1-2-slice-35.md))가 행 `#20`·`#21` 을
닫은 근거는 크기가 아니라 **선행 패스가 수취인을 이름으로 적어 두었다**는 것이었다. 같은 기준으로
원장 전수를 다시 세었다 — 결과 칸에 「`①② 재판정 후보`」 문구가 있는 행은 두 표면 99행 중
**`#20`(닫힘)과 `#23` 둘뿐**이다. `#23` 이 이 표면에 남은 **유일한 지목**이다.

`#23` 만 물으면 안 되는 이유가 하나 더 있다. 그 행의 4줄과 **docstring 행 `#8` 의 34줄이 같은 네
파일**(`dear_baby_reset_user_ac1/2/3.py` · `ping_ac1.py`)을 덮는다. 줄 주석 쪽 제거의 ① 근거 중
하나가 `ac2.py` 케이스 docstring 의 「the tool resolves the pod through the documented defaults」
문장인데, 그 문장은 docstring 쪽에서 보면 **② 재진술로 제거 후보**다. 한 표면만 닫으면 다음 패스가
앞 판정의 근거를 사후에 지울 수 있다(슬라이스 35 가 ⚠️ 로 남긴 위험이 여기서 실제로 성립한다).
그래서 **두 행을 한 슬라이스로** 묶고, 줄 주석 제거의 근거를 사라지지 않는 쪽(② `docs/prd-*` ·
`docs/test-*` 와 ① `internal/mcp/mcp.go`)에 세웠다.

Go 서비스 패키지 6행 가족(`#2`·`#3`·`#6`·`#8`·`#9`·`#10` · 404줄)은 슬라이스 35 와 같은 근거로
범위 밖이다 — 거의 바이트 동일한 공유 사본이라 6행을 한 슬라이스로 묶어야 한다.

## 🔴 2026-09-08 의 「복원 경로 넷 밖」 단정 둘이 거짓이었다

docstring 행 `#8` 의 1차 판정(2026-09-08)은 「나머지 172줄은 유지」의 근거로 두 자리를 이름으로
들며 **복원 경로 넷 밖**이라고 적었다:

- `ac1.py` 의 「kind 픽스처의 `/reset-user` 는 busybox 스텁이라 필드 수준 효과는 관측 불가」
- `ac2.py` 의 「판별자는 호출이 **실패한다**는 것 — 무시된 selector 였다면 그대로 성공했을 것」

두 자리의 ② 원본은 `docs/test-dear-baby-reset-user.md` 에 **그때 이미 있었다.**
`git log -S` 로 두 문면의 도입 커밋을 직접 쟀다 — 둘 다 **`56d2ef6`(#41, 2026-08-14)**,
즉 판정보다 **25일 앞선다**. PRD 쪽 ② 도 `f486b9e`(2026-06-21)다.

이것은 [정책 문면이 개정돼 논거가 낡은 형](../README.md)이 아니고, **아직 묻지 않은 경로가 남아
있던 형**이다(슬라이스 20 이 `_oidc.py` 에서 관찰한 것과 같은 결). ③④ 래칫은 2026-09-24 에 이 행을
지났지만 그 축은 PR 본문·커밋만 묻기 때문에 이 어긋남을 볼 수 없었다 — 그리고 ①② 축에는 그때까지
래칫이 없어 **판정이 낡아도 게이트가 초록이었다**. 번복은 **그 두 논거에만** 적용하고, 1차 판정의
문면은 지우지 않고 원장에 그대로 남겼다.

## 줄 주석 행 `#23` — 제거 2줄 (4 → 2)

두 파일에 같은 2줄 블록 두 벌이 있다(`ac1.py` 17–18 · `ac2.py` 19–20):

```
# Defaults the tool applies when selector/container are omitted
# (internal/mcp/mcp.go: dearBabyDefaultSelector / dearBabyDefaultContainer).
```

명제는 둘이다 — ⓐ 「이 값들은 미지정 시 도구가 적용하는 기본값이다」 ⓑ 「그 원본은 `mcp.go` 의
그 두 상수다」(포인터 + 「상수 == 도구 기본값」 거울 관계).

| 경로 | 원본 | 실측 |
| --- | --- | --- |
| ① | `internal/mcp/mcp.go:29–30` | `dearBabyDefaultSelector = "app=dear-baby"` · `dearBabyDefaultContainer = "backend"`, 사용 지점 512·516 은 **인자가 없을 때만** 상수를 쓴다 |
| ① | 같은 파일 | `ac1.py` 는 selector/container 를 **빼고** 호출하고(46행) 응답이 `DEFAULT_SELECTOR`·`DEFAULT_CONTAINER` 와 같음을 단정한다(57–58) — ⓐ 가 코드로 선다 |
| ② | `docs/prd-dear-baby-reset-user.md` 12–13 | 도구 개요 「`selector`(기본 `app=dear-baby`)」·「`container`(기본 `backend`)」 |
| ② | 같은 문서 28–29 | AC2 설명 「`selector`·`container`는 기본값(`app=dear-baby`/`backend`)을 사용하되 재정의할 수 있다」 |
| ② | `docs/test-dear-baby-reset-user.md:26` | 시나리오 2 기대 결과 「기본 selector=`app=dear-baby`·container=`backend` 사용」 |

⇒ **ⓐ 는 ①② 이중 복원**이라 첫 줄을 지웠다.

**ⓑ 는 유지.** 포인터가 막는 손은 「`app=dear-baby`/`backend` 는 픽스처 라벨」로 읽고 픽스처만
고치거나 `mcp.go` 의 기본값만 고쳐 둘을 어긋나게 두는 편집이다 — 앞으로의 편집을 구속하는
불변식이고(3분 표 둘째 줄), 이 행의 ③④ 판정이 「고유분은 포인터와 「상수 == 도구 기본값」
관계뿐」이라 적은 그 자리다. 괄호만 걷어 한 줄로 접었다:

```
# internal/mcp/mcp.go: dearBabyDefaultSelector / dearBabyDefaultContainer.
```

추가 2줄은 base 둘째 줄의 **문자 부분집합**이다(괄호·마침표 위치만 다르다) — 새 주장 0건.

## docstring 행 `#8` — 제거 18줄 (34 → 16)

게이트의 `docstring_lines()` 를 빌려 **행 전수 34줄**을 뽑아(지문 `6db41ad6420b` 재현) 줄 단위로
다시 물었다. 이월 목록 둘에서 멈추지 않았다.

### 복원 경로 ② — `docs/prd-*.md` · `docs/test-*.md`

1. **`ac1.py` 「Not asserted …」 문단 6줄** — `docs/test-dear-baby-reset-user.md` 시나리오 1
   **자동화** 절이 「단, 온보딩 필드 초기화 자체는 e2e에서 관측하지 않는다 — kind 픽스처의
   `/reset-user`는 busybox 스텁 스크립트라 뒤에 DB가 없다(필드 단위 효과는 실제 백엔드 이미지 +
   시드 DB 필요)」로 **절 단위 축자**다. 필드 열거(onboarded_at · due_date · coachmark ·
   first_record_at · ai_preview)와 「레코드는 보존된다」는 `prd-dear-baby-reset-user.md` AC1 설명
   21–22 다. 괄호 포인터 `tests/k8s/kind/dear-baby-fixture.yaml` 도 함께 걷었다 — 문서가 그
   픽스처를 **역할로** 부르고(「kind 픽스처의 `/reset-user`」) 레포의 dear-baby kind 픽스처는
   `ls tests/k8s/kind/ | grep dear` 로 **정확히 하나**다.
2. **`ac1.py` 첫 문단의 단정 요약 + 꼬리 절** — 「Asserts the tool resolves a Running fixture pod,
   execs `/reset-user` … the exit code is 0 and `success` is true」와 「a failing reset surfaces as
   an error result that still carries the exit code and stderr」는 시나리오 1 **기대 결과** (a)(b)
   축자이고, 동시에 바로 아래 두 단정 블록(48–62 · 69–83)의 재진술이다(①).
3. **`ac2.py` 세 절 열거** — 시나리오 2 **자동화** 절이 「email 누락 → `McpError: email is
   required`, 기본 selector·container 에코, selector 재정의 → no Running pod, container 재정의 →
   실패 = 재정의가 실제로 반영됨의 판별자」로 세 절을 **순서대로** 적는다.
4. **`ping_ac1.py` 모듈 꼬리 2줄(층 승격 선언)** — `docs/test-ping.md` 13–15 자동화 절이 같은
   시나리오 아래 통합 `ping_ac1.py::test_ping_ac1_always_pong`(「배포 서버에 인자 없이 `ping`을
   호출해」)과 Go 단위 `internal/server/mcp_test.go::TestPingToolReturnsPong` 을 **짝지어** 적어
   그 관계가 복원된다. 2026-09-08 이 케이스 본문의 같은 문장을 「모듈 docstring 과 같은 말」로
   지웠으니 이것이 그 문장의 **정본**이고, 이번에 ② 로 닫혔다.

### 복원 경로 ① — 코드 자체 · 같은 파일

5. 3 의 「email 누락은 exec **전에** 거부된다」 — `mcp.go` 의 `errf(-32602, "email is required")`
   가드가 `h.k8s.ExecInPod` **앞**에 있다(소스 순서로 닫힌다).
6. 3 의 「SDK 가 `McpError` 로 올린다」 — 같은 케이스의 `except McpError as exc` 와
   `assert "email is required" in str(exc)`.

### 유지 16줄

**무접촉 8줄** — 모듈 1줄 요약 넷과 케이스 `AC:` 요약 셋(+`ping_ac1` 모듈 첫 줄)은 관례 최소 doc
이라 손대지 않았다. `ac3.py` 는 그 둘뿐이라 이 패스에서 **한 줄도 움직이지 않았다**.

**판정으로 남긴 것 셋** — ②·① 무히트를 실측했다:

- **ⓐ `ac1.py` 대조군 논거** — not-found 를 같이 돌리는 이유는 성공 단정이 고정 payload 가 아니라
  CLI 의 **실제 출력**을 읽는다는 것을 증명하기 때문이다. 문서는 (b) 를 **케이스로만** 적고 「왜
  대조군인가」를 적지 않는다 = 무엇이 조용히 통과하는가(3분 표 둘째 줄).
- **ⓑ `ac2.py` 판별자 독법** — 무시된 `selector` 였다면 픽스처 파드를 그대로 찾았을 것이고, 무시된
  `container` 였다면 `backend` 로 exec 되어 성공했을 것. 문서는 「판별자」라는 *결과*만 적고 그
  **기전**은 적지 않는다.
- **ⓒ `ac2.py` 문구 소유권** — 거부 문구는 클러스터가 정할 말이지 이 서버가 정할 말이 아니다 =
  그 문구에 단언을 붙이지 말라는 경계.

추가 5줄은 **유지한 문장에서 제거 절만 걷고 지시어를 명시화한 재배치**다 — ⓑ 의 「the discriminator
in each case」가 앞 열거 (1)(2)(3) 이 사라지며 「in each override clause」로 같은 지시 대상을 이름으로
박았다. 새 주장 0건.

## ①② 를 판정하면 ③④ 축이 함께 열린다 — 두 행 재앵커

주석을 지우면 행 지문이 움직이고 두 행에는 ③④ 산문이 이미 있으므로 R9·R10 의 `stale` 분기가
발화한다(**잔량 상향으로 구제되지 않는다**). 재앵커의 근거는 「다 다시 물었다」가 아니라
**주장 집합의 포함 관계**다.

| 행 | 추가 | 제거 | 판정 |
| --- | ---: | ---: | --- |
| `#23` | **2** | 4 | 추가 2줄이 base 둘째 줄의 **문자 부분집합**(괄호 제거) ⇒ 새 주장 0건 |
| `#8` | **5** | 23 | 추가 5줄이 유지 문장의 재배치 + 지시어 명시화 ⇒ 새 주장 0건 |

두 트리의 줄 집합은 게이트 파서(`comment_lines()` · `docstring_lines()`)로 base 워크트리
(`git worktree add --detach origin/main`)와 이 트리에서 각각 뽑아 집합 차로 냈다.

**③ 판정은 그대로 선다.** `#23` 의 2026-09-21 판정(모집단 열일곱 PR · 히트 #41 하나 · 유지)과
`#8` 의 2026-09-24 판정(모집단 여덟 PR · 히트 0 · 유지)의 모집단이 진부분집합 위에서 불변이다.
⚠️ `#8` 의 ③ 가 「고유분」으로 든 셋 중 둘(`ac1` 의 Not-asserted 경계 · `ping_ac1` 의 층 승격
선언)을 이 패스가 ② 로 닫았지만, 그것은 **③ 히트 0 이라는 결론을 바꾸지 않는다** — 축이 다르다
(그 셋은 「PR 본문이 대신 말하지 않는다」의 예시였고, 지금 닫힌 것은 「저장소 문서가 대신 말한다」다).

**④ 는 적용 대상 부재가 유지된다** — 이 패스도 PR 로 착지한다. 두 행의 기준 커밋 이후 창에서
정책 범위의 main 직접 push 는 0 이라는 직전 실측(`#8` 은 최근 120커밋 전수, `#23` 은 `405edb4..`
계열)이 이 head 에서도 유효하다: 제목에 `(#N)` 이 없는 커밋은 전부 `chore(deploy):` 이미지 핀
(범위 밖 `k8s/deployment.yaml` 한 파일)이다.

## 검증

| 게이트 | 결과 |
| --- | --- |
| `scripts/check_comment_policy.py` | rc=0 — 3309줄 / 146파일 · 등재 67 · ③④ 67/67(잔량 0) · **①② 3/67(잔량 64)** · docstring 1884줄 · ③④ 32/32 · **①② 3/32(잔량 29)** |
| `scripts/check_mock_policy.py` | rc=0 |
| `scripts/check_doc_inventory.py` | rc=0 — 문서 93 · 정책 53 · 허브 도달 93/93 |
| `tests/integration/check_ac_mapping.py` | rc=0 |

음성 프로브 — 두 래칫이 공전이 아님을 양방향으로 재확인:

- ⒜ `복원경로①②-잔량` 을 64 → 65 로 되돌리면 **[R11] fail**(`완료 3행 + 잔량 65 != 67행`).
- ⒝ `docstring-복원경로①②-잔량` 을 29 → 30 으로 되돌리면 **[R12] fail**.
- ⒞ 행 `#23` 의 ①② 앵커 지문 12자를 위조하면 **[R11] fail**(`stale` + 산술 동시 발화).
- ⒟ 행 `#8` 의 ③④ **재앵커**를 지우면 **[R10] fail** — 재앵커가 장식이 아님이 선다.

**코드 무영향은 바이트로 닫았다.** `ast` 로 모듈·함수 docstring 을 전부 걷어낸 트리를
`ast.unparse` 로 직렬화해 base 와 대조 — 세 파일 **3/3 일치**. 단정·상수·제어 흐름이 한 바이트도
바뀌지 않았으므로 kind e2e 없이 무영향이 닫힌다. `check_ac_mapping.py` 가 러너 배차와 케이스
호출을 다시 확인한다(모듈 docstring 의 `검증 시나리오:`·`실행 대상:`·`병렬 레인:` 선언 무접촉).

## 이 패스가 매번 밟는 자리

새 `passes/*.md` 한 파일이 `scripts/check_doc_inventory.py` **D4**(허브 집계 ↔ 실측)를 깬다.
고칠 자리는 `docs/doc-tracker/2026-09.md` 의 「허브 도달 가능 문서」 한 줄이고 **총계와 「정책」을
함께** 올려야 한다(도달 수·총계·종류별이 각각 따로 검사돼 FAIL 3건이 뜬다).
이 패스는 **92 → 93 · 정책 52 → 53**.

## 범위 밖(후속)

- 줄 주석 표면의 **남은 64행**과 docstring 표면의 **29행**의 ①② 판정. 다음 감지 주기가 to-be
  변화(이 원장·패스 파일)로 연다.
- **Go 서비스 패키지 6행 가족**(`#2`·`#3`·`#6`·`#8`·`#9`·`#10` · 404줄) — 공유 사본이라 한
  슬라이스로 묶어야 한다.
- 두 행의 ③④ **재판정**(이번에 한 것은 새 지문으로의 **재앵커**뿐이고, 모집단이 진부분집합이라
  판정 자체는 그대로 선다).
- ⚠️ **①② 축에 「이월 지목」이 더는 없다** — 이 패스로 원장 두 표면의 「`①② 재판정 후보`」 문구가
  모두 소진됐다. 다음 슬라이스는 크기·가족(공유 사본) 기준으로 골라야 한다.
