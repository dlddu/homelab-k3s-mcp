# 2026-09-21 — 복원 경로 ③④ 소급 판정 슬라이스 2 · **단일 PR 행 일곱**

`rct_20260921-0004` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 1](2026-09-20-paths-3-4-first-removals.md)이 「다음 패스의 입력」으로 남긴 가설을 그대로
집행한다 — **파일을 들여온 PR 이 하나뿐인 행은 ③ 의 모집단이 닫혀 있어 판정 비용이 가장 작고, 슬라이스 1
의 19줄 중 14줄이 그런 행에서 나왔다.** 잔량 43행을 「들여온 PR 수」 오름차순으로 정렬해 **1 인 행 일곱**을
전부 집었다. 자매 PR #172(docs-impl)가 주석 집합을 움직이는 잔여 6행(`internal/` 쪽)은 이 슬라이스에
넣지 않았다 — #172 착지 전에 판정하면 지문이 바로 낡는다.

## 이 패스의 범위 (완료 기준)

| | 기준 | 값 |
| --- | --- | --- |
| ⑴ | 줄 주석 ③④ 잔량 마커 | **43 → 36**(③④ 20/63 → **27/63**행) |
| ⑵ | 실제 제거 | **21줄** — `3ec066d625eb` 4 · `92c53441af53` 3 · `5a68b963ac0b` 9 · `17850d2eae26` 5 · 나머지 셋 0 |
| ⑶ | 줄 주석 판정 합계 | 3332 → **3311**(착지 base `7065a14`/#172 기준 — 준비 시점 base #171 에서는 3195 → 3174) |
| ⑷ | docstring 표면 | **무이동**(7/29 · 잔량 22 · 합계 1935) |

**범위 밖**: 줄 주석 잔량 36행(그중 #172 가 움직이는 6행은 #172 뒤에), docstring 잔량 22행, `e82057fcdeba`(k8s
픽스처 6파일 29줄 — 들여온 PR 이 여럿이라 이번 정렬 기준 밖).

## 측정 — ③ 의 모집단 (전부 닫혀 있다)

| 행 | 파일 | PR | 본문 | 이슈 코멘트 | 리뷰 코멘트 | 머지 커밋 |
| --- | --- | --- | ---: | ---: | ---: | --- |
| `3ec066d625eb` | `github_app_installation_token_ac5.py` | **#138** | 3,394자 | 0 | 0 | `00c6a113149f` |
| `92c53441af53` | `resource_generic_ac12.py` | **#145** | 3,071자 | 0 | 0 | `880fe2ae3c6a` |
| `5a68b963ac0b` | `resource_generic_ac14.py` | **#148** | 3,613자 | 0 | 0 | `36656aa24da5` |
| `ef1af58d9577` | `resource_generic_ac13.py` | **#149** | 3,450자 | 1(리베이스 기록) | 0 | `e49417167ad5` |
| `17850d2eae26` | `resource_generic_ac15.py` | **#153** | 6,233자 | 0 | 0 | `17efb5974a87` |
| `faafbbc84d7b` | `resource_generic_ac11.py` | **#142** | 2,862자 | 1(리베이스 기록) | 0 | `88d9e8c15b3e` |
| `34dc91e0ba63` | `resource_read_smoke.py` | **#75** | 6,578자 | 0 | 0 | `9d2855fe7daa` |

「들여온 PR 수」는 `git log --format=%h -- <파일>` 의 커밋 수로 쟀다(전부 1). 이슈 코멘트 둘은 둘 다 형제 착지에
따른 리베이스 기록이라 주석과 무관하다. **④ 는 뒤지지 않았다** — README 가 ④ 의 적용 범위를 「PR 없이 main 에
직접 push 된 커밋」으로 한정했고 일곱 파일을 건드린 커밋은 전부 PR 경유다(대상 부재).

## 판정 기준 — 슬라이스 1 이 세운 두 구분을 그대로 쓴다

1. **본문이 그 주장을 대신 말하면 지운다**(`service.go`·`delete_collection.go` 형). 본문이 *결과*만 적고 주석이
   지는 *이유*·*귀속*·*순서*는 적지 않으면 「다른 주장」이라 남긴다(슬라이스 1 의 ⑷ 「Growth 만 AC11 이 이름
   붙인 경우」 형).
2. **본문이 스스로 「주석에 적었다」고 가리키면 원본은 주석 쪽**이라 남긴다(`proxy.go`↔#150 형).

## 항목별 — `3ec066d625eb` → `1a0b7a0999eb` (제거 4줄)

이 행은 [2026-09-19 패스](2026-09-19-restoration-paths-3-4.md)가 이미 ③ 까지 물어 **히트 둘**을 세어 두고,
「③ 이 *앞으로 이 파일을 고치는 사람*에 대한 복원 경로인가」가 미결이라 절차 5번에 남긴 행이다 — 슬라이스 1 이
`010bbf3e1e73` 에서 한 것과 정확히 같은 자리다. 경계가 확정된 지금 그 둘을 지운다.

### ⑴ `finally` 위 3줄 → 제거

```python
        finally:
            # The mock is shared process state; the github-app lane runs one
            # file at a time, so leaving a knob on would silently rewrite what
            # the next file in the lane observes.
            reset_mock()
```

#138 본문 「왜 상시가 아니라 모드 토글인가」: 「mock 은 프로세스 상태를 공유하지만 네 파일 모두 `병렬 레인:
github-app` 이라 레인 안에서 한 번에 한 파일만 돌고, 새 파일은 `try/finally` 로 노브를 반드시 되돌린다」 —
세 사실(공유 상태 · 레인당 한 파일 · 그래서 되돌린다)이 축자로 있다.

**저울질한 반대 독법**: 「노브를 켠 채 끝내면 다음 파일이 다른 mock 을 본다」를 README 3분 표 둘째 줄의
*서 있는 불변식*(「여기에 파일을 두면 사전 조건이 죽는다」)으로 읽을 수 있다. 채택하지 않은 이유 둘 — ⓐ 그
불변식을 집행하는 것은 주석이 아니라 바로 아래 `finally` 자체다(주석이 지는 것은 *왜 finally 인가* 라는 경위).
ⓑ 09-19 패스가 이 3줄을 ③ 히트로 세고 **경계 하나만** 미결로 남겼는데, 그 경계가 「그렇다」로 닫혔다.
슬라이스 1 의 「0건 분기 … 이 층이 사후에 할 수 있는 주장이 아니다」(#143 본문 축자, 제거)와 같은 형이다.

### ⑵ `MOCK_URL` 위 첫 두 줄의 포트포워드 절 → 한 줄로 접음(−1)

「port-forwarded by the "Run integration tests (primary deployment)" step」은 #138 변경 표의
「`.github/workflows/ci.yml` — primary 스텝에 `svc/github-mock` 포트포워드(8093) + `GITHUB_MOCK_URL`」로 복원된다
(09-19 패스도 이 부분을 ③ 히트로 셌다). 같은 문장의 나머지 — **왜 `_helpers` 가 아니라 여기인지** — 는 본문
0 히트라 그대로 두고, 그 절만 걷어 `# The admin surface of tests/k8s/kind/github-mock.yaml. It is reached directly`
로 접었다.

### 유지 — 8줄

`EXPECTED_INSTALLATION_ID` 2줄(CI 시크릿 값과의 결합) · `MOCK_URL` 4줄 · 요청 기록 2줄(「AC5 는 대부분 *일어나면 안
되는 요청*에 대한 주장이라 요청 로그가 판정 근거」) — 09-19 패스의 판정대로 본문 0 히트.

## 항목별 — `92c53441af53` → `8777ea3e146c` (제거 3줄)

| # | 주석 | ③ (#145 본문) | 판정 |
| --- | --- | --- | --- |
| ⑵ | namespace 누락 케이스 3줄 — 「namespace 를 비운 호출은 apiserver 가 **모든 네임스페이스**로 읽는다. 승인 화면에 그 호출이 올라가는 것 자체가 사고이고 …」 | 「이 e2e 가 재는 것」 1번 — 「`namespace` 누락 → 거부되고 **승인 요청이 만들어지지 않는다**(빈 namespace 를 apiserver 는 전 네임스페이스로 읽는다 — 그 호출이 승인 화면에 올라가는 것 자체가 사고다)」 | **제거** |
| ⑴ | `ABSENT` 가 레이블 값이자 0건 마커라는 겸직 | 본문에 없음 | 유지 |
| ⑶ | 수와 이름을 한 댄스에서 재는 의도 | 3번은 「`targets: 5` 와 다섯 이름」이라는 결과만 | 유지 |
| ⑷ | 늦게 오는 객체는 **PENDING 관측 뒤에** 만든다(= 스냅샷 이후) | 본문의 「늦게 오는 3번째가 이미 있으면 `kubectl apply` 가 아무것도 바꾸지 않아 …」는 케이스 직전 **재생성**의 이유이지 생성 **순서**의 이유가 아니다 | 유지 — 다른 주장 |

같은 문장 「빈 namespace 를 apiserver 는 전 네임스페이스로 읽는다」가 슬라이스 1 의 `delete_collection.go`(⑹)에서는
**유지**였다 — #143 본문에 없었기 때문이다. 여기서는 #145 본문이 그것을 적었다. 갈리는 것은 문장이 아니라 **그
파일을 들여온 본문이 무엇을 말했는가**다.

## 항목별 — `5a68b963ac0b` → `4887df13dbff` (제거 9줄)

#148 본문 「e2e 가 무엇을 재는가」의 네 케이스가 네 자리를 담는다.

| # | 주석 | ③ (#148 본문) | 판정 |
| --- | --- | --- | --- |
| ⑷ | 승인 화면 케이스 3줄 — 포트와 페이로드를 같은 화면에서, 페이로드는 **전문**(마커만 찾으면 헤더 한 줄로도 통과) | 케이스 1 「`"port": 8080` 과 **페이로드 전문**(마커 한 줄이 아니라 JSON 이 싣는 그대로)」 | **제거** |
| ⑹ | 두 번째 왕복 2줄 — 「첫 승인이 터널째 재사용된다면 이 호출은 아무도 부르지 않고 그대로 답을 들고 돌아올 것이다」 | 케이스 2 「글자 그대로 같은 호출을 한 번 더 하면 첫 승인이 재사용되지 않고 새 id 의 요청이 서고, 그것을 거절하면 두 번째 왕복이 일어나지 않는다」 | **제거** |
| ⑺ | 상한 케이스 2줄 — 길이를 상한과 정확히 대조하는 것이 판별식, 창 만료는 더 짧고 `responseTruncated` 거짓 | 케이스 3 「길이를 상한과 정확히 대조하는 것이 「창 만료로 끊긴 것」과 「상한이 끊은 것」을 가르는 판별식이다」 | **제거** |
| ⑻ | 인자 거부 2줄 — 승인 요청 0건까지 재는 이유 | 케이스 4 「이 거부가 승인 뒤에 오면 운영자는 이미 포트 도달을 승인한 뒤다」 | **제거** |
| ⑴ | `BIG_BYTES` 2줄 — 상한보다 크되 **창 안에 끝날 만큼만** | 본문은 「400 KB 응답이 정확히 256 KiB 에서」 값만 | 유지 — 수치의 근거 |
| ⑵ | `MAX_OUTPUT_BYTES` 가 `streamMaxOutputBytes` 의 사본 | 본문에 없음 | 유지 |
| ⑶ | `ROUND_TRIP_MARKER` 2줄 — 마커가 다르면 「새 요청」 관측이 공허해진다 | 케이스 2 는 「같은 호출」을 요구할 뿐 **왜** 는 없다 | 유지 — 다른 주장 |
| ⑸ | 본문 단언 2줄 — 「응답이 왔다」는 빈 문자열로도 참 | 본문은 쓰기 반쪽 기전을 **파일의 존재 이유**로 적고 이 단언의 형태에 매지 않는다 | 유지 |

## 항목별 — `ef1af58d9577` (제거 **0줄**, 지문 불변)

`read` 실패 갈래 4줄(EOF 가 창 끝에 오고 바이트 상한의 조기 취소가 페이로드 중간에 겹칠 수 있다)은 #149 본문
「#147 로 거짓이 된 주석 하나를 고쳤다 … 살아 있는 경우로 **다시 적었다**」와 겹친다. 그러나 그 문장은 PR 이
**주석을 고쳤다고 보고하는 것**이지 그 지식을 본문이 대신 말하는 것이 아니다 — README 3분 표 셋째 줄(원본이
주석 쪽) 그대로다. 나머지 두 자리(`SCRIPT` 의 stdout 공유 · `stdinOnce:false` 의 이유)는 본문 0 히트.

## 항목별 — `17850d2eae26` → `feae17f5d3c4` (제거 5줄)

#153 본문은 「**설계 결정 둘을 여기 남긴다**(복원 경로 ③)」로 두 자리를 명시적으로 자기 것으로 가져갔다.

| # | 주석 | ③ (#153 본문) | 판정 |
| --- | --- | --- | --- |
| ⑴ | `PORT` 위 3줄 — apiserver 파드 프록시는 이름에 포트가 없으면 80 | 결정 2 「파드가 80 번을 듣는다. apiserver 의 파드 프록시는 이름에 포트를 적지 않으면 80 으로 가고, 이 파일은 시나리오 문면 그대로 포트 없는 이름을 쓴다」 | **제거** |
| ⑽ | `/exec` 승인 뒤 2줄 — 경로 허용목록이 있었다면 승인 뒤에도 거부됐을 자리 | 결정 1 「여기서 재는 것은 그 호출이 승인 뒤 우리 층에서 막히지 않는다는 것 — 경로 허용목록이 있었다면 거부됐을 자리다」 | **제거** |
| ⑶ | 파드 경로 표시 2줄 — 같은 철자에 표시가 붙으면 안 된다는 쪽은 **단위가** 잰다 | 표에 「파드의 `/exec` 철자는 kubelet 의 것이 아니므로 표시 없음」은 있으나 단위 귀속은 없다 | 유지 — **갈림** |
| ⑻ | `/healthz` 본문 2줄 — 여기가 초록이어야 아래 `/exec` 이 「닿았고 막히지 않았다」로 읽힌다 | 결정 1 이 「대상이 정말 답한다는 쪽은 … kubelet 의 `ok`」로 역할을 나누지만 두 케이스의 **의존**은 없다 | 유지 — **갈림** |
| ⑵⑷⑸⑹⑺⑼⑾ | Service 단일 포트 · 본문 단언 · 전문 단언 · 상태 코드 미단언 · 표시의 의미 · 쿼리 전문 · PENDING 잔여 | 본문 0 히트 | 유지 |

⑶⑻ 은 절차 5번이다 — 본문이 결론을 담고 주석이 귀속·의존을 더 담는 형이라 「본문이 대신 말한다」로 읽을 수도
있지만, 더 담긴 것이 제거 뒤 어디에도 남지 않으므로 보존한다. 다음 ③ 재판정 후보로 이름을 남긴다.

## 항목별 — `faafbbc84d7b` (제거 **0줄**) · `34dc91e0ba63` (제거 **0줄**)

- `resource_generic_ac11.py` 성공 갈래 2줄의 첫 절 「#137 이 닫은 첫 자리다」는 #142 본문에 있다. 그러나 그 2줄이 지는
  값은 둘째 문장(이 세 줄이 붉어지면 아래 「거부」 단언들이 전부 공허해진다)이고 본문에 없다. 첫 절만 걷는 부분
  편집은 **다음 패스 후보**로 이름만 남기고 통째 유지(슬라이스 1 의 ⑷ 「유지(부분)」과 같은 처리).
- `resource_read_smoke.py` 2줄 — #75 본문 ⑷ 는 「두 도구를 따로 재면 목록이 빈 표인 동안에도 각자는 통과로 보인다」
  (케이스를 **잇는** 이유)를 적고, 주석의 「행 수만으로는 다른 좌표를 구별하지 못한다」(단언 **강도**의 이유)는
  다른 주장. 유지.

## 결론

| | 값 |
| --- | --- |
| 이 패스의 제거 | **21줄**(4 + 3 + 9 + 0 + 5 + 0 + 0) |
| 줄 주석 판정 합계 | 3332 → **3311**(#172 착지 뒤 리베이스해 재측정) |
| ③④ 판정 완료 / 전체 | 20/63 → **27/63**, 잔량 **43 → 36** |
| docstring 표면 | **무이동**(7/29 · 잔량 22 · 합계 1935) |
| 갈려서 남긴 것 | 3건(`ac15` ⑶⑻ · `ac11` ⑴ 첫 절) |

가설은 유지된다 — 단일 PR 행 일곱 중 넷에서 21줄이 나왔고(행당 3줄), 나머지 셋의 0 은 「본문이 주석을 가리킨다」·
「다른 주장」이라는 **이유가 있는 0** 이다.

## 다음 패스의 입력

- 줄 주석 잔량 **36행**. 들여온 PR 이 하나인 행은 이제 없다 — 다음 정렬 기준은 **PR 2~3개 행**(`082e1fc7e45e` ac20 3건 ·
  `66ff2fad5f28` ac3 2건 · `122e507fe7c4` ac5 3건 등 `tests/integration/` 의 작은 행)이다.
- #172 가 움직이는 6행(`d9c561b69bcc`·`3bdaa24398db`·`f48e36feba66`·`1b5e509876c7`·`de3104ca12e4`·`26ca06459b9e`)은
  **#172 착지 뒤에**.
- `e82057fcdeba`(k8s 픽스처 29줄) — 보류 조건은 풀렸으나 들여온 PR 이 여럿.
- ③ 재판정 후보 누적: `ac15` ⑶⑻ · `ac11` ⑴ 첫 절 (이 패스) + `recordTarget` doc 3줄 · `Principal` AC1 철자 2줄 ·
  `rbac-narrow-fixture.yaml` 머리말 첫째·셋째 불릿(② 후보, 앞 fire).
- docstring 잔량 **22행** — 여전히 한 번도 이 축으로 묻지 않은 표면.
