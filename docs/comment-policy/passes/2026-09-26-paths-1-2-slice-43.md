# 복원 경로 ①② 판정 — 슬라이스 43: `attach` · `port_forward` 축 (원장 `#43`·`#49`)

- **task**: `rct_20260926-0008` (`tbm_homelab-k3s-mcp-comment-redundancy`)
- **기준 커밋**: `d32774a` (슬라이스 42 / PR #231 착지 직후)
- **대상**: 줄 주석 원장 행 `#43`(`internal/k8s/attach.go` · `attach_test.go` ·
  `internal/mcp/attach.go` · `attach_test.go` 102줄) + 행 `#49`
  (`internal/k8s/port_forward.go` · `internal/mcp/port_forward.go` ·
  `internal/k8s/port_forward_test.go` · `internal/mcp/port_forward_test.go` 124줄) — 합 **226줄**
- **결과**: **제거 16줄 · 개작 6줄 · 추가 0줄**. `#43` 102 → **92**(`92477ed3e3e2` →
  `8be7fdb36eee`) · `#49` 124 → **118**(`3d7f820c8536` → `86bc7c7cddd3`).
  `판정-합계` 3377 → **3361** · 복원 경로 ①② 판정 **12/69행 → 14/69행**(잔량 57 → **55**) ·
  `등재 범위` 69 · `판정-잔량` 0 · ③④ 69/69(잔량 0, **두 행 재앵커**) ·
  docstring 표면 전건 불변(①② 4/33 · 잔량 29).

> **gross ≠ net.** `git diff --numstat` 는 `+5 −21` 이다. 지운 문장이 줄 경계에 맞지 않는 자리가
> 다섯이라 마지막 줄을 짧게 고쳐 이었고(개작), 그래서 **총계는 −16**이다. 「제거 16줄」만 적으면
> 표면을 과소 진술한다.

## 슬라이스 선택 — 짝짓기 근거를 상속하지 않고 다시 쟀다

슬라이스 42 가 「지목 채널 소진」을 선언하고 **사본 집합이 닫히는 조합** 넷을 실측해 넘겼다.
그 값을 그대로 받지 않고 **판별식을 내 후보에 직접 돌렸다** — 게이트의 `comment_lines()` 를
`importlib` 로 빌려 미판정 86행 전체를 `정규화된 줄 → {원장 행}` 으로 모으고, 후보 조합마다
「조합 안의 줄이 조합 밖 미판정 행에도 있는가」를 셌다(길이 3 이하의 `//`·`#` 구분자 제외).

| 후보 | 줄 | 단독 누출 | 조합 누출 | 처분 |
| --- | --- | --- | --- | --- |
| `(43,49)` attach·port_forward | 226 | 각각 **3줄** | **0** | **이 슬라이스** |
| `(56,58)` + 자매 docstring `#24`·`#26` | 155 | 각각 2줄 | 1(`않는다.` → `D#30`) | 후속 |
| `(60,63)` kind 픽스처 | 79 | 각각 2줄 | 0 | 후속 |
| `(11)` `main.go`·`version` | 36 | 0 | 0 | 후속(크기) |
| `(22,46,57,59)` | 65 → **381** | — | 0 | 후속(아래 ⚠️) |

⚠️ **인계값 하나는 실측에서 과소였다.** 슬라이스 42 는 `(22,46)`·`(57,59)` 를 「넷을 함께 65줄」로
넘겼는데, 그 네 행의 파일 집합은 **docstring 원장 `D#7`·`D#18`·`D#25`·`D#27` 의 파일 집합과
정확히 같다**(`tests/integration/*_ac*.py` 17 + 1 + 3 + 3 파일). 「자매 표면 행이 같은 파일을
덮으면 한 슬라이스로」를 적용하면 그 조합은 **8행 381줄**이고, 최근 어느 패스보다 크다. 65줄로
읽고 집으면 다음 docstring 패스가 이 행들의 ① 근거를 지운다.

`(43,49)` 는 반대다 — 여덟 파일이 전부 `.go` 이고 docstring 원장 33행은 전부 `.py` 라 **자매
겹침이 0** 이고, 226줄은 슬라이스 40(222줄)이 세운 선례 안이다.

### 두 행을 묶은 진짜 이유 — 바이트 동일 사본 3줄

```
// the handler, so a call the handler would reject has already cost an approval
// request by then, and pair resolution is the only thing that runs earlier.
// need a cluster: what the tool hands down, what pair it spends, and what it
```

이 셋은 미판정 86행 가운데 **이 두 행에만** 있다. 한 행만 집으면 쌍둥이가 미판정으로 남아
「같은 문장이 한 표면 안에서 갈린다」가 성립한다. 셋째 줄이 속한 문단은 두 행에서 **함께**
지운 자리이고(아래 ⓓ / ⓑ), 그 ② 원본도 같은 문서의 이웃 시나리오 두 칸이다.

## 제거 — 행 `#43` 네 자리(−10)

| 자리 | 지운 문면 | 복원 |
| --- | --- | --- |
| `internal/k8s/attach.go` `27–29` | `Unlike ExecResource it starts nothing: there is no command, no exit code, and the process keeps running after the window closes.` | **①+②** |
| `internal/mcp/attach.go` `99–101` | `The pair is the same grade as exec's on purpose: AC13 says so, because a pod whose PID 1 is an interactive shell cannot tell the two apart.` | **②** |
| `internal/mcp/attach.go` `125–128` | `resourceAttach` doc 의 AC3·AC6 절 | **②** |
| `internal/mcp/attach_test.go` `13–15` | `The other half — that the output came from the existing process rather than a new one — is only observable against a real pod, and is the integration case's to prove.` | **②** |

- **복원경로 ①** — 첫 자리의 「no command, no exit code」는 같은 패키지 두 선언의 대조가
  복원한다: `AttachResource(ctx, ref, container, stdin, readSeconds)` 에는 `ExecResource` 의
  `command []string` 가 없고, `AttachOutcome` 에는 `ExecOutcome` 의 `ExitCode *int32`·
  `Success bool` 가 없다 ✅. 나머지 셋은 ① 로 복원되지 않는다(등급 판단·승인 화면 계약·층
  분담을 코드는 말하지 않는다) — 근거는 ② 다.
- **복원경로 ②**
  - 「`exec` 과 달리 새 프로세스를 띄우지 않고 이미 돌고 있는 주 프로세스의 스트림에 붙는다」
    → `docs/prd-resource-generic.md` AC13 ¶1 **그 문장 그대로** ✅
  - 등급이 같은 이유 → 같은 AC13 ¶2 가 「대화형 셸이 PID 1 로 도는 파드에서는 `attach` 로
    stdin 을 보내는 것이 `exec` 으로 셸을 여는 것과 구별되지 않는다」로 **축자** ✅.
    주석이 스스로 `AC13 says so` 로 그 자리를 지목하고 있었다
  - 승인 화면 → `docs/prd-approval-gate.md` AC3 동사별 상세 `attach` 행(「컨테이너 이름,
    `readSeconds`, 그리고 `stdin` 이 있으면 **그 내용 전문**」) ✅ · uid 재읽기 → AC6
    (「같은 이름의 새 파드는 다른 파드다」) ✅
  - 층 분담 → `docs/test-resource-generic.md` 시나리오 13 `자동화` 칸 **마지막 문장**이
    「파드 로그로 「새 프로세스가 뜨지 않았다」를 확인하는 절과 stdin 에 대한 대상 프로세스의
    반응은 단위로 관측할 수 없어 이 파일의 몫이다」로 같은 분담을 적는다 ✅
- **남긴 1줄 doc** — `// AttachResource joins the streams of the process already running in
  one named pod's container, collects whatever it says for readSeconds, and returns it
  (prd-resource-generic AC13).` 와 `// resourceAttach joins one named pod's streams after the
  approval.` 는 유지 대상 2번이다. **한 문장이 두세 줄로 감긴 것은 줄바꿈이지 본문 중복이
  아니다** — 여기서 줄 수를 줄이는 것은 판정이 아니라 재포맷이다.

## 🔴 만료된 유지 논거 하나 — 결론은 서고 근거만 갈린다

2026-09-20 의 ③④ 패스가 `#43` 의 실측치 절(`attachStdinReader` doc, 3.00초 / 0.016초)을
유지하며 적은 근거는 이것이었다:

> 같은 수치가 `test-resource-generic.md` 시나리오 13 과 `doc-tracker` 의 같은 행에 이미 있다
> (② 히트). 그럼에도 유지인 근거는 그 ② 표면이 **규칙 6 의 「임시」 등재**라 해제와 함께
> 철거가 예약돼 있다는 것이다.

**그 조건절은 만료됐다.** `docs/doc-tracker/2026-09.md` 의 ⏳ 구현 대기 표는 현재 **5행**
(`test-session-read#2` · `test-session-write#2` · `#4` · `test-approval-gate#1` · `#11`)이고
`test-resource-generic.md#시나리오 13` 은 그 안에 **없다** — 1:1 표에서
`✅ 전용 파일 resource_generic_ac13.py` 로 서 있다. 그 ② 표면은 영속이다.

그런데도 이 자리는 **유지**다. 근거를 다시 재면 그 단락의 명제는 넷이고, 영속 ② 면이 덮는 것은
둘뿐이다:

| 명제 | 영속 ② 면 |
| --- | --- |
| stdin 을 실으면 창이 **0.016초**로 걷혔다 | ✅ 시나리오 13 자동화 칸 |
| 그런데도 **성공 응답**이 돌아갔다 | ✅ 같은 칸 |
| 같은 파드·같은 창에서 stdin 없이는 **3.00초**였다(대조군) | ❌ |
| 그래서 **어떤 단언도 그것을 세지 못했다** | ❌ |
| `client-go` 가 원격 stdin 을 닫고 apiserver 가 그 닫힘을 접속 종료로 읽는 **인과 사슬** | ❌ |

`docs/doc-tracker/2026-09.md` 의 히트는 **변경 이력 행**이다 — README 가 「당시 사실을 적은
기록의 옛 경로는 고치지 않는다」로 손대지 않기로 한 자리라 살아 있는 ② 면이 아니다.
⇒ **갈림 → 유지.** 「애매하면 남긴다」.

🔴 그러므로 이 자리를 「2026-09-20 의 유지가 낡았다」로 읽고 걷지 말 것. 낡은 것은 **논거
하나**이고, 갈아 낀 논거로 **결론은 그대로 선다**.

## 제거 — 행 `#49` 두 자리(−6)

| 자리 | 지운 문면 | 복원 |
| --- | --- | --- |
| `internal/mcp/port_forward.go` `152–156` | `resourcePortForward` doc 의 AC14·AC3·AC6 절 | **②** |
| `internal/mcp/port_forward_test.go` `14–16` | `The other half — that the tunnel is really gone afterwards, so a second round trip cannot ride the same approval — is only observable against a real apiserver, and is the integration case's to prove.` | **②** |

- 앞 자리의 절 셋이 전부 문서에 있다 — `docs/prd-resource-generic.md` AC14 ¶2 가
  「이 도구는 네트워크 정책이 막아 둔 파드 포트에 도달할 수 있으므로, 승인 `context` 에
  **포트와 페이로드가 그대로** 들어가야 한다(`prd-approval-gate` AC3)」로 **한 문장에 축자** ✅,
  `docs/prd-approval-gate.md` AC3 의 `port_forward` 행이 같은 명제를 한 번 더 ✅,
  uid 재읽기는 AC6 ✅.
- 뒤 자리는 시나리오 14 `자동화` 칸이 통합 케이스의 관측 항목을 열거하며 「같은 호출을 한 번 더
  하면 첫 승인이 재사용되지 않고 **새 id** 의 승인 요청이 서며 그것을 거절하면 두 번째 왕복이
  일어나지 않는 것」을 **그 파일의 몫으로** 적는다 ✅.

### ⚠️ 사본 3줄 중 **둘은 이 축에서 걷지 않았다**

`parseExecTarget → parseAttachTarget → parsePortForwardTarget` 는 각각 앞자리를
「for the reason … states」로 **지목한 뒤 그 내용을 되풀이한다**. 지목은 유지 대상이지만
되풀이는 사본이다 — 다만 그 원본이 **다른 주석**이고, 복원 경로는 ①~④ 넷뿐이다.
「다른 주석이 이미 말한다」는 그 넷 어디에도 없으므로 이 축에서는 **판단이 갈린다 ⇒ 유지**.
없는 경로를 만들어 걷지 않았다.

## 유지 — 이웃 전량

| 자리 | 근거 |
| --- | --- |
| `AttachDefaultReadSeconds`·`MaxReadSeconds`(14–19) · `PortForwardDefault/Max`(18–24) | 숫자 5·30 은 AC 에 있지만 **창이 왜 존재하는가**(attach 에는 자연스러운 끝이 없다)는 어디에도 없다 |
| `attachStdinReader` doc(116–135) | 위 표 — 대조군·「단언이 세지 못했다」·인과 사슬·이름 있는 타입인 이유가 ② 면 밖 |
| `roundTrip` 의 에러 스트림 선행 개설(102–105) · 포트 거부가 **본문**으로 온다(128–131) | 상류 `client-go`·apiserver 의 문서화되지 않은 동작. 틀리면 증상이 「조용한 빈 응답」이다 |
| 쓰기 반쪽 닫기와 EOF 교착(145–148) | 실패 모드의 함정 |
| U+FFFD 분기(186–192) | ③ 가 「근거는 코드 주석에 있다」로 이쪽을 지목한다 — 원본이 주석 쪽 |
| `defer conn.Close()`(85–87) | AC14 의 「유지하지 않는다」를 코드로 만드는 지점 — 서 있는 계약 |
| `optionalString` 을 쓰지 않는 이유(75–78) · `command` 를 거부하는 이유(50–53) · 컨테이너 수 검사의 의도적 부재(30–32) | **무엇을 넣지 말라** / 부재의 이유. 부재를 말하는 주석은 경로 ①~④ 어디에도 없다 |
| 테스트 케이스 머리말 잔량 전부 | 「이 케이스가 무엇을 재는가 / 반증 조건」 |

## 무접촉 증명

- **비주석 diff 0줄**: `git diff -U0` 의 `+`/`-` 중 `//` 로 시작하지 않는 줄 **0**. 여덟 파일이
  전부 `.go` 라 docstring 함정이 없다(AST 대조가 필요한 혼합 슬라이스가 아니다).
- 게이트 4종 **rc=0**: `check_comment_policy.py` · `check_mock_policy.py` ·
  `check_doc_inventory.py` · `tests/integration/check_ac_mapping.py`.
- `gofmt -l .` 빈 출력 · `go build ./...` · `go vet ./...` · `go test ./...` 전부 ok.
- **음성 프로브**(단정마다 양방향): ⒜ 원장만 부모로 되돌리면 `[R2]` 두 행(지문 어긋남) + `[R4]`
  ⒝ `복원경로①②-잔량` 을 56 으로 두면 `[R11]`(14+56 != 69) ⒞ `#43` 의 `복원경로 ①② 판정 @지문`
  앵커만 지우면 `[R11]`(그 축을 물은 흔적은 있는데 현재 지문에 묶이지 않은 행) ⒟ 패스 파일을
  두고 허브 집계를 올리지 않으면 `[D4]`.

## 범위 밖(후속)

- 🔴 **`internal/k8s/attach.go` `120–129` 는 이 패스의 논거로 걷지 말 것.** 유지 논거가 갈렸을
  뿐 결론은 섰다(위 표). 다시 물으려면 대조군·「단언이 세지 못했다」·인과 사슬을 **영속 문서로
  먼저 옮겨야** 한다(정책 절차 4번).
- 🔴 **주석→주석 사본 축은 열려 있지 않다.** `parseExec → parseAttach → parsePortForward` 의
  되풀이 2줄과 `AttachDefaultReadSeconds` ↔ `PortForwardDefaultReadSeconds` 의 「a second copy
  of the numbers is a second place for them to drift」 절이 그 형태다. 복원 경로 넷에 그 축이
  없으므로 **정책 개정(README)** 없이는 한 패스의 재량으로 걷을 수 없다.
- 남은 ①② 잔량: 줄 주석 **55행** · docstring **29행**(합 84). 다음 슬라이스의 닫히는 후보는
  `(60,63)` kind 픽스처 **79줄**(자매 겹침 0) · `(56,58)`+`D#24`·`D#26` **155줄**(혼합 —
  무접촉 증명은 `git diff` 필터가 아니라 **docstring 걷은 AST 대조**로) · `(11)` 36줄.
  ⚠️ `(22,46,57,59)` 는 자매 넷(`D#7`·`D#18`·`D#25`·`D#27`)까지 **8행 381줄**이다 — 65줄로 읽고
  집지 말 것.
