# 2026-09-21 — 복원 경로 ③④ 소급 판정 슬라이스 3 · **단일 PR 행의 잔여 다섯 + PR 2~3개 행 셋**

`rct_20260921-0006` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 2](2026-09-21-paths-3-4-single-pr-rows.md)는 「들여온 PR 이 하나뿐인 행은 이제 없다 — 다음 정렬 기준은
PR 2~3개 행」을 다음 패스의 입력으로 남겼다. 이 패스는 그 인계를 받기 전에 정렬을 **다시 쟀고**, 첫 문장이 틀렸음을
확인했다. 슬라이스 2 는 `tests/integration/` 의 **단일 파일** 행만 정렬했고, **다중 파일이지만 들여온 PR 은 하나**인
행 셋과 판정 대상 줄이 **0** 인 행 둘이 정렬 밖에 남아 있었다. 그래서 이 패스는 (a) 그 다섯으로 단일 PR 행을 실제로
소진하고, (b) 인계된 다음 정렬의 첫 칸 — `tests/integration/` 단일 파일 PR 2~3개 행 셋 — 을 같은 PR 에서 판정한다.

## 이 패스의 범위 (완료 기준)

| | 기준 | 값 |
| --- | --- | --- |
| ⑴ | 줄 주석 ③④ 잔량 마커 | **36 → 28**(③④ 27/63 → **35/63**행) |
| ⑵ | 실제 제거 | **2줄** — `082e1fc7e45e`(`resource_generic_ac20.py`) 대조군 2줄. 나머지 일곱 행 0 |
| ⑶ | 줄 주석 판정 합계 | 3311 → **3309**(base `586e338`/#173 기준 — 자매 착지로 base 가 움직이면 게이트 실측으로 재기입) |
| ⑷ | docstring 표면 | **무이동**(7/29 · 잔량 22 · 합계 1935) |

**범위 밖**: 줄 주석 잔량 28행(다음 정렬 = 들여온 PR 4개 이상 행; #172 가 움직인 `internal/` 6행은 새 지문
`6d60307a513a`·`d8fa67959048`·`118864349507`·`f6d31e81324c`·`8a7dc1e7869f`·`c9bbc177fe78` 으로 별도 슬라이스),
docstring 잔량 22행, `e82057fcdeba`(k8s 픽스처 6파일 — PR 8개).

## 측정 — 여덟 행과 ③ 의 모집단

「들여온 PR 수」는 슬라이스 2 와 같이 `git log --format=%h -- <파일>` 의 커밋 수(전부 squash PR)로 쟀다. 코멘트 둘은
reopen 기록(#106)·재푸시 기록(#132)이라 주석과 무관하고 리뷰 코멘트는 여덟 PR 전부 0 이다. **④ 는 대상 부재** — 여덟
행의 파일을 건드린 커밋은 전부 PR 경유이고 README 실태(main 직접 push 는 배포 핀뿐)가 그대로다.

| 행 | 파일 | 줄 | PR | 본문 | 이슈 코멘트 | 리뷰 | 머지 커밋 |
| --- | --- | ---: | --- | ---: | ---: | ---: | --- |
| `86b0510d18a0` | `internal/k8s/collection_precondition{,_test}.go` | 6 | **#119** | 3,295자 | 0 | 0 | `ed5c7c210158` |
| `8d5c02cc7a82` | `_gatekeeper.py` · `approval_gate_ac{2,4}.py` · `resource_generic_ac{8,10}.py` | 15 | **#106** | 3,138자 | 1(reopen) | 0 | `fb2eeb15b064` |
| `1026ce8c1fc3` | `approval_gate_ac{6,10}.py` · `resource_generic_ac{6,7,16}.py` | 20 | **#132** | 9,206자 | 1(재푸시) | 0 | `a7300cddf30b` |
| `e3b0c44298fc` | `internal/k8s/update_precondition_test.go` · `internal/mcp/update_precondition_test.go` | **0** | #109 | 3,241자 | 1 | 0 | `1a25995acfae` |
| `e3b0c44298fc` | `internal/k8s/patch_precondition{,_test}.go` | **0** | #114 · #120 · #106 · #135 | — | — | — | `fdf895f802f6` … `ec18e7595915` |
| `66ff2fad5f28` | `resource_generic_ac3.py` | 6 | **#100** · #126 | 4,158 · 2,579자 | 0 | 0 | `fea75acf3559` · `25532fff1c24` |
| `122e507fe7c4` | `resource_generic_ac5.py` | 10 | **#103** · #106 · #126 | 5,739 · 3,138 · 2,579자 | 0 · 1 · 0 | 0 | `41ee356ad105` · `fb2eeb15b064` · `25532fff1c24` |
| `082e1fc7e45e` | `resource_generic_ac20.py` | 14 | **#87** · #90 · #126 | 4,131 · 7,187 · 2,579자 | 0 | 0 | `44ae81c87675` · `56d88be45090` · `25532fff1c24` |

`#126`(병렬 레인)은 세 파일에 `병렬 레인:` 선언 한 줄씩만 더했고 본문의 레인 표는 주석의 주장을 하나도 담지 않는다 —
세 행에서 ③ 모집단에 넣되 히트 0 이다.

## 판정 기준 — 슬라이스 1·2 의 두 구분 + README 3분 표

1. **본문이 그 주장을 대신 말하면 지운다.** 본문이 *결과*만 적고 주석이 지는 *이유*·*귀속*·*순서*는 적지 않으면
   「다른 주장」이라 남긴다.
2. **본문이 스스로 「주석에 적었다」고 가리키면 원본은 주석 쪽**이라 남긴다.
3. **본문이 결론을 담더라도 그 내용이 앞으로의 편집을 구속하는 서 있는 불변식이면 남긴다**(README 「복원 경로는
   넷이다」 절 3분 표 둘째 줄). 슬라이스 2 는 `finally` 3줄에서 이 줄을 저울질하고 「집행은 코드가 한다」로 기각했다 —
   이 패스에서 같은 저울이 반대로 기운 자리가 둘 있고(아래 `1026ce8c1fc3` ⑴⑵), 그 경계를 이 문서에 남긴다.

## 항목별 — `86b0510d18a0` (제거 0줄)

6줄 전부 exported 타입·인터페이스·메서드의 **이름으로 시작하는 1줄 doc** 이다(`CollectionTargetRef` · `CollectionTarget` ·
`CollectionTargetState` · `CollectionTargetReader` · `ListTargets` ×2). 판정 절차 **2번**(관례 최소치)에서 멈추므로 ③④ 를
물을 본문이 없다 — 공허 판정. #119 본문은 리더의 계약·경계·검증만 적고 여섯 문장과 겹치는 절도 없다.

## 항목별 — `8d5c02cc7a82` (제거 0줄)

#106 본문은 픽스처 **착지**의 경위(병합 이력 · 제품 수리 번들의 경계 판단 · 집계)만 적는다. 이 행의 15줄은 전부 `#:`
상수 doc 이고 지는 주장은 ⓐ `E2E_USER` 가 픽스처 gatekeeper 의 첫 forward-auth upsert 와 ci.yml 시드 스텝의 결합점이라는
것 ⓑ `CREATE_BODY_FIELDS` 가 시나리오 2 의 본문 계약 목록이라는 것 ⓒ 변형 타임아웃이 `gatekeeper-variant.yaml` 과
같아야 한다는 교차 제약(ac2·ac4) ⓓ DaemonSet 거부 사유가 `resource.go::scaleTargetRefFor` 의 문면 앵커라는 것(ac8)
ⓔ 유예 0 · 관측 예산 · SIGTERM 무시 컨테이너가 판별식을 이루는 이유(ac10) — 본문 키워드 대조 12종 **0 히트**.
전건 유지.

## 항목별 — `1026ce8c1fc3` (제거 0줄, 갈림 2)

#132 본문 「재계획 결과」 표가 `resource_generic_ac6.py` 의 두 자리와 겹친다.

| # | 주석 | ③ (#132 본문) | 판정 |
| --- | --- | --- | --- |
| ⑴ | 194–198 `AUTO_REJECT` 를 쓰지 않는 5줄 — 그 모드는 담당 사용자에게 걸리는 설정이라 `GATEKEEPER_USER_ID` 를 물고 있는 변형에서만 발화하고, primary 의 요청은 PENDING 으로 시한까지 남았다가 타임아웃 | 표 3행 「자동 응답 모드(`AUTO_REJECT`)는 담당 사용자가 있는 `gatekeeper-variant` 에서만 발화 — primary 에서는 거부가 아니라 **5분 무응답**」 → 「직접 판정(REJECTED)으로 전환」 | **유지 — 갈림** |
| ⑵ | 217–223 승인 **직후**가 스트림이 열리는 시점이 아니다(`defaultPollInterval` 2s · env) · 한 번만 흔들면 창이 서기 전에 끝나 합성 ADDED 만 본다(CI run 35370872555 `['ADDED']`) · 그래서 여러 번 흔든다 | 표 4행 「승인 판정 시점 ≠ 스트림이 열리는 시점(게이트 폴링 2초) — 한 번만 흔들면 창이 서기 전에 끝난다」 → 「창이 도는 동안 반복해서 흔든다(단언은 그대로)」 | **유지 — 갈림** |
| ⑶~⑻ | `confirmTargetUnchanged` · `WatchMaxEvents` 포인터 · NotFound 재확인 문면 3줄(ac6) · 바늘 1줄(ac10) · 마커 = 도구 이름 2줄(ac16) | 본문 0 히트(「네 표면에서 토큰 부재」·「여섯 verb 를 한 파일에서」는 *무엇*만) | 유지 |

**⑴⑵ 를 남긴 이유와 그 경계.** 본문은 두 결론을 축자에 가깝게 담는다 — 구분 1 만 보면 제거다. 그런데도 남긴 것은
README 3분 표 **둘째 줄** 때문이다: 두 주석이 지는 것은 「그때 왜 그렇게 바꿨나」가 아니라 **배포 배선**(담당자 없는
primary 요청은 거부가 아니라 무응답으로 끝난다)과 **서버 타이밍**(판정 폴링 뒤에야 watch 가 선다)이라는 환경 사실이고,
그 사실은 코드에 보이지 않는다 — 코드에는 직접 `REJECTED` 판정과 5회 루프만 있어 「간단히 자동 모드로」·「중복 흔들기를
하나로」 되돌리는 손을 막는 것이 이 주석뿐이다(둘 다 실제로 CI 에서 그렇게 죽은 뒤 들어온 주석이고, ⑵ 의 관측
`['ADDED']` 와 상수·env 포인터는 본문에 없다). 슬라이스 2 가 `finally` 3줄을 지운 논거 ⓐ 「불변식을 집행하는 것은 주석이
아니라 `finally` 자체」는 여기 그대로 옮겨지지 않는다 — `finally` 는 코드 형태가 곧 불변식(되돌린다)을 드러내지만,
`REJECTED` 직접 판정과 반복 루프는 형태만으로는 임의의 선택으로 읽힌다. **다만 이 경계는 얇다.** 본문이 결론을 담는
자리를 「환경 사실」로 읽어 남기는 것을 어디까지 허용할지는 다음 패스가 다르게 그을 수 있으므로 절차 5번으로 두고 ③
재판정 후보에 올린다.

## 항목별 — `e3b0c44298fc` ×2 (0줄 행, 공허 판정)

두 행의 판정 대상 줄 주석은 **0줄**이다(지문 `e3b0c44298fc` = 빈 입력의 sha256). ③④ 로 물을 내용이 없으므로 「판정 완료」로
세는 것이 옳고, 잔량에 두면 어떤 이동으로도 풀리지 않는 영구 잔량이 된다. 주석이 들어오는 순간 지문이 움직여 R2 가
재판정을 열므로 래칫은 그대로 산다.

## 항목별 — `66ff2fad5f28` (제거 0줄)

#100 본문은 「왜 전용 레이블인가(좁히지 않으면 모집단 123)」·「텍스트 본문에도 `(truncated at limit=100 …` 가 보인다」·
`limit > ListMaxLimit(500)` 을 적는다. 이 행의 6줄이 지는 것은 그와 다른 층이다.

| # | 주석 | ③ (#100 본문) | 판정 |
| --- | --- | --- | --- |
| 35 · 38 | 픽스처가 페이징 ConfigMap 에만 다는 레이블 · 같은 픽스처가 세우는 개수(기본 페이지보다 커야 1회차가 잘린다) | 본문은 레이블을 **왜 따로 뒀는지**를 적는다 — 이 파일과 픽스처 파일 사이의 **교차 제약**(픽스처가 바뀌면 이 테스트가 죽는다)은 다른 주장 | 유지 |
| 41 · 44 | `ListDefaultLimit` · `ListMaxLimit` 과의 짝 지목 | 본문은 `ListMaxLimit(500)` 값만 | 유지 — 교차 제약 |
| 125–126 | 절단은 본문에도 보여야 한다 — 도구를 쓰는 쪽이 구조화 필드를 보지 않고도 「덜 받았다」를 알 수 있어야 한다는 것이 요지 | ② 「텍스트 본문에도 … 보인다」는 단언의 *결과*, 주석은 그 *이유* | 유지 — 다른 주장 |

## 항목별 — `122e507fe7c4` (제거 0줄)

#103(저작) · #106(실물 gatekeeper 착지 — 이 파일의 게이트 대조군을 옮긴 PR) · #126(레인) 셋을 물었다.

| # | 주석 | ③ | 판정 |
| --- | --- | --- | --- |
| 70–71 | 게이트 대조군이 겨누는 Secret 을 **이 파일이 세운다** — 게이트가 승인 요청을 POST 하기 전에 대상을 읽으므로(`gate.go::readGateTarget`) 실재해야 거절 댄스까지 간다 | #103 본문은 **반대**(「대조군은 없는 이름을 겨눈다 — 대상이 실재하면 『없어서 실패했다』와 섞인다」). #106 본문은 대조군을 옮긴 경위를 적지 않는다 | **유지** — 현재 주석의 이유는 어느 본문에도 없다. 낡은 쪽은 PR 본문이고, 정책의 「충돌하면 주석이 틀린 것」은 주석↔코드·문서 사이의 규칙이라 여기 적용되지 않는다 |
| 60 · 74 · 77 · 80 | 픽스처 파일 · `seq 1 40` · `logsDefaultTailLines` · `logsMaxTailLines` 와의 짝 | 본문은 `chatty`/`quiet` 픽스처와 5000 거부라는 값만 | 유지 — 교차 제약 |
| 63–64 | 컨테이너 이름 순서가 매니페스트와 같다 — apiserver 의 후보 목록이 spec 순서를 따른다 | 본문은 「후보 이름을 실어 온다」까지 | 유지 — 상류 동작 |
| 67 | 로그를 내는 쪽 · 나머지는 `pause` 라 한 줄도 내지 않는다 | 본문 「정해진 줄 수를 찍는 `chatty` + 조용한 `quiet`」 | 유지 — 픽스처 교차 제약(다른 파일이 세우는 것) |
| 84 | 절단이 관측되는 작은 값 — 모집단보다 작아야 의미가 있다 | 본문 「모집단이 0줄이면 줄었다가 0 → 0」은 모집단 쪽 조건 | 유지 — 다른 주장 |

## 항목별 — `082e1fc7e45e` → `204ffbc86fa7` (제거 2줄)

#87(저작) · #90(이 파일의 ①②·자기중복 판정 — 「지우지 않은 것」 절이 이 행의 유지 근거를 적는다) · #126(레인).

### 제거 — 설치 전 케이스의 대조군 2줄

```python
    # 대조군: 같은 답에 클러스터가 반드시 갖는 종류는 들어 있다. 이게 없으면 위의 「없다」는
    # api_resources 가 빈 답을 준 것과 구별되지 않는다.
    assert "apps/v1/Deployment" in kinds, (
```

#87 본문 「관측 순서가 곧 단언이다」 2번: 「`api_resources` 에 프로브 종류가 **없음**(**대조군으로 `apps/v1/Deployment` 존재를
함께 확인 — 빈 답과 구별하기 위해**)」 — 대조군의 존재와 그 이유가 축자로 있다. 슬라이스 2 의 ac14 ⑻(승인 요청 0건까지
재는 이유) · ac12 ⑵(namespace 누락에서 0건을 재는 이유)와 같은 형 — **단언의 이유를 본문이 대신 말한다**. #90 이 이 2줄을
「vacuous 방지(원장 #4 계열)」로 남긴 것은 ①② 판정이었고 ③ 을 묻지 않았다(그 PR 이 ③ 경계를 그은 것은 docstring 쪽이다).
남는 `assert` 의 메시지(「답 자체가 온전하지 않다」)가 단언의 역할을 코드에서 말한다.

### 유지 — 12줄

| # | 주석 | ③ | 판정 |
| --- | --- | --- | --- |
| 45 | `PROBE_NAMESPACE` — `test-deployment.yaml` 이 세운다 | #90 이 교차 제약으로 명시 유지 | 유지 |
| 59–60 | established 뒤에도 discovery 에 실릴 때까지 전파 지연이 있다 · 재기동과 구별되지 않는 구간이라 파드 정체 단언이 함께 있어야 한다 | 첫 문장은 상류 apiserver 의 문서화되지 않은 동작(본문 0). 둘째 문장은 #87 「6이 없으면 『재기동 불필요』는 discovery 전파 폴링과 구별되지 않는다」와 겹친다 | 유지 — 둘째 문장만 걷는 부분 편집은 **③ 재판정 후보(부분)** 로 이름만 |
| 91–92 · 290–291 | 프린터 컬럼을 주는 이유 · 자기 Table 정의로 왔다는 것 | 본문은 「프린터 컬럼 포함」이라는 *무엇*만 | 유지 |
| 99 | 픽스처 절 포인터 | ② 포인터, ③ 무관 | 유지 |
| 227–228 | 가장 가까운 것이 먼저 — 다섯으로 잘리므로 순서가 무너지면 밀려난다 · 한 글자 차이라 단독 최근접 | 본문은 후보가 `apps/v1/Deployment` 라는 결과만 | 유지 |
| 257 | 아래 목록 조회 전이라 이 종류는 도구 경로로 한 번도 읽히지 않았다 | 본문 「1을 3보다 먼저 두지 않으면 … 캐시를 처음 만들었다를 잰다」는 오타 호출↔CRD 설치의 순서, 이 줄은 CR 생성↔목록 조회의 순서 | 유지 — 다른 주장 |
| 333 | 프로브 종류는 **클러스터 전역**에 보이므로 뒤따르는 파일에 남기지 않는다 | 본문 「`finally` 로 지우므로 잔존물이 남지 않는다」는 결과, 「전역이라 다음 파일에 샌다」는 이유 | 유지 — **갈림** |

## 결론

| | 값 |
| --- | --- |
| 이 패스의 제거 | **2줄**(0 + 0 + 0 + 0 + 0 + 0 + 0 + 2) |
| 줄 주석 판정 합계 | 3311 → **3309** |
| ③④ 판정 완료 / 전체 | 27/63 → **35/63**, 잔량 **36 → 28** |
| docstring 표면 | **무이동**(7/29 · 22 · 1935) |
| 갈려서 남긴 것 | 3건(`rg_ac6` ⑴⑵ · `ac20` 333) + 부분 후보 1건(`ac20` 59–60 둘째 문장) |

슬라이스 2 의 가설(「모집단이 닫힌 행일수록 ③ 축자가 잘 잡힌다」)은 이번엔 **약하게만** 유지된다 — 단일 PR 잔여 셋의 41줄에서
0 이 나왔다. 그 셋이 0 인 이유는 하나다: 슬라이스 2 의 일곱은 **시나리오 저작 PR** 이라 본문이 「이 e2e 가 무엇을 재는가」를
케이스별로 적었고, 이번 셋은 **픽스처 착지 PR(#106)** · **선행 리더 PR(#119)** · **6파일 묶음 PR(#132)** 이라 본문의 층이
파일 안 주석의 층과 다르다. ③ 히트의 밀도를 정하는 것은 PR 수가 아니라 **그 PR 이 케이스 단위로 썼는가**다 — 다음 정렬에서는
PR 수와 함께 본문의 「이 e2e 가 재는 것」 절 유무를 볼 것.

## 다음 패스의 입력

- 줄 주석 잔량 **28행**. 들여온 PR 이 3개 이하인 `tests/integration/` 단일 파일 행은 소진됐다 — 남은 것은 PR 2~3개의
  Go·YAML 행(`02f201383c52` 2줄 · `118864349507` 39줄 · `e6747e1eba38` 43줄 · `0cffc931b2c9` 78줄(#175 가 재판정 중) ·
  `4ea5b103f043` 103줄 · `4c9ff2211279` 6줄 · `cba720423913` 8줄 · `6448f26b0e72` 59줄 · `20eaf535b30c` 75줄)과 PR 4개 이상.
- #172 가 움직인 `internal/` 6행은 **새 지문**으로 — 패스 문서(슬라이스 2)의 옛 지문 6개는 낡았다.
- ③ 재판정 후보 누적: `rg_ac6` ⑴⑵(이 패스, 경계 얇음) · `ac20` 333 · `ac20` 59–60 둘째 문장(부분) + 슬라이스 2 의 `ac15` ⑶⑻ ·
  `ac11` ⑴ 첫 절 + `recordTarget` doc 3줄 · `Principal` AC1 철자 2줄 · `rbac-narrow-fixture.yaml` 머리말 불릿.
- docstring 잔량 **22행** — 여전히 한 번도 이 축으로 묻지 않은 표면.
