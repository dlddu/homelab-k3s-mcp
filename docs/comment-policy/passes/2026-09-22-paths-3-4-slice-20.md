# 2026-09-22 — 복원 경로 ③④ 소급 판정 슬라이스 20 · **`tests/integration/` 공유 헬퍼 행(132줄)**

`rct_20260922-0004` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 19](2026-09-22-paths-3-4-slice-19.md)가 `tests/k8s/kind` 픽스처 행을 닫으면서 다음 칸을
**`5aa2ddea6731`** 로 못박고 「이 행이 닫히면 줄 주석 표면의 ③④ 잔량이 0 이 된다」까지 적었다.
이 패스가 그 칸을 묻는다 — **줄 주석 표면의 마지막 행**이다.

**선택은 재실측했다.** 게이트 로직(`check_paths34`)을 `importlib` 로 올려 두 원장을 행별로 다시 세면
줄 주석 표면의 앵커 부재 행은 **정확히 하나**(`5aa2ddea6731`, 132줄)이고 docstring 표면은 22행 1571줄이다
(`done 66 + 잔량 1 == 67` · `done 10 + 잔량 22 == 32`). 「한 칸만」 제약이 이번엔 자동으로 충족되지만
**docstring 행은 같은 task 에 끼워 넣지 않았다** — 슬라이스 17 의 의존 경고(`resource_read_smoke.py`
docstring = 원장 docstring #9 가 `tableAccept` 제거의 ① 소유자)가 그대로 서 있고, 두 표면을 한 PR 에
섞으면 R9·R10 앵커가 서로를 낡게 만든다.

줄 번호는 base `6e3d6ca`(#196 = 슬라이스 19) 기준이다.

## 이 패스의 범위 (완료 기준)

줄 주석 원장 **한 행**(`5aa2ddea6731`, `tests/integration/` 공유 헬퍼 7파일 132줄)에 대해 복원 경로 ③④ 를
물어 판정하고, 그 판정을 현재 지문에 못박는다(R9). 게이트 헤드라인으로 확인 가능한 네 칸:

| | 전 | 후 |
| --- | --- | --- |
| 줄 주석 ③④ 판정 | 66/67 (잔량 **1**) | **67/67 (잔량 0)** |
| 판정 합계 | 3359줄 / 149파일 | **3338줄 / 149파일** (Python 버킷 500 → 479 한 축만) |
| 원장 행 | `5aa2ddea6731` 132줄 | **`9c3df35c899b` 111줄** |
| docstring 표면 | 10/32 · 잔량 22 · 2124줄 · `897a853d…` | **무이동** |

범위 밖: docstring 잔량 22행, `README.md` 개정(정책 소유자 몫 5건), 주석의 **정확성**(이 모델의 표면 밖).

## 측정 — 모집단은 두 갈래로 쟀다

이 행의 7파일은 **분할 산물**이라 `git log -- <현재 경로>` 가 저작 PR 을 놓친다(슬라이스 16 에서 실측된 함정).
`--follow` 로 계보를 잇고, 블록마다 레포 **전역** `git log -S<distinctive>` 로 최초 저작 커밋을 되짚었다.
네 블록이 경로 기준보다 **이른 PR** 에서 나왔다:

| 파일 | 부모 | 추출 PR |
| --- | --- | --- |
| `_aws_config.py` · `_opensearch.py` | `aws_config.py` · `opensearch.py` | #43 |
| `_workload.py` | `workload.py` | #49 |
| `_auth_variant.py` | `no_config.py` · `auth.py` · `smoke.py` | #44 |
| `_helpers.py` | `helpers.py`(#13 → Go 이관 #21) | — (장수 부모) |
| `_session_platform.py` · `_oidc.py` | — | #50 · #52 에서 신설 |

블록 기준 저작 PR 은 **10건**: #23 · #39 · #41 · #43 · #44 · #49 · #50 · #52 · #58 · #67.
경로 기준으로만 셌다면 #39(`_workload` 마커 계약) · #23(`_aws_config` 버킷/키) · #41(`_opensearch` aoss ·
`_aws_config` ETag)을 놓쳤다.

**모집단 크기는 판독량의 상한이지 판독량이 아니다**(슬라이스 19 가 재확인한 교훈)는 여기서도 섰다 —
경로 기준 29 PR 중 실제로 본문을 읽은 것은 10 이다.

**리뷰 코멘트는 10 PR 전부 0건**(`/pulls/<n>/comments` 가 모두 `[]`), 이슈 코멘트는 #43 에 2건(CI 재실행).
**④ 는 대상 부재** — 10 커밋 전부 `(#N)` squash 착지이고 본문이 비어 있지 않은 셋(#44 12줄 · #49 5줄 ·
#50 8줄)도 각자 PR 본문의 부분집합이다. README 의 「④ 의 적용 대상은 오늘 0」 실태 그대로다.

## 판정 — 제거 21줄

제거선은 README 「③④는 *다음 편집자*에 대한 복원 경로다」 절의 **3분 표**다. 첫째 줄(그때의 결정·경위)만
걷고, 둘째 줄(앞으로의 편집을 구속하는 서 있는 불변식)과 셋째 줄(원본이 주석 쪽)은 남긴다.

| # | 자리 | 줄 | ③ 원본 |
| --- | --- | ---: | --- |
| ⑴ | `_helpers.py` `EXPECTED_TOOLS` 머리 2문장 | −6 | **#44** 「도구 표면은 `internal/mcp/toolslist.go` 가 **구성과 무관하게 정적으로** 선언하므로 두 배포에서 같아야 한다 — `_helpers.EXPECTED_TOOLS` 한 벌로 합쳐 `platform_auth_safety_ac5.py`(변형)와 `smoke.py`(주 배포)가 함께 읽는다」 |
| ⑵ | `_helpers.py` 포트포워드 절의 이동 경위 | −4 | **#58** 「`port_forward` 를 공유 표면으로 이동(소비자가 둘이 됐다). `_oidc` 가 이름을 다시 내보내므로 `platform_auth_safety_ac{2,8}.py` 는 **무변경**이고 60줄 복제도 생기지 않는다」 |
| ⑶ | `_oidc.py` 재수출 주석 | −2 | 같은 #58 문장. 게다가 **같은 파일 모듈 docstring 넷째 문단**이 축자로 담는다(① 자기 중복) |
| ⑷ | `_oidc.py` 구성 (b)·(c)·(d) 라벨 | −3 | **#52 의 배포 변형 표** — `homelab-k3s-mcp-oauth`=(c) · `…-oauth-only`=(b) · `…-no-auth`=(d)「기동 실패가 의도」 · `oauth-variant` 러너 그룹 대상까지 |
| ⑸ | `_oidc.py` `PROTECTED_RESOURCE_PATH` · `OPENID_CONFIGURATION_PATH` doc | −4 | **#52** 「`internal/server/server.go` 가 OAuth 구성 시에만 디스커버리 라우트를 건다」 · 「`auth.FromEnv` 가 기동 시 `<issuer>/.well-known/openid-configuration` 을 가져와 `jwks_uri` 가 …」 |
| ⑹ | `_workload.py` `wait_for_single_ready_pod` 인라인 1줄 | −1 | **#49** 「셀렉터에 매칭되는 파드가 **정확히 하나이고 Ready**가 될 때까지 폴링」 + 바로 다음 줄의 비교 리터럴(① 재진술) |

지문 `5aa2ddea6731` → **`9c3df35c899b`**(132 → 111). Python 버킷 500 → 479, 파일 수는 149 불변
(어느 파일도 표면 0줄이 되지 않았다).

### ⑷ 는 갈림의 해소다 — 판정 근거가 낡는 두 번째 형

이 세 줄은 **2026-09-08 ①② 재판정이 「애매하면 남긴다」로 보존한 자리**다. 당시 논거는
「라벨의 의미 자체는 `oidc-fixture.yaml` 코드로 복원되지만 **그 매핑을 잃으면 상수 이름만 남는다**」였다.
③ 를 열자 그 갈림이 닫힌다 — **매핑 자체가 #52 본문의 표에 있다**(배포 이름 ↔ 구성 라벨 ↔ 의도).

슬라이스 19 의 관찰 ⑴(정책 문면이 개정되어 판정 근거가 낡는다)과 형이 다르다. 여기서는 **정책도 코드도
움직이지 않았고, 아직 묻지 않은 복원 경로가 하나 남아 있었을 뿐**이다. 「갈려서 유지」는 *그때 물은 경로들
안에서* 갈렸다는 뜻이지 영구 면제가 아니다 — ①② 축의 「갈림」 목록은 ③④ 축이 그 행을 지날 때 **전부 한 번씩
다시 물어야 하는 목록**이다. 남은 갈림(`_session_platform` 예산값 등)은 아래대로 ③ 에서도 갈리지 않았다.

## 남긴 111줄

**ⓐ 서 있는 불변식**(3분 표 둘째 줄 — ③ 은 *그때의 결정*을 적을 뿐 *앞으로의 금지*를 집행하지 않는다)

- `EXPECTED_TOOLS` 의 「도구를 더하는 PR 이 **같은 커밋에서** 이 집합을 고친다」 규칙과 그 근거인 관측 이력
  (`session_list` 2026-09-03 · `session_read` 2026-09-04 두 번 조용히 약해졌다). ③ 는 둘 중 **하나**(#50 의
  `session_list`)만 담고 `session_read` 건은 어느 본문에도 없다 — 부분 히트라 지울 수 없다.
- Go 쪽 `internal/server/mcp_test.go::TestToolsListIncludesAllTools` 와 이 집합이 같아야 한다는 결합.
- 「apiserver 서비스 프록시는 대안이 아니다 — `Authorization` 을 덮어쓰고 GET 밖에 못 낸다」. #52 본문이
  같은 말을 하지만 그것은 *그때의 검토*이고 이 줄은 *앞으로의 금지*다(포워드를 프록시로 「단순화」하려는
  다음 편집자를 그 자리에서 멈춘다).
- `_helpers` http-trace 절의 「결과만 보는 단정은 공허하다」와 `assert_assumed_role_only` 의 같은 논거 —
  픽스처가 실환경과 갈리는 지점(MinIO 가 두 자격증명을 다 받고, OpenSearch 픽스처는 security 플러그인이
  꺼져 있다)이자 앞으로의 단정 형태를 구속한다.
- 「…와 같아야 한다」 결합 여섯 자리 — `_aws_config` 의 ci.yml 시크릿 · `_opensearch` 의 ci.yml 시크릿 ·
  `_auth_variant` 의 `MCP_API_KEYS` · `_oidc` 의 `MCP_OAUTH_ISSUER`·API 키 「다른 값이어야 한다」 ·
  `_workload` 의 `test-deployment.yaml` 마커 계약.
- `_session_platform` 의 「프로덕션과 같은 이름이라 base 매니페스트의 `SESSION_PLATFORM_ENDPOINT` 가 그대로
  해석된다」와 「소유 라벨이 붙어야 제어면 `List` 에 보인다」 — 둘 다 #50 본문에 있으나 이름을 바꾸는 편집과
  라벨을 빠뜨리는 시드를 금지하는 자리다.

**ⓑ 원본이 주석 쪽**(3분 표 셋째 줄)

- `_auth_variant` 의 거부 텍스트 블록. `K8S_REFUSAL` 예외 3줄을 걷으면 **바로 위 문장이 거짓이 된다**
  (「빈 사유는 주 env 미설정일 때 main.go 가 넘기는 것」 — `K8S_REFUSAL` 은 명시 사유를 받는다).
  2026-09-08 재판정이 세운 자리이고, #67 본문이 같은 사실을 적어도 **그 파괴는 막지 못한다**. ③ 히트가
  있어도 지울 수 없는 형이다 — 제거가 남은 주석을 낡게 만든다면 그 주석은 원본이다.

**ⓒ 수치·픽스처 근거**(유지 대상 3)

- `_session_platform` 의 생성 예산 3줄 — #58 본문은 「2분(`defaultReadyTimeout`)」 까지만 적고
  **attach 스트림까지 열어 보고서야 201** 과 **클라이언트가 먼저 포기하면 세션이 고아로 남는다**는 귀결을
  적지 않는다(부분 히트). 삭제 예산(기본 종료 유예 30초)·워크로드 타입 제약도 같다.
- `_opensearch` 의 「따뜻한 픽스처에 대한 재실행도 묵은 문서를 보지 않는다」 — #41 은 **케이스 간 격리**만
  적고 재실행 축은 적지 않는다.
- `_oidc` 의 audience / `MCP_OAUTH_RESOURCE` 절 — #52 가 같은 말을 하지만 「일부러 다른 값으로 둔다」는
  앞으로의 편집을 구속한다(같게 만들면 폴백 경로가 대신 돌아 AC2 가 다른 것을 재게 된다).

## 검증 (로컬 실측, base `6e3d6ca` 대비)

- **게이트 rc=0**, 헤드라인 네 칸이 위 표대로 움직였다 —
  `판정 대상 주석 3338줄 / 149파일 (Go 소스 1585 · Go 테스트 853 · YAML 421 · Python 479) ·
  판정 완료 3338줄(100.0%) / 등재 범위 67 · 미판정 잔량 0줄 · 복원경로 ③④ 판정 67/67행(잔량 0)`,
  docstring 절은 `10/32(잔량 22)` · 2124줄 · `897a853d…` **무이동**.
- **비주석 바이트 0** — 일곱 파일 전부 `ast.parse` → `ast.dump` 가 편집 전후 **동일**(Python 이므로 가장 싼
  측정이다). docstring 노드도 AST 안에 있으므로 이 한 번의 대조가 「docstring 무이동」까지 함께 증명한다.
  원장 docstring #4(`1f77587a4101`, 같은 7파일 220줄)를 따로 재측정해도 값이 같다.
- **자매 축 무영향** — `check_mock_policy.py` 출력 md5 `084bfbeb1c98` · `check_ac_mapping.py` `6aa2fb1e795a`,
  둘 다 base 와 **동일**. 이 행의 파일이 `tests/integration/` 공유 헬퍼라 두 축과 물리적으로 가까운 자리다.
  `check_doc_inventory.py` 는 이 패스 문서 1건만큼 움직인다(76 → 77 · 정책 36 → 37).
- **원장 산술 자기정합** — 행 줄수 합 == `판정-합계` 마커(3338), docstring 쪽도 2124 로 성립.

## 결론

| | |
| --- | --- |
| 물은 행 | `5aa2ddea6731` → **`9c3df35c899b`** (132 → 111) |
| ③ 모집단 | 10 PR(경로 기준 29 의 상한 아래) · 리뷰 코멘트 0 · 이슈 코멘트 2 |
| ④ | 대상 부재(10건 전부 squash) |
| 제거 | **21줄** (제거율 16% — Go/Python 표본 범위 안. 슬라이스 19 의 YAML 41% 와 대비된다) |
| 줄 주석 표면 | **67/67 · 잔량 0 — 이 축이 줄 주석 표면을 전부 물었다** |
| docstring 표면 | **무이동**(10/32 · 22 · 2124) |
| 갈려서 남긴 것 | 갈림 **+0** · ③ 재판정 후보 **+3**(`EXPECTED_TOOLS` 관측 이력 · `_session_platform` 예산 · `_opensearch` 재실행 축 — 셋 다 부분 히트) · ① 재판정 후보 **+0** |
| 해소한 갈림 | **1** — `_oidc` 의 (b)·(c)·(d) 라벨(2026-09-08 ①② 보존분) |

**관찰.** 「갈려서 유지」는 *그때 물은 복원 경로들 안에서* 갈렸다는 뜻이다. 이 패스는 ①② 축이 남긴 갈림
하나를 ③ 하나로 닫았고, 그 반대편에서 **③ 히트가 있어도 지울 수 없는 형**(제거가 남은 주석을 거짓으로
만드는 `_auth_variant` 블록)을 처음 명시적으로 세웠다. 둘은 같은 규칙의 앞뒷면이다 — 판정은 *히트 여부*가
아니라 *제거 후에 남는 것이 참인가*로 닫힌다.

## 다음 패스의 입력

- **줄 주석 표면은 닫혔다**(67/67 · 잔량 0). 이 축의 다음 칸은 **docstring 잔량 22행 1571줄**이고,
  그것이 이 모델에 남은 유일한 ③④ 미판정 표면이다.
- **첫 docstring 칸의 선결**: 슬라이스 17 의 의존 경고가 유효하다 — `resource_read_smoke.py` docstring
  (원장 docstring #9, 22줄)은 `tableAccept` 제거의 **① 소유자**이므로 그 행을 물 때는 이 의존을 알고 내릴 것.
  선결이 0 인 칸부터 고를 것을 권한다(docstring #3 `smoke.py` 14줄 · #13 `check_doc_inventory.py` 29줄 ·
  #11 `resource_generic_ac1.py` 22줄 — 셋 다 파일 하나짜리라 ③ 모집단이 작다).
- ③ 재판정 후보 누적: 이 패스 **+3** → 41 + 3 = **44**.
- 갈림 누적: **4 − 1 = 3**(이 패스가 `_oidc` 라벨 건을 닫았다).
- ① 재판정 후보: 승계 **6** 그대로.
- 정책 소유자 몫(승계 5, 신규 **1**): **「③ 히트가 있는 자리를 지우면 같은 블록의 다른 문장이 거짓이 되는
  경우」를 3분 표의 어느 줄로 셀 것인가** — 지금은 셋째 줄(원본이 주석 쪽)로 접어 넣었지만, 원본 판정이 아니라
  **제거의 부작용**이라 성격이 다르다.
