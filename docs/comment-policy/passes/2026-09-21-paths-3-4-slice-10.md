# 2026-09-21 — 복원 경로 ③④ 소급 판정 슬라이스 10 · **PR 8개 정렬의 첫 행(`internal/github/github.go` · `github_test.go`)**

`rct_20260921-0016` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 9](2026-09-21-paths-3-4-slice-9.md)가 `resource_test.go` 행을 닫고 다음 칸을 「PR 8 정렬 — `8a7dc1e7869f`(`internal/github/`) ·
`e1daa9adec21`(`tests/integration/run_all.py`+`check_ac_mapping.py`) · `b5869fe61fba`(`tests/k8s/kind/auth-fixture.yaml`·`test-deployment.yaml`)
→ …. 모집단은 행마다 `git log -- <파일>` 로 재실측」이라 명명했다. 이 패스는 **그 첫 행 = 44줄**을 묻는다. 한 행만 묶은 이유는 슬라이스
7·8·9 와 같다 — 전체 이력(227 커밋)으로 잰 ③ 모집단이 나머지 두 행과 **하나도 겹치지 않는다**(둘은 #44 하나를 공유하고 이 행과는 0 공유).
③ 모집단은 PR 본문 **8건**(#21 · #26 · #59 · #136 · #155 · #166 · #167 · #172 — 전부 squash 착지, 코멘트·리뷰 **0**, `git log -- internal/github/github.go`
`github_test.go` 의 제목이 전부 `(#N)` 을 단다 → ④ 대상 부재)이고, `git blame` 으로 44줄 전부가 이 여덟 커밋에 귀속된다(#21 10 · #136 12 ·
#155 11 · #26 7 · #172 2 · #59 1 · #167 1 — #166 은 파일을 건드렸지만 남긴 주석은 #167 이 접어 0). 줄 번호는 base `932e94c`(#186) 기준이다.

## 이 패스의 범위 (완료 기준)

| | 기준 | 값 |
| --- | --- | --- |
| ⑴ | 줄 주석 ③④ 잔량 마커 | **14 → 13**(③④ 53/67 → **54/67**행 — base `932e94c`/#186 기준) |
| ⑵ | 실제 제거 | **1줄** — `8a7dc1e7869f` → `86180f41bf6a`(44 → 43). 비주석 바이트 0 |
| ⑶ | 줄 주석 판정 합계 | 3418 → **3417**(base `932e94c` 기준 — 자매 착지로 base 가 움직이면 게이트 실측으로 재기입) |
| ⑷ | docstring 표면 | **무이동**(10/32 · 잔량 22 · 합계 2124) |

**범위 밖**: 줄 주석 잔량 13행 — 다음은 PR 8 정렬의 나머지 둘(`e1daa9adec21` run_all/check_ac_mapping 17줄 · `b5869fe61fba`
auth-fixture/test-deployment 49줄 — #44 를 공유하므로 한 패스로 묶을 수 있다) → …, docstring 잔량 22행, `e82057fcdeba`(k8s 픽스처 6파일 —
PR 16개), 아래 ③ 재판정 후보.

## 측정 — 한 행과 ③ 의 모집단

| 행 | 파일 | 줄 | PR | 본문 | 머지 커밋 |
| --- | --- | ---: | --- | ---: | --- |
| `8a7dc1e7869f` → `86180f41bf6a` | `internal/github/github.go` · `github_test.go` | 44 → 43 | **#21** · **#26** · #59 · **#136** · **#155** · #166 · **#167** · **#172** | 2,134 · 1,220 · 4,573 · 6,041 · 3,400 · 1,287 · 2,783 · 4,875자 | `d1de59436056` · `a526e8e8548f` · `88b3b171c84e` · `05ef09792091` · `48bb9b70e74f` · `858b57699467` · `6680a4c1337d` · `7065a1449b8b` |

굵은 것이 주석을 들여온 PR 이다 — #21 이 패키지 신설(exported 1줄 doc 10), #26 이 AC4 비노출 테스트(7), #136 이 토큰 AC5(테스트 노브 2 +
`revokeToken` 2 + `TestGitHubToken*` doc 6 + 단언 이유 2), #155 가 commit status 도구(`Service`·`installationOwner`·`mintInstallationToken` doc 9 +
노브 2), #167 이 `sha` 포인터 1, #172 가 `Unavailable()` 2. #59 는 이 정책의 첫 판정 — 「This previously had no automated coverage.」 한 문장을
지웠고(줄 수 불변) 나머지를 「exported 1줄 doc 과 패키지 주석뿐」·「앱 JWT 를 되돌려주면 그 자체가 파생 서명 자료의 유출」로 유지했다. #166 은
`fakeGitHub` 에 `sha` 부재 주석 2줄을 넣었지만 #167 이 그것을 ② 축으로 재판정해 포인터 1줄로 접었으므로 ③ 히트의 상대는 #167 이다.

**이 층의 ③ 모양 — 저작 PR 본문이 「설계 결정」·「검토자가 볼 곳」 절에 구현의 근거를 적는다.** #155 의 「AC5 가드를 지나가는 자리」는
`mintInstallationToken` doc 의 셋째·넷째 줄을, 「`owner` 는 호출자가 넘기지 않는다」는 `installationOwner` doc 의 둘째 문장을, #172 의 「여섯 통합의
`Error.Unavailable()` 로 식별」은 `Unavailable()` doc 을, #26 의 「장수명 베이스 시크릿」은 `TestCreateInstallationTokenNeverExposesSigningKey` 머리를
축자로 담는다. 그러나 이 행은 **exported·패키지 관례 doc 13자리(1줄 11 + `Service` · `FromEnv` 첫 문장) + 테스트 노브 인코딩 4 + 단언 이유 3 + 「부재의 주석」** 이 대부분이라, 첫째 줄로
떨어지는 것은 **같은 패키지 안에 ③④ 판정을 마친 강한 사본이 살아 있는 명제의 되풀이** 뿐이다 — 슬라이스 9 가 `gate.go::updatePairs` doc 에 적용한
판별식(③ 축자 + ① 등재된 강한 사본 = 이중 복원 → 사용 지점 사본은 지우고 포인터로 접는다) 그대로다.

**본문과 코드의 대조**(#24 선례): #21 의 `Service` + `Unavailable` 스텁 패턴, #136 의 AC5 세 조항과 다섯 테스트 이름, #155 의 `account.login`·`/`
거부·`statuses: write` 폐기·접두사 fail-closed, #167 의 표 두 줄, #172 의 `Unavailable()` 식별 — 여덟 본문 모두 현재 코드와 어긋나지 않는다(#21 의
「9 tools」·「tools degrade to error results」는 그때의 값·응답 모양이고 — #172 뒤 레코드는 refusal 이지만 클라이언트 응답은 그대로 `isError` — 판정
대상 주석과 무관; #155 의 도구 수 25 도 그때 값).

## 판정 기준 — 슬라이스 1~9 의 구분 그대로

1. **본문이 그 주장을 대신 말하면 지운다.** 본문이 *결과*·*무엇*만 적고 주석이 지는 *이유*·*교차 제약*은 적지 않으면 「다른 주장」.
2. **본문이 스스로 주석을 가리키면 원본은 주석 쪽** — 유지. 이 층에서는 #172 「이 표와 판단의 원본은 코드의 doc 쪽이다」와 #167 「포인터는
   지식을 되풀이하지 않는다(위치만)」가 그것이고, **코드 자신의 포인터**(`commitstatus.go` 142–143 「see mintInstallationToken」)도 같은 자리다.
3. **본문이 결론을 담더라도 앞으로의 편집을 구속하는 서 있는 불변식이면 유지**(README 3분 표 둘째 줄). 이 층에서는 **부재의 주석**(「AC5 의
   가드가 여기 없다」 — 없는 것은 코드 형태로 보이지 않고 되돌려 놓는 편집이 다른 경로를 죽인다)과 인터페이스 계약(「토큰으로 내주지 않는
   권한을 스스로 쓴다」)이 여기 든다 — 슬라이스 7 의 `Unavailable.Authorize` 269–271 과 같은 형.

이 층에서 새로 확인한 것: **같은 명제가 한 패키지의 두 파일에 있고 한쪽 행(#54)이 이미 ③④ 판정을 마쳐 소유자로 서 있을 때, 다른 행의 사본은
첫째 줄이다.** #155 가 `installationOwner` doc 과 `commitstatus.go` 72–75 를 같은 PR 에 넣었고, 행 #9 의 ①② 판정(`rct_20260920-0001`)은 「구조적
보장의 이유 — PRD 는 사실만 적고 귀결은 적지 않는다」로 유지했지만 그때는 `commitstatus.go` 쪽과의 관계를 세지 않았다(슬라이스 8 관찰 ⑵ — 앞
판정의 「유지」는 ①② 한정 진술로 읽는다).

## 항목별 — `8a7dc1e7869f` → `86180f41bf6a` (`internal/github/`, 제거 1줄)

### 제거 ⑴ — `installationOwner` doc 의 둘째 문장 (223–225행 → 223–224, −1)

```
// installationOwner is the owner half of every repository path this server
// builds. A caller cannot aim a status outside the installation because it
// never supplies that half.
```
→ 「A caller cannot aim … that half.」 를 지우고 첫 문장 끝에 소유자 포인터 「— CreateCommitStatus says why a caller never supplies that half」 를
접어 남긴다.

**③ #155 「AC1 기록 — `owner` 는 호출자가 넘기지 않는다: `GET /app/installations/{id}` 의 `account.login` 에서 읽는다. `repository` 에 `/` 가 있으면
거부한다 — GitHub 은 그것을 거부하지 않고 경로에 그대로 붙여, 발급한 토큰의 범위 밖 레포를 가리키게 된다」** ✅ 축자. 그 위에 **①
`internal/github/commitstatus.go` 72–75**(행 #54 — ③④ 판정 @`0b268bf022e1` 에서 유지된 소유자)가 같은 명제를 owner 가 경로에 붙는 그 자리에서
더 강하게 말한다 — 「The owner half is the installation account, resolved server-side. A caller-supplied "owner/repo" would not be rejected by GitHub — it
would be pasted into the path and address a different repository than the one the minted token is scoped to」. 지운 문장은 그 doc 의 **정의 자리
사본**이고(함정의 절반 — 「GitHub 이 거부하지 않는다」 — 을 빼놓은 약한 되풀이), 첫 문장 「owner half of every repository path this server builds」
(① — 호출부가 그대로 말하지만 ①② 판정이 유지한 자리, 이 패스의 축이 아니다)와는 다른 문장이다.

### 유지 — 43줄

| # | 주석 | ③ | 판정 |
| --- | --- | --- | --- |
| 1 · 41 · 68 · 83 · 88 · 100 | 패키지 주석 · `Error`·`InstallationToken`·`Unavailable`·`NewUnavailable`·`Client` 1줄 doc | #21 「`Service` interface + `Unavailable` stub pattern」 — *무엇* | 관례(정책이 유지 예시로 든 `// Error is the error type returned by Service.` 포함) |
| 31 `errKind` | 「not configured」 실패와 apiserver 오류를 가른다 | ❌ | 유지 — unexported 타입 doc, 09-04 판정 그대로 |
| 64–65 `Unavailable()` | 미설정 종류 = 디스패처가 error 가 아니라 refusal 로 기록하는 종류(AC4) | **#172 「여섯 통합의 `Error.Unavailable()` 로 식별」** ✅ + **#172 「원본은 코드의 doc 쪽」** | 유지 — **셋째 줄**(`rct_20260921-0004` 판정 그대로: 소유자는 `mcp.toolError` doc, 이 줄은 포인터 — 슬라이스 6·8 의 같은 줄과 동일) |
| 76–77 `Service` | 토큰을 발급하고, 토큰으로 내주지 않는 그 한 권한을 스스로 쓴다 | **#155 「AC5 가 막는 것은 토큰을 호출자에게 내주는 것이고 이 경로는 토큰을 돌려주지 않는다」** ✅ | 유지 — exported 관례 + 인터페이스 계약의 불변식(둘째 줄: 구현체·새 메서드가 `statuses: write` 를 토큰으로 내주지 않는다) |
| 112–114 `FromEnv` | `GITHUB_APP_CLIENT_ID` 미설정이면 `(nil, nil)` — 미설정과 오설정을 가른다 | ❌(#21 은 스텁 패턴만) | 유지 — 관례 + 호출자 계약 |
| 261–262 `revokeToken` | 폐기 대상 토큰 자신으로 인증한다(다른 두 호출은 app JWT) | ❌(#136·#155 는 `DELETE /installation/token` 만) | 유지 — 상류 API 동작, 09-20 판정 그대로 |
| 288–291 `mintInstallationToken` | 토큰 도구와 `CreateCommitStatus` 가 공유하는 원시 교환 · **AC5 가드는 일부러 여기 없다** — 토큰을 호출자에게 내주는 경로의 것이고, 이 자리는 `statuses: write` 를 돌려주지 않고 쓰는 좁은 액션도 지난다 | **#155 「검토자가 볼 곳 — 저수준 `mintInstallationToken` 을 공유로 뽑고 가드는 토큰 도구 경로에만 남겼다 …」** ✅ 축자 | 유지 — **둘째 줄**: 부재의 주석(가드가 없는 것은 코드에 보이지 않고 되돌려 놓으면 commit status 경로가 죽는다 — 09-20 판정 「없앤 줄 알고 되돌려 놓는 편집이 곧 커밋 status 경로를 죽인다」) + `commitstatus.go` 142–143 「see mintInstallationToken」이 이 doc 을 소유자로 지목(코드 안의 셋째 줄). **③ 재판정 후보(부분)** — 첫째 줄과 둘째 줄 사이(③ 축자 · ① 은 부재) |
| 27 · 29 · 31 · 33 `fakeGitHub` 노브 | `nil` ⇒ 500 · `nil` ⇒ 요청 에코 · 빈 문자열 ⇒ `account` 생략 · `0` ⇒ 201, 422 ⇒ GitHub 문면 | ❌ | 유지 — 타입만으로 복원되지 않는 노브 인코딩(09-18·09-20 판정 그대로) |
| 89 `No "sha" on purpose` | 리터럴에서 키의 부재 — 문서 포인터 | **#167 표 + 「포인터는 지식을 되풀이하지 않는다(위치만)」** ✅ | 유지 — **셋째 줄**: 저작 PR 이 이 줄을 원본 자리(위치)로 스스로 지목 |
| 144 · 182 · 202 · 241–242 · 256 `TestGitHubToken*` | `covers AC5 clause N` | #136 「Go 단위 5종 … 시나리오 5 가 이름으로 지목한 그대로」 — 이름 목록 | 관례 1줄 doc + 조항 포인터(`RefusesWhenInstallationUnreadable` 만 이름이 길어 2줄) |
| 232 | downgrade 가 scope 변경을 겸하면 안 된다 | #136 「`statuses` 만 read 로 낮춘 명시 맵」 — *무엇*(「만」) | 유지 — 그 단언이 왜 있는가(둘째 줄) |
| 279–283 `TestCreateInstallationTokenNeverExposesSigningKey` 머리 | AC4 + 「RSA 키와 그것으로 서명한 app JWT 는 장수명 비밀 — 단명 설치 토큰만 프로세스를 떠난다」 | **#26 「장수명 베이스 시크릿 … AC4 는 그 베이스 시크릿이 도구 출력에 절대 노출되지 않을 것」** ✅ | 유지 — 첫 문장은 관례, 둘째는 시험 대상의 진술(② PRD AC4 와 같은 자리, 결정·경위가 아니다) — 슬라이스 6 `exposure_test.go` 11–14 쌍둥이와 같은 판정. **③ 재판정 후보(부분)** |
| 297 `gotAuth` | mint 호출의 헤더여야지 AC5 조회의 헤더가 아니다 | ❌ | 유지 — 조용히 통과하는 실패 모드(09-18 판정) |
| 323–324 | app JWT 로 인증됐어야 하고, 그 JWT 를 되울리면 파생 서명 자료의 유출 | #26 「시크릿이 실제로 사용됐음을 먼저 증명 … 공허한 테스트가 되지 않습니다」 — 이유의 절반 | 유지 — 둘째 줄(vacuous 방지 + JWT 가 왜 비밀인가 — 후자는 ③ 에 없다) · #59 「유지 판정」 명시, 슬라이스 6 의 44–46 과 동형 |
| 333 | 반환 필드 어디에도 app JWT·개인키가 없어야 한다 | #26 「반환된 `InstallationToken` 에 그 JWT 도 개인키 PEM 도 담기지 않음을 단언」 ✅ | 유지 — 아래 단언 블록의 항해 표지 1줄(① 코드, 슬라이스 6 의 54 와 동형) |

## 결론

| | 값 |
| --- | --- |
| 이 패스의 제거 | **1줄** — 비주석 바이트 0(`grep -vE '^\s*//'` diff 0 · `gofmt -l` 빈 출력 · `go build ./...`·`go vet`·`go test ./internal/github/...` 무이동) |
| 줄 주석 판정 합계 | 3418 → **3417** |
| ③④ 판정 완료 / 전체 | 53/67 → **54/67**, 잔량 **14 → 13** |
| docstring 표면 | **무이동**(10/32 · 22 · 2124) |
| 갈려서 남긴 것 | 갈림 0 · 부분 후보 2(288–291 `mintInstallationToken` · 279–283 AC4 머리) |

**관찰 둘.** ⑴ 실행 코드 패키지의 행이라도 제거는 작았다 — ③ 축자 히트 일곱 자리(64–65 · 76–77 · 89 · 223–225 · 279–283 · 288–291 · 333) 중
첫째 줄로 떨어진 것은 **같은 패키지에 ③④ 판정을 마친 소유자(행 #54)가 살아 있는 명제의 정의 자리 사본** 하나뿐이다. 나머지는 관례(exported
doc)이거나 부재의 주석·계약(둘째 줄)이거나 PR·코드가 스스로 원본으로 지목한 자리(셋째 줄)다. ⑵ **행 사이의 사본은 먼저 판정을 마친 행이
소유자다** — 같은 PR 이 두 파일에 같은 명제를 넣었을 때, 어느 쪽을 지울지는 「어느 행이 이미 ③④ 앵커로 서 있는가」와 「어느 쪽이 함정의 전문을
드는가」로 정했다(둘 다 `commitstatus.go` 쪽). 이는 슬라이스 8 의 `ReadResult`(같은 파일 `Service` doc 이 소유자)와 슬라이스 9 의
`updatePairs`(구현 doc 이 소유자)를 「행을 넘어」 확장한 것이지 새 규칙이 아니다.

## 다음 패스의 입력

- 줄 주석 잔량 **13행** — 다음은 PR 8 정렬의 나머지 둘 `e1daa9adec21`(`tests/integration/run_all.py`·`check_ac_mapping.py` 17줄 — #42·#44·#52·#66·#106·#126·#144 + 트래커 PR) ·
  `b5869fe61fba`(`tests/k8s/kind/auth-fixture.yaml`·`test-deployment.yaml` 49줄 — #8·#28·#35·#37·#39·#44·#62·#67). 둘은 #44 를 공유하므로 한 패스로 묶을 수
  있다(슬라이스 6 의 기준). 모집단은 행마다 `git log -- <파일>` 로 재실측 — 이번엔 전체 이력으로 쟀다(감지 클론의 `--depth 60` 은 하계만 본다).
- ③ 재판정 후보 누적: 이 패스의 둘(`github.go` 288–291 부재의 주석 · `github_test.go` 279–283 AC4 머리) + 슬라이스 9 의 하나(`resource_test.go`
  646–647 교차 참조) + 슬라이스 8 의 하나(`sessionplatform.go` 24–26 괄호 인용) + 슬라이스 7 의 둘(`gatekeeper.go` 194–200 · 327–328) + 슬라이스 6 의
  셋(`grafana.go` 73–75 · `exposure_test.go` 11–14 · `opensearch.go` 309–310) + 슬라이스 5 의 셋(`http-trace` 119–123 · `gatekeeper-variant` 117–118 ·
  `session-platform` 23–27) + 슬라이스 4 의 `precondition.go` 15–18 첫 문장 + 슬라이스 3 의 `rg_ac6` ⑴⑵ · `ac20` 333 · `ac20` 59–60 둘째 문장 + 슬라이스
  2 의 `ac15` ⑶⑻ · `ac11` ⑴ 첫 절 · `recordTarget` doc 3줄 · `Principal` AC1 철자 2줄 · `rbac-narrow-fixture.yaml` 머리말 불릿. 갈림 승계: `ac4` 빈 맵
  1줄(#81 이래) · `TRACE_ROUTES` 앵커 1줄(#62 이래) · `resource_test.go` 154–155 · 324–326(슬라이스 9) — 이 패스 신규 0.
- docstring 잔량 **22행** — 여전히 한 번도 이 축으로 묻지 않은 표면.
- 정책 소유자 몫(승계 3 · 신규 0): PR 안의 개별 커밋 메시지(squash 로 main 이력에 없는)가 ③ 인가 — `http-trace.yaml` 15–17·19–20·75–79. ③ 본문이
  코드와 어긋날 때의 규칙(슬라이스 6, #24). 한 덩어리로 「유지」된 다줄 논증 안의 개별 절을 ①② 축이 다시 물어야 하는가(슬라이스 7·9). 이 패스는
  README 개정 없이 판정 가능했다 — 부재의 주석(288–291)은 3분 표 둘째 줄의 문면(「앞으로의 편집을 구속하는 서 있는 불변식」)이 그대로 든다.
