# 2026-09-22 — 복원 경로 ③④ 소급 판정 슬라이스 22 · **docstring 표면 둘째 칸(원장 #12·#14·#20, 86줄)**

`rct_20260922-0006` / `tbm_homelab-k3s-mcp-comment-redundancy`.

[슬라이스 21](2026-09-22-paths-3-4-slice-21.md)이 docstring 표면의 첫 칸을 열어 잔량을 22 → 19 로
내렸다. 이 패스가 둘째 칸을 묻는다.

줄 번호는 base `27db083`(#199) 기준이다.

## 선택 — 인계의 「선례 피인용 0」을 재실측해 둘이 뒤집혔다

슬라이스 21 은 다음 칸으로 `#12`(36줄) · `#14`(23줄) · `#20`(27줄)을 권하며 **셋 다 파일
하나짜리이고 선례 피인용이 없다**고 적었다. 칸의 *선택*은 그대로 받았지만 그 근거는 다시 쟀다.

먼저 잔량 자체를 게이트 출력이 아니라 **원장 파서로 독립 재계산**했다(초록을 근거로 삼기 전
라벨 집합을 뽑는다). 행별로 「결과 칸이 `복원경로 ③:`·`복원경로 ④:` 를 둘 다 담고
`복원경로 ③④ 판정 @지문 <12자>` 앵커가 그 행의 **현재** 지문 셀과 같은가」를 판정하니
**32행 = 완료 13(611줄) + 잔량 19(1,501줄)**, 합계 2,112 로 게이트·마커와 전건 일치했다.

**그다음 「선례 피인용 0」을 원장 두 표면에서 기계로 훑으니 셋 중 둘이 걸렸다.**

| 행 | 실측된 얽힘 | 이 패스의 처리 |
| --- | --- | --- |
| `#12` `resource_generic_ac20.py` | 피인용 0 (확인) — 다만 **자기 결과 칸의 유지 논거가 정책 개정으로 낡았다** | 그 한 절에만 번복 적용 |
| `#14` `resource_generic_ac3.py` | 유지 근거 ⑵ 가 **원장 #11 의 판정을 선례로 인용**하는데 슬라이스 21 이 그 문단을 #11 에서 **제거**했다 | 뒤집힌 선례를 따라 같은 형태를 제거 |
| `#20` `resource_generic_ac12.py` | 피인용은 `#21` 하나뿐이고 그것은 **런타임 방식 언급**(ac14 가 ac12 의 방식을 따랐다)이지 판정 인용이 아니다 | 얽힘 없음으로 확정 |

슬라이스 21 의 스캔은 「**나를** 선례로 인용하는 행이 있는가」(들어오는 방향)를 봤다. `#14` 의
얽힘은 반대 방향이다 — **내가 인용한 선례가 그 패스 자신에 의해 뒤집혔다.** 같은 낱말을 쓰는
두 방향이라 한쪽만 재면 놓친다.

## 이 패스의 범위 (완료 기준)

docstring 원장 **세 행**에 ③④ 를 물어 판정하고 현재 지문에 못박는다(R10).

| | 전 | 후 |
| --- | --- | --- |
| docstring ③④ 판정 | 13/32 (잔량 **19**) | **16/32 (잔량 16)** |
| 판정 합계 | 2,112줄 | **2,095줄** |
| `#12` `resource_generic_ac20.py` | 36줄 `2412aefb8ac7` | **29줄 `b46015f2f422`** |
| `#14` `resource_generic_ac3.py` | 23줄 `1047bcfddc79` | **21줄 `3ef4e202f1a6`** |
| `#20` `resource_generic_ac12.py` | 27줄 `fa165b07a33c` | **19줄 `a535940f18a2`** |
| 줄 주석 표면 | 67/67 · 잔량 0 · 3,338줄 | **무이동** |

범위 밖: docstring 잔량 **16행**(`#3` `smoke.py` 의 선례 얽힘과 `#9` `resource_read_smoke.py` 의
① 소유자 의존을 포함) · `README.md` 개정(정책 소유자 몫) · 주석의 **정확성**(이 모델 표면 밖).

## 측정

세 칸 다 파일 하나짜리라 분할 산물 함정이 서지 않는다. 저작 PR: `#12` → **#87**(`44ae81c`) ·
`#14` → **#100**(`fea75ac`) · `#20` → **#145**(`880fe2a`). 리뷰 코멘트는 세 PR 모두 0건이라
③ 의 실체는 본문뿐이다.

**④ 는 재실측했고 여전히 대상 부재다.** 최근 120커밋에서 제목에 `(#N)` 이 없는 27건을 뽑아
커밋마다 `/commits/<sha>/pulls` 를 조회했다 — 26건은 연결 PR 이 없는 `chore(deploy):` 이미지
핀이고 건드린 파일은 이 정책의 범위 밖 `k8s/deployment.yaml` 하나, 나머지 `e04e5d1` 은 제목에
번호가 없을 뿐 PR 로 착지했다. 정책 범위의 main 직접 push 는 **0**.

> ⚠️ 「제목에 `(#N)` 이 없다 = 직접 push」는 틀린 대리 지표다(슬라이스 21 이 남긴 경고를 그대로
> 따랐다). 커밋별 조회가 맞는 측정이다.

## 판정 — 제거 17줄

제거선은 README 「③④ 는 *다음 편집자*에 대한 복원 경로다」 절의 **3분 표**다.

### `#12` `resource_generic_ac20.py` — 제거 7 · 유지 29

🔴 **이 행은 자기 유지 논거가 낡은 채 초록이었다.** 결과 칸이 네 문단을 통째로 유지하며 든 근거
셋 중 하나가

> 그 불변식을 읽어야 하는 사람은 이 파일을 고치려는 사람이고, 그가 닫힌 PR 본문을 먼저
> 뒤지리라 기대하는 것은 복원 경로가 아니라 희망이다

인데, README 「2026-09-20 확정」 절이 **정확히 이 문장을 뒤집었다**(「…는 제거를 막는 사유가
되지 못한다」). 그 행 자신이 「다음 패스가 다르게 판정할 수 있고, 그때는 새 패스 문서로 할 것」
이라 남겼으므로 여기서 문단별로 다시 물었다.

**번복은 그 한 절에만 적용했다.** 같은 결과 칸이 든 다른 두 절 — 「③ 은 *그때의 결정*을 적을 뿐
*앞으로의 금지*를 집행하지 않는다」와 「PR 본문이 스스로 「파일 docstring 에 적었다」고 가리키는
자리는 원본이 주석 쪽이다」 — 은 3분 표 둘째·셋째 줄 그 자체라 지금도 유효하고, 그래서 ¶1 과 ¶4
는 유지다. 낡은 것은 논거 하나지 그 행의 판정 전체가 아니다.

- **¶2(관측 순서가 곧 단언이다) −5** — #87 의 굵은 제목이 축자이고 「1을 3보다 먼저 두지 않으면
  5가 「캐시가 무효화됐다」가 아니라 「캐시를 처음 만들었다」를 재고」가 같은 명제다 ✅. 더해서
  **같은 원장 행 안에 완전한 ① 사본이 살아 있다** —
  `test_resource_generic_ac20_typo_is_refused_with_candidates` 의 케이스 docstring 이 `resolve`
  의 NoMatch 무효화와 「설치 뒤의 목록 조회가 「무효화됐다」를 재려면 그 전에 캐시가 차 있어야
  한다」까지 담아 **잃는 명제가 없다.** 소유자는 그 순서를 구속하는 편집이 일어나는 케이스 쪽
  이므로 모듈 머리말의 사본이 첫째 줄이다(슬라이스 17 이 구현+테스트 한 행에서 쓴 가름과 같은 형).
- **¶3 의 중간절 −2** — 「폴링만 두면 「서버가 재기동되어 다시 읽었다」와 구별되지 않는다」는 #87
  의 「6이 없으면 「재기동 불필요」는 discovery 전파 폴링과 구별되지 않는다」와 같고 ✅,
  `..._server_never_restarted` 의 케이스 docstring 이 같은 말을 진다. **줄 주석 슬라이스 3 이 이미
  이 명제를 「#87 과 겹치는 부분 후보」로 이름 붙여 두었다** — 그 예고를 여기서 집행했다.
- **유지 ¶1** — 「위치 자체가 그 사고의 방지선이다」는 ③ 축자이지만 **앞으로의 편집을 금지하는
  서 있는 불변식**이다(둘째 줄).
- **유지 ¶3 의 마지막 문장** — 파드 uid 만으로 부족한 **이유**(캐시가 프로세스 안에 있어 파드가
  살아 있어도 컨테이너가 죽으면 비워진다)를 #87 은 적지 않고 필드 목록만 든다. ①② 패스가
  `server_pod_identity` 본문을 지웠으므로 이 자리가 **유일 소유자**다.
- **유지 ¶4** — #87 이 「담지 못한 것 (파일 docstring 에 적었다)」로 이 자리를 가리킨다(셋째 줄).

⚠️ **제거 후에 남는 것이 참인지 다시 읽었다.** ¶3 은 앞절을 걷으면 「그래서」의 선행어가 사라진다
— 접속사만 다시 이어 붙였고 **새 명제는 0줄**이다.

### `#14` `resource_generic_ac3.py` — 제거 2 · 유지 21

🔴 **인용한 선례가 뒤집혔다.** 이 행은 「거부 하나만 봐서는 성립하지 않는다」 4줄을 유지하며
근거로 「원장 #11 이 `ac1.py` 의 같은 형태(「셀렉터를 통째로 무시하는 구현도 통과한다」)를
유지로 판정한 계열이다」를 들었는데, **슬라이스 21 이 바로 그 문단을 #11 에서 제거했다**(#86
본문이 반증 조건을 두 자리에서 담는다는 이유로). 같은 형태를 여기서 유지하면 한 표면 안에서
판정이 갈린다. R10 의 앵커는 자기 행 지문에만 묶여 이 방향의 낡음을 게이트가 원리적으로 보지
못하므로, 판정으로 닫는다.

- **반증 조건 문단 −2** — #100 「계획 단계」 ④ 가 「`limit=1000` 은 클램프가 아니라 거부
  (`McpError -32602`), 그리고 상한값 `limit=500` 은 정상 응답 — 거부 하나만 보면 인자를 아예 못
  받는 구현도 통과하므로 경계를 나란히 건다」로 **반증 조건까지 축자** ✅.
- 🔴 **유지 ⚠️ `remainingItemCount` 3줄 — 두 근거가 독립으로 선다.** 첫째, #100 「주의사항」이
  스스로 「`remaining` 단언을 나중에 더하려는 손이 여기서 멈추도록 **모듈 docstring 에 적어
  두었다**」고 적어 **원본이 주석 쪽**임을 지목한다(셋째 줄). 둘째, 줄 주석 표면 원장 `#30` 이
  픽스처 YAML 의 사본 4줄을 지우며 「실제로 그 단언을 쓰는 `resource_generic_ac3.py` 의 모듈
  docstring 이 진다」로 **이 자리를 ① 소유자로 명시 지목**했다 — 여기서 지우면 그 지식이 레포
  어디에도 남지 않는다. ③ 히트가 있어도 지울 수 없는 형이다.
- **유지** `McpError`(-32602) 로 오는 **이유**(k8s 에 닿기 전 인자 검증에서 잘린다 — #100 은
  `-32602` 라는 결과만 적는다) · 픽스처 머리말 포인터 · 케이스 docstring 넷.
- **③ 재판정 후보 1건** — 선행 조건을 검증 대상 자신으로 세우지 않는다는 2줄은 #100 계획 ① 이
  **결정**을 축자로 담으나 그 **근거**(두 실패가 구분되지 않는다)는 담지 않아 판단이 갈렸다.
  애매하면 남긴다.

### `#20` `resource_generic_ac12.py` — 제거 8 · 유지 19

이 행은 ①② 판정에서 **제거 0줄**이었다 — ③④ 를 한 번도 묻지 않았기 때문이고, 물으니 저작 PR
본문이 두 자리를 축자로 담는다.

- **¶1 −7** — #145 「이 e2e 가 재는 것」 머리 문단이 「Go 단위 여섯이 계약을 이미 단언하지만 전부
  **가짜 목록 판독기·가짜 게이트** 위에서 돈다. 이 파일은 그 자리의 실물을 잰다」로 축자이고 ✅,
  이어지는 세 「실물」 항목도 본문 1~4번이 그대로 든다. 괄호 안 여섯 계약의 열거는
  `delete_collection_test.go` 의 **테스트 이름으로 복원되는 ①** 이라 이중 복원이다.
- **¶5 의 앞부분 −2** — 「늦게 오는 3번째가 이미 남아 있으면 `kubectl apply` 는 아무것도 바꾸지
  않아 선택이 늘지 않는다 — 거부를 기대한 자리가 조용히 성공으로 통과한다」가 본문 축자다 ✅.
  **같은 파일의 줄 주석 행 `#51` 이 이미 이 명제를 #145 축자로 인정한 자리**다. 거기서는 주석이
  지는 「PENDING 관측 **뒤에** 만든다」는 *순서* 제약이 다른 주장이라 유지됐는데, 모듈 머리말 쪽
  사본은 그 순서 제약을 담지 않아 남길 것이 없다.
- **유지** ¶2(「다른 레이블 2개는 남는다」가 실 apiserver 층에서만 관측된다 — 겹쳐 보이는 두
  테스트 중 어느 쪽을 고쳐야 하는지를 가르는 서 있는 불변식이라 ③ 축자여도 둘째 줄) · ¶3
  (ConfigMap 을 고른 이유) · ¶4(세 그룹을 나눈 **이유** — 본문은 이름과 수만 든다) · ¶5 의 마지막
  문장(「청소가 아니라 판별식을 세우는 장치다」) · ¶6(`병렬 레인:` 미선언이 의도라는 선언) ·
  `_group` 의 한 줄 doc.

⚠️ ¶1 을 걷으면 ¶2 의 「이 층」이 선행어를 잃는다 — **「실 apiserver 층」으로 한 낱말만** 다시
못박았고 새 명제는 0줄이다.

## 런타임 무영향

docstring 편집은 AST 안에서 일어나므로 줄 주석 슬라이스가 쓰던 `ast.dump` 동등은 이 표면에서
성립하지 않는다. 슬라이스 21 이 세운 대로 **docstring 노드를 걷어낸 뒤의 AST 동등**을 썼다 — 세
파일 모두 편집 전후 동일. 기계가 읽는 선언(`검증 시나리오:`·`실행 대상:`)은 세 파일 모두 축자로
살아 있다. `internal/`·`main.go`·`scripts/`·`tests/k8s/`·다른 `tests/integration/` 0바이트.

**자매 축 무영향** — `check_mock_policy` `084bfbeb1c98` · `check_ac_mapping` `6aa2fb1e795a` 두
출력 md5 가 base 와 동일. `check_doc_inventory.py` 는 이 패스 문서 1건만큼 움직인다.

**게이트를 양방향으로 굴렸다** — 고친 트리 rc=0, 원장만 되돌린 트리 rc=1 로 R6 셋(세 행의 줄 수·
지문)과 R8(합계+잔량)이 정확히 걸린다.

## 결론

| | |
| --- | --- |
| 물은 행 | **3**(`#12` · `#14` · `#20`, 86줄) |
| 제거 | **17줄** (제거율 20% — Go/Python 표본 범위 안) |
| docstring 표면 | 13/32 → **16/32 · 잔량 16** |
| 줄 주석 표면 | **무이동**(67/67 · 잔량 0 · 3,338) |
| 갈려서 남긴 것 | 갈림 **+0** · ③ 재판정 후보 **+1** · ① 재판정 후보 **+0** |

**관찰 — 「판정 근거의 낡음」에는 방향이 둘 있고, 한쪽만 재면 놓친다.** 슬라이스 21 이 연 것은
「**내 판정을** 남이 선례로 인용한다」(나가는 방향)였고, 그래서 자기가 물 칸을 고를 때 피인용을
쟀다. 이 패스에서 실제로 걸린 것은 그 반대다 — `#14` 는 **자기가 인용한 선례가 인용 대상 쪽에서
뒤집혔고**, `#12` 는 **자기가 인용한 정책 문면이 개정됐다.** 세 낡음 모두 R9·R10 이 원리적으로
보지 못한다(앵커는 자기 행 지문에만 묶이고, 정책 문면은 아예 앵커의 대상이 아니다). 행을 집을
때 피인용뿐 아니라 **그 행의 결과 칸이 인용하는 것들 — 자매 행의 판정과 README 의 절 — 이 지금도
참인지**를 함께 물어야 한다.

## 다음 패스의 입력

- **다음 칸 제안**: `#15` `resource_generic_ac5.py`(70줄) · `#19` `resource_generic_ac11.py`
  (41줄) · `#23` `resource_generic_ac15.py`(39줄) — 셋 다 파일 하나짜리다. ⚠️ 이 제안은 **양방향
  으로 재지 않았다** — 고르기 전에 피인용과 **결과 칸이 인용하는 것**을 둘 다 훑을 것.
- **얽힌 칸 둘은 여전히 함께 물 것**: `#3` `smoke.py`(14줄)는 `#4`·`#9` 가 선례로 인용하므로 세
  행을 한 PR 에서 재판정해야 하고, 그 `#9` `resource_read_smoke.py` 는 슬라이스 17 의 ① 소유자
  의존도 함께 진다.
- ③ 재판정 후보 누적: **45**(이 패스 +1).
- 갈림 누적: **4** 그대로. ① 재판정 후보: 승계 **6** 그대로.
- 정책 소유자 몫(승계 7, 신규 **1** → 8): **한 행의 유지 근거가 인용한 정책 문면이 개정됐을 때,
  그 행을 소급 재판정하는 의무를 게이트가 질 수 있는가.** R9·R10 의 앵커는 행 지문에만 묶여
  README 개정을 보지 못한다 — 이 패스는 손으로 찾아 번복했으나 다음에도 누가 그럴 보장이 없다.
