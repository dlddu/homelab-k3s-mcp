# 2026-09-19 — 복원 경로 ③④ 재판정 (트리거 창 `05ef097` → `cba91ac`)

`rct_20260919-0001` / `tbm_homelab-k3s-mcp-comment-redundancy`.

이 패스는 **주석을 지우지 않는다.** 이미 등재된 60줄에 대해 [`README.md`](../README.md) 판정 절차
3번이 **묻지 않은 두 경로**를 마저 묻고, 그 답과 **갈린 자리**를 남긴다(절차 5번이 요구하는 기록).

## 왜 다시 여나

트리거 창 `05ef097`(baseline) → `00c6a11`(#138) → `cba91ac`(#137) 이 판정 대상 주석 **+60줄**을
들여왔고, 세 원장 행이 모두 **같은 PR 에서 재판정**해 등재했다(행 40 `17→23` · 행 56 `52→94` ·
행 70 신설 12). 합계 산술도 정합이다 — `2236 + 6 + 42 + 12 = 2296`.

정합하지 않은 것은 **판정 자체의 범위**다. 세 행의 판정 산문을 읽으면 검토한 복원 경로가
①(코드 자체)과 일부 ②(저장소 문서)뿐이다 — 「코드가 스스로 말할 수 없다」 · 「순서는 자기 이유를
말하지 못한다」 · 「코드에는 그 사실이 없다」 · 「테스트 이름으로는 대체되지 않는다」. **③(PR 본문)·
④(커밋 메시지)를 검토한 문장은 이 창의 어느 행에도 없다.** 그런데 README 는 「이 중 **어디서든**
확인되면 주석의 자리가 아니다」라고 적는다.

게이트는 이 축을 원리적으로 보지 못한다. 부모 `05ef097` 와 tip `cba91ac` 두 지점 모두
`check_comment_policy.py` rc=0 이고, README 스스로 「CI 게이트는 「이 주석이 중복인가」를 판정하지
않는다」라고 적는다. 즉 **다시 열지 않으면 영구 초록인 자리**다.

## 측정 — ③④ 의 실체

| | 실측 |
| --- | --- |
| ③ PR #137 본문 | 73줄 / 4157자. 「무엇을 고치나」 · 「왜 아무도 못 봤나」 · **「퇴행 여지 — 갈래 순서가 계약이다」(전용 절 + 5행 표)** · 「완료 기준」 · 「범위 밖」 · 「롤백」 |
| ③ PR #138 본문 | 73줄 / 3394자. 「무엇을 · 왜」(gap 표) · 「무엇을 바꿨나」(파일별 표) · **「왜 상시가 아니라 모드 토글인가」** · 「검증」 · 「인계」 |
| ④ `cba91ac` (#137) | **본문 비공백 17줄** — 산문 16줄 + `Refs:` 트레일러 1줄 |
| ④ `00c6a11` (#138) | 본문 비공백 1줄(`Co-authored-by`). 제목 외 산문 없음 |

### ④ 전제의 정정

원장 docstring 행 #12(`tests/integration/resource_generic_ac20.py`)과 그 상세
[`2026-09-14-resource-generic-scenario20.md`](2026-09-14-resource-generic-scenario20.md) 이 적어 둔 전제:

> ⚠️ **④(커밋 메시지)는 이 레포에서 사실상 비어 있다** — squash 머지라 본문이 제목 한 줄 +
> `Co-authored-by` 뿐이고(`44ae81c`·`e4304b3`·`bd127aa` 확인), 이 패스는 ④ 를 근거로 아무것도 지우지 않았다.

재측정: 인용된 세 커밋은 **지금도 본문 비공백 1줄**이다. 최근 30커밋 중 본문 비공백 ≥5줄은
**3건**(`cba91ac` 17 · `fb2eeb1` 5 · `32be7a5` 5)뿐이다. **전제가 쓰인 시점에 참이었던 것은 맞고,
이 창이 그것을 깼다** — `cba91ac` 가 17줄짜리 첫 대형 사례다.

그래서 이 패스가 고쳐 두는 것은 하나다: **④ 는 「비어 있다」고 일반화하지 말고 커밋별로 재고
판정할 것.** 2026-09-14 패스 문서 본문은 그 문서가 스스로 「뒤집는다면 새 패스 문서로 하고 이
문서는 그대로 둘 것」이라 적었으므로 **한 글자도 고치지 않았다**(정정은 여기와 원장 행 주석에 있다).

## 항목별 판정

`③`·`④` 칸은 **그 경로에서 같은 내용이 확인되는가**다. `최종` 칸은 절차 3~5 를 적용한 결과.

### 원장 행 40 — `tests/k8s/kind/github-mock.yaml` (+6줄, #138)

| 항목 | ③ (#138 본문) | ④ (`00c6a11`) | 최종 |
| --- | --- | --- | --- |
| ⑴ `DEFAULT_CONFIG` 4줄 — 세 노브의 용도와 **「기본값이 전부 꺼짐」** 계약 | ✅ 축자 — 「**노브 기본값은 전부 꺼짐**이라 `ac1`·`ac2`·`ac4` 가 보는 스톡 응답은 한 바이트도 안 바뀐다」 + 전용 절 「왜 상시가 아니라 모드 토글인가」 | ❌ (본문 없음) | **유지 — 갈림** |
| ⑵ `_admin/config` 2줄 — 「본문이 설정 전체를 싣는다 ⇒ 빠진 키는 기본값으로 돌아가고 `{}` 가 곧 초기화」 | ❌ 엔드포인트 이름만 나열(`POST /_admin/config`). 계약은 없음 | ❌ | **유지 — 갈림 없음** |

### 원장 행 56 — `internal/k8s/exec.go` · `exec_test.go` (+42줄, #137)

| 항목 | ③ (#137 본문) | ④ (`cba91ac`) | 최종 |
| --- | --- | --- | --- |
| ⑴-a 「스트림은 비영 종료만 `CodeExitError` 로 말하고 0 에는 아무 말 없다」 | ✅ 「종료 코드를 `StreamWithContext` 가 돌려준 에러에서**만** 뽑는데 … `nil` 이라 `exitCode` 가 세팅되지 않는다」 | ✅ 같은 문장 | **유지 — 갈림** |
| ⑴-b 「바이트 상한의 취소는 `DeadlineExceeded` 가 아니라 `Canceled`」 | ✅ + AC12 인용문 | ✅ 같은 문장 | **유지 — 갈림** |
| ⑴-c 「SPDY 실행기는 단위 테스트가 닿지 못해 이 결정이 관측되지 않은 채 남았다」 | ✅ 「분류가 `ExecResource` 안에 묻혀 있고 그 함수는 SPDY 실행기를 요구해 단위 테스트가 닿지 못한다」 | ✅ 같은 문장 | **유지 — 갈림** |
| ⑵ 갈래 순서 4줄 — 「오늘 올바르게 답하는 갈래는 자리를 지키고 새 둘은 지금 틀리게 답하는 경우만 가로챈다」 | ✅ **전용 절 + 5행 표**로 가장 강하게 겹친다 | ✅ 같은 문장 | **유지 — 갈림(가장 첨예)** |
| ⑶ 호출부 2줄 — 「두 writer 가 멈춘 뒤라 플래그 읽기에 경합이 없다」 | ❌ (`경합`·`race` 0 히트. `writer` 1히트는 `limitedWriter` 식별자) | ❌ | **유지 — 갈림 없음** |
| ⑷-a 「가짜 서비스에 `StdoutTruncated:true` 를 심는 기존 mcp 계층 단언은 이 분류를 한 번도 돌리지 않는다」 | ✅ **축자로 존재** | ❌ | **유지 — 갈림** |
| ⑷-b 「Observed live before it was asserted here」 | ✅ 「`rct_20260918-0003` 시도 2 가 전용 e2e 를 CI 에서 처음 실물로 돌렸을 때 관측됐다(run 35368945500)」 | ✅ 「(rct_20260918-0003 시도 2, run 35368945500 실측)」 | **유지 — 갈림** |
| ⑷-c 「`!capped` 를 거는 이유 — 그 경로는 이전과 같다」 | ✅ 「성공 갈래에 **`!capped` 를 걸어** 그 경로를 이전과 바이트 단위로 같게 뒀다」 | ✅ 같은 문장 | **유지 — 갈림** |
| ⑷-d 시간 상한 갈래·`default` 갈래가 **왜 그 답이어야 하는가**(「상한 플래그가 없으면 호출자가 중간에 떠난 것과 구별되지 않는다」 등) | ❌ 표는 「불변」만 적고 이유는 적지 않는다 | ❌ | **유지 — 갈림 없음** |

⑷-b 는 **직전 판정이 「② 에 가깝지만 출처 표시라 「애매하면 남긴다」로 보존」이라 적어 둔 경계
사례**다. ③④ 양쪽에서 run id 까지 확인되므로 절차 3번만 보면 제거 쪽이 강해졌다 — 그럼에도
아래 ⓑ 가 미확정인 동안은 5번이 이긴다.

### 원장 행 70 — `tests/integration/github_app_installation_token_ac5.py` (12줄, #138)

| 항목 | ③ (#138 본문) | ④ (`00c6a11`) | 최종 |
| --- | --- | --- | --- |
| ⑴ `EXPECTED_INSTALLATION_ID` 2줄 — 값이 CI 「Create test GitHub App secret」 스텝과 한 자리에서 맞아야 한다 | ❌ (`67890`·`시크릿` 0 히트) | ❌ | **유지 — 갈림 없음** |
| ⑵ `MOCK_URL` 5줄 — 포트 8093 ↔ 포트포워드 | ✅ 「primary 스텝에 `svc/github-mock` 포트포워드(8093)」 | ❌ | **유지 — 갈림(부분)** |
| ⑵′ 같은 5줄의 **「왜 `_helpers` 가 아니라 여기인지」** | ❌ (`_helpers` 0 히트) | ❌ | **유지 — 갈림 없음** |
| ⑶ 요청 기록 2줄 — 「AC5 의 주장 대부분이 *일어나면 안 되는 요청*이라 반환값이 아니라 요청 로그가 판정 근거」 | ❌ 엔드포인트 나열·확인 항목 수만 있고 이 전제는 없다 | ❌ | **유지 — 갈림 없음** |
| ⑷ `finally` 3줄 — 레인 의미론 | ✅ 「네 파일 모두 `병렬 레인: github-app` … 새 파일은 `try/finally` 로 노브를 반드시 되돌린다」 | ❌ | **유지 — 갈림** |

## 결론 — 이 패스의 제거는 **0줄**이다

60줄 중 **8항목이 ③ 또는 ④ 에서 확인**되고 6항목은 어디에도 없다. 그럼에도 제거가 0인 이유는 셋이다.

1. **절차 5번.** 확인된 8항목은 전부 「③④ 가 *이 파일을 앞으로 고치는 사람*에 대한 복원 경로인가」에서
   판단이 갈린다. 5번은 갈리면 **남기고 갈렸다는 사실을 남기라**고 한다 — 이 문서가 그 기록이다.
2. **이미 그어진 경계.** 2026-09-14 패스가 「③ 이 담는 것은 「왜 이 PR 이 이렇게 바꿨는가」이고
   「앞으로 이렇게 고치지 마라」를 집행하지는 않는다 … 닫힌 PR 본문을 먼저 뒤지리라 기대하는 것은
   복원 경로가 아니라 희망이다」로 그었고, 이번 창의 겹침 대부분(특히 행 56 ⑵ 갈래 순서)이 정확히
   그 성질이다. 그 경계를 **뒤집는 것은 README 개정**이지 한 패스의 재량이 아니다.
3. **비대칭 비용.** README 「애매하면 남긴다」 절 그대로 — 남긴 비용은 몇 줄, 잘못 지운 비용은 복구 불가.

따라서 이 패스가 바꾸는 것은 **판정 기록뿐이고 주석·코드는 0줄**이다. 그래서 원장의 줄 수·지문·
합계 마커도 움직이지 않는다(합계 2296 · 잔량 0 · 전체 지문 `06c86a58…` 불변).

## 올리는 질문 — 정책 소유자에게

> **③(PR 본문)·④(커밋 메시지)는 「그 파일을 고치려는 다음 사람」에 대한 복원 경로인가?**

README 는 「③·④는 `git log`·`gh pr view` 로 항상 도달할 수 있다」고 적고, 2026-09-14 패스는 「도달
가능성과 *집행*은 다르다」고 적는다. 둘 다 살아 있어 이 창의 8항목이 매번 5번(갈림)으로 떨어진다.
결론이 어느 쪽이든 **README 의 「복원 경로는 넷이다」 표가 그 구분을 문면에 담아야** 이 자리가
매 창마다 다시 갈리지 않는다.

- 「집행이 아니다」가 답이면 → ③④ 에 **단서**를 달고(예: 「앞으로의 편집을 구속하는 불변식은 ③④ 로
  복원되지 않는다」), 이 창의 8항목은 근거가 정리된 채 유지된다.
- 「그래도 복원 경로다」가 답이면 → 이 창의 8항목 중 최소 ⑵ 갈래 순서 4줄 · ⑴ 15줄 · ⑷-b 는
  **제거 대상**이 되고, 별도 슬라이스가 원장 행 56 의 줄 수·지문과 합계 마커를 함께 옮긴다.

이 문서는 어느 쪽도 고르지 않는다. **README 는 정합성 모델의 to-be 정의 표면**이라
`reconciler-tobe-modeler`·사람의 자리다.

## 다음 패스의 입력

- 위 질문의 답과, 그 답이 정해진 뒤의 재판정(제거가 열리면 행 56 중심).
- **④ 는 이제 커밋별로 재야 한다.** 「squash 라 비어 있다」는 일반화는 이 창에서 깨졌다.
- 이번 창에서 확인된 **③ 부분 겹침**(행 70 ⑵ 의 포트 8093 은 PR 에 있고 「왜 `_helpers` 가 아닌가」는
  없다)처럼, 한 주석 블록 안에서 경로가 갈리는 사례는 **블록 단위가 아니라 문장 단위로** 재야 한다.
