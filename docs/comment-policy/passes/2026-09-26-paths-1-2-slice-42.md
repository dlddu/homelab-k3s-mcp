# 복원 경로 ①② 판정 — 슬라이스 42: `internal/eventlog` 축 (원장 `#61`)

- **task**: `rct_20260926-0007` (`tbm_homelab-k3s-mcp-comment-redundancy`)
- **기준 커밋**: `4015d33` (슬라이스 41 / PR #230 착지 직후)
- **대상**: 줄 주석 원장 행 `#61`(`internal/eventlog/eventlog.go` · `eventlog_test.go` ·
  `internal/mcp/eventlog_test.go` · `internal/server/eventlog_test.go` 127줄)
- **결과**: **제거 3줄 · 추가 0줄** (127 → 124). `판정-합계` 3380 → **3377** ·
  복원 경로 ①② 판정 **11/69행 → 12/69행**(잔량 58 → **57**). `등재 범위` 69 · `판정-잔량` 0 ·
  ③④ 69/69(잔량 0, 재앵커) · docstring 표면 전건 불변(①② 4/33 · 잔량 29).
- **이 패스가 닫는 것**: 감지가 이름으로 넘긴 후속 넷 중 **유일하게 살아 있던 행**.

## 인계 재실측 — 감지가 넘긴 후속 넷 중 셋은 이미 죽어 있었다

감지(`rct_20260926-0007`)는 ⑸ⓒ 에서 후속 넷을 이름으로 넘겼다. 지목을 그대로 받지 않고
**한 단계 아래 단위로 다시 쟀다**(게이트 파서 `parse_ledger()` 를 `importlib` 로 빌려 두 표면
전수 102행의 결과 칸을 정확 문구로 grep).

| 인계 항목 | 실측 | 처분 |
| --- | --- | --- |
| `errKind` **여섯** 행(`#2`·`#3`·`#6`·`#8`·`#9`·`#10`) 동시 재판정 | 여섯 행 **전부 ①② 판정 완료**(판정 집합 = `{2,3,6,8,9,10,20,21,23,68,69}`) | **소진** — 남은 의무 0 |
| 행 `#24` `internal/gatekeeper/gatekeeper.go:123–125` | 가족 밖 사본 · 교차 패키지 절 · 2026-09-13 ③④ 가 「반환값 계약 → 유지」로 **서 있다** | **보류**(아래 「범위 밖」) |
| `client.go` `28–29`(정책 미비로 미발화) | `client.go` 는 행 **`#6`** 소속이고 그 행은 슬라이스 41 이 **이미 ①② 판정**했다 | **소진** |
| 행 `#61` `internal/eventlog/*` 절 단위 | 미판정 · 살아 있음 | **이 슬라이스** |

> 🔴 **「①② 재판정 후보」 지목 채널도 함께 소진돼 있다.** 두 표면 전수에서 그 정확 문구는
> `#20`·`#23` 둘뿐이고 **둘 다 이미 판정**됐다(부분 문자열 `후보`·`갈려서` 로 세면 40행 넘게
> 걸리는 거짓 양성 — 전체 문구로 셀 것). 그래서 이 슬라이스의 선택 근거는 지목이 아니라
> **감지가 남긴 유일한 생존 행**이다.

## 사본 집합이 닫히는가 — 닫는다(단독)

행을 고르기 전에 「모든 후보 줄의 사본 수 분포」를 쟀다(판정 표면 전수를
`norm(line) → {행 서수}` 로 모으고, `path` 분리는 `partition(":")`).

- 행 `#61` 의 주석 줄 중 **다른 원장 행과 바이트 동일한 줄은 0개**다(길이 3 이하의 `//`·`#`
  구분자 제외). ⇒ 단독으로 집어도 **홀로 남는 사본이 0** 이고 「같은 문장이 한 표면 안에서
  갈린다」가 성립하지 않는다.
- **자매 표면과의 겹침 0**: 이 행의 파일 넷은 전부 `.go` 이고 docstring 원장 33행은 전부
  `.py` 다 — 「자매 표면 행이 같은 파일을 덮으면 한 슬라이스로」가 적용될 자리가 없다.

## 🔴 이 행은 「② 를 이미 실질 분석한 유지형」이다 — 지목이 곧 제거 대상이 아니다

감지는 `98–99`·`115` 를 「실재 위반」으로 축자 특정했다. 그 행의 결과 칸을 **끝까지 읽고**
판별식(「결과 칸이 ② 축 논거를 문면째 적었는가」)을 돌리면 이 행은 **유형 ⑵** 다 —
2026-09-21 칸이 `Reason` doc 을 이렇게 닫아 두었다:

> `Reason` doc 4줄 — … **AC2 문면의 재진술에 가까우나 *왜 이 타입이 저쪽 AC 를 인용하는가*를
> 이 자리에 못박는 절이라 유지**

즉 새 위반이 아니라 **절 단위 부분 후보**이고, 필요한 것은 번복이 아니라 **좁힘**이다.
그리고 지목 둘 중 **`115` 는 이 물음에서 유지로 뒤집혔다**(아래).

## 제거 3줄 — `eventlog.go` `99–101`

```go
 // Reason is why a refused call was refused (AC2, AC4). The eight values are
-// prd-metrics AC2's, verbatim: that AC is the single source of the set, and
-// the two layers have to split refusals the same way or a refusal rate seen
-// in the metrics cannot be followed into the records.
 type Reason string
```

- **복원경로 ①(코드)**: 「여덟 값」이라는 수와 그 집합·순서는 바로 아래 `const` 블록이
  이름으로 전부 열거하고(`ReasonAuthFailed` … `ReasonGateUnconfigured`) `Reasons` 가 순서까지
  코드로 고정한다 ✅.
- **복원경로 ②(저장소 문서)**: 제거한 세 줄의 명제는 둘이다.
  ⑴ **「그 AC 가 값 집합의 단일 출처」** → `docs/prd-metrics.md` AC2 가
  「사유 값 집합의 **단일 출처는 이 AC다**」로 **자기를 지목**한다 ✅
  ⑵ **「두 층이 같게 쪼개지 않으면 메트릭의 거부율을 레코드로 파고들 수 없다」** →
  `docs/prd-event-log.md` AC2 가 「두 층이 다르게 쪼개면 **「메트릭에서 본 거부율을 기록으로
  파고든다」는 동선이 끊긴다**」로 **같은 문장**을 적는다 ✅
- 🔴 **두 ② 원본은 판정보다 앞선다.** `git log -S` 로 두 문면을 각각 재면 도입 커밋이 모두
  **`f5ca0bd`(#162, 2026-09-20)** 이고, 이 행의 ③④ 판정은 **2026-09-21** 이다. 정책 개정으로
  논거가 낡은 형(→ 「판정 근거도 낡는다」)도, 실현된 예고도 아니고 **그 축을 아직 묻지 않았던
  형**이다. 원인을 「낡았다」로 적으면 정책 개정을 찾으러 헛돈다.
- **남기는 것**: `// Reason is why a refused call was refused (AC2, AC4).` — exported 식별자의
  **1줄 doc + AC 포인터**. 포인터 괄호는 재진술이 아니라 **소유자 지목**이라 유지 대상이다.

### ⚠️ 번복이 아니라 좁힘이다

2026-09-21 의 유지는 **③④ 축** 판정이었고(「PR 본문이 대신 말하는가」), 이 패스는 **①② 축**
(「코드·저장소 문서가 대신 말하는가」)을 처음 묻는다. 그 판정의 결론 — ③ 히트 0 · ③④ 유지 —
은 **그대로 선다**. 다만 그 칸이 유지 근거로 든 「왜 이 타입이 저쪽 AC 를 인용하는가」 절
자체가 ② 원본 **두 곳에 문면으로** 있으므로, ①② 축에서는 그 절이 되풀이다.

## 유지 — 그리고 감지 지목 `115` 가 뒤집힌 이유

| 자리 | 판정 | 근거 |
| --- | --- | --- |
| `Results` `93–95` | **유지** | 「둘째 목록을 만들지 말라 / 위 상수와 어긋날 수 없다」는 **서 있는 불변식**(무엇을 넣지 말라). 어느 ② 원본도 이 금지를 적지 않는다 |
| `Reasons` `115–117` | **유지 — 지목에서 뒤집힘** | 감지가 「실재 위반」으로 특정했으나, 내용은 「메트릭이 **둘째 목록을 만들지 않고** 여기서 계열을 선등록한다」는 불변식 + 다음 슬라이스를 위한 자리 지목이다. PRD 는 「0 으로 노출된다」(AC1)를 적을 뿐 **「목록을 두 벌 두지 말라」를 적지 않는다** ⇒ ② 미복원 |
| `Duration`·`GateWait` `146–152` | **유지** | 「로그 줄에 렌더하지 않는다」는 필드 계약과 그 이유. `Attrs` 코드는 「없다」만 보이고 「왜 없나」는 없다(① 미복원) |
| 패키지 doc · `Principal` · `Target` · `Record` · `Sink` · `Fanout` · `Gate` | **유지** | 구조 판단(왜 별 패키지인가 · 왜 빈 키도 내는가 · 왜 두 필드인가). 의존 그래프가 *결과*는 복원해도 *왜 그렇게 그렸는가*는 복원하지 않는다 |
| 테스트 케이스 머리말 전량(`mcp`·`server`·`eventlog` 3파일 46줄) | **유지** | 「이 케이스가 무엇을 재는가 / 반증 조건」이고 ② 원본에 옮겨 적힌 적이 없다 |

## 무접촉 증명

- **비주석 diff 0줄**: `git diff -U0` 의 `+`/`-` 중 `//` 로 시작하지 않는 줄 **0** (파일이 `.go`
  하나뿐이라 docstring 함정은 없다 — AST 대조가 필요한 혼합 슬라이스가 아니다).
- 게이트 4종 **rc=0**: `check_comment_policy.py` · `check_mock_policy.py` ·
  `check_doc_inventory.py` · `tests/integration/check_ac_mapping.py`.
- `gofmt -l .` 공백 · `go vet ./...` · `go build ./...` 통과.

## 범위 밖(후속)

- 🔴 **행 `#24` `internal/gatekeeper/gatekeeper.go:123–125` 는 이 논거로 걷지 말 것.**
  이 패스의 근거는 *`prd-metrics` AC2 와 `prd-event-log` AC2 가 서로를 단일 출처로 지목한다*는
  **그 두 문서의 문면**이다. `#24` 에는 그 문면이 없고, 둘째 절 「which the caller turns into a
  refusing gate rather than a fatal error」는 **교차 패키지 진술**이라 그 파일 코드로 복원되지
  않으며, 2026-09-13 ③④ 판정이 그 자리를 이름을 들어 「반환값 계약 → 유지」로 닫아 **서 있다**.
  「애매하면 남긴다」 ⇒ 보류.
- 남은 ①② 잔량: 줄 주석 **57행** · docstring **29행**(합 86). 지목 채널은 소진됐으므로 다음
  슬라이스는 **사본 집합이 닫히는 조합**으로 고를 것. 실측해 둔 닫히는 후보:
  `(43,49)` attach·port_forward 226줄 · `(60,63)` kind 픽스처 79줄 · `(56,58)` 53줄
  (자매 docstring `#24`·`#26` 이 같은 파일을 덮으므로 **묶어서** 집을 것) · `(11)` 36줄.
  ⚠️ `(22,46)`·`(57,59)` 는 **닫히지 않는다** — `# Must match GITHUB_APP_INSTALLATION_ID …` 가
  `{22,46,59}` 에, 포트포워드 짝 2줄이 `{57,59}` 에 걸쳐 있어 넷을 함께 집어야 닫힌다(65줄).
