# 주석 비중복성 판정 원장

정책 본문은 [`README.md`](README.md)에 있다 — 무엇을 유지하고 무엇을 지우는지, 한 주석을
어떻게 판정하는지는 그쪽이 정한다. 이 문서는 **그 판정이 실제로 적용된 범위**만 담는다.
`scripts/check_comment_policy.py`가 파싱하는 마커는 전부 이 파일에 있다.

## 판정 이력

정책이 실제로 적용된 범위를 여기에 누적한다. 등재되지 않은 범위는 **아직 판정받은 적이
없다**는 뜻이다.

각 행은 범위와 함께 **그 범위 주석의 줄 수와 지문**을 담는다(지문 = as-is와 같은 방식으로
추출·정규화·정렬한 `경로:주석` 목록 sha256의 앞 12자리). `scripts/check_comment_policy.py`가
매 CI에서 재측정해 대조하므로 **등재된 범위의 주석을 바꾼 PR은 같은 PR에서 다시 판정하고 그
행을 갱신해야 한다.** 재판정은 `README.md`의 판정 절차를 따른다. 범위를 파일 단위로 적는
것도 그래서다 — 한때 이 표는 범위를 `internal/sessionplatform/`처럼 **패키지 이름으로**
적었고, 그 뒤 그 패키지에 들어온 주석 96줄이
「판정 완료」로 위장된 채 남았다. 등재는 그 범위를 **그때 그 내용으로** 판정했다는 뜻이지,
그 이름 아래 앞으로 올 것까지 판정했다는 뜻이 아니다.

<!-- 판정-원장 -->

| 판정일 | 범위 | 주석 줄 | 지문 | 결과 |
| --- | --- | ---: | --- | --- |
| 2026-09-04 | `internal/opensearch/opensearch.go` · `internal/opensearch/opensearch_test.go` | 52 | `d9c561b69bcc` | 제거 7줄 — ① 선언 재진술 ② `docs/prd-opensearch-search.md` AC2 재진술 ③ 패키지 주석과의 자기 중복. 판정 근거는 `rct_20260904-0001` |
| 2026-09-04 | `internal/sessionplatform/sessionplatform.go` · `internal/sessionplatform/sessionplatform_test.go` | 172 | `3bdaa24398db` | 1차 판정(`rct_20260904-0001`, 제거 1줄) 뒤 `session_read`·`session_write` 구현이 들여온 **96줄을 재판정**(`rct_20260904-0004`). 제거 18줄은 전부 ③ 자기 파일 중복 — `ReadSession`·`WriteSession`의 doc 본문이 같은 파일의 `errKind` 상수 doc과 `WriteResult` doc을 되풀이했다. 제거 블록에서 유일하게 복원되지 않던 한 절은 `kindTooLarge` doc으로 접었다(+1줄). 테스트 doc 주석 51줄은 「이 어서션이 왜 이 형태인가」라 전부 유지 |
| 2026-09-04 | `internal/auth/auth.go` · `internal/auth/auth_test.go` | 41 | `347cb1098477` | 제거 37줄 — ② `README.md` 「Authentication」 절이 그대로 담은 서술(`FromEnv` doc 13줄: 두 자격증명 경로·env 이름·「둘 다 없으면 기동 거부」, `OAuthConfigured`·`APIKeyCount`·`RequireBearer` 의 본문)과 ② `docs/prd-platform-auth-safety.md`·`docs/test-platform-auth-safety.md` 재진술(테스트 상단 AC 목록 10줄 — AC8 을 「OAuth 선택화」라 적어 PRD 의 「인증 방식 구성 유연성」과 이미 갈려 있었다, 시나리오 7 doc 3줄). `matchAPIKey` 의 「early return 을 두지 않는 이유」와 `verify` 의 「API 키 전용 모드에서 nil JWKS 를 건드리기 전에 거부한다」는 복원 경로가 없어 유지. 판정 근거는 `rct_20260904-0006` |
| 2026-09-04 | `internal/server/server.go` · `internal/server/mcp_test.go` · `internal/server/auth_routing_test.go` · `internal/server/health_test.go` · `internal/server/fakes_test.go` | 89 | `bfd4d0b0d64d` | **재판정 2026-09-15**(`rct_20260915-0006` — `resource_watch` 등록): **+9줄**(`mcp_test.go` 단독), 제거 0. 판정: **유지**. 셋 다 「이 어서션이 왜 이 형태인가」다 — ⑴ `TestToolsListIncludesAllTools` 의 총수를 `len(want)` 로 바꾼 이유(손으로 적은 총수는 도구가 늘 때마다 **모양 규칙으로 풀리지 않는 산술 충돌**이 되고, 그 사실은 `len(want)` 라는 코드에서 복원되지 않는다). ⑵ `watchSeconds` 의 상한을 스키마에 대고 단언하는 이유(강제만 하고 광고하지 않으면 클라이언트는 **거부당해야** 경계를 안다). ⑶ `TestResourceWatchReturnsEventsWithoutDumpingObjects` 의 **부정 절이 본체인 이유**(객체를 텍스트에 통째로 붙이는 렌더러도 「이벤트가 하나 왔다」류 긍정 단언은 전부 통과한다 — 이 파일의 Table 테스트가 같은 자리에 같은 이유를 이미 갖고 있다). `fakes_test.go` 는 watch 대역이 들어왔지만 주석은 여전히 0줄이다. 이하 직전 판정 그대로 — **재판정 2026-09-15**(`rct_20260915-0002` — `workload_scale` 구현 제거): **+7줄**(`mcp_test.go` 단독), 제거 0 — 지운 네 케이스(`TestWorkloadScale*`)에는 주석이 한 줄도 없었다. 판정: **유지**. 7줄은 `TestToolsListAdvertisesUpdateAnnotations` 의 doc 이고 **이 케이스가 왜 존재하는가**를 진다 — 「`workload_scale` 이 들고 있던 *멱등한 파괴적* 자리를 `resource_update` 가 물려받는다. 옆의 `resource_patch` 는 파괴적이지만 **멱등하지 않아서**, 이 케이스가 없으면 그 짝이 동시에 참일 수 있다는 것을 아무 도구도 단언하지 않고 `idempotentHint` 를 조용히 떨어뜨린 후계자가 그대로 통과한다」. 코드는 세 값을 대조한다는 사실만 보여 주고 **왜 하필 이 도구인가**는 보여 주지 않는다 — #91 이 `workload_restart` 의 파괴적 자리를 `resource_patch` 로 옮기며 세운 선례를 이 슬라이스가 그대로 따른 것이고, 그 판단의 근거는 여기에만 남는다. 뒷절(사라진 kind enum 반절에는 후계자가 없다 — 이 도구는 임의 좌표를 받는 것이 곧 더 넓은 대체재인 이유다)도 코드에서 복원되지 않는다. 이하 직전 판정 그대로 — **재판정 2026-09-13**(`rct_20260913-0002` — `resource_*` 읽기 축 슬라이스): +11줄. 폐기 4종의 케이스 22개가 빠지고 `resource_*` 케이스 16개가 들어오면서 `mcp_test.go` 의 주석이 통째로 갈렸다. **초안은 7개 주석이 「AC번호: 그 AC의 문언」 형태였고, 그중 5개를 판정으로 지웠다** — ② `docs/prd-resource-generic.md` 재진술이다(「목록은 표로」·「`limit` 기본 100 상한 500」·「`name` 필수」·「`tailLines` 를 그대로 승계」·「discovery 는 권한을 행사하지 않는다」는 전부 그 문서에서 한 홉에 읽힌다). **남긴 넷은 「이 어서션이 왜 이 형태인가」다**: ⑴ 셀렉터를 *서비스가 받은 질의*에 대고 단언하는 이유(핸들러가 셀렉터를 받아 놓고 직접 걸러도 바깥에서는 구별되지 않는다), ⑵ Table 테스트의 **부정 절이 본체인 이유**(객체를 통째로 붙여 넣는 렌더러도 긍정 단언은 전부 통과한다), ⑶ 상한 초과가 **거부여야 하는 이유**(클램프는 「호출이 통과했다」류 단언을 그대로 통과시키고 호출자는 덜 받았다는 사실을 끝내 모른다), ⑷ 클러스터 실패를 `isError` 로 싣고 JSON-RPC 오류로 싣지 않는 이유(둘을 섞으면 될 리 없는 요청을 재시도하게 된다). 이 넷은 복원 경로 넷 어디에도 없다. `fakes_test.go` 는 새 인터페이스에 맞춰 갈렸고 주석은 여전히 0줄이다. 이하 1차 판정 그대로 — 제거 3줄 — ① `App` doc 본문이 바로 아래 라우팅 분기(`authCfg == nil`, `OAuthConfigured()`)와 README 를 되풀이. `mcp_test.go` 52줄은 전부 「이 어서션이 왜 이 형태인가」(vacuous 통과 방지 · 커서와 증분의 구분 · 상류 wording 을 고정한 이유)라 유지. `silentPaths` 의 「kubelet 프로브가 로그를 덮는다」도 유지. `fakes_test.go` 는 주석 0줄 — 등재해 두면 이후 유입이 R2 에 걸린다 |
| 2026-09-04 | `internal/k8s/client.go` · `internal/k8s/service.go` · `internal/k8s/types.go` | 20 | `033400f54a6c` | **재판정 2026-09-15**(`rct_20260915-0006` — `resource_watch` 등록): **+3줄**(`service.go` 단독), 제거 0. 판정: **유지**. `Service.WatchResources` 의 doc 은 **스트림을 돌려주지 않는 이유**를 진다 — 「도구 계층은 한 번에 한 요청을 답한다」는 제약이고, 메서드 시그니처는 `*WatchResult` 를 돌려준다는 사실만 보여 줄 뿐 그것이 **선택**이었다는 것은 보여 주지 않는다. `prd-resource-generic` AC6 이 같은 것을 적지만 그 문서는 도구를 말하고 이 줄은 인터페이스를 말한다 — ② 경계에 있으나 「애매하면 남긴다」로 보존. 이하 직전 판정 그대로 — **재판정 2026-09-15**(`rct_20260915-0002` — `workload_scale` 구현 제거): **−2줄**, 둘 다 **코드와 함께 사라진 것**이다 — `types.go` 의 「`WorkloadKind` enumerates the workload types」와 「`ParseWorkloadKind` maps a user-supplied string」. `workload_scale` 이 그 타입군의 마지막 사용처였고 도구가 나가면서 타입군 전체가 죽었다. 새로 든 줄 0(제거된 `ScaleWorkload` 본문 세 벌 — `client.go` 구현 · `service.go` 인터페이스 · `Unavailable` 대역 — 에는 주석이 없었다). 판정할 잔여 없음. 이하 직전 판정 그대로 — **재판정 2026-09-13**(`rct_20260913-0002` — `resource_*` 읽기 축 슬라이스): −18줄, 거의 전부 **코드와 함께 사라진 것**이다 — `ListNamespaces`·`ListWorkloads`·`WorkloadLogs`·`DescribePod` 와 그 헬퍼(`firstPodMatching`·`pickPod`·`workloadPodSelector`·`buildPodDescription` 계열)가 제거되면서 1차 판정이 「유지」로 남겼던 세 절(Events best-effort · 정렬 결정성 · crash loop 뒤 Previous 폴백)도 그 코드와 함께 나갔다. **새로 든 것은 2줄뿐**: `Service.APIResources` 의 「이것이 나머지 둘이 좌표를 해석하는 대상이다」(인터페이스 순서만 보고는 셋의 의존 방향을 알 수 없다 — 유지)와 `LogOptions` 의 1줄 doc(관례가 요구하는 최소치 — 유지). **새 파일 `resource.go`·`resource_test.go` 는 이 행에 끌어오지 않았다** — `gate.go` 선례대로 신규 범위의 등재는 그 판정을 한 PR 의 몫이다. 이하 1차 판정 그대로 — 제거 4줄 — ③ `firstPodMatching` doc 이 같은 폴백 규칙을 바로 아래 `pickPod` doc 과 `Service.WorkloadLogs` 인터페이스 doc 에 이어 세 번째로 되풀이(2줄), ① `ParseWorkloadKind` 의 comma-ok 설명과 `Unavailable` 의 사용처 설명(각 1줄). 「Events 는 best-effort(RBAC 로 실패해도 나머지는 나간다)」·「정렬은 결정성 때문」·「crash loop 뒤에도 Previous 가 되도록 Running 이 아닌 파드로 폴백」은 유지 |
| 2026-09-04 | `internal/mcp/mcp.go` · `internal/mcp/toolslist.go` | 44 | `8a72ee66dc90` | **재판정 2026-09-13**(`rct_20260913-0002` — `resource_*` 읽기 축 슬라이스): −1줄, **범위도 한 파일 줄었다** — `describe.go` 가 `pod_describe` 와 함께 삭제돼 그 파일의 1줄 doc(`renderPodDescribeText`)이 코드와 함께 나갔다. `toolslist.go` 는 도구 정의 4개가 빠지고 3개가 들어왔지만 그 내용은 JSON 문자열 안이라 주석 표면이 아니다. `mcp.go` 는 핸들러 4개가 빠졌을 뿐 주석 줄은 움직이지 않았다. **새 파일 `resource.go` 는 이 행에 끌어오지 않았다**(신규 범위 등재는 그 판정의 몫). 이하 앞선 판정 그대로 — **재판정 2026-09-13**(`rct_20260913-0001` — 승인 게이트 판정 코어 슬라이스): +13줄 — 게이트 배선이 `mcp.go` 에 `defaultSensitiveKind` doc 2줄, `gate`·`registry` 필드 doc 6줄, `Option`·`WithGate` doc 3줄, 디스패처 강제 지점 2줄을 더했다. 판정: **전부 유지**. 필드 doc 둘은 「게이트는 never-nil 이라 미설정이 곧 거부가 된다」·「레지스트리를 패키지 전역이 아니라 핸들러에 둔 이유는 프로덕션이 싣지 않는 도구로 강제 경로를 시험하기 위해서다」로, 둘 다 선언에서도 문서에서도 복원되지 않는 **구조적 이유**다. 디스패처의 2줄(「핸들러가 스스로 게이트를 부르는 구조는 잊을 수 있는 구조다」)은 그 자리에 게이트 호출이 있는 이유 자체이고, 코드는 순서만 보여 준다. `defaultSensitiveKind` 는 `prd-approval-gate` 의 `RESOURCE_GATED_KINDS` 표와 겹쳐 ② 경계에 있으나 **기본값이 코드에 박히는 지점**을 표시하는 한 절이라 「애매하면 남긴다」로 보존. `WithGate` 는 exported 라 관례 최소 doc 이다. **새 파일(`gate.go`)은 이 행에 끌어오지 않았다** — 신규 범위는 아직 판정받은 적이 없고, 등재는 그 판정을 한 PR 의 몫이다. 이하 1차 판정 그대로 — 제거 0줄 — 세션 3종의 doc 은 「빈 목록과 도달 불가를 구분한다」·「스냅샷 세션은 pod 을 생략한다(빈 이름으로 있는 척하지 않는다)」·「쓰기는 도착만으로 대상을 깨우므로 인자 검증이 요청보다 앞선다」처럼 복원 경로 넷 어디에도 없는 구조적 이유다. 경계 사례는 `sessionFields` 의 「세 도구가 세션을 같은 모양으로 보고한다」 한 절 — 호출부로 복원되지만 *제약*의 선언이라 「애매하면 남긴다」로 유지 |
| 2026-09-04 | `internal/grafana/grafana.go` · `internal/grafana/grafana_test.go` · `internal/grafana/exposure_test.go` | 37 | `f48e36feba66` | 제거 0줄(문장 1개 삭제, 줄 바꿈에 흡수돼 줄 수 불변) — `exposure_test.go` 의 「This previously had no automated coverage.」는 ③④(PR·커밋)로 복원되므로 삭제. `defaultAPIBase` 의 「`GRAFANA_API_URL` 은 `.../api` 베이스여야 한다」, `tokenName` 의 「Grafana Cloud 가 정책 내 유일 이름을 요구한다」, 「발급자 토큰을 실제로 썼는지 먼저 단언하지 않으면 비노출 검사가 무의미해진다」는 전부 유지 |
| 2026-09-04 | `internal/awsconfig/awsconfig.go` · `internal/awsconfig/awsconfig_test.go` | 24 | `1b5e509876c7` | 제거 1줄 — ③ `LoadDefaultConfig` 위의 「Base credentials: the default chain (instance profile in production).」 이 패키지 주석의 같은 문장을 되풀이. 정책 §제거 대상 ③ 이 예시로 든 형태 그대로이고, 자매 파일 `internal/opensearch/opensearch.go` 가 1차 판정에서 같은 결론을 받았다. `AWS_CONFIG_S3_ENDPOINT`/MinIO 단락(STS 와 S3 를 한 포트에 얹는다, 스모크 전용)은 픽스처가 실환경과 갈리는 지점이라 유지 |
| 2026-09-04 | `internal/github/github.go` · `internal/github/github_test.go` | 19 | `3e94443eb4d1` | 제거 0줄(문장 1개 삭제, 줄 수 불변) — 「This previously had no automated coverage.」 삭제. 나머지는 exported 1줄 doc(정책이 유지 예시로 든 `// Error is the error type returned by Service.` 포함)과 「앱 JWT 를 되돌려주면 그 자체가 파생 서명 자료의 유출」이라 유지 |
| 2026-09-04 | `main.go` · `internal/version/version.go` | 14 | `65961d15289b` | **재판정 2026-09-13**(`rct_20260913-0001` — 승인 게이트 판정 코어 슬라이스): +10줄 — `main.go` 에 레지스트리 검증 블록 5줄과 `buildGate` doc 5줄이 붙었다. 판정: **전부 유지**. 검증 블록은 「선언 없는 도구가 광고되면 게이트가 현실과 다른 표로 판정하게 되고 그 어긋남은 런타임에 조용히 통과한다」는 **기동을 멈추는 이유**로, `mcp.Validate()` 호출만 보고는 왜 치명적인지 알 수 없다. `buildGate` doc 은 「미설정을 기동 실패로 만들지 않는 이유」 — 이 레포의 graceful degradation 관례와 fail-closed 가 어긋나 보이는 지점을 그 둘이 같은 말임을 밝혀 잇는다. `prd-approval-gate` AC5 와 주제가 겹치지만 AC 는 「거부한다」를 정하고 이 주석은 「그런데도 왜 여기서 죽지 않는가」를 적어 ② 재진술이 아니다. 이하 1차 판정 그대로 — 제거 0줄 — 넷 다 Command·패키지 doc 과 exported 상수의 1줄 doc 이라 관례가 요구하는 최소치다 |
| 2026-09-05 | `scripts/check_comment_policy.py` · `scripts/check_mock_policy.py` | 29 | `e85c2279825f` | **재판정 2026-09-08**(축 개정 PR, `rct_20260908-0002`): +6줄 — `DOCSTRING_DECL_RE` 위에 「`검증 시나리오:` 는 왜 `DIRECTIVE_RE` 가 아니라 여기 있는가」를 적었다. **교차 제약의 근거이자 죽은 패턴 하나의 소재를 지목**하므로 남긴다 — 지우면 다음 사람이 두 정규식을 「같은 자리인데 왜 갈라져 있나」로 읽고 공유 넷 쪽을 고쳐 자매 모델의 지문을 깨뜨린다. 나머지는 무변경. 이하 1차 판정 그대로 — 제거 0줄, `ID_TOKEN_RE` 의 「`(?![A-Za-z0-9_])` 가 없으면 정책 문서 경로 자체가 미등재 모킹으로 잡힌다」는 함정이고, 「모델 as-is 스크립트와 글자 그대로 같아야 한다」는 교차 제약이다. `# R1 —` 류 규칙 앵커는 모듈 docstring 의 재진술에 가깝지만 100줄짜리 `main()` 의 항해 표지라 「애매하면 남긴다」로 유지 · **재판정 2026-09-13**(`rct_20260913-0002` — 줄 주석 표면 잔량 래칫): 줄 수 불변(29), 지문만 이동 — 이 PR 이 R4 에 「합계 + 잔량 == 실측」을 더하며 `# R4 —` 앵커 한 줄의 문구를 고쳤다. 판정: **유지** — 앵커의 성격(긴 `main()` 의 항해 표지)은 그대로다 |
| 2026-09-05 | `tests/k8s/kind/session-platform.yaml` | 75 | `20eaf535b30c` | 제거 6줄 — ③ 자기 파일 중복 둘. `data-plane` ServiceAccount 위 4줄은 머리말이 이미 말한 것(그 SA 가 없으면 세션 파드가 뜨지 않는다 · 프로덕션 ClusterRoleBinding 을 재현하지 않는 이유)의 되풀이이고, `DATA_PLANE_IMAGE` 위 2줄은 머리말의 「한 워크플로가 쌍을 발행하므로 같은 agent 계약을 쓴다」를 다시 적은 것이다. 머리말 나머지는 전부 유지 — 왜 stand-in 이 아니라 실 컴포넌트인가, `CRIU_ENABLED`·`DATA_PLANE_CLAUDE_CODE_IMAGE` 를 비워 두는 것이 게이트 완화가 아닌 이유, 268 MiB 를 테스트 전에 미리 로드하는 이유(제어면이 파드에 주는 2분 예산을 pull 에 쓰면 안 된다), SHA 태그가 불변이라 하네스가 재현 가능하다는 근거 |
| 2026-09-05 | `tests/k8s/kind/oidc-fixture.yaml` | 78 | `0cffc931b2c9` | 제거 1줄 — 「허용목록·상한은 이 파일 때문에 변하지 않는다 (등재 5 · 상한 5 그대로)」. 바로 앞 문단이 `UPS`·`IMG` 어느 카테고리도 성립하지 않는다고 이미 결론냈고(③), 괄호 안 현재값은 `check_mock_policy.py` R6 이 양방향으로 강제하는 수치라 아무도 검증하지 않는 자리에 두면 낡는다. dex 최소 설정의 근거(`server.NewServer` 가 커넥터 0 개를 거부한다), digest 핀, readiness 를 `/healthz` 가 아니라 discovery 로 둔 이유, (d) 변형의 CrashLoop 이 곧 AC2 논증을 정직하게 만든다는 절은 전부 복원 경로가 없어 유지 |
| 2026-09-05 | `tests/k8s/kind/http-trace.yaml` | 43 | `e6747e1eba38` | 제거 1줄 — `TRACE_ROUTES` 값 **바로 위**의 형식 주석(① 아래 줄이 형식을 그대로 보여준다). 같은 형식을 적은 임베드 스크립트 쪽 1줄은 파싱 지점의 앵커라 판단이 갈렸고 「애매하면 남긴다」로 보존했다. 프록시가 왜 있는가(픽스처는 assumed-role 요청과 base 자격증명 요청을 구분하지 못해 결과 기반 단정이 공허해진다), hop-by-hop·`Expect`·중복 `Content-Length`·Host 서명 투명성, AssumeRole 레코드는 축출하지 않는 이유는 전부 유지 |
| 2026-09-05 | `tests/k8s/kind/auth-fixture.yaml` · `tests/k8s/kind/test-deployment.yaml` | 49 | `b5869fe61fba` | **재판정 2026-09-08**(`rct_20260908-0003` — namespace-list 시나리오 2 슬라이스): +11줄 — 이 슬라이스가 `auth-fixture.yaml` 에 `MCP_K8S_DISABLED` 를 세우며 머리말에 5줄, env 항목 위에 5줄(+구분 1줄)을 더했다. 판정: **전부 유지**. 머리말 문단은 「k8s 만은 시크릿 부재로 꺼지지 않는다 — in-cluster ServiceAccount 를 읽기 때문」이라는 **부재 이유**이고, 이것은 매니페스트 어디에서도 복원되지 않는다(끄는 코드는 `main.go` 에 있고 켜는 배선은 그냥 없음이다). env 위 5줄은 「이 줄을 지우면 `namespace_list_ac2.py` 가 빨개진다」는 **교차 제약**으로, `_auth_variant.py` 의 `API_KEY` 교차 제약이 바로 위 항목에서 유지된 것과 같은 계열이다. `test-deployment.yaml` 무변경. 형태·판정 근거가 1차와 같으므로 결론을 유지한다. 이하 1차 판정 그대로 — 제거 1줄(3줄 → 2줄) — `MCP_API_KEYS` 위의 「Auth ENABLED: `MCP_AUTH_DISABLED` 는 일부러 미설정」 두 문장이 파일 머리말의 같은 서술을 되풀이해(③) 잘라내고, `_auth_variant.py` 의 `API_KEY` 와 값이 같아야 한다는 교차 제약만 남겼다. 머리말의 「자격증명 시크릿을 여기 붙이지 말 것」은 계약이라 유지. `test-deployment.yaml` 은 제거 0줄 — 영구 crash loop 을 피한 이유(빠른 재시작이 containerd 의 이전 인스턴스 GC 와 겹쳐 E2E 가 플레이키했다)는 관측된 실패 모드다 |
| 2026-09-05 | `tests/k8s/kind/minio.yaml` · `tests/k8s/kind/opensearch.yaml` · `tests/k8s/kind/github-mock.yaml` · `tests/k8s/kind/grafana-mock.yaml` · `tests/k8s/kind/dear-baby-fixture.yaml` · `tests/k8s/kind/kustomization.yaml` | 17 | `4b9ee1aa233b` | 제거 20줄 — ② `docs/e2e-mocking-policy.md` 재진술 16줄 + `minio.yaml` 4줄. `github-mock`·`grafana-mock`·`dear-baby-fixture` 의 머리말은 그 문서의 **대상**·**실환경 불가 사유**·재배선 절을 그대로 옮겨 적은 것이고(dear-baby 의 「busybox 에 성공/미발견 종료코드를 흉내 내는 스텁」까지 문서에 있다), 세 파일 모두 그 문서를 가리키는 `등재:` 줄과 `mock-exception:` 주석을 이미 달고 있어 SSOT 로 가는 경로가 끊기지 않는다. `minio.yaml` 은 seed ConfigMap 머리말 3줄 — 아래 Job 스펙으로 복원되는 데다 **존재하지 않는 파일**(`tests/integration/aws_config.py`)을 가리켜 이미 거짓이었다(실제 단정은 `aws_config_get_ac1.py` 이고, 역방향 링크는 `_aws_config.py` 에 정확히 있다) — 과 Job 위 1줄(바로 아래 명령의 재진술). `opensearch.yaml` 의 heap 512Mi 근거와 GATE 등재 주석, `minio.yaml` 의 「S3 + STS 대역」 서술은 유지 |
| 2026-09-05 | `tests/integration/_helpers.py` · `tests/integration/_session_platform.py` · `tests/integration/_oidc.py` · `tests/integration/_workload.py` · `tests/integration/_auth_variant.py` · `tests/integration/_aws_config.py` · `tests/integration/_opensearch.py` | 132 | `5aa2ddea6731` | **재판정 2026-09-08**(`rct_20260908-0003` — namespace-list 시나리오 2 슬라이스): +2줄 — `_auth_variant.py` 의 거부 텍스트 블록 주석에 `K8S_REFUSAL` 이 왜 예외인지(빈 사유가 아니라 명시 사유를 받는다 — 시크릿이 아니라 ServiceAccount 를 읽는 통합이라 픽스처가 `MCP_K8S_DISABLED` 로 끈다) 두 줄을 더했다. 판정: **유지** — 그 블록 주석이 이미 「이 상수들이 어디서 오는가」를 담는 자리이고, 새 두 줄이 없으면 바로 위 문장(「빈 사유는 주 env 미설정일 때 main.go 가 넘기는 것」)이 새 상수에 대해 거짓이 된다. 나머지 6파일 무변경. 이하 1차 판정 그대로 — 제거 0줄 — 전부 「왜 이 형태인가」다. `_helpers.py` 의 도구 표면 집합은 두 번(`session_list` 2026-09-03, `session_read` 2026-09-04) 빠뜨려 검사가 조용히 약해졌다는 관측 이력과 그래서 생긴 규칙을 달고 있고, 포트포워드 헬퍼는 apiserver service proxy 가 대안이 아닌 이유(Authorization 을 덮어쓰고 GET 밖에 못 낸다)를, http-trace 헬퍼는 결과 기반 단정이 공허한 이유를 담는다. `_session_platform.py` 의 `#:` 예산값들은 제어면이 파드 Ready 를 기본 2분 기다린 뒤에야 201 을 낸다는 근거다. `_oidc.py` 의 `#:` 상수 doc 은 구성 (b)·(c)·(d) 라벨을 픽스처와 잇는 자리라 판단이 갈렸다 — 라벨의 의미 자체는 `oidc-fixture.yaml` 코드로 복원되지만 그 매핑을 잃으면 상수 이름만 남으므로 「애매하면 남긴다」로 보존 |
| 2026-09-05 | `tests/integration/run_all.py` · `tests/integration/check_ac_mapping.py` | 16 | `6ed9fc8c0525` | **재판정 2026-09-08**(축 개정 PR): +5줄 — 축이 AC → 시나리오로 옮겨지며 `check_ac_mapping.py` 의 규칙 앵커가 늘고(규칙 4·불변식 절 신설) `run_all.py` 의 `#:` 가 비-시나리오 마커를 가리키도록 바뀌었다. 형태·판정 근거는 1차와 동일하므로 결론을 유지한다. 이하 1차 판정 그대로 — 제거 0줄, `check_ac_mapping.py` 의 `# --- 규칙 N ---` 앵커는 자매 판정(위 #11 `check_comment_policy.py`)이 같은 형태를 「긴 `main()` 의 항해 표지」로 유지한 것과 같은 이유로 유지한다. `run_all.py` 의 `#:` 둘은 규칙 3(비-AC 파일)이 읽는 값의 의미를 적은 것이다 |
| 2026-09-05 | `tests/integration/session_list_ac1.py` · `tests/integration/session_list_ac2.py` · `tests/integration/session_list_ac3.py` · `tests/integration/session_read_ac1.py` · `tests/integration/session_read_ac3.py` · `tests/integration/session_read_ac4.py` · `tests/integration/session_write_ac1.py` · `tests/integration/session_write_ac3.py` · `tests/integration/session_write_ac5.py` | 46 | `dfc76f31359a` | 제거 2줄 — ① 어서션 재진술. `session_write_ac1.py` 의 「세션이 이미 active 였으므로 승격도 복원도 필요 없었다」와 `session_read_ac1.py` 의 「읽기는 수동적이지 않고 결과가 어느 분기가 처리했는지 말한다」는 각각 바로 아래 `path == "active"` 단정이 말하는 것이다. 반대로 마커를 두 조각으로 갈라 넣는 이유(PTY 가 명령을 되울리므로 붙여 쓰면 에코만으로 단정이 성립해 버린다)와 커서가 바이트 오프셋이어야 하는 이유는 이 파일들에만 있는 지식이라 유지. 주석 0줄인 `session_list_ac3.py`·`session_write_ac3.py` 도 같이 등재한다 |
| 2026-09-05 | `tests/integration/opensearch_document_delete_ac1.py` · `tests/integration/opensearch_document_delete_ac2.py` · `tests/integration/opensearch_document_delete_ac3.py` · `tests/integration/opensearch_document_delete_ac4.py` · `tests/integration/opensearch_document_delete_ac5.py` · `tests/integration/opensearch_document_put_ac1.py` · `tests/integration/opensearch_document_put_ac2.py` · `tests/integration/opensearch_document_put_ac3.py` · `tests/integration/opensearch_document_put_ac4.py` · `tests/integration/opensearch_document_put_ac5.py` · `tests/integration/opensearch_search_ac1.py` · `tests/integration/opensearch_search_ac2.py` · `tests/integration/opensearch_search_ac3.py` · `tests/integration/opensearch_search_ac4.py` | 4 | `31d67e48d0e5` | 제거 0줄 — 페이로드 해시까지 서명해야 하는 이유(서명을 깨지 않고 본문을 바꿔치기할 수 있다)와, 비매칭 문서가 색인에 실재하므로 그 부재가 「질의 필터링」이지 「문서 없음」이 아니라는 전제. 나머지 12파일은 주석 0줄이다 |
| 2026-09-05 | `tests/integration/aws_config_get_ac1.py` · `tests/integration/aws_config_get_ac2.py` · `tests/integration/aws_config_get_ac3.py` · `tests/integration/github_app_installation_token_ac1.py` · `tests/integration/github_app_installation_token_ac2.py` · `tests/integration/github_app_installation_token_ac3.py` · `tests/integration/github_app_installation_token_ac4.py` · `tests/integration/grafana_token_ac1.py` · `tests/integration/grafana_token_ac2.py` · `tests/integration/grafana_token_ac3.py` · `tests/integration/grafana_token_ac4.py` · `tests/integration/platform_auth_safety_ac1.py` · `tests/integration/platform_auth_safety_ac2.py` · `tests/integration/platform_auth_safety_ac5.py` · `tests/integration/platform_auth_safety_ac6.py` · `tests/integration/platform_auth_safety_ac7.py` · `tests/integration/platform_auth_safety_ac8.py` | 32 | `469b96c4df5a` | 제거 3줄 — ① 어서션 재진술. `github_app_installation_token_ac1.py` 의 「만료 주석」·「범위 주석」 두 줄은 바로 아래 `in env_text` 단정이 리터럴로 말하고, `grafana_token_ac4.py` 의 「payload 전체가 text/plain .env 이고 구조화 내용은 없다」는 `structuredContent is None` 의 재진술이다. 같은 파일의 「발급 토큰은 반환된다 ↔ 발급자 자격증명은 어떤 형태로도 새면 안 된다」 대비쌍은 부정 단정이 공허해지지 않게 하는 장치라 유지하고, `platform_auth_safety_ac3.py` 의 「AC 문언을 옮겨 적은 ClusterRole 전체」는 등가 비교의 피연산자 자체라 재진술이 아니다 · **재판정 2026-09-14**(RBAC 경계 해제 슬라이스): 범위에서 삭제된 `platform_auth_safety_ac3.py` 를 제외하고 재측정했다. 그 파일의 주석 판정(「AC 문언을 옮겨 적은 ClusterRole 전체는 등가 비교의 피연산자」)은 대조 대상 AC 가 결번이 되어 함께 소멸했고, 남은 파일의 판정은 앞선 판정 그대로다. |
| 2026-09-05 | `tests/integration/.gitignore` · `tests/integration/dear_baby_reset_user_ac1.py` · `tests/integration/dear_baby_reset_user_ac2.py` · `tests/integration/dear_baby_reset_user_ac3.py` · `tests/integration/ping_ac1.py` · `tests/integration/requirements.txt` · `tests/integration/smoke.py` | 4 | `87dce628e24d` | **재판정 2026-09-08**(`rct_20260908-0003` — namespace-list 시나리오 2 슬라이스): 범위에 `namespace_list_ac2.py`(이 슬라이스의 신규 파일) 편입 — **줄 주석 0줄**이라 줄 수·지문 불변이다. 편입하는 이유는 이 행이 스스로 적어 둔 그대로다(「등재해 두면 이후 유입이 R2 에 걸린다」). **내가 저작한 파일만 편입했고 남의 파일은 끌어오지 않았다.** 이하 1차 판정 그대로 — 제거 0줄 — kubelet 이 `timestamps=true` 에 붙이는 접두 형태, 한 kind 의 요약이 다른 kind 의 모양을 갖지 않는다는 단정의 의도, 도구가 selector/container 미지정 시 적용하는 기본값의 출처. 나머지는 주석 0줄이며, **등재해 두면 이후 유입이 R2 에 걸린다**(위 #4 `fakes_test.go` 와 같은 이유) · **재판정 2026-09-12**(V1 도구 6종 통합 슬라이스): 범위에서 폐기된 e2e 파일을 제외하고 재측정했다. 남은 파일의 주석 판정은 앞선 판정 그대로이고, 이 행이 준 보증(「등재해 두면 이후 유입이 R2 에 걸린다」)도 남은 범위에 대해 유지된다. |
| 2026-09-13 | `internal/gatekeeper/gatekeeper.go` · `internal/gatekeeper/gatekeeper_test.go` | 69 | `7c324b44ddac` | 제거 11줄 — 전부 ② `docs/prd-approval-gate.md` 재진술이고, 지운 자리마다 원문이 한 문장씩 대응한다. `ErrNotConfigured`(AC5 의 「미설정도 거부 경로」 + 「해당 도구만 멈추고 서버는 계속 산다」), `ErrConsumed`·`Consume`(AC7 의 「승인 하나는 단 한 번의 쿠버네티스 API 호출만 인가한다」·「실행이 실패해 재시도할 때도 승인을 새로 받는다」), `Call`(AC3 의 「무엇을 승인하는지 모르는 승인 요청은 승인 버튼을 형식적 절차로 만든다」), `poll`(AC4 의 「만료를 조회 시점에 평가하므로 폴링 없이 최초 응답만 믿으면 만료를 영영 관측하지 못한다」). **AC 포인터는 지우지 않았다** — 이 레포는 「어느 AC 인가」를 유지 대상으로 다뤄 왔고(등재된 범위에 AC 인용 주석이 69줄 살아 있다), 제거한 것은 포인터가 아니라 그 옆에 붙은 재진술이다. `TestApprovalIsSpentOnce` 의 `// AC7: one approval buys one execution.` 은 AC7 **제목의 번역**이라 그 자리를 계층 정보로 다시 썼다(줄 수 불변). 유지: 패키지 주석, 「auto-response 는 생성 시점에 결정되므로 폴링은 확인일 뿐」(상류 동작, 어느 문서에도 없다), 「`Resource` 가 서브리소스를 인라인으로 담는 것은 그것이 apiserver 가 주고 뺏는 단위이기 때문」, `GatedKinds` 가 설정 가능한 이유, 픽스처가 PENDING 을 거쳐야 폴링을 관측할 수 있다는 설계. 상세는 [`passes/2026-09-13-approval-gate-core.md`](passes/2026-09-13-approval-gate-core.md), 판정 근거는 `rct_20260913-0002` |
| 2026-09-13 | `internal/mcp/gate.go` · `internal/mcp/gate_test.go` | 176 | `13f306c09ff2` | **재판정 2026-09-15**(`rct_20260915-0006` — `resource_watch` 등록): **+7줄**(`gate.go` 3 · `gate_test.go` 4), 제거 0. 판정: **유지**. ⑴ `gate.go` 의 세 줄은 이 레지스트리 항목이 **자기 규칙을 하나도 들고 있지 않은 이유**다 — `readIsSensitive` 가 이미 `watch` 를 `get` 과 나란히 판정하므로 여기엔 적을 것이 없는데, **적을 것이 없다는 사실 자체**가 리뷰어가 「민감 종류 게이트를 빠뜨렸다」고 읽기 쉬운 자리다. 코드는 `genericPairs("watch")` 한 줄일 뿐 그 줄이 AC16·AC17 을 만족시킨다는 것을 보여 주지 못한다. ⑵ `gate_test.go` 의 네 줄은 fake 가 질의를 **기록**하는 이유다(창과 재개 지점이 그 자체로 단언 대상이고, 거부된 호출은 「도착하지 않은 호출」로 보여야 한다) — 바로 위 `PatchResource`·`UpdateResource` 대역이 같은 이유를 같은 형태로 이미 적고 있다. 이하 직전 판정 그대로 — **재판정 2026-09-15**(`rct_20260915-0002` — `workload_scale` 구현 제거): **−10 +1 = −9줄**, `gate.go` 단독(`gate_test.go` 는 주석 0 변동). 제거 10줄은 **전부 코드와 함께 나갔다**: ⑴ `exemptRetiredPendingRemoval` 의 doc 6줄 — 「PR #72 가 문서에서 폐기한 V1 도구들이 코드에만 살아 있고, 문서가 말하지 않는 규칙을 발명하지 않기 위해 게이트 대신 여기에 이름을 적고 기동 시 보고한다」. 그 상수를 쓰던 마지막 도구가 이 슬라이스에서 나가 상수 자체가 죽었으므로 doc 도 함께 죽는다. ⑵ 레지스트리 항목 안의 3줄 — 「scale 서브리소스를 쓰지 않고 `spec.replicas` 를 직접 패치하므로 쌍은 실제로 워크로드에 대한 patch 다」. **이 주석이 설명하던 구현이 바로 대체재가 뒤집은 것**이다(후계자는 `scale` 서브리소스에 PUT 한다) — 코드와 함께 나가지 않으면 반대 사실을 가리키는 화석이 된다. ⑶ 상수 블록 머리 1줄이 「두 reasons … Both come from」에서 「한 reason … It comes from」으로 바뀌었다(+1 −1) — 예외가 2건에서 1건으로 줄어 **수가 문면에 박혀 있던 자리**를 고친 것이고, 남은 절반(「문서에서 오지 이 패키지에서 오지 않는다」)은 그대로다. 새로 판정할 근거 줄 없음. 이하 직전 판정 그대로 — **재판정 2026-09-15**(`rct_20260914-0017` — `resource_update` 등록): **+13줄**(`gate.go` +6 · `gate_test.go` +7), 제거 0. 판정: **전부 유지**. `gate.go` 의 6줄은 `updatePairs` doc 하나이고, **왜 이 검증이 핸들러가 아니라 쌍 해석 안에 있는가**를 적는다 — 「`authorize` 가 핸들러보다 먼저 돌므로, 핸들러가 거절할 호출은 그때 이미 승인 요청 하나를 치렀다. 시나리오 8 은 음수·누락 `replicas` 에 **승인 요청조차 만들지 말 것**을 요구하고, 게이트보다 이른 것은 쌍 해석뿐이다」. 코드는 「resolve 안에서 검증한다」는 사실만 보여 주고 **그래야만 하는 이유**는 보여 주지 않는다 — 지우면 다음 사람이 「이건 핸들러 일인데 왜 여기 있지」로 읽고 옮겨 그 성질을 조용히 깬다(그리고 그 손실은 단위 테스트가 잡지만, **왜 그 테스트가 gate 호출 수를 세는지**는 다시 이 주석이 진다). ② 재진술이 아닌 근거는 시나리오 문면을 **인용이 아니라 제약의 출처로** 가리키는 형태이며, 원장 #24 가 「AC 의 문언이 아니라 그 문언이 코드 구조에 부과하는 제약」으로 유지 판정한 계열 그대로다. `gate_test.go` 의 7줄은 `countingK8s` 의 새 필드 둘에 붙었다 — `updateErr` 3줄은 **대역의 한계를 말한다**(「AC8 이 이름 붙인 유일한 거부는 discovery 의 답이라 이 층의 인자에서는 보이지 않는다 — 늘 성공하는 대역으로는 그 경로를 칠 수 없다」)이고, `UpdateResource` 4줄은 「**스케일 본문은 호출자가 쓴 것이 아니라 서버가 지은 것**이라 ref 를 기록하는 것이 곧 단언이다」로 `PatchResource` 쪽 사본과 성질이 갈린다(patch 는 「운영자의 바이트가 그대로 갔다」, update 는 「호출자가 쓰지 않은 본문이 무엇이 됐나」). 원장 #24 가 `gate.go`↔`gate_test.go` 쌍을 「애매하면 남긴다」로 처리한 선례 그대로다. 이하 직전 판정 그대로 — **재판정 2026-09-14**(`rct_20260914-0008` — `resource_patch` 등록 + 민감 종류 쓰기 마스킹): **+39줄**(`gate.go` +34 · `gate_test.go` +5), 제거 0. 판정: **전부 유지**. `gate.go` 의 34줄은 세 덩어리다 — ⑴ `resourceIsSensitive` 를 `readIsSensitive` 에서 갈라낸 이유(「두 호출자가 서로 다른 verb 를 묻는다 — 하나의 정규화를 나눠 쓰지 않으면 둘이 갈린다」)는 **분할의 근거**라 코드에서 복원되지 않는다. ⑵ `writesASensitiveKind`·`maskCredentialValues`·`maskedValue` 의 doc 은 **AC 의 문언이 아니라 그 문언이 부과하는 제약**을 적는다 — 「읽기는 좌표만 나르므로 마스크할 것이 없다」·「거절해도 이미 본 것이 된다」·「`Secret.data` 는 base64 라 길이를 먼저 디코드한다 — 인코딩된 길이는 다른 질문에 답한다」. ⑶ `maskJSONPatchOps`·`withheld` 는 **관측되지 않으면 놓치는 함정**이다: RFC 6902 는 값을 `data` 키 아래가 아니라 `path` 옆에 실어 트리 탐색이 원리적으로 못 보고, 파싱 실패 시 「가리지 못하니 통째로 withhold 한다」는 안전한 방향의 선택이다. ② 재진술은 없다 — AC3·AC10·AC16 은 인용하지 않고 포인터로만 가리킨다. `gate_test.go` 의 5줄은 `countingK8s.PatchResource` 가 **본문까지 기록하는 이유**(「서버가 운영자의 바이트를 그대로 보냈다」가 그 자체로 단언이다)와 `testHandler` doc 의 정정이다 — 「프로덕션에 게이트 대상 쓰기 도구가 아직 없다」가 이 PR 로 **거짓이 됐다**(원장 #24 의 1차 판정이 유지 사유로 들었던 바로 그 문장이다). 새 문면은 「쓰기 verb 여섯 중 `patch` 만 실재하므로 나머지 다섯은 여전히 합성 레지스트리로 친다」이고, `resource_patch` 자신에 관한 케이스는 `toolRegistry` 를 쓴다는 이유(합성 대역은 테스트 자신의 선언을 단언하게 된다)를 함께 적는다. **신규 파일 `internal/mcp/resource_test.go` 는 이 행에 끌어오지 않았다** — `gate.go` 선례대로 신규 범위의 등재는 그 판정을 한 PR 의 몫이고, 그만큼은 잔량 마커로 선언했다. 이하 직전 판정 그대로 — **재판정 2026-09-14**(`rct_20260914-0006` — AC18 403 메시지 슬라이스): ±0줄 — `TestGenericPairsFollowTheCoordinate` 머리 주석 2줄이 가리키던 소비자를 고쳤다. 「운영자가 `k8s/rbac.yaml` 과 대조하는 쌍」 → 「승인 요청이 리뷰어 앞에 놓는 쌍」. `prd-approval-gate` AC11 이 「선언은 남기되 검증 방법은 요청 기록 관측 — `rbac.yaml` 대조는 AC19 와 함께 없어졌다」로 개정된 뒤, 이 주석만 사라진 소비자를 가리키고 있었다. **판정은 유지 그대로**(쌍 해석이 왜 서브리소스까지 따라가는가를 말하는 자리다)이고 줄 수도 같아 합계·잔량 마커는 불변이다. 이하 직전 판정 그대로 — **재판정 2026-09-14**(`rct_20260913-0002` — `resource_*` 읽기 축 슬라이스): +29줄. 게이트의 쌍 선언이 **런타임 해석**으로 넓어지면서(좌표가 인자인 도구는 정적 표로 기술할 수 없다) `gate.go` 에 `resolve` 필드 doc 6줄·`genericPairs` doc 5줄·서브리소스 판정 3줄·미해석 호출의 거부 근거 2줄이, `gate_test.go` 에 신규 케이스들의 「이 단언이 왜 이 형태인가」 주석이 들어왔다. **제거 2줄** — `callPairs` 의 doc 이 ① 선언 재진술이다(두 줄짜리 본문이 「선언된 상수, 아니면 `resolve` 가 주는 것」을 그대로 보여 준다 — 정책이 예시로 든 형태 그대로). **나머지는 전부 유지**: `resolve` 의 「정적 표는 클러스터의 모든 kind 를 적거나 하나도 못 적고 거짓말하거나 둘 중 하나이고, discovery 는 부를 수 없다 — 평결이 apiserver 를 건드리기 전에 나야 하므로」와 `genericPairs` 의 「민감 종류 읽기는 쿠버네티스 호출 수가 0인 채로 거부돼야 하고, 「Secret 이 뭐라고 불리는지」를 클러스터에 묻는 것이 이미 그 한 번이다」는 AC16 의 문언이 아니라 **그 문언이 코드 구조에 부과하는 제약**이다. 서브리소스 3줄(「`secrets/<무엇이든>` 은 여전히 Secret 읽기이고, 문자열 전체로 맞추면 서브리소스 이름이 그대로 지나간다」)은 함정의 기술이라 복원 경로가 없다. 테스트 쪽은 이 원장이 일관되게 유지해 온 계열 — `testHandler` 의 「프로덕션에 게이트 대상 쓰기 도구가 아직 없어 합성 레지스트리로 강제 경로를 친다」, AC16 읽기 절반의 「**카운터가 곧 단언이다** — 쿠버네티스에 닿은 거부는 이미 값을 읽은 뒤라는 뜻이다」. `gate_test.go` 의 서브리소스 2줄은 `gate.go` 쪽 3줄과 같은 함정을 가리키지만 한쪽은 「코드가 왜 이렇게 맞추는가」, 다른 쪽은 「이 단언이 없으면 무엇이 지나갔겠는가」라 어느 쪽도 다른 쪽으로 복원되지 않는다 — 「애매하면 남긴다」로 유지. **신규 파일 `internal/k8s/resource.go`·`resource_test.go` 와 `tests/integration/resource_read_smoke.py` 는 이 행에 끌어오지 않았다** — `gate.go` 선례대로 신규 범위의 등재는 그 판정을 한 PR 의 몫이고, 그만큼은 잔량 마커로 선언했다. 이하 1차 판정 그대로 — 제거 15줄 — 대부분 ② `docs/prd-approval-gate.md` 재진술. `gatedVerbs`(AC1 의 「내용으로 판정하면 … 분류기가 하나 더 생기고 그 분류기가 곧 우회 경로가 된다」를 예시까지 그대로 옮긴 4줄), `toolDeclaration`(AC1 의 「선언과 실제 행사가 어긋나면 게이트 표가 거짓말을 하게 되고, 그 어긋남은 런타임에 조용히 통과한다」), `exemptPendingOwnerDecision`(PRD 의 「남은 예외 1건」 블록인용 번역 — **게다가 const 값 자신이 이미 그 인용이다**), `authorize`(AC1 의 디스패처 강제), 감사 로그 앞의 AC8(「승인 기록 없이 실행된 호출은 로그상 존재할 수 없다」), `approvalContext`(AC3 의 「요약하면 그 판단을 서버가 대신하게 된다」), `annotateAutoApproval`(AC9), `TestApprovedCallReachesKubernetesWithAJudgeableContext`(같은 AC3 문장의 세 번째 사본). **낡은 이름 1건을 함께 고쳤다** — `gatedPairs` doc 이 존재하지 않는 `reportExemptions` 를 가리키고 있었다(실제 함수는 `Exemptions`). 코드로 복원되는 서술이 이미 거짓이 된 자리라 제거 근거가 강화된 예다. 유지: `outsideGate` 가 bool 이 아니라 문장인 이유, `workload_scale` 이 scale 서브리소스를 쓰지 않는다는 사실, `RESOURCE_GATED_KINDS`(group/version/Kind)와 RBAC 표기(복수형)의 임피던스 차, watch 가 get 과 같은 것을 넘긴다는 근거, 「`pods/exec` 의 create 는 SPDY executor 가 POST 로 스트림을 열기 때문」(상류 client-go 동작 — PRD 에 없다), 「프로덕션에는 아직 게이트 대상 도구가 없어 합성 레지스트리로 강제 경로를 친다」, 「새 쓰기 도구를 둘 중 어느 쪽으로 둘지 정하지 않으면 이 테스트가 깨진다」. 상세는 [`passes/2026-09-13-approval-gate-core.md`](passes/2026-09-13-approval-gate-core.md), 판정 근거는 `rct_20260913-0002` |
| 2026-09-14 | `internal/k8s/resource.go` · `internal/k8s/resource_test.go` | 209 | `be1454a29da8` | **재판정 2026-09-15**(`rct_20260915-0006` — `resource_watch` 등록): **+34줄**(`resource.go` 단독), 제거 0. 판정: **유지**. 넷으로 갈린다 — ⑴ **상수 셋의 근거 13줄**. `WatchDefaultSeconds`·`WatchMaxSeconds` 는 AC6 에서 한 홉에 읽히지만 「초과를 클램프하지 않고 거부한다」가 `int64 = 60` 이라는 선언에서는 복원되지 않는다. `WatchMaxEvents` 는 더 강하다 — **AC6 이 수를 말하지 않으므로** 100 이 어디서 왔는지는 이 주석 말고 어디에도 없다(문서에 적으면 to-be 가 구현 선택을 떠안는다). ⑵ **타입 셋의 doc 8줄** — `WatchQuery` 가 `ListQuery` 의 좌표에서 `limit`/`continue` 를 **빼고** `ResourceVersion` 을 그 자리에 둔 이유, `Truncated` 가 「아무 일도 없었다」와 「듣기를 멈췄다」를 가르는 이유. 필드 목록은 그 대비를 보여 주지 못한다. ⑶ **`WatchResources` 의 doc 6줄** — 로컬 타이머가 **두 번째 정책이 아니라** 같은 약속을 연결이 협조하지 않을 때도 참으로 만드는 것이라는 구분이고, `time.NewTimer` 옆에서는 두 창이 경쟁하는 것처럼 읽힌다. ⑷ **본문 인라인 7줄** — Error 이벤트에서 **수집분을 함께 돌려주지 않는** 이유(부분 창을 온전한 창으로 제시하게 된다)와 이벤트 객체에도 AC4 의 잡음 두 필드를 거르는 이유(잡음인 것은 **그 필드의 성질**이지 어떤 verb 가 읽었는지가 아니다 — 이것은 AC4 의 확장 적용이라 `doc-tracker.md` 에도 뒤집을 수 있는 결정으로 적어 두었다). 이하 직전 판정 그대로 — **재판정 2026-09-15**(`rct_20260914-0015` — `container` 누락 거부의 후보 이름 보존): **+23줄**(`resource.go` +9 · `resource_test.go` +14), 제거 0. 판정: **전부 유지**. `podLogsError` doc 9줄은 **상류 client-go 의 거동과 그것이 만든 실패 모드**다 — `DoRaw` 는 본문을 에러와 함께 돌려주면서도 응답이 *텍스트일 때만* 그 본문을 메시지로 읽고, apiserver 는 `pods/log` 를 JSON `Status` 로 거절하므로 400 이 「the server rejected our request for an unknown reason」 으로 일반화된다. 어느 저장소 문서에도 없고 코드에서 복원되지 않는다 — `json.Unmarshal` 은 「Status 를 읽는다」만 보여 주지 **왜 다시 뜯어야 하는가**를 보여 주지 않는다. Forbidden 을 분기에서 갈라 두는 이유(「빠진 grant 가 무엇인지는 이 서버의 RBAC 에 관한 사실이라 apiserver 의 문면이 지지 못한다」)도 `if` 자체로는 복원되지 않는다. **② AC5 재진술이 아니다** — AC5 는 「`container` 누락이 거부된다」를 정하고 이 주석은 「그래서 코드가 왜 이 모양인가」를 적는다. 테스트 14줄은 전부 「이 단언이 왜 이 형태인가」·「관측되지 않으면 무엇이 지나가는가」 계열이다: `podLogServer` 3줄은 **픽스처의 content type 이 장식이 아니라 분기 조건**이라는 교차 제약(정리 패스가 헤더를 지우면 케이스가 조용히 옛 경로를 재게 된다), `logServiceAgainst` 2줄은 이 verb 만 dynamic client 가 아니라 typed clientset 을 타서 `serviceAgainst` 와 갈렸다는 것, 세 케이스 머리 9줄은 각각 **대조군의 근거**다 — ⑴ 「호출이 실패했다」만 단언하면 지금 교체하는 메시지도 그대로 통과한다, ⑵ 403 케이스가 없으면 「무슨 실패든 본문으로 덮는다」 구현이 통과하면서 이 서버의 grant 문면을 조용히 apiserver 문면으로 바꿔 놓는다, ⑶ `Status` 가 아닌 본문(프록시의 HTML 오류 페이지·잘린 응답)이 apiserver 의 논리로 보고되면 안 된다. 원장 #4 가 고정한 「부정 절이 본체인 이유」 계열이라 전수 유지다. 이하 직전 판정 그대로 — **재판정 2026-09-15**(`rct_20260914-0017` — `resource_update` 등록): **+37줄**(`resource.go` +24 · `resource_test.go` +13), 제거 0. 판정: **전부 유지**. `resource.go` 의 24줄은 셋으로 갈린다. ⑴ `UpdateResource` doc 8줄 — **읽지 않고 쓰는 이유**다: 「`kubectl scale` 은 `resourceVersion` 을 프리컨디션으로 실으려 get-후-put 을 하지만 `get` 은 둘째 verb 이고 이 도구는 하나만 든다. `resourceVersion` 이 비면 무조건 갱신이라 같은 쓰기가 되고, 이 호출자가 실제로 원하는 프리컨디션은 게이트의 것(`prd-approval-gate` AC6)이지 이것이 아니다」. 코드에 남는 것은 **없는 호출**이라 원리적으로 복원되지 않는다 — 지우면 다음 사람이 「읽고 쓰는 게 안전하지」로 한 줄 더해 verb 1:1 을 깬다. ⑵ `requireSubresource` doc 8줄 — **거부 사유가 왜 discovery 에서 와야 하는가**: 「`daemonsets/<name>/scale` 로의 PUT 과 지워진 deployment 의 scale 로의 PUT 은 같은 상태 코드이고, 운영자가 행동을 고르는 것은 바로 그 구별이다. discovery 는 리소스 권한이 아니라(AC20) 묻는 값이 둘째 verb 가 아니다」. AC8 은 「밝힌다」만 말하고 **어떻게** 는 말하지 않으므로 재진술이 아니다. ⑶ `scaleObject` 2줄과 필드 doc 6줄(`UpdateRef` 3줄 · `ScaleSubresource` 3줄) — 「`resourceVersion` 없음은 의도다」와 「둘 중 정확히 하나만 설정된다 — 그 배제는 이 타입이 만들어지기 전에 도구 층이 강제한다」는 **불변식의 소유자를 지목**한다(타입 선언만으로는 둘 다 nil 인 값도 합법이다). ② 재진술은 없다 — AC8 문면은 인용하지 않고 포인터로만 가리킨다. `resource_test.go` 의 13줄은 전부 「이 단언이 왜 이 형태인가」 계열이다: `discoveryServer` 3줄(「apps/v1 이 정직한 사례다 — 실제로 apiserver 가 `deployments/scale` 은 서빙하고 `daemonsets/scale` 은 서빙하지 않는다」 = 픽스처가 지어낸 것이 아니라는 근거), `TestUpdateScaleRejectsReplicalessKind` 4줄(**부정 절이 본체**다 — 「운영자가 행동을 고르는 것은 「권한 없음」·「객체 없음」·「그런 것이 없음」 중 무엇이었는가이고, 맨 PUT 은 셋 다 같은 404 로 답했을 것이다」), `TestScaleObjectIsAnUnconditionalWrite` 4줄(「이 단언이 없으면 get-후-put 이 『읽기 한 번쯤』으로 다시 기어든다」 = **회귀의 모양**을 적는다), `TestScaleObjectOmitsAnEmptyNamespace` 2줄(「빈 문자열은 apiserver 가 이름이 "" 인 네임스페이스로 읽는다」 = 함정). 이하 직전 판정 그대로 — **재판정 2026-09-14**(`rct_20260914-0008` — `resource_patch` 등록): **+27줄**(`resource.go` +21 · `resource_test.go` +6), 제거 0. 판정: **전부 유지**. `patchTypes` doc 3줄은 **어휘 선택의 근거**다(「인자로 `application/strategic-merge-patch+json` 을 적게 하는 설계는 아무도 맞히지 못한다」) — 맵 자체는 무엇을 매핑하는지만 보여 준다. `PatchTypeNames` 2줄은 **정렬의 이유**(맵 순회는 거부 메시지를 호출마다 뒤섞는다)이고, `objectNamespace` 4줄은 **함수를 뽑아낸 이유**(「사본이 둘이면 get 과 patch 가 언젠가 클러스터 스코프의 뜻을 두고 갈린다」)라 둘 다 호출부에서 복원되지 않는다. `PatchResource` doc 4줄과 `manager` 3줄은 각각 「본문을 고쳐 쓰는 서버는 보여 준 것과 다른 것을 승인한 셈이 된다」와 「apply 의 `fieldManager` 는 라벨이 아니라 소유권 키라 이름을 공유하면 필드를 공유한다」 — 상류 server-side apply 의 성질이고 PRD 에 없다. 테스트 6줄은 「이 단언이 왜 이 형태인가」 계열: 미디어 타입을 **상수 왕복이 아니라 값으로** 단언하는 이유(「`merge` 를 strategic 으로 보내는 매핑은 자기 일관적이면서 리스트 필드에 다른 의미를 적용한다 — `Accept` 헤더의 `v=1` 과 같은 계열이다」)다. 이하 직전 판정 그대로 — **재판정 2026-09-14**(`rct_20260914-0004` — AC20 후보 산출 정정 슬라이스): **+24줄, 전부 신규**이고 제거는 없다. `rankSimilarKinds` doc 9줄 — 「부분 문자열은 앞뒤만 답한다 · 가운데 글자가 빠진 `Deploymnt` 는 어느 쪽으로도 걸리지 않아 AC20 이 존재하는 바로 그 입력에서 안내 문구로 떨어졌다 · 그런데도 부분 문자열을 남긴 것은 `Deploy` 같은 부분 이름이 오타가 아니기 때문」 = **관측된 실패 모드**라 코드에서 복원되지 않는다(유지). 정렬 2줄 — 「이름순으로 자르면 한 글자 떨어진 종류가 긴 부분 문자열 일치에 밀려 잘려 나간다」 = 절단과 정렬의 **구조적 결합 이유**(유지). `kindEditBudget` doc 3줄 — **수치의 근거**(왜 고정값이 아니라 길이별인가: `PodDisruptionBudget` 의 탈자를 놓치거나 `Pod` 를 카탈로그 전체로 답하게 된다)이고 값 1/2/3 자체만 코드에 있다(유지). `editDistance` 1줄 — 알고리즘 이름 (Levenshtein)은 본문을 읽어야 복원되는 관례 doc 최소치(유지). 테스트 8줄 — 네 행이 **서로 다른 오타 모양**이라는 것과 부정 케이스 둘(「예산이 넓으면 메시지가 소음이 된다」·「빈 답이 api_resources 안내로 보내는 경로다」)의 **왜 이 단언인가**(유지). ② 재진술은 없다 — AC20 문면은 인용하지 않고 포인터로만 가리킨다. 이하 직전 판정 그대로 — **재판정 2026-09-14**(`rct_20260914-0006` — AC18 403 메시지 슬라이스): ±0줄 — `TestForbiddenBecomesAGrantStatement` 머리 주석 3줄이 「빠진 쌍과 **그것을 정하는 파일**을 지목한다」에서 「빠진 쌍을 지목한다 … AC18 은 이제 어떤 파일도 지목하지 않으니 이 메시지도 그래서는 안 된다」로 바뀌었다. 실행 코드 쪽 변경은 주석이 아니라 포맷 문자열이다(`apiCallError` 가 내보내던 「`k8s/rbac.yaml` 이 단일 출처」 절을 뺐다 — 그 문장은 #83 이 AC18 에서 「그 파일이 더는 범위를 정하지 않으므로」 지운 것이다). **판정은 1차 그대로 유지** — 「이 단언이 왜 이 형태인가」 계열이고, 이제는 새 부정 단언(메시지에 `rbac.yaml` 이 재등장하면 실패)이 왜 있는지까지 그 주석이 진다. 줄 수가 같아 합계·잔량 마커는 불변이다. 아래 1차 판정문의 AC18 인용은 **그날의 문면을 적은 기록**이라 소급해 고치지 않는다. 이하 1차 판정 그대로 — 제거 12줄 — 전부 ② `docs/prd-resource-generic.md` 재진술이고, 지운 자리마다 원문이 한 문장씩 대응한다. `tableAccept`(AC17 의 「Secret 의 Table 은 apiserver 가 서버 사이드로 만들어 NAME/TYPE/DATA/AGE 만 담으므로 값이 애초에 전송되지 않는다」 + PRD 가 같은 근거로 적은 「`list` 는 예외다」), `ListDefaultLimit`/`ListMaxLimit` 의 「클램프가 아니라 거부」 절(AC3 이 정하고 `internal/mcp/resource.go` 가 그 자리에서 거부한다 — **거부여야 하는 *이유*는 원장 #4 의 `mcp_test.go` 판정이 「이 단언이 왜 이 형태인가」로 이미 보존하고 있다**), `ListResult` doc 의 「객체 전문이 아니라」(AC2 문언 + 필드 둘이 이미 보여 준다), `GetResource` doc 의 잡음 서술(AC4 문언이고 **같은 파일 `stripNoise` doc 이 두 번째 사본**이라 ③ 이기도 하다 — 한쪽만 남겼다), `ListResources` 안의 「폴백하지 않는 이유」(AC17 의 「원시 목록으로 떨어지는 폴백 경로를 두지 않는다」 + 바로 아래 에러 문자열이 같은 말을 한다 + `tableAccept` doc 과 ③), `apiCallError` 의 AC18 서술(「어떤 `(verb, resource)` 쌍이 없어서 막혔는지를 밝혀 운영자가 `k8s/rbac.yaml` 에서 곧바로 대조할 수 있게 한다」가 축자이고 바로 아래 포맷 문자열이 그 쌍과 파일명을 싣는다). **AC 포인터는 지우지 않았다** — 원장 #23 이 고정한 처리이고, Go 단위 테스트에는 `검증 AC:` 같은 기계 판독 선언이 없어 그 포인터가 AC ↔ 코드를 잇는 유일한 자리다. **`resource_test.go` 는 25줄 전수 유지** — 「이 단언이 왜 이 형태인가」 계열이다: `tableNegotiatingServer` 의 「Accept 파라미터가 어긋나면 요청이 *실패하지 않고* 평범한 List 로 답이 온다」(상류 apiserver 의 문서화되지 않은 거동), 헤더 모양만 단언하던 1차 판이 「어떤 apiserver 도 서빙하지 않는 `v=1`」을 통과시켰다는 **관측된 실패**, `stripNoise` 두 케이스의 부정 절(「spec/status 까지 가져가는 stripper 도 긍정 단언은 전부 통과한다」·「`annotations: {}` 를 「설정된 것이 없다」로 읽지 않게」), 「비어 있다는 답은 목록이 결코 지어내서는 안 되는 것」. 상세는 [`passes/2026-09-14-resource-read-axis.md`](passes/2026-09-14-resource-read-axis.md), 판정 근거는 `rct_20260914-0002` |
| 2026-09-14 | `internal/mcp/resource.go` | 43 | `cde12e4cdbca` | **재판정 2026-09-15**(`rct_20260915-0006` — `resource_watch` 등록): **+13줄**, 제거 0. 판정: **유지**. ⑴ 상한 초과를 **거부**하는 3줄은 이 파일이 `limit`·`tailLines` 에 대해 이미 두 번 적은 것과 같은 이유이며(조용히 줄인 창은 호출자가 끝내 모른다), 그 이유는 `errf` 한 줄에서 복원되지 않는다. ⑵ `renderWatch` 의 4줄은 **객체를 텍스트에 싣지 않는 이유**다 — 이벤트 수 상한이 막으려는 바로 그 범람을 렌더러가 되살릴 수 있고, 코드는 한 줄씩 찍는다는 사실만 보여 준다. ⑶ `watchEventResourceVersion` 의 4줄은 **이 서버가 자기 필드를 더한 것이 아니라 객체를 소리 내어 읽는 것**이라는 구분이다(AC6 은 `resourceVersion` 을 **받는다**고만 적는다 — 돌려주는 필드를 새로 만들면 문서에 없는 표면이 된다). ⑷ `watchEventName` 의 2줄은 클러스터 스코프 폴백의 근거다. 이하 직전 판정 그대로 — **재판정 2026-09-15**(`rct_20260915-0002` — `workload_scale` 구현 제거): **줄 수 불변(30), 지문만 이동** — `parseUpdateTarget` doc 의 첫 문장 한 줄이 「the bounds the scale half **carries over from workload_scale**」에서 「the bounds the scale half **holds: replicas is an integer in [0, MaxInt32]**」로 바뀌었다. 판정: **유지**. 고친 이유는 문체가 아니라 **가리키던 대상이 이 슬라이스에서 사라졌기 때문**이다 — 출처를 이름으로 가리키는 문장은 그 이름이 레포에 없어지는 순간 다음 사람에게 「그게 뭐였는지 찾아보라」는 지시가 되고, 찾아도 나오지 않는다. 그래서 출처 대신 **경계 자체**를 적었다. 그 경계는 코드가 세 군데에 흩어 놓은 값(`!ok` · `< 0` · `> MaxInt32`)이라 한 줄로 읽히지 않으므로, 이 줄은 여전히 코드가 말하지 않는 것을 말한다. 이하 직전 판정 그대로 — **재판정 2026-09-15**(`rct_20260914-0017` — `resource_update` 등록): **+16줄**, 제거 0. 판정: **전부 유지**. 둘로 갈린다. ⑴ `parseUpdateTarget` doc 11줄 — **함수가 하나인 이유**이고, 이 슬라이스에서 가장 복원되지 않는 자리다: 「양쪽이 이것을 돌린다 — 게이트는 승인 요청이 생기기 **전에**, 핸들러는 그 **뒤에**. 시나리오 8 은 음수·누락 `replicas` 에 승인 요청조차 만들지 말 것을 요구하는데 핸들러는 `authorize` 하류라 그것만으로는 불가능하다. 사본이 둘이면 **운영자가 본 적 없는 거부와 본 거부가 무엇이 유효한 호출인지를 두고 갈린다**」. 원장 #25 가 `objectNamespace` 4줄을 「함수를 뽑아낸 이유 — 사본이 둘이면 get 과 patch 가 클러스터 스코프의 뜻을 두고 갈린다」로 유지 판정한 바로 그 계열이고, 호출부 두 곳 어디에서도 복원되지 않는다. ⑵ `parseManifest` doc 5줄 — 「apiserver 도 그 어긋남을 거부하지만 그 답은 **승인이 소비된 뒤에** 도착하고(`prd-approval-gate` AC7), 실행될 리 없던 호출에 쓰인 승인은 운영자가 두 번 받아야 하는 승인이다」. 같은 함정을 `resource_test.go`(원장 #31)의 `apply`-without-manager 주석이 가리키지만 한쪽은 「**코드가 왜 이 순서인가**」, 다른 쪽은 「**이 단언이 없으면 무엇이 지나갔겠는가**」라 어느 쪽도 다른 쪽으로 복원되지 않는다 — 원장 #24 가 `gate.go`↔`gate_test.go` 서브리소스 쌍을 「애매하면 남긴다」로 처리한 선례 그대로다. **AC 포인터는 지우지 않았다**(원장 #23 이 고정한 처리). 이하 직전 판정 그대로 — **재판정 2026-09-14**(`rct_20260914-0008` — `resource_patch` 등록): **+10줄**, 제거 0. 판정: **전부 유지**. `resourcePatch` 안의 3줄은 **인자를 원문에서 뜨는 이유**(「apiserver 가 받는 바이트가 운영자가 승인한 바이트와 키 순서까지 같아진다」 — 디코드된 맵에서 재인코딩하면 그 등식이 깨진다)이고, `fieldManager` 3줄은 **왜 apply 에서만 필수이고 나머지에서는 거부인가**(소유권 키라 apply 는 기본값을 가질 수 없고, 나머지 셋에는 쓰임이 없어 받아 주면 아무것도 하지 않는 인자를 광고하게 된다)다. `rawPatchBody` doc 3줄은 **JSON 패치만 배열이라는 모양의 차이**를 적는다. 나머지 1줄은 `subresource` 를 받지 않는 이유의 포인터다. 어느 것도 `prd-resource-generic` AC9 문언의 재진술이 아니다 — AC 는 「네 `patchType` 을 받는다」를 정하고 이 주석들은 「그래서 코드가 왜 이 모양인가」를 적는다. 이하 직전 판정 그대로 — 제거 5줄 — 셋 다 ② `docs/prd-resource-generic.md` 재진술이다. `getSubresources` 의 「셋 다 get verb 라 1:1 이 유지된다」(AC5 문언 그대로이고 바로 아래 맵 리터럴이 셋을 보여 준다), `parseLogOptions` 의 「상한은 클램프하지 않고 거부한다」(AC5 의 「초과 값은 클램프하지 않고 **거부**한다」 축자 — 같은 함수 20줄 아래가 그 거부를 한다), `renderTable` 의 「절단된 목록이 전부인 것처럼 읽힌다」(AC3 의 「절단이 일어났다는 사실을 응답에 명시해, 목록이 전부인 것처럼 오인되지 않게 한다」 축자). 유지 4줄 — 셋의 이름 선행 1줄 doc(AC 포인터)과 `coordinate` 의 1줄 doc. **AC5 의 「거부 대 클램프」 근거는 이 판정으로 사라지지 않는다** — 원장 #4 가 `mcp_test.go` 에서 「상한 초과가 거부여야 하는 이유(클램프는 「호출이 통과했다」류 단언을 그대로 통과시키고 호출자는 덜 받았다는 사실을 끝내 모른다)」를 유지로 판정해 두었고, 이 파일의 세 사본은 그 문장의 약한 되풀이였다. 상세는 [`passes/2026-09-14-resource-read-axis.md`](passes/2026-09-14-resource-read-axis.md), 판정 근거는 `rct_20260914-0002` |
| 2026-09-14 | `tests/integration/resource_read_smoke.py` | 2 | `34dc91e0ba63` | 제거 0줄 — 줄 주석 2줄은 「`행이 하나라도 있다`보다 강한 단언을 고른 이유 — 다른 좌표를 읽고 있어도 행 수만으로는 구별되지 않는다」로, 단언 코드가 말하지 않는 **선택의 이유**다. 이 파일의 docstring 22줄은 아래 docstring 원장에서 같은 패스가 함께 판정했다(표면이 갈려 있어 행도 갈린다). 판정 근거는 `rct_20260914-0002` |
| 2026-09-14 | `tests/integration/resource_generic_ac2.py` · `tests/integration/resource_generic_ac4.py` | 6 | `4c9ff2211279` | 제거 3줄 — ⑴ `ac2` 의 「렌더된 텍스트도 표다 — 헤더 행이 대문자 컬럼명으로 시작한다」(**① 어서션 재진술**: 바로 아래 두 단언이 `block.type == "text"` 와 `startswith("NAME")` 으로 리터럴로 말한다 — 원장 #19·#21 이 지운 것과 같은 형태), ⑵ `ac2` 의 NAME/READY 주석 첫 문장 「`kubectl get deploy` 의 NAME/READY 에 대응하는 열」(**② `docs/test-resource-generic.md#시나리오 2` 기대 결과** 「`NAME`/`READY` 등 `kubectl get` 에 준하는 컬럼이 보존됨」 + ① 바로 아래 튜플 리터럴) — 뒤 문장 「이 둘은 apiserver 의 Deployment Table 정의가 항상 내는 것이라 픽스처가 바뀌어도 흔들리지 않는다」는 **상류 apiserver 동작**이라 남기고 한 줄로 접었다, ⑶ `ac4` `:123` 의 「「그 둘만」 — 키 집합 등가로 과잉 제거까지 잡는다」(**③ 자기 파일 중복**: 같은 파일 모듈 docstring ¶2 가 「포함 관계가 아니라 **등가**로 잰다 … 포함으로 재면 과잉 제거가 통과한다」를 더 길게 말한다 — 사용 지점 사본을 지우고 머리말을 남기는 원장 docstring #5·#8 선례). **유지 6줄**: `MIN_SHRINK_FACTOR` 와 `_DUMP` 의 `#:` 상수 doc(앞엣것은 4.0 이라는 **수치의 근거**로 이어지는 앵커이고, 뒤엣것은 아래 리터럴이 옵션은 보여 주지만 「두 경로를 같은 규칙으로 잰다」는 제약은 보여 주지 않아 「애매하면 남긴다」), 「두 경로가 같은 대상을 보고 있다는 것부터 고정한다 — 다른 좌표를 견주면 크기 비교가 아무것도 말하지 않는다」(**vacuous 방지** — 원장 #4 가 유지로 판정한 계열), 위 ⑵ 의 잔여 한 줄, `ac4` 의 「어노테이션이 last-applied 뿐이었으면 빈 맵을 남기지 않고 키째 지운다」(분기의 이유라 ① 경계에 있으나 판단이 갈려 보존). 이 두 파일의 docstring 42줄은 아래 docstring 원장에서 같은 패스가 함께 판정했다. 상세는 [`passes/2026-09-14-resource-generic-e2e.md`](passes/2026-09-14-resource-generic-e2e.md), 판정 근거는 `rct_20260914-0004` |
| 2026-09-14 | `tests/k8s/kind/resource-generic-fixture.yaml` · `tests/integration/resource_generic_ac1.py` · `tests/k8s/kind/resource-generic-samples.yaml` | 31 | `55477beaf558` | **재판정 2026-09-15**(`rct_20260915-0003` — 시나리오 3 전용 e2e 저작): **+9줄**, 제거 4줄. `resource-generic-fixture.yaml` 에 페이징 ConfigMap 120개가 들어오면서 그 블록에 13줄 머리말이 붙었고, 그중 **4줄을 판정에서 지웠다** — ⚠️ `remainingItemCount` 를 셀렉터 아래서 못 쓴다는 3줄 + 구분 1줄은 **픽스처의 성질이 아니라 그것을 세는 쪽의 사정**이라 실제로 그 단언을 쓰는 `resource_generic_ac3.py` 의 모듈 docstring 이 진다(두 곳에 두면 한쪽이 조용히 낡는 자리다 — 원장 #23·#29 가 「포인터는 남기고 재진술은 지운다」로 처리한 계열의 변형이며, 여기서는 **어느 쪽이 그 사실의 소유자인가**로 갈랐다). **유지 9줄**: 「기본 페이지가 100 이므로 120 이면 두 페이지가 다 관측되는 가장 작은 모집단」(**왜 그 수인가** — 값은 코드로 복원되지만 *왜 120 인가* 는 아니다)과 「전용 레이블로 좁히지 않으면 모집단이 123 이 되고 「나머지 20건」이 성립하지 않는다 · 자동 생성 객체(`kube-root-ca.crt`)에 기대어 수를 맞추는 길은 클러스터 버전과 컨트롤러 타이밍에 의존하므로 택하지 않았다」(**택하지 않은 길과 그 이유** — 지우면 다음 사람이 픽스처를 119개로 줄여 「깔끔하게」 만들고 그 의존을 되살린다). ⑵ `resource_generic_ac1.py` 와 `resource-generic-samples.yaml` 은 **무변경**이다. 이하 직전 판정 그대로 — 제거 11줄 — ⑴ **② 저장소 문서 재진술 6줄.** 같은 커밋(#86)이 `docs/test-resource-generic.md` 「픽스처」 절에 이 픽스처의 성장 규칙과 현재 내용, 그리고 CRD/CR 을 갈라 둔 이유를 새로 써 넣었다. 그래서 `resource-generic-fixture.yaml` 머리말의 「시나리오가 착지할 때마다 그 시나리오가 요구하는 대상만 더한다 … 지금 담긴 것은 시나리오 1 이 요구하는 것」 2줄과 「`kubectl apply` 는 파일을 읽을 때 디스커버리로 종류를 해석하므로 …」 1줄, `resource-generic-samples.yaml` 의 같은 사유 **괄호 사본** 2줄, 그리고 셀렉터 대조군 문단의 「시나리오의 기대 결과가 「셀렉터가 결과 수를 실제로 줄임」이다」 1줄이 전부 그 문서로 한 홉에 복원된다. **세 자리 모두 포인터는 남겼다** — 원장 #23 이 고정한 처리이고(「제거한 것은 포인터가 아니라 그 옆에 붙은 재진술이다」), samples 쪽은 애초에 「별도 파일인 이유는 그 파일 머리말에 있다」는 포인터를 적고도 그 내용을 복사해 두고 있었다. ⑵ **③ 자기 파일 중복 3줄**(`ac1.py`): `#: v1/Event 만 셀렉터가 None 이다 — 좁히는 축이 fieldSelector 라 …` 는 모듈 docstring ¶2 가, `#: 픽스처가 대조군(-other)을 갖는 종류 — 이름 단위 단정까지` 는 ¶1 이, `# 거부가 스코프 때문임을 보이는 대조군: 같은 종류가 namespace 없이는 읽힌다` 는 ¶3 이 각각 더 길게 말한다. 셋 다 **사용 지점 사본을 지우고 머리말을 남겼다**(원장 docstring #5·#8·#10 선례). ⑶ **① 코드 + ② PRD 2줄**: `#: 세는 목록이 잘리면 개수 비교가 무의미해진다. 도구 상한이 500 이므로 그 값으로 뜬다` 는 뒷문장이 `internal/k8s/resource.go:41 ListMaxLimit = 500` 과 `docs/prd-resource-generic.md` AC3(「기본 100, 상한 500」)으로 **이중 복원**되고 앞문장은 같은 파일 `_list` 의 `truncated is False` 단언 위 주석이 같은 것을 말한다 — 그 단언 자리 쪽을 남겼다(원장 #4·#26 이 「상한 500」·「클램프가 아니라 거부」 재진술을 지운 계열). `#: 시나리오가 「일곱 종류」로 세는 좌표와, 그 종류를 좁히는 셀렉터` 도 ② + ①(아래 7개 튜플 리터럴)이고, 이 파일은 `검증 시나리오:` 선언 줄로 이미 그 문서를 가리킨다. **유지 22줄**: 픽스처가 실환경과 갈리는 지점 둘(「kind 클러스터에는 인그레스 컨트롤러가 없다 — 그래도 되는 이유」 3줄, 「이 Service 는 트래픽을 받으려고 있는 것이 아니라 `v1/Service` 좌표로 조회될 대상」 2줄), **무엇이 조용히 통과하는가** 둘(「셀렉터를 통째로 무시하는 구현도 그대로 통과한다」 2줄, 「기본 컬럼(NAME/AGE)만 있으면 CRD 가 자기 컬럼을 실어 보낼 수 있는지가 이 경로에서 관측되지 않는다」 3줄), 「잘린 목록으로 개수를 비교하면 「줄었다」가 페이지 경계의 그림자일 수 있다」(이 단언이 왜 이 형태인가), 그리고 `#:` 상수 doc 셋(픽스처 파일 ↔ 상수 매핑 — 값 자체는 코드로 복원되지만 매핑을 잃으면 상수 이름만 남아 원장 #17 의 `_oidc.py` 판정과 같은 이유로 「애매하면 남긴다」). 상세는 [`passes/2026-09-14-resource-generic-scenario1.md`](passes/2026-09-14-resource-generic-scenario1.md), 판정 근거는 `rct_20260914-0009` |
| 2026-09-14 | `tests/integration/resource_generic_ac20.py` | 14 | `082e1fc7e45e` | 제거 4줄 — ⑴ `#: SERVER_NAMESPACE` 의 「서버 배포가 사는 곳. 파드 정체(재기동 여부)를 여기서 뜬다」(**① 선언 재진술**: 상수 이름 자체와 세 줄 아래 유일한 사용처 `server_pod_identity` 로 한 홉에 복원된다), ⑵ `#: TYPO_KIND` 의 「시나리오가 이름으로 지목한 오타와, 그 오타가 데려와야 하는 실존 종류」(**①+②**: 바로 아래 `TYPO_KIND = "Deploymnt"`·`EXPECTED_CANDIDATE = "apps/v1/Deployment"` 리터럴 + 시나리오 20 의 실행 단계·기대 결과가 두 이름을 다 든다), ⑶ 「CRD 와 그 CR 을 한 매니페스트에 넣지 않는다 — `kubectl apply` 는 파일을 읽는 시점에 …」(**② `docs/test-resource-generic.md` 「픽스처」 절**이 축자로 같은 이유를 적는다 — **같은 주장의 다섯 번째 사본**이고, 직전 패스 `rct_20260914-0009` 가 픽스처·샘플 YAML 의 3·4 번 사본을 포인터로 축약했으므로 **같은 결론·같은 형태**를 따라 포인터 한 줄로 접었다 · 순 1줄), ⑷ 「답이 discovery 메타데이터라는 것 — 객체를 세어 답한 것이 아니라는 것 — 이 여기서 보인다」(**③ 자기 파일 중복**: 모듈 docstring ¶4 의 마지막 절과 같은 말 — 사용 지점 사본을 지우고 머리말을 남기는 원장 docstring #5·#8·#10 선례). **유지 14줄**: `#: PROBE_NAMESPACE` 의 「`tests/k8s/kind/test-deployment.yaml` 이 세운다」(**교차 제약** — 원장 #15 계열), `#: DISCOVERY_TIMEOUT` 2줄(**상류 apiserver 의 문서화되지 않은 동작** — established 뒤에도 discovery 문서에 실릴 때까지 전파 지연이 있다 + 그래서 파드 정체 단언이 함께 있어야 한다는 교차 제약), 프린터 컬럼 2줄과 「이 종류가 **자기** Table 정의로 왔다는 것」 2줄(**둘 다 유지 · 판단 갈림** — 한쪽은 「픽스처가 왜 이 모양인가」, 다른 쪽은 「이 단언이 없으면 무엇이 지나갔겠는가」라 어느 쪽도 다른 쪽으로 복원되지 않는다. 원장 #24 가 `gate.go` ↔ `gate_test.go` 의 서브리소스 주석을 같은 이유로 둘 다 유지했다), 「대조군: 같은 답에 클러스터가 반드시 갖는 종류는 들어 있다 … 빈 답을 준 것과 구별되지 않는다」 2줄(**vacuous 방지** — 원장 #4 계열), 「가장 가까운 것이 먼저 와야 한다 … `Deploymnt` 와 `Deployment` 는 한 글자 차이라 단독 최근접이다」 2줄(**`candidates[0]` 단언이 왜 취약하지 않은가**의 관측. `internal/k8s/resource.go` 의 `rankSimilarKinds` 주석이 같은 계열을 말하지만 **다른 파일의 주석은 복원 경로가 아니다** — 정책의 경로 넷은 코드·문서·PR·커밋이다 — 그리고 저쪽은 「왜 정렬하고 자르는가」라 몫이 다르다), 위 ⑷ 의 잔여 한 줄, `finally` 의 「프로브 종류는 클러스터 전역에 보이므로 뒤따르는 파일에 남기지 않는다」. 이 파일의 docstring 43줄은 아래 docstring 원장에서 같은 패스가 함께 판정했다. 상세는 [`passes/2026-09-14-resource-generic-scenario20.md`](passes/2026-09-14-resource-generic-scenario20.md), 판정 근거는 `rct_20260914-0010` |
| 2026-09-14 | `internal/mcp/resource_test.go` | 42 | `69f92c29cbd1` | **재판정 2026-09-15**(`rct_20260915-0006` — `resource_watch` 등록): **+11줄**, 제거 0. 판정: **유지**. 셋 다 **케이스의 구성 자체가 단언**이라는 것을 적는다 — ⑴ `TestWatchWindowBounds` 의 4줄: 거부 표와 수용 표를 한 케이스에 둔 이유(「61 은 거부된다」는 「60 은 거부되지 않는다」가 옆에 있어야 경계에 대한 단언이 된다). ⑵ `TestWatchCarriesTheResumePoint` 의 3줄: 재개 지점을 나르지 못하는 창은 **다시 시작하는 창**이고 변화가 새는 자리가 바로 거기라는 것 — 통과 조건만 보면 단순 통과 시험으로 읽힌다. ⑶ `TestWatchOnGatedKindRequiresApproval` 의 4줄: 평범한 종류를 **같은 케이스에** 둔 이유(대조군이 없으면 「Secret 이 거부된다」는 게이트가 아니라 도구 전체의 고장으로도 참이다) — 이 파일과 `gate_test.go` 가 같은 vacuous-통과 방지 논거를 여러 자리에서 공유한다. 이하 직전 판정 그대로 — **재판정 2026-09-15**(`rct_20260914-0017` — `resource_update` 등록): **+12줄**, 제거 0. 판정: **전부 유지**. `TestUpdateScaleBounds` 머리 8줄이 셋을 말한다 — ⑴ **왜 허용과 거부가 한 테스트인가**(「이 도구의 경계는 한 문장이다: 0 은 레플리카 수이고 -1 은 아니다」), ⑵ **게이트 호출 수를 세는 이유**(「시나리오 8 이 축자로 적은 절반이다 — 『승인 요청조차 만들지 않음』. 그 성질은 이 검증이 핸들러가 아니라 쌍 해석에서 도는 덕에만 성립한다」), ⑶ **그 처리를 받는 집합의 경계**(「시나리오 8 이 이름 붙인 것뿐이다 — `name` 누락은 여전히 핸들러가, `resource_patch` 와 같은 방식으로 거부한다」). ⑶ 이 없으면 다음 사람이 비대칭을 버그로 읽고 전부 앞으로 당겨 게이트 선언에 인자 검증기를 짓는다. `TestUpdateReplacesWithTheCallersManifest` 머리 4줄은 같은 계열의 반대쪽 — 「apiserver 의 거부는 승인이 소비된 뒤에 온다(AC7)」가 **이 단언이 왜 존재하는가**이고, 실행 코드 쪽 사본(원장 #26 `parseManifest`)과는 서로 복원되지 않는다(위 #26 판정과 같은 근거). ② 재진술은 없다 — AC8 문면은 인용하지 않고 포인터로만 가리킨다. 이하 직전 판정 그대로 — **신규 범위 첫 판정**(`rct_20260914-0012`). #89 가 이 파일을 들여오며 35줄을 잔량으로 선언하고 「자매 모델 `comment-redundancy` 의 몫」이라 적었다(원장 #24 의 「신규 범위의 등재는 그 판정을 한 PR 의 몫」). 그 인계를 여기서 받는다. **제거 16줄** — 셋으로 갈린다. ⑴ **② PRD·테스트 문서 축자 재진술 7줄.** `restartPatch` doc 의 「파드 템플릿을 찍는 strategic 패치, 그 밖은 없다」는 `docs/prd-resource-generic.md` AC9 (「롤링 재시작은 … 파드 템플릿에 `kubectl.kubernetes.io/restartedAt` 어노테이션을 얹는 `strategic` 패치다」) 축자이고 **바로 아래 const 리터럴이 두 번째 사본**이다. `TestPatchTypes` 의 「네 patchType 이 동작하고 호출자가 쓴 이름이 클러스터 층에 도달한다」는 AC9 검증 방법(「네 `patchType`이 각각 동작한다」) + `docs/test-resource-generic.md` 시나리오 9 자동화 절(「`TestPatchTypes`(네 `patchType` + 인자 거부 여섯)」). `TestRestartPatchTouchesOnlyAnnotation` 머리의 두 절은 같은 자동화 절이 **축자로** 적는다 — 「서버가 본문을 고쳐 쓰거나 필드를 더하지 않는다」·「파드 교체와 `spec.replicas` 보존은 apiserver 의 거동이라 단위 층이 볼 수 없고 통합 쪽 몫이다」. `TestRestartPatchIsGatedLikeAnyPatch` 의 「게이트는 verb 로 판정하므로 재시작도 다른 patch 와 똑같이 승인을 받는다」는 AC9 축자(「게이트도 내용이 아니라 verb로 판정하므로, 재시작은 다른 patch와 똑같이 승인을 받는다」)이고, 이어지는 「예외 자체가 우회 경로다」는 `docs/prd-approval-gate.md` AC1 의 「분류기가 하나 더 생기고 그 분류기가 곧 우회 경로가 된다」 — **원장 #24 가 1차 판정에서 `gatedVerbs` doc 4줄을 지운 바로 그 문장**이다. ⑵ **② PRD + ③ 등재된 사본의 약한 되풀이 8줄.** `TestSecretWriteContextRedactsValues` 머리 4줄·그 안 `(masked, …)` 단언 앞 3줄·`TestOrdinaryWriteKeepsItsBodyInTheContext` 머리의 뒷 2절이 전부 **원장 #24 가 유지로 판정해 둔 `internal/mcp/gate.go` 주석의 두 번째 사본**이다 — 「a value on the approval screen has already leaked whether or not the operator then clicks reject」(`maskCredentialValues` doc), 「Secret.data is base64 on the wire, so its length is decoded first — the encoded length answers a different question」(`maskedValue` doc), 「AC16 rests the distinction on whether the content is a credential rather than on what the call does」(`writesASensitiveKind` doc). 셋 다 `prd-resource-generic` AC16 으로도 한 홉에 복원되어 **이중 복원**이다. 사용 지점 사본을 지우고 머리말을 남기는 원장 #28 ⑶ · #29 ⑵ 선례를 그대로 따랐고, **강한 사본이 등재된 범위 안에 살아 있으므로 이 판정으로 지식이 사라지지 않는다**(원장 #26 이 「AC5 의 거부 대 클램프 근거는 이 판정으로 사라지지 않는다」로 고정한 처리). ⑶ **① 어서션 재진술 1줄.** `TestJSONPatchCredentialValuesAreMasked` 의 「보통 필드의 op 는 손대지 않는다」는 바로 아래 `"/metadata/labels/rotated"`·`"true"` 리터럴이 말한다. **AC 포인터는 지우지 않았다** — 원장 #23 이 고정한 처리이고, Go 단위 테스트에는 `검증 AC:` 같은 기계 판독 선언이 없어 그 포인터가 AC ↔ 코드를 잇는 유일한 자리다. 재진술만 걷어 낸 자리는 원장 #23 의 `TestApprovalIsSpentOnce` 선례대로 포인터 1줄로 접었다. **유지 19줄** — 전부 「이 단언이 왜 이 형태인가」·「관측되지 않으면 놓치는 함정」 계열이다. ⑴ **순서 함정** 3줄: 「`apply` 를 매니저 없이 부르면 apiserver 도 거부하지만 그 오류는 **승인이 소비된 뒤에** 도착하므로(`prd-approval-gate` AC7) 도구가 먼저 잡아야 한다」 — AC 문언이 아니라 실패 모드이고, 이 인자 검증이 왜 게이트 *앞*에 있어야 하는지의 근거다. ⑵ **단위/통합 이음매** 3줄: 「통합이 보는 절반의 여집합을 이 층이 단언한다」 — 테스트 문서는 두 절반을 나열할 뿐 *왜 여집합을 단언하는 것이 유효한 대리인가*는 적지 않는다. ⑶ **vacuous 방지** 3줄: 자격증명 상수를 「호출자가 보내는 모양 그대로, 찾아서 부재를 단언할 수 있는 특징적인 문자열」로 두는 이유(원장 #4 의 「부정 절이 본체인 이유」 계열). ⑷ **RFC 6902 함정** 2줄: 「값이 `data` 키 아래가 아니라 `path` 옆에 실려 필드 이름만 훑는 마스커는 이 모양을 그냥 통과시킨다」 — `gate.go` 쪽 `maskJSONPatchOps` 사본과 같은 함정을 가리키지만 한쪽은 「코드가 왜 이렇게 맞추는가」, 다른 쪽은 「이 단언이 없으면 무엇이 지나갔겠는가」라 어느 쪽도 다른 쪽으로 복원되지 않는다 — 원장 #24 가 `gate.go`↔`gate_test.go` 서브리소스 쌍을 「애매하면 남긴다」로 처리한 선례 그대로다. ⑸ 나머지 8줄은 AC 포인터 4줄과, 판단이 갈려 보존한 둘 — 「대조군이다: 전부 가리는 마스커는 유출 테스트를 통과하고 운영자를 배신한다」와 「경로는 남는다 — 어느 키가 다시 쓰이는지 운영자에게 말하는 것이 그것이다」(뒤엣것의 상대편인 「보통 필드의 op 는 손대지 않는다」만 ①로 지웠다). **지문 사각지대 1건을 함께 판정했다** — `internal/mcp/resource_test.go:128` 의 줄 끝 주석 `// "super-secret-bytes"` 은 줄 접두사 패턴(줄 시작이 `//` 이거나 `#` 인 것)에 걸리지 않아 이 행의 19줄에도 게이트에도 보이지 않는다. **유지** — 불투명한 base64 리터럴이 무엇인지 말하는 유일한 자리이고, `(masked, 18B)` 단언의 `18` 이 어디서 오는지도 그 줄만이 준다. 정책 §지문의 사각지대가 예고한 유형이라, 세지 못하는 표면의 판정을 여기 적어 둔다. 상세는 [`passes/2026-09-14-resource-patch-unit.md`](passes/2026-09-14-resource-patch-unit.md), 판정 근거는 `rct_20260914-0012` |
| 2026-09-14 | `scripts/check_doc_inventory.py` | 8 | `cba720423913` | **신규 범위 첫 판정**(`rct_20260914-0015`). #94 가 이 파일을 들여오며 10줄을 잔량으로 선언하고 「판정은 자매 모델 `tbm_homelab-k3s-mcp-comment-redundancy` 의 소관」이라 적어 이 모델 앞으로 넘겼다. 제거 2줄 — ① `MD_LINK_RE` 의 패턴을 한국어로 옮겨 적은 한 줄(캡처가 `.md` 에서 끝나 앵커가 떨어지고 `\s` 가 제목을 자르는 것이 패턴에 그대로 있다), ①+③ `CATEGORIES` 의 「종류 판정 … 인벤토리 표가 말하는 다섯 종류와 같은 구분이다」(리터럴이 앞 절을, **같은 파일 D1 실패 메시지**가 뒤 절을 더 자세히 말한다 — 「doc-tracker 의 「문서 인벤토리」 표와 이 스크립트의 `categorize()` 를 함께 갱신할 것」). 여기에 **줄 수가 움직이지 않은 문장 삭제 2건**(원장 #7·#9 선례): `HUB_LINK_RE` 의 「허브가 뷰어를 통해 여는 문서」(①)를 걷고 **교차 제약**(「`reader.html?doc=` 규약은 doc-tracker 의 「이 레포에 맞춘 뷰어 규약」 절이 정의한다」 — 규약이 바뀌면 이 정규식도 함께 바뀌어야 한다는 사실은 코드에 없다)은 남겼고, `CLAIM_RE` 의 「집계 줄.」(①)을 걷고 **함정**(「형태가 바뀌면 파싱 실패로 멈춘다 — 조용히 0 으로 떨어지지 않게」 · 원장 #11 의 `marked_block` 과 같은 문장형)은 남겼다. **유지 8줄**: 구역 배너 3줄(`# --- D1: 분류 ---` 류)은 **원장 #11 이 같은 레포·같은 종류의 스크립트에서 `# R1 —` 앵커를 「긴 `main()` 의 항해 표지」로 두 번 유지한 선례**를 그대로 따랐다(⚠️ R2 는 판정이 옳은지는 보지 않으므로 이 자리는 다시 열지 않는 한 영구 초록이다 — 다시 연다면 새 패스 문서로 할 것), 「도달 불가 문서 안의 끊긴 링크도 끊긴 링크다 — BFS 는 그 문서를 펴 보지 않으므로 따로 돈다」는 **왜 그 루프가 있는가**라 유지. **판단이 갈려 유지**: 「PRD(도구)와 PRD(공통)는 표에서만 갈리고 집계에서는 한 칸이므로 여기서도 합친다」 — 두 표를 나란히 놓으면 ② 로 도출되지만 **어느 쪽을 따르기로 했는가**는 어느 표에도 없다. 상세는 [`passes/2026-09-14-doc-inventory-gate.md`](passes/2026-09-14-doc-inventory-gate.md), 판정 근거는 `rct_20260914-0015` |
| 2026-09-15 | `tests/integration/resource_generic_ac3.py` | 6 | `66ff2fad5f28` | **신규 범위 첫 판정**(`rct_20260915-0003` — 시나리오 3 전용 e2e 저작). 제거 2줄 — ⑴ **③ 자기 파일 중복**: 모집단을 셀렉터로 좁히는 이유를 적은 2줄이 같은 파일 모듈 docstring ¶1 과 `resource-generic-fixture.yaml` 머리말 양쪽에 있었다(사용 지점 사본을 지우고 머리말을 남기는 원장 #29 ⑵ 의 처리 그대로). **유지 6줄**: `#:` 상수 doc 넷 중 둘은 **교차 제약의 출처를 지목한다** — `DEFAULT_LIMIT`·`MAX_LIMIT` 이 `internal/k8s/resource.go` 의 `ListDefaultLimit`·`ListMaxLimit` 을 가리키므로, 그 상수가 움직이면 이 파일도 따라 움직여야 한다는 사실이 이름만 남으면 사라진다(원장 #17 `_oidc.py` 의 「픽스처 ↔ 상수 매핑은 애매하면 남긴다」 계열). 나머지 둘은 `PAGING_SELECTOR`·`PAGE_FIXTURE_COUNT` 가 **어느 픽스처와 짝인가**와 「기본 페이지보다 커야 1회차가 잘린다」는 제약이다. 본문 2줄은 **왜 구조화 필드만이 아니라 텍스트 본문까지 단언하는가**(「도구를 쓰는 쪽이 구조화 필드를 보지 않고도 「덜 받았다」를 알 수 있어야 한다」)로, 지우면 다음 사람이 그 두 줄을 중복 단언으로 읽고 걷어낸다 |
<!-- /판정-원장 -->

판정 완료 합계 **<!-- 판정-합계 -->1643<!-- /판정-합계 -->줄**, 미판정 잔량
**<!-- 판정-잔량 -->0<!-- /판정-잔량 -->줄**. 전체 대비 비율은 게이트가 출력한다(프로즈에
적으면 낡는다).

> **합계 1532 → 1555, 잔량 0 그대로 (2026-09-15, `rct_20260914-0015` — `container` 누락 거부의
> 후보 이름 보존)**: **+23줄이 전부 이미 등재된 한 범위 안에서 늘었다** — #25
> `internal/k8s/resource{,_test}.go` +23(`resource.go` +9 · `resource_test.go` +14). 그래서 R2 가
> 그 행 하나를 세웠고 같은 PR 에서 재판정해 판정문과 새 지문을 붙였다(전부 유지, 제거 0).
> **잔량은 움직이지 않는다** — 이 슬라이스도 파일을 하나도 새로 만들지 않았다. 바로 위 항목과
> 같은 모양이 연달아 두 번 난 것이고, 「잔량 0 인 채 합계만 움직인다」가 예외가 아님을 보탠다.
> ⚠️ **baseline 이 또 낡았다 — 이번엔 두 번째다.** 이 브랜치의 저작 시점 합계는 1446(→1469)이었는데
> #95 가 1454 로, 이어 #97 이 1532 로 올렸다. **델타는 처음부터 옳았다**(+23): 낡은 것은 기준점뿐이고
> 행 줄 수도 138(=115+23)에서 **175**(=152+23)로 따라 옮겼다. 두 값 모두 손으로 더하지 않고
> `check_comment_policy.py` 의 실패 메시지가 보고한 실측값(`실측 175` · `실측 전체 1555`)에서 뽑았다.
> 같은 행을 두 슬라이스가 연달아 재판정했으므로 판정문도 **#97 의 것 위에 이 슬라이스의 것을 쌓았다** —
> 한쪽을 고르지 않는다(양쪽 다 실재하는 주석을 설명한다).

> **합계 1454 → 1532, 잔량 0 그대로 (2026-09-15, `rct_20260914-0017` — `resource_update` 등록)**:
> **+78줄이 전부 이미 등재된 네 범위 안에서 늘었다** — #24 `internal/mcp/gate{,_test}.go` +13 ·
> #25 `internal/k8s/resource{,_test}.go` +37 · #26 `internal/mcp/resource.go` +16 ·
> #31 `internal/mcp/resource_test.go` +12. 그래서 R2 가 네 행을 전부 세웠고 **같은 PR 에서 재판정**해
> 행마다 판정문과 새 지문을 붙였다(전부 유지, 제거 0). **잔량은 움직이지 않는다** — 이 슬라이스는
> 파일을 하나도 새로 만들지 않았고, 신규 범위가 없으면 선언할 미판정도 없다. 래칫이 이렇게 도는
> 경우가 기록상 처음이라 적어 둔다: 지금까지의 슬라이스는 전부 신규 파일을 들고 와 「합계 + 잔량」이
> 함께 움직였고, **잔량이 0 인 채로 합계만 움직이는 모양도 정상**이다.
> ⚠️ **baseline 은 한 번 낡았다** — 저작 시점 합계는 1446 이었고 바로 위 항목(#95, `rct_20260914-0015`)이
> `scripts/check_doc_inventory.py` 10줄을 원장 #32 로 등재하며 1454 로 올린 뒤 이 브랜치를 리베이스했다.
> **델타는 처음부터 옳았다**(+78): 낡은 것은 기준점뿐이다(1454 + 78 = 1532, 1532 + 0 = 1532 = 실측 전체).
> 두 값 모두 손으로 더하지 않고 `check_comment_policy.py` 의 실패 메시지가 보고한 실측값과 그 게이트의
> `comment_lines()` 파일별 재계수에서 뽑았다.

> **잔량 10 → 0 (2026-09-14, `rct_20260914-0015` — 허브 집계 게이트 판정 슬라이스)**: 바로 아래
> 문단이 잔량으로 선언한 `scripts/check_doc_inventory.py` 10줄을 원장 #32 로 등재했다(제거 2 · 유지 8).
> 합계는 1446 → 1454, 판정 대상 전체는 1456 → 1454 이고 **1454 + 0 = 1454 로 R4 가 닫힌다.**
> 같은 PR 이 docstring 표면의 31 도 함께 닫아 **두 표면의 잔량이 동시에 0 이 되는 첫 사례**가 됐다.
> 래칫 → 잔량 선언 → 소관 모델의 재감지 → 판정 → 잔량 회수로 한 바퀴 도는 **다섯 번째 사례**다.
> 모든 값은 손으로 더하지 않고 `check_comment_policy.py` 출력과 그 게이트의 `comment_lines()`
> 파일별 재계수에서 뽑았다.

> **잔량 0 → 10 (2026-09-14, `rct_20260914-0012` — 허브 집계 게이트 슬라이스)**: 신규 파일
> `scripts/check_doc_inventory.py` 가 줄 주석 10줄을 들고 들어왔다. 다른 파일의 주석은 한 줄도
> 움직이지 않았다(게이트 자신의 `comment_lines()` 로 파일별 재계수해 확인: 1446 + 10 = 1456).
> 판정이 아니라 잔량을 올리는 쪽을 고른 이유는 아래 선례들과 같다 — 판정은 자매 모델
> `tbm_homelab-k3s-mcp-comment-redundancy` 의 소관이고, 이 슬라이스의 축은 허브 집계다.

> **잔량 0 → 33 (2026-09-14, `rct_20260914-0006` — 시나리오 1 슬라이스)**: 신규 세 파일이
> 줄 주석 33줄을 들고 들어왔다 — `tests/k8s/kind/resource-generic-fixture.yaml` 20줄,
> `tests/integration/resource_generic_ac1.py` 9줄, `tests/k8s/kind/resource-generic-samples.yaml`
> 4줄. 다른 파일의 주석은 한 줄도 움직이지 않았다(게이트 자신의 `comment_lines()` 로 파일별
> 재계수해 확인: 1315 + 33 = 1348). 판정이 아니라 잔량을 올리는 쪽을 고른 이유는 아래
> docstring 절의 선례와 같다 — 판정은 자매 모델 `tbm_homelab-k3s-mcp-comment-redundancy` 의 소관이다.
> **2026-09-14 — 잔량이 18 → 53 으로, 합계가 1337 → 1413 으로 움직였다**(`rct_20260914-0008`, `resource_patch` 슬라이스). ⚠️ **이 문단의 baseline 은 두 번 낡았다** — 저작 시점에는 33 이었고, 계획 중 #87 이 머지되며 `resource_generic_ac20.py` 가 줄 주석 18줄·docstring 43줄을 들여와 51/68 로 고쳐 적었다. 그런데 **머지 직전 #88 이 착지해 그 33 을 원장 #29 로 등재**하는 바람에 실제 baseline 은 51 이 아니라 18 이었고, 위 수치는 그 사실을 반영하지 못한 채 머지됐다(`rct_20260914-0012` 가 바로잡았다 — 「51 → 86 / 1315 → 1391」이 당시 본문이다). **델타는 처음부터 옳았다**(+35 · +76): 낡은 것은 기준점뿐이다. **두 슬라이스의 잔량은 더해진다**(18 + 35 = 53). 둘은 성질이 다르다. **합계 +76** 은 등재된 세 범위(#24 `gate{,_test}.go` +39 · #25 `k8s/resource{,_test}.go` +27 · #26 `mcp/resource.go` +10)가 R2 에 걸려 **같은 PR 에서 재판정된 결과**이고 — 그래서 그 행들의 결과 칸에 판정문이 붙어 있다 — **잔량 +35** 는 신규 파일 `internal/mcp/resource_test.go` 한 개다. 신규 범위의 판정은 자매 모델 `tbm_homelab-k3s-mcp-comment-redundancy` 의 소관이라 여기서는 잔량만 올린다(#79 선례). 두 값 모두 손으로 더하지 않고 `check_comment_policy.py` 출력과 그 게이트의 `comment_lines()` 파일별 재계수에서 뽑았다.
>
> ⚠️ **합계는 리베이스에서 한 번 움직였다** — 저작 시점 baseline 은 1291 이었으나 그 사이 #82 가
> `internal/k8s/resource{,_test}.go` 의 +24줄을 등재해 1315 가 됐다. 잔량 33 은 우리 신규 세 파일의
> 실측이라 불변이고, 두 값 모두 손으로 더하지 않고 `check_comment_policy.py` 출력에서 다시 뽑았다.

> **잔량 35 → 0 (2026-09-14, `rct_20260914-0012` — `resource_patch` 단위 테스트 슬라이스)**:
> 위 문단이 잔량으로 선언한 `internal/mcp/resource_test.go` 35줄을 원장 #31 로 등재했다
> (제거 16 · 유지 19). 합계는 1427 → 1446, 판정 대상 전체는 1462 → 1446 이고
> **1446 + 0 = 1446 으로 R4 가 닫힌다.** 잔량이 0 으로 떨어진 것은 형제 슬라이스
> `rct_20260914-0010`(PR #90, 원장 #30)이 몇 분 앞서 착지해 `resource_generic_ac20.py` 쪽
> 잔량을 이미 닫아 둔 덕이다 — 두 슬라이스의 파일 집합은 서로소라 서로의 판정에 닿지 않았고,
> 겹친 것은 이 원장의 표와 마커뿐이라 **리베이스 후 마커를 게이트 출력에서 다시 뽑았다.**
> 이번에도 유입(#89 머지 13:33Z)에서 판정까지 같은 날 안에 닫혔고,
> 래칫 → 잔량 선언 → 소관 모델의 재감지 → 판정의 **네 번째 사례**다. 모든 값은 손으로 더하지 않고
> `check_comment_policy.py` 출력과 그 게이트의 `comment_lines()` 파일별 재계수에서 뽑았다.

> **잔량 마커는 2026-09-13 에 세웠다.** 그전까지 이 표면에는 합계 마커밖에 없었고, R1~R3 은
> **등재된 행의 범위만** 재측정하므로 **어느 행에도 없는 새 파일은 검사 대상 자체가 아니었다.**
> 승인 게이트 판정 코어 슬라이스(PR #73)가 새 파일 넷으로 주석 192줄을 들여왔을 때 게이트는
> 그대로 rc=0 이었고, 움직인 것은 stdout 의 백분율 한 줄뿐이었다(100.0% → 84.3%). 아래
> docstring 표면이 R8 로 막아 온 것을 이 표면에서는 아무것도 막지 않고 있었던 셈이다.
> R4 의 뒷절반(`합계 + 잔량 == 실측`)이 그 자리를 메운다 — 잔량을 올려 통과시키는 것은
> 여전히 정상적인 선택이지만, 이제 **diff 에 남는 명시적 선언**이 된다.

> **잔량 0 → 87 (2026-09-14, `rct_20260913-0002`)**: `resource_*` 읽기 축 슬라이스가 신규 파일
> 셋(`internal/k8s/resource.go`·`internal/k8s/resource_test.go`·
> `tests/integration/resource_read_smoke.py`)으로 주석 87줄을 들여왔다. **세운 지 하루 만에 이
> 래칫이 실제로 걸린 첫 사례이고, 걸린 방식이 설계대로다** — 게이트가 조용히 rc=0 을 주는 대신
> 이 줄을 diff 에 쓰게 만들었다. 판정이 아니라 잔량을 고른 이유는 주석 비중복성 판정이 자매 모델
> `tbm_homelab-k3s-mcp-comment-redundancy` 의 소관이고, 이 슬라이스가 겸하면 자기 PR 에서 자기
> 주석을 판정하는 모양이 되기 때문이다. **반면 등재된 범위(`gate.go`·`gate_test.go`)는 바뀌었으므로
> 재판정했다** — R2 에는 잔량 같은 유예가 없고, 있어서도 안 된다.

> **잔량 87 → 0 (2026-09-14, `rct_20260914-0002`)**: 위 잔량이 지목한 신규 범위를 자매 모델이
> 인계받아 전수 판정하고 위 세 행으로 등재했다 — 래칫이 **인계까지 한 바퀴 돈 첫 사례**다(걸림 →
> 잔량 선언 → 소관 모델의 재감지 → 판정 → 잔량 0). **그 잔량 노트의 괄호는 신규 파일을 셋만
> 열거했으나 실측은 넷이고, 빠진 것은 `internal/mcp/resource.go`(9줄)다** — 숫자 87 은 그 9줄을
> 포함해 맞았고 원장 #6 행도 그 파일을 따로 인지했으므로 산술과 인지는 정상이었다. 낡은 것은 그
> 열거 산문 하나였고, 이 줄이 그 정정이다(당시 사실을 적은 기록이므로 위 노트 자체는 고치지 않는다).

> **잔량 0 → 9 (2026-09-14, `rct_20260914-0002`)**: 시나리오 ↔ e2e 슬라이스가 신규 파일 둘
> (`tests/integration/resource_generic_ac2.py`·`resource_generic_ac4.py`)로 줄 주석 9줄을 들여왔다.
> 위 `0 → 87` 항목과 같은 처리이며 같은 이유다 — 주석 비중복성 판정은 자매 모델
> `tbm_homelab-k3s-mcp-comment-redundancy` 의 소관이라 자기 PR 에서 겸하지 않는다.
> **등재된 범위는 한 행도 바뀌지 않았다**(이 슬라이스는 기존 `.py`·`.go` 의 주석을 건드리지 않고
> 문서와 신규 e2e 만 더한다) — 27개 행의 지문이 그대로 재현되는 것이 그 증거다.
> 바로 위 항목이 잔량을 0 으로 닫은 **직후** 다시 9 가 선다는 사실이 래칫의 정상 동작이다 —
> 0 은 종점이 아니라 다음 유입이 즉시 걸리는 상태다(아래 docstring 표면의 같은 문단 참조).

> **잔량 9 → 0 (2026-09-14, `rct_20260914-0004`)**: 위 잔량이 지목한 신규 파일 둘을 소관 모델이
> 인계받아 전수 판정하고 위 마지막 행으로 등재했다 — **래칫이 한 바퀴 돈 두 번째 사례**이고, 이번엔
> 걸림에서 인계까지 **같은 날 안에** 닫혔다. 제거 3줄은 전부 이 원장이 이미 같은 이름으로 지워 온
> 형태다(① 어서션 재진술 · ② 시나리오 문서 재진술 · ③ 자기 파일 중복). **판정 절차 4번(「원본이
> 부실하면 원본을 고친다」)은 발화하지 않았다** — 지운 ② 두 건을 `docs/test-resource-generic.md`
> 시나리오 2·4 와 한 줄씩 대조했고, 주석이 문서보다 더 정확한 자리는 없었다. 그래서 이 패스는
> 문서를 고치지 않는다.

> **잔량 33 → 51 (2026-09-14, `rct_20260914-0007`)**: 시나리오 20 의 전용 e2e
> `tests/integration/resource_generic_ac20.py` 가 줄 주석 18줄을 들고 들어왔다. 판정이 아니라 잔량을
> 고른 이유는 위 `rct_20260913-0002`·`rct_20260914-0002` 항목과 같다 — 주석 비중복성 판정은 자매 모델
> `tbm_homelab-k3s-mcp-comment-redundancy` 의 소관이고, 이 슬라이스가 겸하면 자기 PR 에서 자기 주석을
> 판정하는 모양이 된다. **등재된 범위는 이 PR 이 한 줄도 건드리지 않았으므로 재판정은 없다** — 신규
> 파일 하나만 늘었다.

> **잔량 51 → 18 (2026-09-14, `rct_20260914-0009`)**: 위 `0 → 33` 이 지목한 신규 세 파일을 소관
> 모델이 인계받아 전수 판정하고 위 마지막 행으로 등재했다 — **래칫이 한 바퀴 돈 세 번째 사례**이고,
> 이번에도 걸림(#86, 12:16Z)에서 판정까지 같은 날 안에 닫혔다. 제거 11줄은 전부 이 원장이 이미 같은
> 이름으로 지워 온 형태다(② 저장소 문서 재진술 · ③ 자기 파일 중복 · ① 코드 재진술). **이번 ② 여섯
> 줄의 복원처는 트리거 자신이 만들었다** — #86 이 주석과 `docs/test-resource-generic.md` 「픽스처」
> 절에 같은 사유를 나란히 쓰면서 그 순간 사본이 됐다. **판정 절차 4번(「원본이 부실하면 원본을
> 고친다」)은 발화하지 않았다** — 지운 ② 여섯 줄을 그 문서와 한 줄씩 대조했고, 주석이 문서보다 더
> 정확한 자리는 없었다. 그래서 이 패스도 문서를 고치지 않는다.
> ⚠️ **0 이 아니라 18 로 내려앉는다** — 이 판정을 저작하는 사이 바로 위 항목(#87)이 머지되어 잔량이
> 33 에서 51 로 올랐다. 남는 18 은 **그 슬라이스의 신규 파일 `resource_generic_ac20.py` 몫**이고 이
> 패스의 범위가 아니다(내 세 파일 33줄은 전량 판정돼 22줄로 등재됐다: 1315 + 22 = 1337, 1337 + 18 =
> 실측 1355). 그 18줄은 재감지가 새 task 로 잇는다.

> **잔량 53 → 35 (2026-09-14, `rct_20260914-0010`)**: 바로 위 항목이 「이 패스의 범위가 아니다」로
> 넘긴 18줄 — 신규 파일 `tests/integration/resource_generic_ac20.py` — 을 같은 소관 모델이 이어받아
> 전수 판정하고 위 마지막 행으로 등재했다. **래칫이 한 바퀴 돈 네 번째 사례**이고, 앞 항목과 **연달아**
> 닫혔다는 점이 새롭다. 제거 4줄은 전부 이 원장이 이미 같은 이름으로 지워 온 형태다(① 선언 재진술
> 2건 · ② 시나리오 문서 재진술 1건 · ③ 자기 파일 중복 1건). ② 한 건은 「CRD 와 CR 을 한 파일에 두면
> `kubectl apply` 가 파일 전체를 거부한다」의 **다섯 번째 사본**이라, 바로 위 패스가 3·4 번 사본
> (픽스처·샘플 YAML)에 내린 것과 **같은 결론·같은 형태**(포인터 축약)를 따랐다 — 같은 주장을 두
> 방향으로 판정하면 그 불일치가 다음 사람의 판단을 흔든다.
> **이 패스가 걷어낸 몫은 정확히 18 이고, 그것이 이 표면의 전부는 아니다** — 1413 + 14 = 1427 = 원장
> 행 줄 수 합, 1427 + 35 = 1462 = 실측. 네 마커는 손으로 더하지 않고 `check_comment_policy.py`
> 출력에서 다시 뽑았다.
> **저작 시점 baseline 이 두 번 움직였다** — 처음엔 51 이었고 준비 중에 #88 이 머지되어 18 이 됐으며,
> 실행 직전 **#89 가 착지해 53 이 됐다**(그 증분 35 는 신규 파일 `internal/mcp/resource_test.go` 하나로,
> 위 `51 → 86` 항목이 스스로 잔량으로 선언한 몫이다). 세 번의 이동을 모두 리베이스에서 재측정으로
> 흡수했고, **이 파일만의 실측인 두 원장 행의 줄 수·지문(`14`/`082e1fc7e45e` · `36`/`2412aefb8ac7`)은
> 세 번 모두 바이트 동일했다** — 남의 유입이 내 판정 범위를 건드리지 않았다는 기계적 증거다.
> **그래서 이 표면은 0 으로 돌아오지 않았고, 그것이 정상이다** — 래칫이 재는 것은 「잔량이 0 인가」가
> 아니라 「이 패스가 맡은 범위가 잔량에 남아 있는가」이고, 남은 35 는 다음 감지가 새 task 로 잇는다.
> (docstring 표면은 같은 시점에 0 으로 돌아왔다 — #89 는 Python 을 들이지 않았다.)

## docstring 판정 이력

위 원장이 재는 것은 `^\s*(//|#)` 표면이다. Python docstring 본문은 **같은 정책이 적용되지만
그 표면에 잡히지 않으므로**(`README.md`의 「지문의 사각지대」), 원장을 하나 더 둔다. 표면이
갈려 있으므로 위 22행은 이 표면이 생겨도 한 줄도 흔들리지 않고, docstring 판정은 슬라이스마다
누적된다.

측정 단위는 `ast` 로 뜯은 **module·class·function docstring 의 본문 줄**이다. 빈 줄은 세지
않고(문단을 옮기기만 해도 재판정 대상이 되면 안 된다), 기계가 읽는 선언 줄(`검증 AC:` ·
`실행 대상:` · `추가 인자:` · `실행 순서:`)도 제외한다 — 그 줄들은 유지 대상 1번이다.

`scripts/check_comment_policy.py` 가 매 CI에서 **R5~R8** 로 이 표를 강제한다. R8 이 특히
중요하다: **합계 + 미판정 잔량 == 실측 전체**를 양방향으로 요구하므로, 새 파일이 docstring 을
들고 들어오면 CI 가 멈춘다. 잔량을 올려 통과시키는 것은 정상적인 선택이지만 **diff 에 남는
명시적 선언**이 된다 — 「아직 판정하지 않았다」가 조용히 넘어가지 않게 하는 것이 목적이다.

<!-- docstring-원장 -->

| 판정일 | 범위 | docstring 줄 | 지문 | 결과 |
| --- | --- | ---: | --- | --- |
| 2026-09-05 | `scripts/check_comment_policy.py` · `scripts/check_mock_policy.py` | 73 | `90354837bf2f` | **재판정 2026-09-10**(`rct_20260910-0001` — 정책 디렉터리 이전): 줄 수 불변(70), 지문만 이동 — 이 PR 이 모듈 docstring 첫 문장의 to-be 서술을 고쳤다(`docs/comment-policy.md` 자신 → `docs/comment-policy/` 디렉터리). 모델 개정으로 to-be 의 실체가 단일 blob 에서 디렉터리 tree hash 로 바뀌었으므로 그 문장은 **경로만이 아니라 내용이 낡은** 자리였다. 판정: **유지** — 문단의 존재 이유(「이 게이트가 왜 있는가」= 프로즈 수치가 조용히 낡는다는 관측된 사건)는 그대로이고 바뀐 것은 사실관계뿐이다. 줄 주석 표면은 한 줄도 건드리지 않아 원장 #11 과 모델 as-is 지문은 불변이다. 이하 1차 판정 그대로 — 제거 0줄 — 두 체커의 모듈 docstring은 **규칙 목록**과 「왜 이 게이트가 있는가」다. 규칙 목록은 코드로 복원되는 것처럼 보이지만 실패 메시지 어디에도 규칙 **집합**은 없다(각 메시지는 자기 규칙만 말한다) — 줄 주석 원장 #11이 같은 두 파일의 `# R1 —` 앵커를 「긴 `main()`의 항해 표지」로 유지한 것과 같은 이유로 유지한다. `check_comment_policy.py`가 적은 「정책 문서가 착지하던 날 848줄이라 적힌 사이 형제 PR로 963이 됐고 아무것도 알려주지 않았다」는 **관측된 사건**이고 이 게이트의 존재 이유 자체라 ③④로 도달 가능하더라도 「애매하면 남긴다」로 보존. `marked_block`/`ledger_block`의 「정규식 끝 앵커(`$`)로 뜯지 않는다 — 0행이 '위반 0'으로 보여 초록으로 새어 나간다」는 두 파일에 같은 문장으로 있으나 각자 그 자리의 함정이고 한쪽을 지우면 다른 파일에서 복원해야 하므로 둘 다 유지. `check_comment_policy.py`의 docstring은 **이 PR이 R5~R8을 더하며 다시 쓴 것**이고, 그 상태로 판정해 등재한다 · **재판정 2026-09-13**(`rct_20260913-0002` — 줄 주석 표면 잔량 래칫): +3줄 — R4 항목이 「합계 + 잔량 == 실측」과 **그 절반이 뒤늦게 붙은 경위**(PR #73 이 새 파일 넷으로 192줄을 들여왔을 때 이 게이트가 rc=0 이었다)를 담도록 다시 쓰였다. 판정: **유지** — R5~R8 을 더하던 때와 같은 이유다. 규칙 **집합**은 실패 메시지 어디에서도 복원되지 않고(각 메시지는 자기 규칙만 말한다), 「왜 이 규칙이 있는가」는 848줄 일화와 같은 성질의 **관측된 사건**이다 |
| 2026-09-05 | `tests/integration/run_all.py` · `tests/integration/check_ac_mapping.py` | 92 | `2bfbd9230d9d` | **재판정 2026-09-08**(축 개정 PR): +17줄 — 두 모듈 docstring 이 시나리오 축으로 다시 쓰였다. 늘어난 것은 ⑴ 체커의 규칙 열거에 규칙 4(예외)·규칙 6(구현 대기)·**불변식**이 추가된 것, ⑵ 「축이 옮겨졌다 / AC 는 파일 → 시나리오 → AC 로 한 홉에 복원된다 / 파일명은 개명하지 않았다」를 적은 개정 노트, ⑶ 서수 식별자의 취약성과 그것을 제목 대조로 잡는다는 근거. 셋 다 **코드가 스스로 말하지 못하는 것**(왜 이 축인지·왜 파일명은 그대로인지·제목 칸이 왜 있는지)이라 남긴다 — 특히 ⑶ 은 지우면 제목 대조가 「불필요한 엄격함」으로 보여 다음 사람이 뗀다. 1차 판정의 근거는 그대로 유효하다(아래) — 제거 0줄, `run_all.py`의 「파일이 신고하는 것」 블록은 **이 레포에서 그 규약의 SSOT**이다(지우면 60개 파일이 무엇을 선언해야 하는지가 어디에도 없다). `check_ac_mapping.py`의 규칙 열거는 `docs/doc-tracker.md`가 같은 번호를 쓰지만 축이 다르다 — 그쪽은 **모델이 요구하는 것**이고 이쪽은 **이 스크립트가 실제로 검사하는 것**이며, 그 증거로 여기엔 규칙 4가 없다(예외 등재는 체커가 계수에서 빼는 데이터이지 검사하는 규칙이 아니다). `check_cases_are_run`의 「배차만으로는 부족하다 — 디스패처가 케이스를 부르는 줄을 빠뜨려도 exit 0이라 CI가 초록으로 통과한다」와 `check_test_docs`의 「2026-09-04에 그 어긋남이 세 번의 감지를 통과했다」는 관측된 실패 모드다. `_scenario_automation`의 「필드는 여러 줄로 이어질 수 있으므로 다음 `- **` 불릿까지를 한 필드로 본다」는 파싱 경계의 근거로 코드보다 앞선다 |
| 2026-09-05 | `tests/integration/smoke.py` | 14 | `651f6ba1f0b5` | **재판정 2026-09-08**(`rct_20260908-0003` — namespace-list 시나리오 2 슬라이스): 줄 수 불변(14), 지문만 이동 — 낡은 산문 3곳을 고쳤다: 개명 전 모델 id `tbm_homelab-k3s-mcp-ac-e2e` → `-scenario-e2e`, 「규칙 3은 AC 대신」 → 「시나리오 대신」, 그리고 게이트가 실제로 파싱하는 절 이름 「비-AC 파일」 → 「비-시나리오 파일」(첫 줄 괄호와 본문 2곳). 판정: **유지** — 문단의 존재 이유가 「이 파일이 규칙 3 등재분인 근거」라 바뀐 것은 사실관계뿐이고 형태가 아니다. 이하 1차 판정 그대로 — 제거 0줄 — 「`실행 순서: 0`으로 그룹 맨 앞에서 돌기 때문에 배포가 깨졌을 때 뒤따르는 파일들이 차례로 모호하게 죽는 대신 여기서 한 번에 원인을 말한다」(배차 상수 자체는 선언 줄이지만 그 **이유**는 어디에도 없다), 「이것은 platform-auth-safety/AC5가 아니다 — 모든 통합이 구성된 배포의 정상 tools/list는 degradation에 대해 아무것도 말해 주지 않는다」, 「두 파일이 같은 `_helpers.EXPECTED_TOOLS`를 읽는 것은 의도한 것: 도구 표면은 구성과 무관하게 `internal/mcp/toolslist.go`가 정적으로 선언한다」가 전부 복원 경로 넷 어디에도 없다. 「(파일 수는 분할이 진행될수록 늘어나므로 적지 않는다)」는 이 문서 `범위` 절과 같은 규율의 실천이라 유지 |
| 2026-09-06 | `tests/integration/_helpers.py` · `tests/integration/_session_platform.py` · `tests/integration/_workload.py` · `tests/integration/_oidc.py` · `tests/integration/_opensearch.py` · `tests/integration/_auth_variant.py` · `tests/integration/_aws_config.py` | 220 | `1f77587a4101` | 제거 7줄(물리 13줄) — **공유 헬퍼 7파일 전수 판정**. ⑴ 지운 것은 딱 한 종류다: 6개 모듈 docstring의 둘째 문단 「`_` 접두라 `run_all.py::matching_unit_paths()` 가 걸러내므로 AC를 주검증하지 않고 … 선언도 갖지 않는다」. ① 코드로 한 홉에 복원된다 — `run_all.py:89`의 `not p.name.startswith("_")` 하나이고, `check_ac_mapping.py`는 `run_all.declarations()`를 통해 같은 필터를 쓴다. 앞선 슬라이스가 `*_ac*.py` 60파일에서 지운 정형 문단과 **같은 계열의 부정형**이다. 요약 줄의 「(매칭 단위가 아니다)」 괄호는 `smoke.py` 행이 같은 자리의 「(규칙 3 비-AC 파일 …)」을 유지한 선례를 따라 남긴다. ⑵ `_session_platform.py`는 **한 docstring 안의 자기 중복** 2줄도 지웠다 — 「`List` reads only these ConfigMaps and never looks at pods, so a seeded session is a complete one as far as listing is concerned」가 *Seeding* 문단과 마지막 문단에 두 번 있어, 뒤엣것이 앞엣것으로 복원된다. ⑶ 나머지 213줄은 전부 유지다: `_helpers.py`의 「MCP SDK가 `notifications/initialized`에 HTTP 200 + JSON-RPC 오류 본문을 받아 read loop가 죽으므로 `session.initialize()`를 건너뛴다」(상류 SDK와 서버의 문서화되지 않은 어긋남)·`port_forward`의 「TCP 연결 성공은 준비 신호가 되지 못한다 — 로컬 포트는 즉시 열리고 파드 연결은 첫 요청에서 맺는다」(함정)·`assert_assumed_role_access`의 세 관측이 각각 무엇을 배제하는가(보안 픽스처가 서명 없는 요청을 **불평 없이 받아들인다**는 것이 근거), `_workload.py`의 「2026-09-03 분할 전 고정 순서 → 파일이 자기 선행 조건을 스스로 성립시킨다」 설계 경위와 `wait_for_single_ready_pod`의 「`rollout status` 반환으로는 부족하다 — 밀려난 파드가 아직 Terminating이면서 같은 라벨을 달고 있다」, `_opensearch.py`의 「토큰을 영숫자로 짓는 이유 — 표준 애널라이저가 나머지를 다 쪼갠다」와 SigV4(`aoss`)·refresh 지연, `_oidc.py`의 「AC8은 네 구성을 대조하는 것 자체가 AC라 그룹당 포워드 하나로는 관측할 수 없다」와 `port_forward` 이전 경위, `_session_platform.py`의 seed↔create 갈림과 ConfigMap 표현이 `control-plane/internal/adapter/configmap` 소유라는 사실 — 어느 것도 복원 경로 넷에 없다. ⑷ 측정 메모: 지운 문단 13줄 중 6줄은 프로즈 안에 `검증 AC:` 리터럴을 담고 있어 **이미 지시어로 분류돼 인구조사에서 빠져 있었다**(그래서 물리 13줄 : 계측 7줄). 기계 판독 선언 줄은 한 글자도 건드리지 않았고, 이 7파일은 애초에 매칭 단위가 아니라 선언을 갖지 않는다 |
| 2026-09-07 | `tests/integration/session_list_ac1.py` · `tests/integration/session_list_ac2.py` · `tests/integration/session_list_ac3.py` · `tests/integration/session_read_ac1.py` · `tests/integration/session_read_ac3.py` · `tests/integration/session_read_ac4.py` · `tests/integration/session_write_ac1.py` · `tests/integration/session_write_ac3.py` · `tests/integration/session_write_ac5.py` | 220 | `85c94a02f46f` | 제거 12줄 — ⑴ `session_list_ac3.py`·`session_read_ac4.py`·`session_write_ac5.py` 의 「The shared assertion lives in `_auth_variant.assert_unavailable_refusal` … 」 문단(각 4줄). ① 한 홉으로 복원된다 — 바로 아래 import 가 그 심볼을 들여오고, `assert_unavailable_refusal` 자신의 docstring 이 같은 두 절(정상 MCP 결과로 `isError` 를 싣는다 · 뒤이은 `ping` 이 산다)을 이미 적는다. ⑵ `session_write_ac3.py` 케이스 docstring 본문 2줄(「메타데이터만 읽고 실행하지 않는다」) — 같은 파일 모듈 docstring(③)과 `assert_destructive_annotation` docstring(①) 양쪽에 있다. 요약 줄은 관례 최소 doc 이라 남긴다. **판단이 갈려 유지한 것 셋**: ⓐ 이 셋의 `실행 대상: auth-variant` 문단은 `_auth_variant.py` 의 것과 **첫 두 줄만** 같고 나머지는 `SESSION_PLATFORM_ENDPOINT` 가 base 배포에만 있다는 고유 논증이라 통째로 유지(형제 6파일의 **완전 동일** 사본과 갈리는 지점이 정확히 이것이다), ⓑ `session_read_ac4.py`·`session_write_ac5.py` 의 「id 는 실재하지 않는 것을 고른다」 문단은 같은 파일 `#:` 줄 주석과 겹치지만 「AC3 의 not-found 와 혼동될 수 없다」를 그쪽이 말하지 않고, 그 `#:` 는 줄 주석 원장 #19 소관이라 이 표면에서 손댈 수 없다, ⓒ `session_write_ac3.py` 의 「같은 단언의 in-process 판은 `TestToolsListAdvertisesAnnotations` 이며 이 파일은 그것을 배포 계층으로 승격한 것이다」는 헬퍼 docstring 으로 복원되지만 「단언이 vacuous 하지 않은 이유」 논증에 엮여 있어 잘라내면 그 논증이 끊긴다. 나머지 220줄은 유지 — `session_read_ac1.py` 의 「2·4번 절이 vacuous 하지 않은 이유는 `scrollback.Since` 가 그것을 **구조로** 만들기 때문」과 `_quiet_end` 의 「쉘이 자기 일정으로 쓰므로 write 중간에 읽으면 AC 와 무관한 이유로 실패한다」, `session_write_ac1.py` 의 「마커를 명령문 안에 두면 PTY 에코만으로 단정이 성립해 버린다」와 「claude-code 절은 `DATA_PLANE_CLAUDE_CODE_IMAGE` 가 없어 배포 자체가 불가」, `session_read_ac3.py` 의 「두 에러의 층이 다른 것은 의도된 설계 — `-32602` 는 HTTP 이전에 거부하므로 「세션을 건드리지 않는다」가 약속이 아니라 구조가 된다」, `session_list_ac2.py` 의 「스텁 앞에서는 파드 불변 단정이 vacuous 하다」가 전부 복원 경로 넷 어디에도 없다 |
| 2026-09-07 | `tests/integration/opensearch_document_delete_ac1.py` · `tests/integration/opensearch_document_delete_ac2.py` · `tests/integration/opensearch_document_delete_ac3.py` · `tests/integration/opensearch_document_delete_ac4.py` · `tests/integration/opensearch_document_delete_ac5.py` · `tests/integration/opensearch_document_put_ac1.py` · `tests/integration/opensearch_document_put_ac2.py` · `tests/integration/opensearch_document_put_ac3.py` · `tests/integration/opensearch_document_put_ac4.py` · `tests/integration/opensearch_document_put_ac5.py` · `tests/integration/opensearch_search_ac1.py` · `tests/integration/opensearch_search_ac2.py` · `tests/integration/opensearch_search_ac3.py` · `tests/integration/opensearch_search_ac4.py` | 100 | `0cd15939e543` | 제거 22줄 — ⑴ `*_ac5.py` 둘과 `opensearch_search_ac4.py` 의 auth-variant 문단(각 5줄): `_auth_variant.py` 모듈 docstring 의 같은 문단과 **바이트 동일**한 사본이고, 세 파일 모두 그 모듈에서 심볼을 import 한다. 원장 4행이 `_*.py` 6개에서 지운 정형 문단과 같은 계열이다. ⑵ 같은 세 파일의 「The shared assertion lives in …」 문단(각 4줄) — `assert_unavailable_refusal` docstring 의 재진술(①). ⑶ `opensearch_document_{delete,put}_ac3.py` 의 케이스 docstring 본문 2줄씩 — 모듈 docstring 의 「`tools/list` 메타데이터만 읽는다」와 `assert_destructive_annotation` docstring 의 「without ever executing the destructive operation itself」 둘 다로 복원된다. 나머지 100줄은 유지 — 「security 플러그인이 꺼진 픽스처는 **무서명 요청도 똑같이 답하므로** 응답으로는 접근 경로를 구분할 수 없다」(AC3/AC4 셋이 http-trace 를 읽는 근거), 「AssumeRole 레코드는 서버가 자격증명을 캐시해 한 번만 발급되지만 프록시가 그 레코드만은 evict 하지 않아 파일이 나뉘어도 상관 관계가 성립한다」(픽스처의 문서화되지 않은 거동), 「12건을 시드하는 이유 — 10건 이하면 「기본값 10」과 「무제한」을 구분할 수 없다」, 「색인 **전** 404 를 먼저 단정하지 않으면 자동 생성이 아무것도 보이지 못한다」, 「페이로드 해시까지 서명해야 본문 바꿔치기가 막힌다」가 전부 복원 경로 넷 밖이다 |
| 2026-09-07 | `tests/integration/aws_config_get_ac1.py` · `tests/integration/aws_config_get_ac2.py` · `tests/integration/aws_config_get_ac3.py` · `tests/integration/github_app_installation_token_ac1.py` · `tests/integration/github_app_installation_token_ac2.py` · `tests/integration/github_app_installation_token_ac3.py` · `tests/integration/github_app_installation_token_ac4.py` · `tests/integration/grafana_token_ac1.py` · `tests/integration/grafana_token_ac2.py` · `tests/integration/grafana_token_ac3.py` · `tests/integration/grafana_token_ac4.py` · `tests/integration/platform_auth_safety_ac1.py` · `tests/integration/platform_auth_safety_ac2.py` · `tests/integration/platform_auth_safety_ac5.py` · `tests/integration/platform_auth_safety_ac6.py` · `tests/integration/platform_auth_safety_ac7.py` · `tests/integration/platform_auth_safety_ac8.py` | 225 | `2944ffe3ef0c` | 제거 42줄 — ⑴ auth-variant 문단 6벌 30줄(`aws_config_get_ac3.py` · `github_app_installation_token_ac3.py` · `grafana_token_ac3.py` · `platform_auth_safety_ac{1,5,7}.py`): 전부 `_auth_variant.py` 의 것과 바이트 동일. `platform_auth_safety_ac1.py` 는 그 문단 **뒤에 붙은 고유 한 줄**(「주 배포는 `MCP_AUTH_DISABLED=1` 이라 게이트 자체를 관측할 수 없다」)만 남겼다 — 덧붙여 그 파일은 MCP 세션을 열지 않고 raw httpx 로 치므로 문단의 마지막 문장(「Sessions therefore carry the static key」)이 그 자리에서 **이미 거짓**이었다(정책 §충돌하면 주석이 틀린 것으로 본다). ⑵ `aws_config_get_ac3.py`·`github_app_installation_token_ac3.py`·`grafana_token_ac3.py` 의 「The shared assertion lives in …」 문단 12줄. **판단이 갈려 유지**: `platform_auth_safety_ac5.py` 의 모듈 2줄(「이 배포가 이 AC 전제가 성립하는 유일한 곳」)은 케이스 docstring 이 같은 주장을 더 자세히 하지만, 모듈 머리말의 방향 지시와 케이스의 논증은 자리가 달라 「애매하면 남긴다」로 보존. 나머지 249줄은 유지 — `grafana_token_ac1.py` 의 「mock 이 건네받은 `expiresAt` 을 되돌려주므로 TTL 이 이 계층에서 **실제로 관측 가능**하다 — GitHub mock 의 고정 만료와 갈리는 지점」, `github_app_installation_token_ac1.py` 의 「~1시간 절은 여기서 단정하지 않는다(mock 이 2099 고정)」와 `_ac4.py` 의 「키 바이트는 CI 마다 생성되므로 비교 대상이 없고, PEM armour 마커라야 내용과 무관하게 유출이 잡힌다」, `aws_config_get_ac2.py` 의 「MinIO 는 CI 시크릿의 정적 키도 똑같이 받아주므로 「성공했다」로는 구분되지 않는다」, `platform_auth_safety_ac2.py` 의 「사슬의 각 칸이 앞 칸이 준 값으로만 이어진다」와 「서버 쪽 동적 로드의 치명성은 AC8 의 (d) 변형이 관측하므로 이 단정은 공허하지 않다」, `_ac3.py` 의 「등가(포함이 아니라) 비교라야 「없다」가 단정이 된다 · 다른 오브젝트가 주는 권한은 매니페스트만 읽어서는 못 잡는다」, `_ac6.py` 의 「비정상 절은 배포를 깨지 않고는 만들 수 없어 단정하지 않는다」, `_ac8.py` 의 「네 배포를 대조하는 것 자체가 검증이다」가 복원 경로 넷 밖이다 · **재판정 2026-09-14**(RBAC 경계 해제 슬라이스): 범위에서 삭제된 `platform_auth_safety_ac3.py` 를 제외하고 재측정했다. 그 파일의 주석 판정(「AC 문언을 옮겨 적은 ClusterRole 전체는 등가 비교의 피연산자」)은 대조 대상 AC 가 결번이 되어 함께 소멸했고, 남은 파일의 판정은 앞선 판정 그대로다. |
| 2026-09-07 | `tests/integration/dear_baby_reset_user_ac1.py` · `tests/integration/dear_baby_reset_user_ac2.py` · `tests/integration/dear_baby_reset_user_ac3.py` · `tests/integration/ping_ac1.py` | 34 | `6db41ad6420b` | **재판정 2026-09-08**(`rct_20260908-0003` — namespace-list 시나리오 2 슬라이스): +18줄 — 범위에 신규 `namespace_list_ac2.py` 편입(그 파일의 모듈 docstring 12줄 + 케이스 docstring 6줄). 판정: **전부 유지**. 모듈 docstring 은 「왜 auth-variant 인가」(다른 두 배포는 in-cluster ServiceAccount 로 붙어 있어 사전 조건이 성립하지 않는다)와 「Go 단위 대체 검증이 무엇을 말해 주지 못하는가」(in-process 주입은 배포된 서버가 그 상태로 기동하는지 말하지 않는다)를 담아 파일의 존재 이유를 짓는다. 케이스 docstring 은 텍스트를 부분일치가 아니라 **정확히** 대조하는 이유(픽스처 계약의 트립와이어)를 적는데, 이것은 단언 코드가 말하지 않는다. **남의 파일은 이 행에 새로 끌어오지 않았다.** 이하 1차 판정 그대로 — 제거 15줄 — ⑴ `dear_baby_reset_user_ac3.py`·`workload_restart_ac2.py`·`workload_scale_ac3.py` 의 파괴적 표기 케이스 본문 2줄씩. 앞의 둘·셋과 달리 `dear_baby_reset_user_ac3.py` 는 모듈 docstring 에 같은 문장이 없어 ③ 이 아니라 **① 만**으로 지운다(`assert_destructive_annotation` docstring). ⑵ `ping_ac1.py` 케이스 본문 5줄 전부 — 앞 문장은 바로 아래 네 단정의 재진술(①)이자 「the AC's stated verification method」라고 스스로 밝힌 ② 재진술이고(게다가 「single content block」은 코드가 세지 않는다), 뒤 문장은 모듈 docstring 의 「in-process 단언을 배포 e2e 계층으로 승격」과 같은 말(③). ⑶ `workload_scale_ac1.py` 케이스 본문의 「Ends back at 1 replica … 각 파일이 `ensure_workload_fixture_baseline()` 을 부른다」 4줄 — 모듈 docstring 이 같은 것을 이미 말한다(③). **판단이 갈려 유지**: `workload_logs_ac4.py` 의 「Not asserted」 문단은 모듈 docstring 의 마지막 문단과 겹치지만 그 모듈 쪽이 **케이스 docstring 을 명시적으로 가리키며** `workload_logs_ac2.py` 의 성공 단언이라는 교차 제약을 더 담고 있어 어느 쪽도 다른 쪽으로 온전히 복원되지 않는다. 나머지 172줄은 유지 — `workload_logs_ac1.py` 의 「pause 는 아무것도 내지 않으므로 그것을 읽으면 셀렉터 해석만 증명되고 로그 **내용**은 증명되지 않는다」, `_ac3.py` 의 「직전 인스턴스 마커가 현재 인스턴스의 것과 달라 라이브 로그로는 통과할 수 없다」, `workload_restart_ac1.py` 의 「delete+create 였다면 uid 와 creationTimestamp 가 바뀐다」, `workload_list_ac1.py` 의 「레플리카 **값**은 고정하지 않는다 — 같은 그룹의 `workload_scale_ac1.py` 가 그 값을 움직인다」, `dear_baby_reset_user_ac1.py` 의 「kind 픽스처의 `/reset-user` 는 busybox 스텁이라 필드 수준 효과는 관측 불가」와 `_ac2.py` 의 「판별자는 호출이 **실패한다**는 것 — 무시된 selector 였다면 그대로 성공했을 것이다」, `namespace_list_ac1.py` 의 「테스트가 만들지 않은 kube-system 도 열거되므로 필터 에코가 아니다」가 복원 경로 넷 밖이다 · **재판정 2026-09-12**(V1 도구 6종 통합 슬라이스): 범위에서 폐기된 e2e 파일을 제외하고 재측정했다. 남은 파일의 주석 판정은 앞선 판정 그대로이고, 이 행이 준 보증(「등재해 두면 이후 유입이 R2 에 걸린다」)도 남은 범위에 대해 유지된다. |
| 2026-09-14 | `tests/integration/resource_read_smoke.py` | 22 | `68613ea704ec` | 제거 0줄 — 모듈 docstring 은 **실 apiserver 만 볼 수 있는 어긋남**의 기술이다: 「Table 을 요구하는 `Accept` 파라미터가 apiserver 가 아는 것과 어긋나면 요청이 실패하는 대신 평범한 List 로 답이 오고, 그 본문은 `metav1.Table` 로 전부 0인 값으로 디코드된다 — Go 단위 테스트는 자기가 만든 Table 을 먹이므로 이 어긋남을 볼 수 없다」. 이것은 1차 시도를 죽인 **관측된 실패**이자 이 파일이 존재하는 이유이고, PRD 어디에도 없다. 「단언을 *행이 있다* 에 건 이유」와 「kind 를 픽스처가 아니라 kind 클러스터가 반드시 갖는 것으로 고른 이유」도 같은 계열이다. 마지막 문단(「AC 단위의 검증이 아니다 — AC 별 시나리오 e2e 는 자매 모델 소관」)은 `docs/doc-tracker.md` 로 도달하지만 **이 파일이 그 공백을 메우는 것으로 오독되지 않게 하는 방향 지시**라, `smoke.py` 행이 같은 성격의 「이것은 platform-auth-safety/AC5 가 아니다」를 유지한 선례를 따른다. 케이스 docstring 셋도 유지 — `_table`·`test_resource_list…` 는 관례 최소 요약이고, `test_resource_get…` 의 「두 도구를 따로 재면 목록이 빈 표를 돌려주는 동안에도 각자는 「통과」로 보인다 — 1차 시도에서 로그 경로가 관측조차 되지 못한 것이 그래서다」는 그 경로를 하나로 이어 붙인 이유다. 그 문단의 「`resource_get` 은 이름을 필수로 받는다」(AC4) 절은 ② 경계에 있으나 뒤 문장의 논증이 그 전제 위에 서 있어 잘라내면 논증이 끊긴다 — 「애매하면 남긴다」로 보존. 상세는 [`passes/2026-09-14-resource-read-axis.md`](passes/2026-09-14-resource-read-axis.md), 판정 근거는 `rct_20260914-0002` |
| 2026-09-14 | `tests/integration/resource_generic_ac2.py` · `tests/integration/resource_generic_ac4.py` | 42 | `acb2c66e15c4` | 제거 8줄 — ⑴ **③ 자기 파일 중복 2건**(각 2줄): `ac2` `rows_carry_no_objects` 케이스 본문(「크기만 재면 객체를 담은 채 그냥 작은 응답과 구별되지 않는다 …」)이 같은 파일 모듈 docstring ¶4(「크기만 재면 **응답이 객체를 담은 채 그냥 작을 수도** 있다 …」)의 거의 축자 사본이고, `ac4` `precondition_has_both_noise_fields` 케이스 본문(「걷어냈음을 단언하기 전에 걷어낼 것이 있었음을 단언한다」)이 같은 파일 모듈 docstring ¶1 의 같은 논증이다. 둘 다 **사용 지점 사본을 지우고 머리말을 남겼다**(원장 #5 의 `session_write_ac3.py`·#8 의 `dear_baby_reset_user_ac3.py` 와 같은 처리). 요약 줄은 관례 최소 doc 이라 양쪽 다 남긴다. ⑵ **② 시나리오 문서 재진술 2건**(각 2줄): `ac2` 모듈 ¶1 의 실행 단계 **괄호까지 축자 인용**(「같은 Deployment 목록을 (a) 도구로 조회, (b) 객체 전문 JSON 으로 직렬화해 크기를 비교」)과 `ac4` 모듈 ¶1 의 사전 조건 축자 인용 + 「시나리오 문서의 「픽스처」 절도 이 픽스처를 승계하라고 적는다」(그 문서 「픽스처」 절이 축자로 그렇게 적는다). 두 자리 모두 **논증은 남겼다** — `ac2` 는 「도구의 다른 호출로 대조군을 만들면 같은 축약 로직을 통과해 「현저히 작다」가 공전한다」, `ac4` 는 「CI 가 `kubectl apply` 로 세워 두 필드가 다 붙는다」(픽스처 교차 제약)와 「두 필드가 애초에 없으면 「걷어냈다」가 공전한다」. **나머지 42줄은 유지**: `ac4` `_kubectl_whole_object` 의 `--show-managed-fields` 문단 전수(kubectl v1.21 부터 **출력에서만** `managedFields` 를 지운다는 **상류 거동** + 「첫 CI 에서 실제로 그랬다」는 **관측된 실패** + 「그 단언이 없었다면 키 집합 등가 비교가 조용히 헐거워졌을 자리」), `_normalized` 의 「두 경로가 같은 apiserver JSON 을 보지만 서로 다른 디코더를 지난다(kubectl 은 타입 있는 스킴, 도구는 unstructured)」, `ac2` 의 `MIN_SHRINK_FACTOR` **수치 근거**(「`managedFields` 때문에 실제 비율은 한 자릿수 후반 이상이 나오지만, 픽스처가 늘거나 줄어도 「현저히」가 무너지지 않는 선」)와 「같은 직렬화 규칙으로 재서 인코딩 차이가 비율에 섞이지 않게」, `ac4` 의 등가 vs 포함 문단(「포함으로 재면 과잉 제거가 통과한다」 · `resourceVersion` 을 피하려 metadata 는 키 집합으로 본다), `McpError` -32602 문단(k8s 에 닿기 전 인자 검증이라는 **구조적 보장** + 「셀렉터를 여기서 풀면 list verb 를 행사하게 되어 verb 1:1 이 깨진다」). **판단이 갈려 유지한 것 둘**: ⓐ 함수 docstring 요약 줄 5개의 「시나리오 N — …」 형태는 함수명 + 기대 결과를 되풀이하는 ② 경계이나, **같은 날 원장 #9 가 `resource_read_smoke.py` 의 같은 형태를 「관례 최소 요약」으로 유지**했고 절차 2번이 그 자리를 유지로 정한다 — 선례를 따른다, ⓑ `ac2` 모듈 ¶4 의 「AC17 이 list 를 게이트 밖에 두는 근거가 바로 「표에는 값이 실리지 않는다」이므로」는 `docs/prd-resource-generic.md` 로 도달하지만 그 뒤 문장(「이 축은 크기가 아니라 형태로 지켜져야 한다」)이 **AC 가 테스트 형태에 부과하는 제약**이라 잘라내면 논증이 끊긴다 — 원장 #9 의 AC4 전제와 같은 처리. 상세는 [`passes/2026-09-14-resource-generic-e2e.md`](passes/2026-09-14-resource-generic-e2e.md), 판정 근거는 `rct_20260914-0004` |
| 2026-09-14 | `tests/integration/resource_generic_ac1.py` | 22 | `bf7e3ea0aef1` | 제거 3줄 — **전부 ② 시나리오 문서 재진술이고, 세 자리 모두 인용만 걷고 논증은 남겼다**(원장 #10 이 고정한 처리). ⑴ 모듈 ¶1 의 「종류마다 셀렉터를 붙인 목록과 붙이지 않은 목록을 같은 호출로 둘 다 떠서 비교한다」 — `docs/test-resource-generic.md#시나리오 1` 의 **실행 단계** 그대로다(「각각 `resource_list`로 조회 → `labelSelector`로 좁혀 재조회」). 남긴 것은 「셀렉터 붙인 쪽만 보면 「줄었다」가 성립하지 않는다 — 통째로 무시하는 구현도 그 단독 관측은 통과한다」(**함정**)와 대조군 네 종류를 이름으로 보는 이유. ⑵ 모듈 ¶2 의 「시나리오가 이벤트에 요구하는 것도 「대상별로 좁혀짐」이라 축이 다르다. 여기서 폐기된 `pod_describe` 의 이벤트 절이 이 경로로 대체됐다는 문면이 …」 — 같은 시나리오 **기대 결과의 괄호까지 축자**다(「이벤트가 대상별로 좁혀짐(폐기된 `pod_describe`의 이벤트 부분이 이 경로로 대체됨)」). 남긴 것은 「이벤트에는 대상별 레이블이 없어 좁히는 축 자체가 다르다」(상류 사실)와 「개수만 보지 않고 남은 행이 전부 그 파드를 가리키는지를 Object 칸으로 확인한다」(왜 이 단언인가). ⑶ 모듈 ¶3 의 「클러스터 스코프 + `namespace` 는 「무시되지 않고 거부」가 요점이라」 — 역시 기대 결과 축자(「클러스터 스코프 + `namespace` 조합은 무시되지 않고 거부」). 남긴 것은 「거부 사실만으로는 부족하다 — 같은 종류를 `namespace` 없이 부르면 성공한다는 것을 나란히 보여야 거부가 스코프 때문임이 성립한다」(**vacuous 방지** — 원장 #4 계열). **유지 22줄**: ¶4 전수(「픽스처 CRD 는 서버가 뜬 **뒤에** 설치될 수 있는데 재기동이 필요 없는 것은 종류 해석이 미지의 종류에 대해 디스커버리를 정확히 한 번 다시 타기 때문」 + 「그런데도 이 파일은 그 경로에 기대지 않는다 — 도구가 못 찾는 것이 「아직 안 섰다」인지 「해석이 틀렸다」인지 구분되어야 해서」)는 **구조적 보장의 이유**이고 `resolve` 의 재시도 루프를 읽어야 알 수 있는 것을 그 코드에 기대지 않기로 한 **선택**까지 담는다, `wait_for_fixture` 본문 3줄(「마지막에 적용되는 인스턴스가 보이면 픽스처는 전부 서 있다」는 추론과 「도구가 아니라 kubectl 로 재는 것은 이 전제를 검증 대상 자신으로 세우지 않기 위해서」 — ci.yml 적용 순서는 ① 경계이나 그 위에 선 추론이 본체다). **판단이 갈려 유지한 것**: 모듈 요약 줄(시나리오 1 제목 + 기대 결과 4개 열거)과 케이스 docstring 요약 줄 4개(「시나리오 1 — …」)는 ② 경계이나 절차 2번이 그 자리를 관례 최소 doc 으로 정하고 원장 #9·#10 이 같은 형태를 같은 방향으로 유지했다 — 선례를 따른다. 상세는 [`passes/2026-09-14-resource-generic-scenario1.md`](passes/2026-09-14-resource-generic-scenario1.md), 판정 근거는 `rct_20260914-0009` |
| 2026-09-14 | `tests/integration/resource_generic_ac20.py` | 36 | `2412aefb8ac7` | 제거 7줄 — ⑴ **② 살아 있는 시나리오 문서 재진술 3줄**: 모듈 ¶1 의 앞 절(「시나리오의 사전 조건은 「설치 전/후 두 상태」인데 `tests/k8s/kind/` 픽스처는 ci.yml 이 셋업에서 심으므로 시작 시점이 이미 「후」다 … 그래서 자기 전용 프로브 CRD 를 런타임에 세우고 지운다」)이 시나리오 20 `자동화` 필드의 축자다 — 「전용 프로브 CRD 를 테스트가 런타임에 세우고 지운다(**픽스처로 두면 설치 「전」 상태가 관측되지 않는다**)」. 파일이 `검증 시나리오:` 선언으로 그 문서를 이미 가리키므로 **한 홉**이다. **뒤 절은 남겼다** — 「매니페스트를 그 디렉터리에 두면 언젠가 ci.yml 에 배선되고, 그 순간 사전 조건이 소리 없이 사라지는데 케이스는 통과하고 게이트도 초록이다. 위치 자체가 방지선이다」는 시나리오 문서가 말하지 않는 **앞으로의 편집을 구속하는 불변식**이다. ⑵ **③ 자기 파일 중복 3줄**: `server_pod_identity` 본문(「**파드 uid 만으로는 부족하다** … `restartCount` 와 현재 인스턴스의 `startedAt` 까지 함께 뜬다」)이 모듈 ¶3 의 「파드 uid 만으로는 부족해 컨테이너 재시작까지 함께 뜬다(캐시는 프로세스 안에 있어 파드가 살아 있어도 컨테이너가 죽으면 비워진다)」와 같은 말이고, **요약 줄** 「(이름, uid, 컨테이너별 재시작 횟수·기동 시각)」이 지운 마지막 문장까지 복원한다 — 사용 지점 사본을 지우고 머리말을 남기는 원장 docstring #5·#8·#10 선례. ⑶ **② 기대 결과 축자 인용 1줄**: ¶4 가 기대 결과의 마지막 절을 따옴표째 옮겨 적고 있었다 — **인용은 접고 논증은 남겼다**(원장 docstring #10 이 `ac2`·`ac4` 의 실행 단계·사전 조건 인용에 내린 판정과 같다). **유지 36줄**: 모듈 ¶2(관측 순서가 곧 단언이다 — 오타 호출이 RESTMapper 캐시를 **데우는** 자리이고 데우지 않으면 뒤의 조회가 「무효화됐다」가 아니라 「처음 만들었다」를 잰다. 시나리오 문서는 **순서만** 적고 그 순서가 무엇을 재는지는 적지 않는다), 모듈 ¶3 의 나머지(discovery 전파 지연 때문에 폴링이 필요한데 폴링만 두면 「재기동 뒤 다시 읽었다」와 구별되지 않는다 — **무엇이 조용히 통과하는가**의 정면), ¶4 의 논증(#83 이후 부여 밖 종류가 없어 강한 부정형의 대조군을 만들 수 없다 → 관측 가능한 최강형까지만 간다 — **부분 커버리지의 선언**이고 PR #87 본문이 「담지 못한 것 (파일 docstring 에 적었다)」로 이 자리를 *가리킨다*), 케이스 docstring 의 「이 단언이 왜 이 형태인가」 셋과 요약 줄들(절차 2번 — 원장 docstring #9·#10 선례). 🔴 **이 패스가 처음 그은 경계 — 복원 경로 ③(PR 본문)을 어디까지 인정하는가**: 이 모듈 docstring 네 문단은 PR #87 본문의 「저작에서 설계를 좌우한 것」 절과 거의 축자로 겹치고 `docs/doc-tracker.md` 변경 이력도 같은 셋을 요약해 담는다. 문면만 보면 통째로 제거 대상이지만, **③ 이 담는 것은 「왜 이 PR 이 이렇게 바꿨는가」이고 「앞으로 이렇게 고치지 마라」를 집행하지는 않는다** — 그 불변식을 읽어야 하는 사람은 이 파일을 고치려는 사람이고, 그가 닫힌 PR 본문을 먼저 뒤지리라 기대하는 것은 복원 경로가 아니라 희망이다. 또 PR 본문이 스스로 「파일 docstring 에 적었다」고 가리키는 자리는 **원본이 docstring 쪽**이라 사본을 근거로 원본을 지우면 SSOT 가 뒤집힌다. **다음 패스가 다르게 판정할 수 있고, 그때는 새 패스 문서로 할 것** — R6 은 등재 범위가 *변했는지*만 재고 *판정이 옳은지*는 보지 않아 이 자리는 다시 열리지 않으면 영구 초록이다. ⚠️ **④(커밋 메시지)는 이 레포에서 사실상 비어 있다** — squash 머지라 본문이 제목 한 줄 + `Co-authored-by` 뿐이고(`44ae81c`·`e4304b3`·`bd127aa` 확인), 이 패스는 ④ 를 근거로 아무것도 지우지 않았다. ⚠️ **이 자리에서 시나리오 문서가 코드보다 약하다** — `자동화` 는 「`uid`·`startTime` 이 그대로임으로 잰다」인데 구현은 `uid` + `restartCount` + `startedAt` 이고 그 차이가 단언이 세지는 이유다. 즉 모듈 ¶3 은 문서를 되풀이하는 것이 아니라 **넘어선다** — 문서 쪽을 강한 문면으로 맞추는 것(절차 4번)은 자매 모델 `tbm_homelab-k3s-mcp-scenario-e2e` 의 표면이라 범위 밖으로 넘긴다. 상세는 [`passes/2026-09-14-resource-generic-scenario20.md`](passes/2026-09-14-resource-generic-scenario20.md), 판정 근거는 `rct_20260914-0010` |
| 2026-09-14 | `scripts/check_doc_inventory.py` | 29 | `87d383322c27` | **신규 범위 첫 판정**(`rct_20260914-0015`) — 줄 주석 #32 와 **같은 파일·같은 PR** 이다. 제거 2줄 — ⑴ 모듈 머리의 **축자 인용 블록**(「`docs/doc-tracker.md` 의 「문서 공개」 표에는 이런 줄이 있다」 + 인용된 집계 줄 스키마). **삼중 복원**이다: ③ 같은 파일 D4 실패 메시지가 같은 형태를 문자 단위로 출력하고(`0` 이 `f` 로 일반화된 쪽이 오히려 더 정확하다), ① `CLAIM_RE` 가 그 형태를 정규식으로 박고 있으며, ② `doc-tracker.md` 의 그 줄 자신이 원본이다. **인용은 걷고 논증은 남긴다**(원장 docstring #10·#11·#12) — 그 줄이 *무엇을 주장하는가*의 분해(⑴ 전부 도달 가능 ⑵ 수와 내역이 맞다)는 뒤 문단의 D2/D4 논증이 서는 전제라 남겼다. ⑵ 「클러스터도 서드파티 의존성도 필요 없다(표준 라이브러리 전용) — CI 의 lint 잡에서 돈다」 — 앞 절은 ① import 넷, 뒤 절의 SSOT 는 **`.github/workflows/ci.yml`** 이다(그 `Doc hub inventory` 스텝 주석이 「표준 라이브러리 전용에 클러스터가 필요 없어 여기서 돈다」를 거의 축자로 적고 형제 스텝 셋도 같은 문장을 쓴다 — 잡이 옮겨지면 이 문장만 조용히 거짓이 된다). **바로 앞 문장은 남겼다** — 세 게이트를 한 계열로 묶는 일반화(「사람의 자기신고를 레포의 실제 상태에서 재도출한 값과 대조한다」)는 어느 한 파일을 읽어도 나오지 않는다. **유지 29줄**: D1~D4 **규칙 열거**는 **원장 docstring #1 의 문장이 그대로 지배한다** — 「실패 메시지 어디에도 규칙 *집합*은 없다(각 메시지는 자기 규칙만 말한다)」이고, 「판정하는 것은 이 넷이다」를 말하는 자리는 이 docstring 뿐이다. 그 안의 D1 「조용히 「정책」으로 흘려보내면 표가 말하는 종류와 집계가 갈라진다」와 D2 「도달 경로는 **이행적**이다 … 허브 직접 링크만 세면 도달 가능성을 과소평가한다」는 유지 대상 3번(무엇이 조용히 통과하는가 · 설계 선택의 근거). **관측된 사건 4줄**(「판정 패스 문서가 하나씩 들어온 **네 번의 PR 이 연속으로** 이 줄을 건드리지 않았고 집계는 34 에 멈춘 채 실측은 38 이 됐다 — 네 번 모두 CI 는 초록이었다」)은 원장 docstring #1 의 「848줄」 일화와 같은 자리이고, **네 PR 을 횡단하는 사실이라 어느 한 PR 본문으로도 복원되지 않아** 그보다 강하다. 「D2 와 D4 를 함께 두는 것이 중요하다 … 문서 하나가 링크에서 떨어져 나가도 개수는 그대로이므로 통과한다」는 두 축을 다 재는 이유. 함수 docstring 요약 줄 4개는 절차 2번 + 원장 docstring #9~#12 선례. **판단이 갈려 유지**: `categorize` 의 「… 분류 불가면 None(= D1 실패)」 — 앞 절은 시그니처 재진술에 가깝지만 뒤 절이 반환값 `None` 을 **규칙 D1 에 묶는다**(시그니처는 그것이 규칙 위반이라고까지는 말하지 않는다). 상세는 [`passes/2026-09-14-doc-inventory-gate.md`](passes/2026-09-14-doc-inventory-gate.md), 판정 근거는 `rct_20260914-0015` |
| 2026-09-15 | `tests/integration/resource_generic_ac3.py` | 23 | `1047bcfddc79` | **신규 범위 첫 판정**(`rct_20260915-0003`) — 줄 주석 표의 같은 날 행과 **같은 파일·같은 PR** 이다. 제거 5줄 — **② 픽스처 문서 재진술**: 모듈 docstring ¶1 이 「좁히지 않으면 `resource-generic-selected`·`-other` 와 `kube-root-ca.crt` 가 섞여 모집단이 123 이 된다」를 `resource-generic-fixture.yaml` 머리말에서 그대로 옮겨 적고 있었다. **포인터는 남겼다**(「왜 120개이고 왜 전용 레이블로 좁히는지는 그 파일의 머리말이 진다」) — 원장 #23·#29 가 고정한 처리. **유지 23줄**: ⑴ ⚠️ 절 3줄은 이 파일이 지는 **대가**다(셀렉터로 좁혔으므로 apiserver 가 `remainingItemCount` 를 안 싣고, 그래서 남은 건수를 `remaining` 이 아니라 2회차 행 수로 잰다) — 코드는 「`remaining` 을 안 본다」는 사실만 보여 주고 **왜 볼 수 없는가**는 보여 주지 않으므로, 지우면 다음 사람이 「빠뜨렸다」로 읽고 단언을 더한다. ⑵ 「거부 하나만 봐서는 성립하지 않는다 — 인자를 아예 못 받는 구현도 그 단독 관측을 통과한다」 4줄은 **왜 상한값 500 호출이 나란히 있는가**이고, 원장 #11 이 `ac1.py` 의 같은 형태(「셀렉터를 통째로 무시하는 구현도 통과한다」)를 유지로 판정한 계열이다. ⑶ 「전제를 검증 대상 자신으로 세우면 「픽스처가 아직 안 섰다」와 「페이징이 틀렸다」가 구분되지 않는다」 2줄은 `_workload.py` 가 세운 레포 규약의 **이 파일에서의 근거**다. ⑷ 케이스 doc 넷은 각각 **그 케이스가 없으면 무엇이 조용히 통과하는가**를 든다(사전 조건: 「픽스처가 줄면 나머지 단언이 전부 공전한다」 / 이어보기: 「토큰을 무시하고 두 번째 페이지를 새로 뜨는 구현은 개수는 맞출 수 있어도 이 단언은 통과하지 못한다」) — 원장 docstring #10·#12 가 케이스 doc 을 유지로 판정한 기준 그대로다 |
<!-- /docstring-원장 -->

판정 완료 합계 **<!-- docstring-합계 -->1152<!-- /docstring-합계 -->줄**, 미판정 잔량
**<!-- docstring-잔량 -->0<!-- /docstring-잔량 -->줄**. `rct_20260907-0001` 이
`tests/integration/*_ac*.py` 60파일을 전수 판정해 위 원장 5~8행으로 등재하면서 **이 표면의
판정이 한 바퀴 닫혔다** — 그 시점 스캔 범위 안의 docstring 중 등재되지 않은 것은 없었다.

> **잔량 31 → 0 (2026-09-14, `rct_20260914-0015` — 허브 집계 게이트 판정 슬라이스)**: 바로 아래
> 문단이 잔량으로 선언한 `scripts/check_doc_inventory.py` 31줄을 원장 #13 으로 등재했다(제거 2 · 유지 29).
> 합계는 1100 → 1129, 판정 대상 전체는 1131 → 1129 이고 **1129 + 0 = 1129 로 R8 이 닫힌다.**
> 같은 PR 이 줄 주석 표면의 10 도 함께 닫았다 — **한 파일에서 두 표면을 동시에 판정한 첫 패스**이고,
> 두 표면의 잔량이 동시에 0 이 된 것도 처음이다. 모든 값은 손으로 더하지 않고
> `check_comment_policy.py` 출력과 그 게이트의 `docstring_lines()` 파일별 재계수에서 뽑았다.

> **잔량 0 → 31 (2026-09-14, `rct_20260914-0012` — 허브 집계 게이트 슬라이스)**: 신규 파일
> `scripts/check_doc_inventory.py` 가 docstring 31줄을 들고 들어왔다(모듈 doc 과 함수 doc).
> 같은 파일별 재계수로 다른 파일은 불변임을 확인했다(1100 + 31 = 1131). 판정은 자매 모델의
> 소관이므로 여기서는 잔량만 움직인다 — 아래 선례와 같은 선택이다.

> **잔량 0 → 25 (2026-09-14, `rct_20260914-0006` — 시나리오 1 슬라이스)**: 신규 전용 e2e
> `tests/integration/resource_generic_ac1.py` 가 docstring 25줄을 들고 들어왔다(모듈 doc 과
> 케이스 doc). 같은 파일별 재계수로 다른 파일은 불변임을 확인했다(1042 + 25 = 1067).
> 판정은 자매 모델의 소관이므로 여기서는 잔량만 움직인다 — 바로 아래 선례와 같은 선택이다.

> **잔량 0 → 22 (2026-09-14, `rct_20260913-0002`)**: 신규 비-시나리오 파일
> `tests/integration/resource_read_smoke.py` 가 docstring 22줄을 들고 들어왔다. 아래 경고가
> 말하는 두 선택지 중 **잔량을 올리는 쪽**을 골랐다 — 주석 비중복성 판정은 자매 모델
> `tbm_homelab-k3s-mcp-comment-redundancy` 의 소관이고, 이 슬라이스가 그 판정까지 겸하면
> 자기 PR 에서 자기 주석을 판정하는 모양이 된다. 신규 범위를 기존 행에 끌어오지 않는
> `gate.go`·`resource.go` 선례와 같은 처리이며, 이 줄이 그 「아직 판정받지 않았다」의 선언이다.

> **잔량 22 → 0 (2026-09-14, `rct_20260914-0002`)**: 그 22줄을 소관 모델이 인계받아 판정하고 위
> 마지막 행으로 등재했다. **제거 0줄** — 모델 정의가 「Python 모듈 docstring 은 유지한다」를 명시하는
> 데다, 내용 자체가 실 apiserver 만 볼 수 있는 어긋남과 1차 시도의 관측된 실패라 복원 경로 넷 밖이다.
> **유지라도 등재는 필요하다** — 잔량은 「판정했고 유지로 결론했다」가 아니라 「아직 판정받지 않았다」의
> 선언이고, 그 둘을 가르는 것이 이 표의 행이다.

> **잔량 0 → 50 (2026-09-14, `rct_20260914-0002`)**: 시나리오 2·4 의 전용 e2e 둘이 docstring
> 50줄을 들고 들어왔다. 두 파일의 docstring 이 긴 것은 **각 단언이 왜 그 형태인지**를 적기
> 때문이다 — 시나리오 2 는 「대조군을 도구의 다른 호출로 만들면 같은 축약 로직을 지나 비교가
> 공전한다」와 「크기만 재면 객체를 담은 채 그냥 작은 응답과 구별되지 않는다」, 시나리오 4 는
> 「걷어냈음을 단언하기 전에 걷어낼 것이 있었음을 단언한다」와 「포함이 아니라 키 집합 등가로
> 재야 과잉 제거가 잡힌다」다. 판정이 아니라 잔량을 고른 이유는 위 두 항목과 같다.
> **바로 위 항목이 잔량을 0 으로 닫은 직후 다시 50 이 선다** — 아래 문단이 말하는 그 상태다.

> **잔량 50 → 0 (2026-09-14, `rct_20260914-0004`)**: 그 50줄을 소관 모델이 인계받아 전수 판정하고
> 위 마지막 행으로 등재했다. **제거 8줄** — ③ 자기 파일 중복 2건과 ② 시나리오 문서 축자 인용 2건이고,
> 네 자리 모두 **논증은 남기고 되풀이만 걷었다**. 두 파일의 docstring 이 긴 것은 각 단언이 왜 그
> 형태인지를 적기 때문이라는 위 항목의 관측은 **판정 뒤에도 그대로 유효하다** — 42줄이 남았고 그중
> kubectl 의 출력 전용 `managedFields` 제거 거동·「첫 CI 에서 실제로 그랬다」는 관측된 실패·
> `MIN_SHRINK_FACTOR` 의 수치 근거는 복원 경로 넷 어디에도 없다.
> **유지라도 등재는 필요하다**(위 `22 → 0` 항목과 같은 이유).
>
> **이 패스가 함께 고친 산문 한 줄**: 바로 위 항목이 「직후 다시 **45** 가 선다」로 적었으나 실측은
> 50 이다(같은 항목 머리·마커·게이트 출력·독립 계수 `ac2` 19 + `ac4` 31 이 전부 50). 마커와 산술은
> 처음부터 정상이었고 낡은 것은 그 산문 하나였다 — 위 `87 → 0` 항목의 「열거 산문만 불완전」과 같은
> 계열이라 독립 gap 으로 올리지 않고, 어차피 이 표면을 다시 쓰는 이 패스에서 맞췄다.

> **잔량 25 → 68 (2026-09-14, `rct_20260914-0007`)**: 같은 신규 파일이 docstring 43줄을 들고 들어왔다.
> 이 파일의 docstring 이 긴 것은 **설계 제약 셋이 전부 「왜 이렇게 하지 않았는가」의 형태**이기
> 때문이다 — 픽스처를 `tests/k8s/kind/` 에 두지 않은 이유(두면 ci.yml 에 배선되어 시나리오의 사전
> 조건인 「설치 전」이 소리 없이 사라진다), 오타 호출을 설치 전에 먼저 태우는 이유(캐시를 데워야
> 「무효화됐다」를 잰다), 파드 정체를 재는 이유(폴링만으로는 재기동과 구별되지 않는다). 셋 다 코드
> 모양으로는 복원되지 않는다. 판정은 소관 모델의 몫으로 남긴다.

> **잔량 68 → 43 (2026-09-14, `rct_20260914-0009`)**: 위 `0 → 25` 가 지목한 25줄을 소관 모델이
> 인계받아 전수 판정하고 위 마지막 행으로 등재했다. **제거 3줄** — 셋 다
> `docs/test-resource-generic.md#시나리오 1` 의 실행 단계·기대 결과를 옮긴 ② 인용이고, 세 자리 모두
> **인용만 걷고 논증은 남겼다**(위 `50 → 0` 항목과 같은 처리). 이 파일의 docstring 이 긴 것은
> **각 단언이 왜 그 형태인지**를 적기 때문이라는 관측은 판정 뒤에도 그대로다 — 22줄이 남았고 그중
> 「셀렉터를 통째로 무시하는 구현도 단독 관측은 통과한다」, 「거부 사실만으로는 부족하다」, 그리고
> CRD 가 서버보다 늦게 서도 되는 **구조적 이유와 그럼에도 그 경로에 기대지 않기로 한 선택**은 복원
> 경로 넷 어디에도 없다. **유지라도 등재는 필요하다**(위 `22 → 0` 항목과 같은 이유).
> ⚠️ **0 이 아니라 43 으로 내려앉는다** — 남는 43 은 바로 위 항목(#87)이 들여온
> `resource_generic_ac20.py` 몫이고 이 패스의 범위가 아니다(1042 + 22 = 1064, 1064 + 43 = 실측 1107).

> **잔량 43 → 0 (2026-09-14, `rct_20260914-0010`)**: 바로 위 항목이 범위 밖으로 넘긴 43줄을 같은
> 소관 모델이 이어받아 전수 판정하고 위 마지막 행으로 등재했다(1042 + 22 + 36 = 1100 = 실측). **제거 7줄** — 위 잔량 문단이 「설계 제약 셋이 전부 「왜
> 이렇게 하지 않았는가」의 형태라 코드 모양으로는 복원되지 않는다」고 적었고 그 관측은 **① 에
> 대해서는 그대로 유효했다.** 판정이 갈린 자리는 다른 데 있었다 — **③(PR 본문)**이다. PR #87
> 본문의 「저작에서 설계를 좌우한 것」 절이 그 셋을 거의 축자로 담고 있어, 문면만 보면 이
> docstring 본문은 통째로 제거 대상이었다. 그 선을 어디에 그었는지는 위 행과 패스 문서에 적었다 —
> **「왜 이렇게 바꿨는가」는 ③ 이 복원하지만 「앞으로 이렇게 고치지 마라」는 집행하지 않는다.**
> 지운 셋은 그 경계 밖의 것들이다(살아 있는 시나리오 문서 재진술 · 자기 파일 중복 · 기대 결과
> 축자 인용). **유지라도 등재는 필요하다** — 잔량은 「판정했고 유지로 결론했다」가 아니라 「아직
> 판정받지 않았다」의 선언이고, 그 둘을 가르는 것이 이 표의 행이다.

> **잔량 0 은 「끝났다」가 아니라 「지금 여기서 다시 시작한다」는 뜻이다.** R8 은
> `합계 + 잔량 == 실측`을 양방향으로 강제하므로, 잔량이 0 인 상태에서 새 `.py` 가 docstring 을
> 들고 들어오면 **그 즉시 CI 가 멈춘다**. 그때 선택지는 둘이다 — 그 범위를 판정해 원장 행으로
> 등재하거나, 잔량 마커를 올려 「아직 판정하지 않았다」를 diff 에 남기거나. 어느 쪽이든 조용히
> 넘어가지 않는 것이 이 규칙의 목적이고, 잔량 0 은 그 래칫이 가장 촘촘하게 걸린 상태다.
> 반대로 **모델 as-is 는 여전히 이 표면을 보지 않는다**(위 「지문의 사각지대」) — docstring 이
> 늘어도 재감지는 트리거되지 않으므로, 이 자리를 지키는 것은 지문이 아니라 R8 하나다.

> **`rct_20260905-0001` 슬라이스가 등재 없이 지웠던 것**(이제는 위 5~8행이 같은 60파일을
> 덮으므로 이 문단은 경위 기록이다). 그 슬라이스는 같은 60파일의 모듈 docstring에서 「이 파일은
> 하나의 AC만 주검증한다(… 규칙 2)」·「AC↔파일 매핑 SSOT은 `docs/doc-tracker.md`이고
> `check_ac_mapping.py`가 … 강제한다」·「`check_ac_mapping.py`가 이 선언을 읽어 …
> `run_all.py`가 `실행 대상`을 읽어 이 파일을 배차한다」 계열의 정형 문단 164줄을 걷어냈다.
> 셋 다 ② 저장소 문서(`docs/doc-tracker.md`의 규칙 2·`run_all.py` 모듈 docstring의 「파일이
> 신고하는 것」)와 ① 코드(`check_ac_mapping.py`·`run_all.py`)로 복원되고, 그것을 요구하는
> 지시문은 어디에도 없다 — 파일이 신고해야 하는 것은 `검증 AC:`·`실행 대상:` 선언 줄이고
> 그 줄들은 유지 대상 1번이라 한 글자도 건드리지 않았다. 그 슬라이스가 60파일을 등재하지
> 못한 이유는 「지운 것이 정형 문단뿐이고 나머지 docstring 은 아직 판정받지 않았다」였다 —
> 등재는 「그 범위를 그때 그 내용으로 **전수** 판정했다」는 뜻이기 때문이다(위 원장 머리말과
> 같은 이유). `rct_20260907-0001` 이 그 전수 판정을 마쳐 그 조건을 채웠다.
